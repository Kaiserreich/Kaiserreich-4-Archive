party_pop = {
	schema = {
		any_key = yes
		data = {
			{
				any_key = yes
				data = fixed_point
			}
		}
	}

	party = {
		none = 1
		coalition_partner = 2
		ruling_party = 3 # also used for non ruling party default pp gain
	}

	gain = {
		small = 0.01
		medium = 0.03
		large = 0.05
		mega = 0.10
		giga = 0.25
	}
	loss = {
		small = -0.01
		medium = -0.03
		large = -0.05
		mega = -0.10
		giga = -0.25
	}

	# examples:
	# constant:party_pop.gain.small to refer to 0.01
	# constant:party_pop.loss.large to refer to -0.05

	# Script constants can be referred to anywhere and used like vars except in more places
	# They also work in localisation such as [?constant:party_pop.gain.giga]
}
