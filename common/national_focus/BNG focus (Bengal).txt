#############################
# Bengal Focus Tree         #
# By Cazadorian             #
#############################

focus_tree = {
	id = bengal_focus

	country = {
		factor = 0
		modifier = {
			add = 10
			tag = BNG
		}
	}
	default = no
	continuous_focus_position = { x = 1700 y = 700 }

	### Political branches ###

	focus = {
		id = BNG_scion_of_the_revolution
		icon = GFX_goal_india_commune
		cost = 5

		x = 4
		y = 4

		ai_will_do = { factor = 500 }

		available = {
			has_socialist_government = yes
		}

		mutually_exclusive = {
			focus = BNG_the_republic_of_the_people
			focus = BNG_the_sensible_approach
			focus = BNG_nation_of_steel
		}
		on_uncomplete = {
			remove_ideas = {
				BNG_continuing_the_revolution
				BNG_continuing_the_revolution_constitution
				BNG_continuing_the_revolution_gandhism
				BNG_continuing_the_revolution_rebel
				BNG_continuing_the_revolution_final
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_scion_of_the_revolution"
			if = {
				limit = { has_government = syndicalist }
				set_temp_variable = { coalition_partner_var = token:radical_socialist }
				add_to_coalition = yes
			}
			else_if = {
				limit = { has_government = radical_socialist }
				set_temp_variable = { coalition_partner_var = token:syndicalist }
				add_to_coalition = yes
			}
			add_ideas = BNG_continuing_the_revolution
		}
		search_filters = { FOCUS_FILTER_INTERNAL_AFFAIRS FOCUS_FILTER_POLITICAL FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_unity_of_the_workers
		icon = GFX_focus_ICE_embrace_the_workers_revolution
		cost = 5

		relative_position_id = BNG_scion_of_the_revolution
		x = -2
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_scion_of_the_revolution
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_unity_of_the_workers"
			add_stability = 0.05
			add_popularity = { ideology = ROOT popularity = 0.05 }
		}
		search_filters = { FOCUS_FILTER_POLITICAL FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_refitting_the_republics_constitution
		icon = GFX_socialist_constitution
		cost = 5

		relative_position_id = BNG_scion_of_the_revolution
		x = 0
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_scion_of_the_revolution
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_refitting_the_republics_constitution"
			swap_ideas = {
				remove_idea = BNG_continuing_the_revolution
				add_idea = BNG_continuing_the_revolution_constitution
			}
		}
		search_filters = { FOCUS_FILTER_INTERNATIONAL_TRADE FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_abandonment_of_gandhism
		icon = GFX_focus_RAJ_a_tiger_unchained
		cost = 5

		relative_position_id = BNG_scion_of_the_revolution
		x = -1
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_refitting_the_republics_constitution
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_abandonment_of_gandhism"
			if = {
				limit = { has_idea = BNG_continuing_the_revolution_rebel }
				swap_ideas = {
					remove_idea = BNG_continuing_the_revolution_rebel
					add_idea = BNG_continuing_the_revolution_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_continuing_the_revolution_constitution
					add_idea = BNG_continuing_the_revolution_gandhism
				}
			}
		}
		search_filters = { FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_upholding_the_rebel_legacy
		icon = GFX_focus_generic_workers_and_farmers_rise
		cost = 5

		relative_position_id = BNG_scion_of_the_revolution
		x = 0
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_refitting_the_republics_constitution
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_upholding_the_rebel_legacy"
			if = {
				limit = { has_idea = BNG_continuing_the_revolution_gandhism }
				swap_ideas = {
					remove_idea = BNG_continuing_the_revolution_gandhism
					add_idea = BNG_continuing_the_revolution_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_continuing_the_revolution_constitution
					add_idea = BNG_continuing_the_revolution_rebel
				}
			}
		}
		search_filters = { FOCUS_FILTER_MANPOWER }
	}

	focus = {
		id = BNG_the_republic_of_the_people
		icon = GFX_goal_ideology_social_democrat
		cost = 5

		x = 7
		y = 4

		ai_will_do = { factor = 500 }

		available = {
			has_soclib_or_socdem_government = yes
		}

		mutually_exclusive = {
			focus = BNG_scion_of_the_revolution
			focus = BNG_the_sensible_approach
			focus = BNG_nation_of_steel
		}

		on_uncomplete = {
			remove_ideas = {
				BNG_the_peoples_republic
				BNG_the_peoples_republic_civil
				BNG_the_peoples_republic_judicial
				BNG_the_peoples_republic_judicial_civil
				BNG_the_peoples_republic_suffrage
				BNG_the_peoples_republic_final
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_the_republic_of_the_people"
			if = {
				limit = { has_government = social_democrat }
				set_temp_variable = { coalition_partner_var = token:social_liberal }
				add_to_coalition = yes
			}
			else = {
				set_temp_variable = { coalition_partner_var = token:social_democrat }
				add_to_coalition = yes
			}
			add_ideas = BNG_the_peoples_republic
		}
		search_filters = { FOCUS_FILTER_INTERNAL_AFFAIRS FOCUS_FILTER_POLITICAL FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_compulsory_mother_tongue_education
		icon = GFX_goal_KTG_Educate_natives
		cost = 5

		relative_position_id = BNG_the_republic_of_the_people
		x = -1
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_scion_of_the_revolution
			focus = BNG_the_republic_of_the_people
		}
		on_uncomplete = {
			remove_ideas = BNG_rice_cooperatives
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_compulsory_mother_tongue_education"
			add_research_slot_until_five = yes
		}
		search_filters = { FOCUS_FILTER_RESEARCH }
	}

	focus = {
		id = BNG_subsidise_rice_cooperatives
		icon = GFX_goal_generic_rice_field
		cost = 5

		relative_position_id = BNG_the_republic_of_the_people
		x = -2
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_scion_of_the_revolution
			focus = BNG_the_republic_of_the_people
		}
		available = {
			OR = {
				has_socialist_government = yes
				has_government = social_democrat
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_subsidise_rice_cooperatives"
			add_ideas = BNG_rice_cooperatives
		}
		search_filters = { FOCUS_FILTER_INDUSTRY FOCUS_FILTER_MANPOWER }
	}

	focus = {
		id = BNG_judicial_executive_separation_act
		icon = GFX_focus_AUS_lawmaking_leniency
		cost = 5

		relative_position_id = BNG_the_republic_of_the_people
		x = 0
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_republic_of_the_people
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_judicial_executive_separation_act"
			if = {
				limit = { has_idea = BNG_the_peoples_republic_civil }
				swap_ideas = {
					remove_idea = BNG_the_peoples_republic_civil
					add_idea = BNG_the_peoples_republic_judicial_civil
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_the_peoples_republic
					add_idea = BNG_the_peoples_republic_judicial
				}
			}
		}
		search_filters = { FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_universal_adult_suffrage
		icon = GFX_goal_generic_elections
		cost = 5

		relative_position_id = BNG_the_republic_of_the_people
		x = -1
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_judicial_executive_separation_act
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_universal_adult_suffrage"
			if = {
				limit = { has_idea = BNG_the_peoples_republic_judicial_civil }
				swap_ideas = {
					remove_idea = BNG_the_peoples_republic_judicial_civil
					add_idea = BNG_the_peoples_republic_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_the_peoples_republic_judicial
					add_idea = BNG_the_peoples_republic_suffrage
				}
			}
		}
		search_filters = { FOCUS_FILTER_PRODUCTION }
	}

	focus = {
		id = BNG_the_fight_against_corruption
		icon = GFX_focus_ARG_crackdown_on_corruption
		cost = 5

		relative_position_id = BNG_the_republic_of_the_people
		x = 1
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_judicial_executive_separation_act
		}

		on_uncomplete = {
			remove_ideas = {
				BNG_bengali_conservatism
				BNG_bengali_conservatism_civil
				BNG_bengali_conservatism_constitution
				BNG_bengali_conservatism_constitution_civil
				BNG_bengali_conservatism_electoral
				BNG_bengali_conservatism_final
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_the_fight_against_corruption"
			add_stability = 0.05
			add_political_power = 100
		}
		search_filters = { FOCUS_FILTER_POLITICAL FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_the_sensible_approach
		icon = GFX_goal_collaborate_local_elites
		cost = 5

		x = 10
		y = 4

		ai_will_do = { factor = 500 }

		available = {
			OR = {
				has_soccon_or_marlib_government = yes
				has_government = authoritarian_democrat
			}
		}

		mutually_exclusive = {
			focus = BNG_the_republic_of_the_people
			focus = BNG_scion_of_the_revolution
			focus = BNG_nation_of_steel
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_the_sensible_approach"
			if = {
				limit = { has_government = authoritarian_democrat }
				set_temp_variable = { coalition_partner_var = token:market_liberal }
				add_to_coalition = yes
				set_temp_variable = { coalition_partner_var = token:social_conservative }
				add_to_coalition = yes
			}
			else_if = {
				limit = { has_government = market_liberal }
				set_temp_variable = { coalition_partner_var = token:authoritarian_democrat }
				add_to_coalition = yes
				set_temp_variable = { coalition_partner_var = token:social_conservative }
				add_to_coalition = yes
			}
			else = {
				set_temp_variable = { coalition_partner_var = token:market_liberal }
				add_to_coalition = yes
				set_temp_variable = { coalition_partner_var = token:social_conservative }
				add_to_coalition = yes
			}
			add_ideas = BNG_bengali_conservatism
		}
		search_filters = { FOCUS_FILTER_INTERNAL_AFFAIRS FOCUS_FILTER_POLITICAL FOCUS_FILTER_PRODUCTION }
	}

	focus = {
		id = BNG_civil_service_pay_rationalisation
		icon = GFX_goal_generic_civilian_oversight
		cost = 5

		relative_position_id = BNG_the_sensible_approach
		x = -2
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_republic_of_the_people
			focus = BNG_the_sensible_approach
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_civil_service_pay_rationalisation"
			if = {
				limit = { has_idea = BNG_bengali_conservatism_constitution }
				swap_ideas = {
					remove_idea = BNG_bengali_conservatism_constitution
					add_idea = BNG_bengali_conservatism_constitution_civil
				}
			}
			else_if = {
				limit = { has_idea = BNG_bengali_conservatism_electoral }
				swap_ideas = {
					remove_idea = BNG_bengali_conservatism_electoral
					add_idea = BNG_bengali_conservatism_final
				}
			}
			else_if = {
				limit = { has_idea = BNG_bengali_conservatism }
				swap_ideas = {
					remove_idea = BNG_bengali_conservatism
					add_idea = BNG_bengali_conservatism_civil
				}
			}
			else_if = {
				limit = { has_idea = BNG_the_peoples_republic_judicial }
				swap_ideas = {
					remove_idea = BNG_the_peoples_republic_judicial
					add_idea = BNG_the_peoples_republic_judicial_civil
				}
			}
			else_if = {
				limit = { has_idea = BNG_the_peoples_republic_suffrage }
				swap_ideas = {
					remove_idea = BNG_the_peoples_republic_suffrage
					add_idea = BNG_the_peoples_republic_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_the_peoples_republic
					add_idea = BNG_the_peoples_republic_civil
				}
			}
		}
		search_filters = { FOCUS_FILTER_PRODUCTION }
	}

	focus = {
		id = BNG_prioritise_agricultural_exports
		icon = GFX_focus_generic_price_controls
		cost = 5

		relative_position_id = BNG_the_sensible_approach
		x = -1
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_republic_of_the_people
			focus = BNG_the_sensible_approach
		}

		available = {
			OR = {
				has_soccon_or_marlib_government = yes
				has_government = authoritarian_democrat
				has_government = social_liberal
			}
		}
		on_uncomplete = {
			remove_ideas = BNG_prioritise_agricultural_exports
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_prioritise_agricultural_exports"
			add_ideas = BNG_prioritising_agricultural_exports
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_foundations_of_a_new_constitution
		icon = GFX_goal_OTT_constitution
		cost = 5

		relative_position_id = BNG_the_sensible_approach
		x = 0
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_sensible_approach
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_foundations_of_a_new_constitution"
			if = {
				limit = { has_idea = BNG_bengali_conservatism_civil }
				swap_ideas = {
					remove_idea = BNG_bengali_conservatism_civil
					add_idea = BNG_bengali_conservatism_constitution_civil
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_bengali_conservatism
					add_idea = BNG_bengali_conservatism_constitution
				}
			}
		}
		search_filters = { FOCUS_FILTER_POLITICAL FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_electoral_reform_act
		icon = GFX_focus_generic_treaty
		cost = 5

		relative_position_id = BNG_the_sensible_approach
		x = 0
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_foundations_of_a_new_constitution
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_electoral_reform_act"
			if = {
				limit = { has_idea = BNG_bengali_conservatism_constitution_civil }
				swap_ideas = {
					remove_idea = BNG_bengali_conservatism_constitution_civil
					add_idea = BNG_bengali_conservatism_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_bengali_conservatism_constitution
					add_idea = BNG_bengali_conservatism_electoral
				}
			}
		}
		search_filters = { FOCUS_FILTER_POLITICAL FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_nation_of_steel
		icon = GFX_goal_ideology_paternal_autocrat
		cost = 5

		x = 13
		y = 4

		ai_will_do = { factor = 500 }

		available = {
			has_dictatorship_government = yes
		}

		mutually_exclusive = {
			focus = BNG_the_republic_of_the_people
			focus = BNG_the_sensible_approach
			focus = BNG_scion_of_the_revolution
		}

		on_uncomplete = {
			remove_ideas = {
				BNG_the_strongest_authority
				BNG_the_strongest_authority_decree
				BNG_the_strongest_authority_recovery
				BNG_the_strongest_authority_society
				BNG_the_strongest_authority_final
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_nation_of_steel"
			add_ideas = BNG_the_strongest_authority
		}
		search_filters = { FOCUS_FILTER_INTERNAL_AFFAIRS FOCUS_FILTER_ARMY_XP FOCUS_FILTER_WAR_SUPPORT }
	}

	focus = {
		id = BNG_fund_university_of_dacca_expansion
		icon = GFX_focus_generic_military_academy
		cost = 5

		relative_position_id = BNG_nation_of_steel
		x = -2
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_sensible_approach
			focus = BNG_nation_of_steel
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_fund_university_of_dacca_expansion"
			add_research_slot_until_five = yes
		}
		search_filters = { FOCUS_FILTER_RESEARCH }
	}

	focus = {
		id = BNG_crackdown_on_socialist_remnants
		icon = GFX_goal_generic_political_purge
		cost = 5

		relative_position_id = BNG_nation_of_steel
		x = -1
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_sensible_approach
			focus = BNG_nation_of_steel
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_crackdown_on_socialist_remnants"
			add_popularity = { ideology = totalist popularity = -0.05 }
			add_popularity = { ideology = syndicalist popularity = -0.05 }
			add_popularity = { ideology = radical_socialist popularity = -0.05 }
			add_popularity = { ideology = social_democrat popularity = -0.05 }
			add_war_support = 0.025
		}
		search_filters = { FOCUS_FILTER_POLITICAL FOCUS_FILTER_WAR_SUPPORT }
	}

	focus = {
		id = BNG_defence_of_public_order_decree
		icon = GFX_focus_LAT_suspend_constitution_of_latvia
		cost = 5

		relative_position_id = BNG_nation_of_steel
		x = 0
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_nation_of_steel
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_defence_of_public_order_decree"
			swap_ideas = {
				remove_idea = BNG_the_strongest_authority
				add_idea = BNG_the_strongest_authority_decree
			}
		}
		search_filters = { FOCUS_FILTER_POLITICAL FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_furthering_militarisation_of_society
		icon = GFX_focus_generic_full_social_mobilization
		cost = 5

		relative_position_id = BNG_nation_of_steel
		x = -1
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_defence_of_public_order_decree
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_furthering_militarisation_of_society"
			if = {
				limit = { has_idea = BNG_the_strongest_authority_recovery }
				swap_ideas = {
					remove_idea = BNG_the_strongest_authority_recovery
					add_idea = BNG_the_strongest_authority_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_the_strongest_authority_decree
					add_idea = BNG_the_strongest_authority_society
				}
			}
		}
		search_filters = { FOCUS_FILTER_MANPOWER }
	}

	focus = {
		id = BNG_recovery_from_socialism
		icon = GFX_focus_generic_economic_recovery
		cost = 5

		relative_position_id = BNG_nation_of_steel
		x = 1
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_defence_of_public_order_decree
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_recovery_from_socialism"
			if = {
				limit = { has_idea = BNG_the_strongest_authority_society }
				swap_ideas = {
					remove_idea = BNG_the_strongest_authority_society
					add_idea = BNG_the_strongest_authority_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_the_strongest_authority_decree
					add_idea = BNG_the_strongest_authority_recovery
				}
			}
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_begin_economic_privatisation
		icon = GFX_goal_generic_privatization
		cost = 5

		relative_position_id = BNG_nation_of_steel
		x = 1
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_nation_of_steel
		}
		on_uncomplete = {
			remove_ideas = BNG_economic_privatisation
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_begin_economic_privatisation"
			add_ideas = BNG_economic_privatisation
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_a_composite_bengal
		icon = GFX_goal_HND_Hinduism
		cost = 5

		x = 4
		y = 0

		ai_will_do = { factor = 600 }

		available = {
			owns_state = 431
		}

		mutually_exclusive = {
			focus = BNG_the_second_partition
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_a_composite_bengal"
			add_stability = 0.05
			set_capital = { #West Bengal
				state = 431
				remember_old_capital = no
			}
		}
		search_filters = { FOCUS_FILTER_INTERNAL_AFFAIRS FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_reserved_seats_for_hindus
		icon = GFX_goal_SAF_reform_voting_law
		cost = 5

		relative_position_id = BNG_a_composite_bengal
		x = -2
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_a_composite_bengal
		}
		mutually_exclusive = {
			focus = BNG_guaranteeing_muslim_supremacy
		}
		available = {
			OR = {
				has_democratic_government = yes
				has_socialist_government = yes
			}
		}
		on_uncomplete = {
			remove_ideas = {
				BNG_one_nation_two_peoples
				BNG_one_nation_two_peoples_ministry
				BNG_one_nation_two_peoples_final
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_reserved_seats_for_hindus"
			add_ideas = BNG_one_nation_two_peoples
			add_popularity = { ideology = ROOT popularity = 0.05 }
		}
		search_filters = { FOCUS_FILTER_POLITICAL FOCUS_FILTER_STABILITY FOCUS_FILTER_MANPOWER }
	}

	focus = {
		id = BNG_the_shared_ministry
		icon = GFX_focus_RAJ_seek_allies_with_the_muslim_league
		cost = 5

		relative_position_id = BNG_reserved_seats_for_hindus
		x = -1
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_reserved_seats_for_hindus
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_the_shared_ministry"
			swap_ideas = {
				remove_idea = BNG_one_nation_two_peoples
				add_idea = BNG_one_nation_two_peoples_ministry
			}
		}
		search_filters = { FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_scheduled_caste_protections
		icon = GFX_goal_HND_Enforce_Article_15
		cost = 5

		relative_position_id = BNG_reserved_seats_for_hindus
		x = 0
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_reserved_seats_for_hindus
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_scheduled_caste_protections"
			add_stability = 0.025
			add_war_support = 0.025
		}
		search_filters = { FOCUS_FILTER_STABILITY FOCUS_FILTER_WAR_SUPPORT }
	}

	focus = {
		id = BNG_never_another_partition
		icon = GFX_goal_generic_national_unity
		cost = 5

		relative_position_id = BNG_reserved_seats_for_hindus
		x = -1
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_shared_ministry
		}
		prerequisite = {
			focus = BNG_scheduled_caste_protections
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_never_another_partition"
			swap_ideas = {
				remove_idea = BNG_one_nation_two_peoples_ministry
				add_idea = BNG_one_nation_two_peoples_final
			}
		}
		search_filters = { FOCUS_FILTER_STABILITY FOCUS_FILTER_WAR_SUPPORT }
	}

	focus = {
		id = BNG_guaranteeing_muslim_supremacy
		icon = GFX_focus_TUR_loosen_the_laws_on_secularism
		cost = 5

		relative_position_id = BNG_a_composite_bengal
		x = 2
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_a_composite_bengal
		}
		mutually_exclusive = {
			focus = BNG_reserved_seats_for_hindus
		}
		available = {
			OR = {
				has_authoritarian_government = yes
				has_soccon_or_marlib_government = yes
			}
		}
		on_uncomplete = {
			remove_ideas = {
				BNG_islamic_dominance
				BNG_islamic_dominance_officers
				BNG_islamic_dominance_disenfranchise
				BNG_islamic_dominance_final
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_guaranteeing_muslim_supremacy"
			add_ideas = BNG_islamic_dominance
		}
		search_filters = { FOCUS_FILTER_POLITICAL }
	}

	focus = {
		id = BNG_prioritise_muslim_officers
		icon = GFX_focus_generic_military_mission
		cost = 5

		relative_position_id = BNG_guaranteeing_muslim_supremacy
		x = 0
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_guaranteeing_muslim_supremacy
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_prioritise_muslim_officers"
			if = {
				limit = { has_idea = BNG_islamic_dominance_disenfranchise }
				swap_ideas = {
					remove_idea = BNG_islamic_dominance_disenfranchise
					add_idea = BNG_islamic_dominance_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_islamic_dominance
					add_idea = BNG_islamic_dominance_officers
				}
			}
		}
		search_filters = { FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_erosion_of_secularism
		icon = GFX_goal_OTT_Abolish_Mecelle
		cost = 5

		relative_position_id = BNG_guaranteeing_muslim_supremacy
		x = 1
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_guaranteeing_muslim_supremacy
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_erosion_of_secularism"
			add_stability = -0.05
			add_war_support = 0.05
			add_political_power = 75
		}
		search_filters = { FOCUS_FILTER_POLITICAL FOCUS_FILTER_WAR_SUPPORT }
	}

	focus = {
		id = BNG_disenfranchise_hindu_voters
		icon = GFX_focus_ARG_keep_it_in_the_party
		cost = 5

		relative_position_id = BNG_guaranteeing_muslim_supremacy
		x = 2
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_erosion_of_secularism
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_disenfranchise_hindu_voters"
			if = {
				limit = { has_idea = BNG_islamic_dominance_officers }
				swap_ideas = {
					remove_idea = BNG_islamic_dominance_officers
					add_idea = BNG_islamic_dominance_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_islamic_dominance
					add_idea = BNG_islamic_dominance_disenfranchise
				}
			}
		}
		search_filters = { FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_autonomy_for_the_seven_sisters
		icon = GFX_goal_RUS_streamline_autonomies
		cost = 5

		relative_position_id = BNG_guaranteeing_muslim_supremacy
		x = -3
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_reserved_seats_for_hindus
			focus = BNG_guaranteeing_muslim_supremacy
		}
		mutually_exclusive = {
			focus = BNG_bangla_language_law
		}
		available = {
			owns_state = 432 #Assam
			owns_state = 434 #Manipur
		}
		on_uncomplete = {
			remove_ideas = BNG_minority_autonomy
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_autonomy_for_the_seven_sisters"
			add_ideas = BNG_minority_autonomy
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_bangla_language_law
		icon = GFX_goal_constitutional_guarantees
		cost = 5

		relative_position_id = BNG_guaranteeing_muslim_supremacy
		x = -1
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_reserved_seats_for_hindus
			focus = BNG_guaranteeing_muslim_supremacy
		}
		mutually_exclusive = {
			focus = BNG_autonomy_for_the_seven_sisters
		}
		available = {
			owns_state = 432 #Assam
			owns_state = 434 #Manipur
		}
		on_uncomplete = {
			remove_ideas = BNG_our_only_tongue
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_bangla_language_law"
			add_ideas = BNG_our_only_tongue
		}
		search_filters = { FOCUS_FILTER_WAR_SUPPORT }
	}

	focus = {
		id = BNG_wealth_of_the_northeast
		icon = GFX_focus_generic_mining_industry
		cost = 5

		relative_position_id = BNG_guaranteeing_muslim_supremacy
		x = -2
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_autonomy_for_the_seven_sisters
			focus = BNG_bangla_language_law
		}
		available = {
			432 = { is_owned_and_controlled_by = ROOT } #Assam
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_wealth_of_the_northeast"
			add_resource = {
				type = tungsten
				amount = 10
				state = 432 #Assam
			}
			add_resource = {
				type = oil
				amount = 3
				state = 432 #Assam
			}
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_the_second_partition
		icon = GFX_focus_RAJ_two_nation_theory
		cost = 5

		x = 11
		y = 0

		ai_will_do = { factor = 600 }

		available = {
			NOT = { owns_state = 431 }
		}

		mutually_exclusive = {
			focus = BNG_a_composite_bengal
		}
		on_uncomplete = {
			drop_cosmetic_tag = yes
			set_party_name = {
				ideology = syndicalist
				name = BNG_syndicalist_party
				long_name = BNG_syndicalist_party_long
			}
			set_party_name = {
				ideology = radical_socialist
				name = BNG_radical_socialist_party
				long_name = BNG_radical_socialist_party_long
			}
			set_party_name = {
				ideology = paternal_autocrat
				name = BNG_paternal_autocrat_party
				long_name = BNG_paternal_autocrat_party_long
			}
			set_party_name = {
				ideology = national_populist
				name = BNG_national_populist_party
				long_name = BNG_national_populist_party_long
			}
			remove_ideas = {
				BNG_amar_sonar_bangla
				BNG_amar_sonar_bangla_economy
				BNG_amar_sonar_bangla_secular
				BNG_amar_sonar_bangla_secular_economy
				BNG_amar_sonar_bangla_islam
				BNG_amar_sonar_bangla_islam_economy
				PJB_resettling_indian_muslims
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_the_second_partition"
			set_cosmetic_tag = BNG_bangladesh
			mark_focus_tree_layout_dirty = yes
			hidden_effect = {
				set_party_name = {
					ideology = syndicalist
					name = BNG_bangladesh_syndicalist_party
					long_name = BNG_bangladesh_syndicalist_party_long
				}
				set_party_name = {
					ideology = radical_socialist
					name = BNG_bangladesh_radical_socialist_party
					long_name = BNG_bangladesh_radical_socialist_party_long
				}
				set_party_name = {
					ideology = paternal_autocrat
					name = BNG_bangladesh_paternal_autocrat_party
					long_name = BNG_bangladesh_paternal_autocrat_party_long
				}
				set_party_name = {
					ideology = national_populist
					name = BNG_bangladesh_national_populist_party
					long_name = BNG_bangladesh_national_populist_party_long
				}
			}
			add_ideas = BNG_amar_sonar_bangla
		}
		search_filters = { FOCUS_FILTER_INTERNAL_AFFAIRS FOCUS_FILTER_POLITICAL FOCUS_FILTER_WAR_SUPPORT FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_replacing_lost_industry
		icon = GFX_focus_CHL_enlarge_the_textile_industries
		cost = 5

		relative_position_id = BNG_the_second_partition
		x = 0
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_second_partition
		}
		available = {
			430 = { is_owned_and_controlled_by = ROOT } #East Bengal
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_replacing_lost_industry"
			add_one_random_civilian_factory = yes
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_chittagong_steel_mills
		icon = GFX_focus_generic_steel
		cost = 5

		relative_position_id = BNG_the_second_partition
		x = -1
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_replacing_lost_industry
		}
		available = {
			430 = { is_owned_and_controlled_by = ROOT } #East Bengal
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_chittagong_steel_mills"
			add_resource = {
				type = steel
				amount = 10
				state = 430 #East Bengal
			}
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_economic_protection_act
		icon = GFX_goal_state_economy
		cost = 5

		relative_position_id = BNG_the_second_partition
		x = 1
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_replacing_lost_industry
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_economic_protection_act"
			if = {
				limit = { has_idea = BNG_amar_sonar_bangla_secular }
				swap_ideas = {
					remove_idea = BNG_amar_sonar_bangla_secular
					add_idea = BNG_amar_sonar_bangla_secular_economy
				}
			}
			else_if = {
				limit = { has_idea = BNG_amar_sonar_bangla_islam }
				swap_ideas = {
					remove_idea = BNG_amar_sonar_bangla_islam
					add_idea = BNG_amar_sonar_bangla_islam_economy
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_amar_sonar_bangla
					add_idea = BNG_amar_sonar_bangla_economy
				}
			}
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_a_secular_constitution
		icon = GFX_focus_PER_forced_secularization
		cost = 5

		relative_position_id = BNG_the_second_partition
		x = -1
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_second_partition
		}
		mutually_exclusive = {
			focus = BNG_officialisation_of_islam
		}
		available = {
			OR = {
				has_democratic_government = yes
				has_socialist_government = yes
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_a_secular_constitution"
			if = {
				limit = { has_idea = BNG_amar_sonar_bangla_economy }
				swap_ideas = {
					remove_idea = BNG_amar_sonar_bangla_economy
					add_idea = BNG_amar_sonar_bangla_secular_economy
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_amar_sonar_bangla
					add_idea = BNG_amar_sonar_bangla_secular
				}
			}
		}
		search_filters = { FOCUS_FILTER_STABILITY }
	}

	focus = {
		id = BNG_officialisation_of_islam
		icon = GFX_goal_islamic_government
		cost = 5

		relative_position_id = BNG_the_second_partition
		x = -3
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_second_partition
		}
		mutually_exclusive = {
			focus = BNG_a_secular_constitution
		}
		available = {
			OR = {
				has_authoritarian_government = yes
				has_soccon_or_marlib_government = yes
			}
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_officialisation_of_islam"
			if = {
				limit = { has_idea = BNG_amar_sonar_bangla_economy }
				swap_ideas = {
					remove_idea = BNG_amar_sonar_bangla_economy
					add_idea = BNG_amar_sonar_bangla_islam_economy
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_amar_sonar_bangla
					add_idea = BNG_amar_sonar_bangla_islam
				}
			}
		}
		search_filters = { FOCUS_FILTER_POLITICAL }
	}

	focus = {
		id = BNG_resettling_indian_refugees
		icon = GFX_goal_refugee_management
		cost = 5

		relative_position_id = BNG_the_second_partition
		x = 1
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_second_partition
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_resettling_indian_refugees"
			add_timed_idea = {
				idea = PJB_resettling_indian_muslims
				days = 365
			}
		}
		search_filters = { FOCUS_FILTER_MANPOWER FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_growth_of_dacca
		icon = GFX_ITA_urban
		cost = 5

		relative_position_id = BNG_the_second_partition
		x = 2
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_resettling_indian_refugees
		}
		available = {
			430 = { is_owned_and_controlled_by = ROOT } #East Bengal
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_growth_of_dacca"
			430 = { #East Bengal
				increase_state_category_by_one_level = yes
			}
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	### Armed forces branches ###

	focus = {
		id = BNG_in_war_in_peace
		icon = GFX_goal_BNG_army
		cost = 5

		x = 20
		y = 0

		ai_will_do = { factor = 100 }

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_in_war_in_peace"
			add_ideas = BNG_ever_high_is_my_head
		}
		search_filters = { FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_specialise_our_doctrines
		icon = GFX_goal_generic_army_high_command
		cost = 5

		relative_position_id = BNG_in_war_in_peace
		x = -2
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_in_war_in_peace
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_specialise_our_doctrines"
			add_doctrine_cost_reduction = {
				name = BNG_specialise_our_doctrines
				cost_reduction = 0.5
				uses = 2
				category = land_doctrine
			}
		}
		search_filters = { FOCUS_FILTER_RESEARCH }
	}

	focus = {
		id = BNG_rifle_recovery_efforts
		icon = GFX_focus_ETH_scavenging_tactics
		cost = 5

		relative_position_id = BNG_in_war_in_peace
		x = -2
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_specialise_our_doctrines
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_rifle_recovery_efforts"
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 2000
			}
		}
		search_filters = { FOCUS_FILTER_PRODUCTION }
	}

	focus = {
		id = BNG_reorganising_the_bengal_regiment
		icon = GFX_focus_RAJ_indianisation_of_army
		cost = 5

		relative_position_id = BNG_in_war_in_peace
		x = 0
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_in_war_in_peace
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_reorganising_the_bengal_regiment"
			swap_ideas = {
				remove_idea = BNG_ever_high_is_my_head
				add_idea = BNG_ever_high_is_my_head_regiment
			}
		}
		search_filters = { FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_protectors_of_the_nation
		icon = GFX_goal_generic_army_mass_assault
		cost = 5

		relative_position_id = BNG_in_war_in_peace
		x = -1
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_reorganising_the_bengal_regiment
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_protectors_of_the_nation"
			if = {
				limit = { has_idea = BNG_ever_high_is_my_head_artillery }
				swap_ideas = {
					remove_idea = BNG_ever_high_is_my_head
					add_idea = BNG_ever_high_is_my_head_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_ever_high_is_my_head_regiment
					add_idea = BNG_ever_high_is_my_head_protectors
				}
			}
		}
		search_filters = { FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_strengthen_the_artillery_corp
		icon = GFX_focus_RAJ_mountain_guns
		cost = 5

		relative_position_id = BNG_in_war_in_peace
		x = 0
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_reorganising_the_bengal_regiment
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_strengthen_the_artillery_corp"
			if = {
				limit = { has_idea = BNG_ever_high_is_my_head_protectors }
				swap_ideas = {
					remove_idea = BNG_ever_high_is_my_head_protectors
					add_idea = BNG_ever_high_is_my_head_final
				}
			}
			else = {
				swap_ideas = {
					remove_idea = BNG_ever_high_is_my_head_regiment
					add_idea = BNG_ever_high_is_my_head_artillery
				}
			}
		}
		search_filters = { FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_expand_the_pioneers
		icon = GFX_focus_SMB_special_forces_option_1
		cost = 5

		relative_position_id = BNG_in_war_in_peace
		x = 1
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_in_war_in_peace
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_expand_the_pioneers"
			enable_tech_expanded_engineer_corps = yes
		}
		search_filters = { FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_tigers_of_the_jungle
		icon = GFX_focus_SWE_en_svensk_tiger
		cost = 5

		relative_position_id = BNG_in_war_in_peace
		x = 1
		y = 4

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_expand_the_pioneers
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_tigers_of_the_jungle"
			enable_tech_jungle_warfare = yes
		}
		search_filters = { FOCUS_FILTER_ARMY_BONUS }
	}

	focus = {
		id = BNG_the_free_skies_of_bengal
		icon = GFX_goal_BNG_airforce
		cost = 5

		x = 16
		y = 0

		ai_will_do = { factor = 100 }

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_the_free_skies_of_bengal"
			add_ideas = BNG_skies_of_bengal
		}
		search_filters = { FOCUS_FILTER_PRODUCTION FOCUS_FILTER_AIRFORCE_BONUS }
	}

	focus = {
		id = BNG_developing_dacca_airfields
		icon = GFX_goal_generic_airports
		cost = 5

		relative_position_id = BNG_the_free_skies_of_bengal
		x = 0
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_the_free_skies_of_bengal
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_developing_dacca_airfields"
			random_owned_controlled_state = {
				prioritize = { 430 }
				limit = {
					is_core_of = ROOT
					impassable = no
					free_building_slots = { building = air_base size > 1 }
				}
				add_building_construction = {
					type = air_base
					level = 2
					instant_build = yes
				}
			}
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_purchasing_old_bombers
		icon = GFX_focus_SMB_purchase_aircraft
		cost = 5

		relative_position_id = BNG_the_free_skies_of_bengal
		x = -1
		y = 2

		ai_will_do = { factor = 100 }

		available = {
			is_subject = yes
			if = {
				limit = {
					is_subject = yes
				}
				overlord = { has_design_based_on = medium_plane_airframe }
			}
		}

		prerequisite = {
			focus = BNG_developing_dacca_airfields
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_purchasing_old_bombers"
			if = {
				limit = {
					is_subject = yes
					overlord = { has_tech = iw_medium_airframe }
				}
				add_equipment_to_stockpile = {
					type = medium_plane_airframe
					amount = 25
					producer = overlord
				}
			}
		}
		search_filters = { FOCUS_FILTER_PRODUCTION }
	}

	focus = {
		id = BNG_air_land_cooperation
		icon = GFX_focus_generic_armored_air_support
		cost = 5

		relative_position_id = BNG_the_free_skies_of_bengal
		x = 1
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_developing_dacca_airfields
		}
		prerequisite = {
			focus = BNG_specialise_our_doctrines
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_air_land_cooperation"
			swap_ideas = {
				remove_idea = BNG_skies_of_bengal
				add_idea = BNG_skies_of_bengal_cooperation
			}
		}
		search_filters = { FOCUS_FILTER_AIRFORCE_BONUS }
	}

	focus = {
		id = BNG_invincible_at_sea
		icon = GFX_goal_BNG_navy
		cost = 5

		x = 22
		y = 0

		ai_will_do = { factor = 100 }

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_invincible_at_sea"
			add_ideas = BNG_invincible_at_sea_idea
		}
		search_filters = { FOCUS_FILTER_PRODUCTION }
	}

	focus = {
		id = BNG_defending_the_bay
		icon = GFX_goal_generic_naval_destroyer2
		cost = 5

		relative_position_id = BNG_invincible_at_sea
		x = 0
		y = 1

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_invincible_at_sea
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_defending_the_bay"
			swap_ideas = {
				remove_idea = BNG_invincible_at_sea_idea
				add_idea = BNG_invincible_at_sea_idea_bay
			}
		}
		search_filters = { FOCUS_FILTER_NAVY_BONUS }
	}

	focus = {
		id = BNG_creating_bns_chittagong
		icon = GFX_goal_generic_naval_base
		cost = 5

		relative_position_id = BNG_invincible_at_sea
		x = 1
		y = 2

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_defending_the_bay
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_creating_bns_chittagong"
			random_owned_controlled_state = {
				prioritize = { 430 }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = dockyard
					level = 1
					instant_build = yes
				}
			}
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}

	focus = {
		id = BNG_sundarbans_fortifications
		icon = GFX_focus_generic_coastal_fort
		cost = 5

		relative_position_id = BNG_invincible_at_sea
		x = 0
		y = 3

		ai_will_do = { factor = 100 }

		prerequisite = {
			focus = BNG_defending_the_bay
		}
		prerequisite = {
			focus = BNG_expand_the_pioneers
		}

		completion_reward = {
			log = "[GetLogRoot]: Focus Completed BNG_sundarbans_fortifications"
			every_owned_state = {
				limit = {
					is_controlled_by = ROOT
					is_in_home_area = yes
					is_coastal = yes
				}
				custom_override_tooltip = {
					add_building_construction = {
						type = coastal_bunker
						province = {
							all_provinces = yes
							limit_to_coastal = yes
						}
						level = 2
						instant_build = yes
					}
					tooltip = {
						localization_key = tooltip_add_building_on_coastal_provinces
						BUILDING = coastal_bunker
						NUMBER = 2
					}
				}
			}
		}
		search_filters = { FOCUS_FILTER_INDUSTRY }
	}
}
