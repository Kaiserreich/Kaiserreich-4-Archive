goals_only_faction_leader = {
	type = member_rules
	trigger = {
		is_faction_leader = yes
	}
	can_remove = {
		always = no
	}
}

goals_only_faction_leader_or_player = {
	type = member_rules
	trigger = {
		OR = {
			is_faction_leader = yes
			is_ai = no
		}
	}
	can_remove = {
		always = no
	}
}
