faction_manifest_asia_pacific_hegemony = {
	name = faction_manifest_asia_pacific_hegemony_name
	description = faction_manifest_asia_pacific_hegemony_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = {
			input = collection:asian_and_pacific_countries
			operators = {
				limit = { NOT = { tag = JAP } }
			}
		}
		completed_amount_collection = {
			input = collection:subject_asian_and_pacific_countries
			operators = {
				limit = { NOT = { tag = JAP } }
			}
		}
		scale = {
			cic_to_overlord_factor = 0.15
			mic_to_overlord_factor = 0.15
			resources_to_overlord_factor = 0.15
		}

		progress_sections = {
			manifest_fulfilled = {
				min = 0.75

				modifier = {
					justify_war_goal_time = -0.25
					extra_trade_to_overlord_factor = 0.20
				}
			}
		}
	}
}
