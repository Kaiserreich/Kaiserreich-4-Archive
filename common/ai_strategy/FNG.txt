FNG_dont_attack_owner_of_beijing_immediately_after_conference = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		has_country_flag = FNG_beijing_war_declaration_cooldown
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = avoid_starting_wars
		value = -400
	}
}

FNG_call_in_japan_if_allied = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		is_in_faction_with = JAP
		JAP = { is_subject = no }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = alliance
		id = JAP
		value = 200
	}
	ai_strategy = {
		type = befriend
		id = JAP
		value = 200
	}

	ai_strategy = {
		type = diplo_action_desire
		id = JAP
		target = join_allies
		value = 1000
	}
	ai_strategy = {
		type = diplo_action_acceptance
		id = JAP
		target = call_allies
		value = 1000
	}
}

FNG_build_army = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = ai_wanted_divisions_factor
		value = 100
	}
}

FNG_elite_army = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		has_tech = tech_mountaineers
		is_in_faction = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = role_ratio
		id = mountaineer
		value = 15
	}
}

FNG_kill_QIE = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		is_neighbor_of = QIE
		NOT = { is_in_faction_with = QIE }
		OR = {
			AND = {
				has_country_flag = FNG_preparing_war_of_national_reclamation
				focus_progress = {
					focus = FNG_Begin_the_War_of_National_Reclamation
					progress < 0
				}
			}
			AND = {
				focus_progress = {
					focus = FNG_Begin_the_War_of_National_Reclamation
					progress > 0
				}
				NOT = { has_completed_focus = FNG_Begin_the_War_of_National_Reclamation }
			}
			AND = {
				QIE = {
					focus_progress = {
						focus = QIE_cross_the_dai_river
						progress > 0
					}
					NOT = { has_completed_focus = QIE_cross_the_dai_river }
				}
			}
			AND = {
				QIE = {
					focus_progress = {
						focus = QIE_false_republic
						progress > 0
					}
					NOT = { has_completed_focus = QIE_false_republic }
				}
			}
			has_wargoal_against = QIE
			QIE = { has_wargoal_against = FNG }
			has_war_with = QIE
			QIE = { has_idea = QIE_zhili_manchu_war_idea }
			QIE = { has_active_mission = QIE_eyes_on_the_border }
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = conquer
		id = QIE
		value = 400
	}
	ai_strategy = {
		type = front_unit_request
		tag = QIE
		value = 100
	}
	ai_strategy = {
		type = front_unit_request
		country_trigger = {
			NOT = { has_war_with = FNG }
			NOT = { is_subject_of = QIE }
			NOT = { original_tag = QIE } #FNG should put units on QIE at peace in preparation for the war
		}
		value = -100
	}
}

FNG_take_beijing = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		has_war_with = QIE
		is_neighbor_of = QIE
		608 = { is_owned_and_controlled_by = QIE }
		has_country_flag = FNG_war_began_three_months
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = QIE
		priority = 100
		ordertype = front
		execution_type = rush_weak
		execute_order = yes
	}
}

FNG_not_friendly_to_other_cliques = {
	reversed = yes

	enable = {
		FNG_JAP_hostile = no
		NOT = { country_exists = CUF }
		NOT = { is_ally_with = JAP }
		NOT = { is_subject_of = FNG }
	}
	abort_when_not_enabled = yes

	target_array = global.china_tags_array

	ai_strategy = {
		type = antagonize
		id = FNG
		value = 1000
	}
}

FNG_aifc_linear_defence = {
	allowed = { original_tag = FNG }
	enable = {
		tag = FNG
		surrender_progress > 0.02
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = force_concentration_factor
		value = -100
	}
}
FNG_basic_production = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		num_of_military_factories > 5
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = infantry_equipment
		value = 2
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = artillery_equipment
		value = 2
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = support_equipment
		value = 1
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = anti_air_equipment
		value = -2
	}
}
FNG_basic_production_2 = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		num_of_military_factories > 10
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = infantry_equipment
		value = 3
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = artillery_equipment
		value = 2
	}
}
FNG_basic_production_3 = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		num_of_military_factories > 15
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = infantry_equipment
		value = 2
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = artillery_equipment
		value = 2
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = support_equipment
		value = 1
	}
}
FNG_acquire_mountaineer_for_reclamation_buff = {
	allowed = {
		original_tag = FNG
	}
	enable = {
		NOT = { has_tech = tech_mountaineers }
		date > 1937.5.1
	}

	abort = {
		has_tech = tech_mountaineers
	}

	ai_strategy = {
		type = research_tech
		id = tech_mountaineers
		value = 1000
	}

	ai_strategy = {
		type = research_weight_factor
		id = tech_mountaineers
		value = 1000
	}
}
FNG_expand_610_airbase = {
	allowed = {
		is_chinese_or_potential_chinese_tag = yes
	}
	enable = {
		controls_state = 610
		is_ally_with = JAP
		608 = { controller = { has_war_with = JAP } }
		OR = {
			612 = { controller = { has_war_with = JAP } }
			611 = { controller = { has_war_with = JAP } }
		}
		610 = {
			air_base < 2
			OR = {
				free_building_slots = { building = air_base size > 8 }
				check_variable = { building_level@air_base < 2 }
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 610
		value = 2
	}
}
FNG_expand_1048_airbase = {
	allowed = {
		is_chinese_or_potential_chinese_tag = yes
	}
	enable = {
		controls_state = 1048
		is_ally_with = JAP
		OR = {
			661 = { controller = { has_war_with = JAP } }
			1013 = { controller = { has_war_with = JAP } }
			602 = { controller = { has_war_with = JAP } }
		}
		1048 = {
			air_base < 3
			OR = {
				free_building_slots = { building = air_base size > 7 }
				check_variable = { building_level@air_base < 3 }
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 1048
		value = 3
	}
}
FNG_expand_799_airbase = {
	allowed = {
		is_chinese_or_potential_chinese_tag = yes
	}
	enable = {
		controls_state = 799
		is_ally_with = JAP
		OR = {
			622 = { controller = { has_war_with = JAP } }
			661 = { controller = { has_war_with = JAP } }
			650 = { controller = { has_war_with = JAP } }
		}
		799 = {
			air_base < 3
			OR = {
				free_building_slots = { building = air_base size > 7 }
				check_variable = { building_level@air_base < 3 }
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 799
		value = 3
	}
}
FNG_expand_602_airbase = {
	allowed = {
		is_chinese_or_potential_chinese_tag = yes
	}
	enable = {
		controls_state = 602
		is_ally_with = JAP
		OR = {
			1055 = { controller = { has_war_with = JAP } }
			1052 = { controller = { has_war_with = JAP } }
			650 = { controller = { has_war_with = JAP } }
		}
		602 = {
			air_base < 3
			OR = {
				free_building_slots = { building = air_base size > 8 }
				check_variable = { building_level@air_base < 2 }
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 602
		value = 2
	}
}
FNG_expand_600_airbase = {
	allowed = {
		is_chinese_or_potential_chinese_tag = yes
	}
	enable = {
		controls_state = 600
		is_ally_with = JAP
		OR = {
			592 = { controller = { has_war_with = JAP } }
			800 = { controller = { has_war_with = JAP } }
			599 = { controller = { has_war_with = JAP } }
		}
		600 = {
			air_base < 3
			OR = {
				free_building_slots = { building = air_base size > 8 }
				check_variable = { building_level@air_base < 2 }
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 600
		value = 2
	}
}
FNG_expand_609_airbase = {
	allowed = {
		is_chinese_or_potential_chinese_tag = yes
	}
	enable = {
		controls_state = 609
		is_ally_with = JAP
		609 = {
			air_base < 3
			OR = {
				free_building_slots = { building = air_base size > 8 }
				check_variable = { building_level@air_base < 2 }
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 609
		value = 3
	}
}

FNG_expand_614_airbase = {
	allowed = {
		is_chinese_or_potential_chinese_tag = yes
	}
	enable = {
		controls_state = 614
		is_ally_with = JAP
		614 = {
			air_base < 3
			OR = {
				free_building_slots = { building = air_base size > 8 }
				check_variable = { building_level@air_base < 2 }
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 614
		value = 3
	}
}
FNG_expand_1068_airbase = {
	allowed = {
		is_chinese_or_potential_chinese_tag = yes
	}
	enable = {
		controls_state = 1068
		is_ally_with = JAP
		1068 = {
			air_base < 3
			OR = {
				free_building_slots = { building = air_base size > 8 }
				check_variable = { building_level@air_base < 2 }
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 1068
		value = 3
	}
}
