CHI_WCA_joint_attack_on_japan = {
	allowed = {
		OR = {
			original_tag = CHI
			original_tag = WCA
		}
	}
	enable = {
		has_wargoal_against = {
			target = JAP
			type = topple_government
		}
		surrender_progress < 0.05
		any_country_of = {
			target = { CHI WCA }
			has_war_with = JAP
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = conquer
		id = JAP
		value = 200
	}
	ai_strategy = {
		type = declare_war
		id = JAP
		value = 500 #strategy >= 500 forces declaration
	}
}
CHI_army_composition_1 = {
	allowed = { original_tag = CHI }
	enable = {
		OR = {
			has_war_with = JAP
			date < 1941.1.1
		}
		check_variable = { num_divisions < 60 }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = role_ratio
		id = mass_infantry
		value = 125
	}
	ai_strategy = {
		type = role_ratio
		id = light_infantry
		value = 125
	}
	ai_strategy = {
		type = role_ratio
		id = infantry
		value = -100
	}
	ai_strategy = {
		type = building_target
		id = arms_factory
		value = 30
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = infantry_equipment
		value = 20
	}
	ai_strategy = {
		type = equipment_production_min_factories
		id = infantry
		value = -10
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = support_equipment
		value = -10
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = artillery_equipment
		value = -10
	}
	ai_strategy = {
		type = equipment_production_min_factories_archetype
		id = anti_air_equipment
		value = -10
	}
	ai_strategy = {
		type = equipment_variant_production_factor
		id = support_equipment
		value = -50
	}
}
CHI_army_composition_2 = {
	allowed = { original_tag = CHI }
	enable = {
		OR = {
			has_war_with = JAP
			date < 1941.1.1
		}
		check_variable = { num_divisions > 59 }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = role_ratio
		id = light_infantry
		value = 100
	}

	ai_strategy = {
		type = role_ratio
		id = infantry
		value = 100
	}
}
CHI_build_military_factories = {
	allowed = {
		original_tag = CHI
	}
	enable = {
		num_of_military_factories < 30
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = building_target
		id = arms_factory
		value = 30
	}
}
CHI_infantry_equipment_modernisation = {
	allowed = {
		original_tag = CHI
	}
	enable = {
		date > 1937.1.1
		NOT = { has_tech = improved_infantry_weapons }
	}

	abort = {
		has_tech = improved_infantry_weapons
	}

	ai_strategy = {
		type = research_weight_factor
		id = infantry_weapons2
		value = 2000
	}

	ai_strategy = {
		type = research_weight_factor
		id = improved_infantry_weapons
		value = 20000
	}
}
CHI_attack_anqing = {
	allowed = {
		original_tag = CHI
	}
	enable = {
		has_war_with = ANQ
		NOT = { has_global_flag = LEP_league_war_over }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = ANQ

		ratio = 0.0

		priority = 200
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
}

CHI_attack_league = {
	allowed = {
		original_tag = CHI
	}
	enable = {
		has_war_with = LEP
		NOT = { has_global_flag = LEP_league_war_over }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = LEP

		ratio = 0.0

		priority = 200
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
}
CHI_attack_shandong = {
	allowed = {
		original_tag = CHI
	}
	enable = {
		has_war_with = SHD
		NOT = { has_global_flag = LEP_league_war_over }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = SHD

		ratio = 0.0

		priority = 200
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
}
CHI_focus_on_active_front = {
	allowed = {
		original_tag = CHI
	}
	enable = {
		has_war = yes
		has_imminent_war = no
		NOT = { any_enemy_country = { is_major = yes } }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = garrison
		value = -9999
	}

	ai_strategy = {
		type = front_unit_request
		country_trigger = {
			has_war_with = CHI
			is_neighbor_of = CHI
		}
		state_trigger = {
			OR = {
				is_core_of = CHI
				is_claimed_by = CHI
				is_owned_by = CHI
			}
		}
		value = 50
	}
}

CHI_freeze_china_conflicts_before_SSJW = {
	allowed = {
		original_tag = CHI
	}
	enable = {
		date > 1939.1.1
		NOT = { has_war_with = JAP }
		NOT = { has_global_flag = JAP_defeated_by_china }
		check_variable = { deployed_army_manpower_k > 0 }
		set_temp_variable = { CHI_casualties_ratio = casualties_k }
		divide_temp_variable = { CHI_casualties_ratio = deployed_army_manpower_k }
		check_variable = { CHI_casualties_ratio > 0.7 }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			NOT = { tag = FNG }
		}
		ratio = 0.0
		priority = 100
		ordertype = front
		execution_type = careful
	}
}
CHI_you_should_be_prepared_for_war = {
	allowed = {
		tag = CHI
	}
	enable = {
		has_war = no
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = force_build_armies
		value = 100
	}
	ai_strategy = {
		type = ai_wanted_divisions_factor
		value = 100
	}
}
SOCRUS_support_LKMT_if_FER_is_in_COPRO = {			#Far Eastern Army only joins CoPro under certain conditions, Socialist Russia should retaliate by supporting China
	allowed = {
		tag = RUS					#CHI will return land to Russia anyway
	}
	enable = {
		has_socialist_government = yes
		NOT = { has_war_with = CHI }
		CHI = {
			has_war_with = FER
			has_war_with = JAP
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = send_lend_lease_desire
		id = CHI
		value = 500
	}
}
