next_internationale_leader_scorer = {
	targets = {
		target_array = faction_members

		target_trigger = {
			has_capitulated = no
			is_actual_major = yes
		}

		score = {
			base = 100
			modifier = {
				factor = 6
				FROM = { tag = ENG }
			}
			modifier = {
				factor = 5
				FROM = { tag = CSA }
			}
			modifier = {
				factor = 4
				FROM = { tag = SOV }
			}
			modifier = {
				set_temp_variable = { ROOT.strength_score@FROM = 0 }
				all_of_scopes = {
					array = FROM.owned_states
					add_to_temp_variable = { ROOT.strength_score@FROM = THIS.arms_factory_level }
					add_to_temp_variable = { ROOT.strength_score@FROM = THIS.industrial_complex_level }
				}
				add_to_temp_variable = { ROOT.strength_score@FROM = FROM.num_divisions }

				add = ROOT.strength_score@FROM
			}
		}
	}
}
