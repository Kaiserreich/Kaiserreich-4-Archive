###########################################
# Republic of Italy Decisions by Herkles
###########################################

political_actions = {
	RUS_expand_ambition_poland = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					region = 38 #Poland
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_poland"
			set_country_flag = RUS_expanded_ambition_poland
			every_state = {
				limit = {
					region = 38 #Poland
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_poland = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_poland
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_poland"
			clr_country_flag = RUS_expanded_ambition_poland
			every_state = {
				limit = {
					region = 38 #Poland
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}

	RUS_expand_ambition_hungary = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					region = 27 #Hungary
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_hungary"
			set_country_flag = RUS_expanded_ambition_hungary
			every_state = {
				limit = {
					region = 27 #Hungary
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_hungary = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_hungary
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_hungary"
			clr_country_flag = RUS_expanded_ambition_hungary
			every_state = {
				limit = {
					region = 27 #Hungary
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}

	RUS_expand_ambition_eastgermany = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					region = 8 #Eastern Germany
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_eastgermany"
			set_country_flag = RUS_expanded_ambition_eastgermany
			every_state = {
				limit = {
					region = 8 #Eastern Germany
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_eastgermany = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_eastgermany
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_eastgermany"
			clr_country_flag = RUS_expanded_ambition_eastgermany
			every_state = {
				limit = {
					region = 8 #Eastern Germany
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}

	RUS_expand_ambition_czech = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					region = 22 #Bohemia
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_czech"
			set_country_flag = RUS_expanded_ambition_czech
			every_state = {
				limit = {
					region = 22 #Bohemia
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_czech = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_czech
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_czech"
			clr_country_flag = RUS_expanded_ambition_czech
			every_state = {
				limit = {
					region = 22 #Bohemia
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}

	RUS_expand_ambition_eastbalk = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					region = 26 #Eastern Balkans
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_eastbalk"
			set_country_flag = RUS_expanded_ambition_eastbalk
			every_state = {
				limit = {
					region = 26 #Eastern Balkans
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_eastbalk = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_eastbalk
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_eastbalk"
			clr_country_flag = RUS_expanded_ambition_eastbalk
			every_state = {
				limit = {
					region = 26 #Eastern Balkans
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}

	RUS_expand_ambition_westbalk = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					region = 24 #Western Balkans
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_westbalk"
			set_country_flag = RUS_expanded_ambition_westbalk
			every_state = {
				limit = {
					region = 24 #Western Balkans
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_westbalk = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_westbalk
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_westbalk"
			clr_country_flag = RUS_expanded_ambition_westbalk
			every_state = {
				limit = {
					region = 24 #Western Balkans
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}

	RUS_expand_ambition_greece = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					region = 25 #Greece
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_greece"
			set_country_flag = RUS_expanded_ambition_greece
			every_state = {
				limit = {
					region = 25 #Greece
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_greece = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_greece
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_greece"
			clr_country_flag = RUS_expanded_ambition_greece
			every_state = {
				limit = {
					region = 25 #Greece
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}

	RUS_expand_ambition_asiaminor = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					region = 129 #Asia Minor
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_asiaminor"
			set_country_flag = RUS_expanded_ambition_asiaminor
			every_state = {
				limit = {
					region = 129 #Asia Minor
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_asiaminor = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_asiaminor
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_asiaminor"
			clr_country_flag = RUS_expanded_ambition_asiaminor
			every_state = {
				limit = {
					region = 129 #Asia Minor
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}

	RUS_expand_ambition_persia = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					region = 116 #Persia
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_persia"
			set_country_flag = RUS_expanded_ambition_persia
			every_state = {
				limit = {
					region = 116 #Persia
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_persia = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_persia
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_persia"
			clr_country_flag = RUS_expanded_ambition_persia
			every_state = {
				limit = {
					region = 116 #Persia
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}

	RUS_expand_ambition_scandinavia = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			any_of_scopes = {
				array = controlled_states
				THIS = {
					OR = {
						region = 10 #Sweden
						region = 11 #Norway
						region = 12 #Northern Scandinavia
					}
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
			}
		}
		visible = {
			is_ai = yes
			has_war = yes
			is_subject = no
			is_actual_major = yes
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_expand_ambition_scandinavia"
			set_country_flag = RUS_expanded_ambition_scandinavia
			every_state = {
				limit = {
					OR = {
						region = 10 #Sweden
						region = 11 #Norway
						region = 12 #Northern Scandinavia
					}
					owner = { has_war_with = ROOT }
					NOT = { is_owned_by = ROOT }
					NOT = { has_variable = ambition_area_@ROOT }
				}
				set_variable = { ambition_area_@ROOT = 1 }
			}
		}
	}

	RUS_end_ambition_scandinavia = {
		icon = generic_political_discourse
		allowed = {
			OR = {
				tag = RUS
				tag = SOV
			}
		}
		available = {
			has_country_flag = RUS_expanded_ambition_scandinavia
		}
		visible = {
			is_ai = yes
			has_war = no
		}
		fire_only_once = no
		cost = freedec?0
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RUS_end_ambition_scandinavia"
			clr_country_flag = RUS_expanded_ambition_scandinavia
			every_state = {
				limit = {
					OR = {
						region = 10 #Sweden
						region = 11 #Norway
						region = 12 #Northern Scandinavia
					}
					has_variable = ambition_area_@ROOT
				}
				clear_variable = ambition_area_@ROOT
			}
		}
	}
}

