political_actions = {
	HNN_Join_Faction = {
		icon = generic_flags
		
		allowed = {
			tag = HNN
		}

		visible = {
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
		}

		targets = {	QIE GXC CHI }
		target_root_trigger = {
			is_in_faction = no
		}
		target_trigger = {
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
			if = {
				limit = { has_government = authoritarian_democrat }
				FROM = { tag = QIE }
			}
			else_if = {
				limit = { has_government = social_conservative }
				FROM = { tag = GXC }
			}
			else_if = {
				limit = { has_government = social_democrat }
				FROM = { tag = CHI }
			}
		}

		cost = freedec?0
		fire_only_once = yes

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				FROM = {
					surrender_progress > 0.2
				}
			}
			modifier = {
				factor = 0
				has_war = yes
				FROM = {
					has_war = yes
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HNN_Join_Faction target: [From.GetName]"
			FROM = {
				country_event = hnn.2
			}
		}
	}
}
