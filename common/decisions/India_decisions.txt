
political_actions = {
	DEH_restore_order_india1 = {
		icon = generic_flags

		allowed = {
			OR = {
				tag = DEH
				tag = BHC
				tag = PRF
			}
		}

		available = {
			ROOT = { has_stability > 0 }
			FROM = { is_fully_controlled_by = ROOT }
		}

		visible = {
			FROM = {
				is_owned_and_controlled_by = ROOT
				NOT = { is_core_of = ROOT }
				OR = {
					is_actual_island_state = yes
					any_neighbor_state = {
						is_owned_and_controlled_by = ROOT
						is_core_of = ROOT
					}
				}
			}
		}

		on_map_mode = map_and_decisions_view
		cost = 25
		days_remove = 60
		fire_only_once = no

		state_target = yes
		targets = { 281 321 445 733 858 888 897 1095 }
		target_trigger = {
			FROM = {
				is_owned_and_controlled_by = ROOT
				NOT = { is_core_of = ROOT }
			}
		}

		cancel_if_not_visible = yes
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancelled DEH_restore_order_india1 target: [From.GetName]"
			add_political_power = 25
			add_stability = 0.02
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision DEH_restore_order_india1 target: [From.GetName]"
			add_stability = -0.02
			FROM = {
				if = {
					limit = {
						NOT = {
							state = 858 #Tawang
							state = 733 #Andaman Islands
							state = 321 #Goa
							state = 281 #Maldives
						}
					}
					add_resistance = 40
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove DEH_restore_order_india1 target: [From.GetName]"
			add_stability = 0.01
			FROM = {
				if = {
					limit = {
						NOT = {
							state = 858 #Tawang
							state = 733 #Andaman Islands
							state = 321 #Goa
							state = 281 #Maldives
						}
					}
					add_resistance = -40
				}
				if = {
					limit = {
						is_core_of = BHC
						ROOT = { NOT = { tag = BHC } }
					}
					remove_core_of = BHC
					add_claim_by = BHC
				}
				if = {
					limit = {
						is_core_of = DEH
						ROOT = { NOT = { tag = DEH } }
					}
					remove_core_of = DEH
					add_claim_by = DEH
				}
				if = {
					limit = {
						is_core_of = PRF
						ROOT = { NOT = { tag = PRF } }
					}
					remove_core_of = PRF
					add_claim_by = PRF
				}
				if = {
					limit = { is_claimed_by = ROOT }
					remove_claim_by = ROOT
				}
				add_core_of = ROOT
			}
		}

		ai_will_do = {
			factor = 100
		}
	}

	DEH_restore_order_india2 = {
		icon = generic_flags

		allowed = {
			OR = {
				tag = DEH
				tag = BHC
				tag = PRF
			}
		}

		available = {
			ROOT = { has_stability > 0 }
			FROM = { is_fully_controlled_by = ROOT }
		}

		visible = {
			FROM = {
				is_owned_and_controlled_by = ROOT
				NOT = { is_core_of = ROOT }
				OR = {
					is_actual_island_state = yes
					any_neighbor_state = {
						is_owned_and_controlled_by = ROOT
						is_core_of = ROOT
					}
				}
			}
		}

		on_map_mode = map_and_decisions_view
		cost = 50
		days_remove = 90
		fire_only_once = no

		state_target = yes
		targets = { 423 424 425 426 428 432 433 434 435 436 437 438 440 441 442 443 444 814 815 867 868 887 889 890 893 1034 1057 1092 1093 1094 }
		target_trigger = {
			FROM = {
				is_owned_and_controlled_by = ROOT
				NOT = { is_core_of = ROOT }
			}
		}

		cancel_if_not_visible = yes
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancelled DEH_restore_order_india2 target: [From.GetName]"
			add_political_power = 50
			add_stability = 0.04
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision DEH_restore_order_india2 target: [From.GetName]"
			FROM = { add_resistance = 40 }
			add_stability = -0.04
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove DEH_restore_order_india2 target: [From.GetName]"
			add_stability = 0.02
			FROM = {
				add_resistance = -40
				if = {
					limit = {
						is_core_of = BHC
						ROOT = { NOT = { tag = BHC } }
					}
					remove_core_of = BHC
					add_claim_by = BHC
				}
				if = {
					limit = {
						is_core_of = DEH
						ROOT = { NOT = { tag = DEH } }
					}
					remove_core_of = DEH
					add_claim_by = DEH
				}
				if = {
					limit = {
						is_core_of = PRF
						ROOT = { NOT = { tag = PRF } }
					}
					remove_core_of = PRF
					add_claim_by = PRF
				}
				if = {
					limit = { is_claimed_by = ROOT }
					remove_claim_by = ROOT
				}
				add_core_of = ROOT
			}
		}

		ai_will_do = {
			factor = 100
		}
	}

	DEH_restore_order_india3 = {
		icon = generic_flags

		allowed = {
			OR = {
				tag = DEH
				tag = BHC
				tag = PRF
			}
		}

		available = {
			ROOT = { has_stability > 0 }
			FROM = { is_fully_controlled_by = ROOT }
		}

		visible = {
			FROM = {
				is_owned_and_controlled_by = ROOT
				NOT = { is_core_of = ROOT }
				OR = {
					is_actual_island_state = yes
					any_neighbor_state = {
						is_owned_and_controlled_by = ROOT
						is_core_of = ROOT
					}
				}
			}
		}

		on_map_mode = map_and_decisions_view
		cost = 100
		days_remove = 120
		fire_only_once = no

		state_target = yes
		targets = { 422 427 429 430 431 439 891 }
		target_trigger = {
			FROM = {
				is_owned_and_controlled_by = ROOT
				NOT = { is_core_of = ROOT }
			}
		}

		cancel_if_not_visible = yes
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancelled DEH_restore_order_india3 target: [From.GetName]"
			add_political_power = 100
			add_stability = 0.06
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision DEH_restore_order_india3 target: [From.GetName]"
			FROM = { add_resistance = 40 }
			add_stability = -0.06
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove DEH_restore_order_india3 target: [From.GetName]"
			add_stability = 0.03
			FROM = {
				add_resistance = -40
				if = {
					limit = {
						is_core_of = BHC
						ROOT = { NOT = { tag = BHC } }
					}
					remove_core_of = BHC
					add_claim_by = BHC
				}
				if = {
					limit = {
						is_core_of = DEH
						ROOT = { NOT = { tag = DEH } }
					}
					remove_core_of = DEH
					add_claim_by = DEH
				}
				if = {
					limit = {
						is_core_of = PRF
						ROOT = { NOT = { tag = PRF } }
					}
					remove_core_of = PRF
					add_claim_by = PRF
				}
				if = {
					limit = { is_claimed_by = ROOT }
					remove_claim_by = ROOT
				}
				add_core_of = ROOT
			}
		}

		ai_will_do = {
			factor = 100
		}
	}

	DEH_join_the_fray = {
		icon = generic_ignite_civil_war
		
		allowed = {
			OR = {
				tag = DEH
				tag = BHC
				tag = PRF
			}
		}
		
		available = {
			ROOT = {
				controls_all_owned_states = yes
				can_declare_war_on = FROM
			}
		}
		
		visible = {
			country_exists = DEH
			country_exists = BHC
			country_exists = PRF
			if = {
				limit = { tag = DEH }
				BHC = { has_war_with = PRF }
				NOT = { has_war_with = BHC }
				NOT = { has_war_with = PRF }
			}
			else_if = {
				limit = { tag = BHC }
				DEH = { has_war_with = PRF }
				NOT = { has_war_with = DEH }
				NOT = { has_war_with = PRF }
			}
			else_if = {
				limit = { tag = PRF }
				BHC = { has_war_with = DEH }
				NOT = { has_war_with = BHC }
				NOT = { has_war_with = DEH }
			}
		}
		
		target_root_trigger = {
			has_army_manpower = { size < 300000 }
		}

		targets = { DEH BHC PRF }

		target_trigger = {
			can_declare_war_on = FROM
		}
		
		fire_only_once = yes
		days_remove = 30
		war_with_target_on_remove = yes

		cost = 0
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision DEH_join_the_fray target: [From.GetName]"
			set_country_flag = { flag = DEH_impending_war days = 35 value = 1 }
			add_war_support = 0.10
			FROM = { country_event = kr.political.30 } #warning event
			setup_decision_attack_AI = yes
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove DEH_join_the_fray target: [From.GetName]"
			clear_decision_attack_AI = yes
			if = {
				limit = {
					NOT = { has_war_with = FROM }
				}
				declare_war_on = {
					target = FROM
					type = annex_everything
				}
			}
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_country_flag = DEH_impending_war
			}
			modifier = {
				factor = 0
				OR = {
					surrender_progress > 0
					any_enemy_country = {
						OR = {
							is_major = yes
							is_neighbor_of = ROOT
						}
					}
				}
			}
			modifier = {
				factor = 0
				strength_ratio = { tag = FROM ratio < 0.60 }
			}
			modifier = {
				factor = 0
				FROM = { surrender_progress > 0.60 }
			}
			modifier = {
				factor = 0
				FROM = {
					any_enemy_country = {
						OR = {
							tag = PRF
							tag = BHC
							tag = DEH
						}
						surrender_progress > 0.60
					}
				}
			}
			modifier = {
				factor = 0
				NOT = {
					BHC = { surrender_progress > 0.05 }
					DEH = { surrender_progress > 0.05 }
					PRF = { surrender_progress > 0.05 }
				}
			}
			modifier = {
				factor = 3
				FROM = { tag = BHC }
			}
			modifier = {
				factor = 0.75
				FROM = { is_in_faction = yes }
				is_in_faction = no
			}
			modifier = {
				factor = 0.5
				FROM = { surrender_progress > 0.25 }
			}
			modifier = {
				factor = 1.5
				FROM = {
					OR = {
						has_war_with = MDR
						has_war_with = MYS
					}
				}
			}
			modifier = {
				factor = 0.75
				tag = DEH
				FROM = {
					tag = PRF
					strength_ratio = { tag = BHC ratio < 1.0 }
				}
			}
			modifier = {
				factor = 0.75
				tag = DEH
				FROM = {
					tag = BHC
					strength_ratio = { tag = PRF ratio < 1.0 }
				}
			}
			modifier = {
				factor = 0.75
				tag = PRF
				FROM = {
					tag = DEH
					strength_ratio = { tag = BHC ratio < 1.0 }
				}
			}
			modifier = {
				factor = 0.75
				tag = PRF
				FROM = {
					tag = BHC
					strength_ratio = { tag = DEH ratio < 1.0 }
				}
			}
			modifier = {
				factor = 0.75
				tag = BHC
				FROM = {
					tag = DEH
					strength_ratio = { tag = PRF ratio < 1.0 }
				}
			}
			modifier = {
				factor = 0.75
				tag = BHC
				FROM = {
					tag = PRF
					strength_ratio = { tag = DEH ratio < 1.0 }
				}
			}
		}
	}
}

