MIE_decisions = {
	icon = GFX_decision_category_generic_independence
	allowed = {
		OR = {
			tag = BAT
			tag = LAT
			tag = EST
			tag = LIT
			tag = FIN
			tag = POL
			tag = WHR
			tag = UKR
			tag = DKB
			tag = AZR
			tag = GEO
			# Add anyone else who may join here
		}
	}
	visible = {
		is_in_faction_with = MIE
		is_in_faction_with = GER
		OR = {
			RUS = { is_faction_leader = yes }
			SOV = { has_completed_focus = SOV_Spreading_The_Revolution }
		}
	}
}
