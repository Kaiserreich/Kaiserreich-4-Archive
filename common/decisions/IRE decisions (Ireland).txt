 #############################################################
 # Ireland Decisions                      					 #
 #############################################################
 # By El Daddy, Zimbabwe Salt Co., Klyntar King & Cazadorian #
 #############################################################
 # Special thanks to The Irredentista     					 #
 #############################################################

 ### CONTENTS ###
 # Economy
 # Black Monday
 # Military
 # Foreign Policy
 # Gateway to the Atlantic
 # Political

 ### Economy Decisions ###

IRE_economy_decisions = {
	IRE_finish_bm = {
		icon = GFX_decision_eng_trade_unions_demand

		allowed = { always = no }
		priority = 100

		available = {
			custom_trigger_tooltip = {
				tooltip = IRE_has_bm_NOT
				IRE_has_bm = no
			}
		}

		is_good = no
		selectable_mission = no
		days_mission_timeout = 540

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_finish_bm"
			effect_tooltip = {
				add_political_power = 50
				add_stability = 0.10
				set_temp_variable = { ideology_change_var = 0.05 }
				change_ruling_party_popularity = yes
				custom_effect_tooltip = tooltip_white_line
				IRE_finish_bm_reward = yes
			}
			hidden_effect = { country_event = ireland.44 }
		}

		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = IRE_can_finish_bm
				is_subject = yes
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_finish_bm"
			hidden_effect = { country_event = ireland.46 }
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_finish_bm"
			effect_tooltip = {
				add_political_power = -50
				add_stability = -0.10
				set_temp_variable = { ideology_change_var = -0.05 }
				change_ruling_party_popularity = yes
			}
			hidden_effect = { country_event = ireland.45 }
		}
	}
	IRE_finish_bm_lab = {
		icon = GFX_decision_generic_construction

		allowed = { always = no }
		priority = 40

		available = {
			custom_trigger_tooltip = {
				IRE_has_bm = yes
				tooltip = IRE_has_bm
			}
			has_full_control_of_state = FROM
			custom_trigger_tooltip = {
				NOT = { has_country_flag = IRE_finish_bm_lab_ongoing }
				tooltip = IRE_finish_bm_lab_ongoing_NOT
			}
			if = {
				limit = {
					FROM = { has_state_flag = { flag = IRE_finish_bm_lab_done value = 1 } }
					has_country_flag = { flag = IRE_finish_bm_lab_total value < 4 }
				}
				custom_trigger_tooltip = {
					tooltip = IRE_finish_bm_lab_done
					has_country_flag = { flag = IRE_finish_bm_lab_total value > 3 }
				}
			}
		}

		visible = {
			hidden_trigger = {
				has_full_control_of_state = FROM
				IRE_has_bm = yes
			}
		}

		fire_only_once = no
		state_target = yes

		days_remove = IRE_finish_bm_lab_days
		cancel_if_not_visible = yes

		modifier = {
			custom_modifier_tooltip = IRE_finish_bm_lab_tt
		}

		ai_will_do = { base = 100000 }

		ai_hint_pp_cost = 50
		custom_cost_trigger = {
			has_political_power > 49
			has_manpower > 999
		}
		custom_cost_text = IRE_finish_bm_lab_cost

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_finish_bm_lab target: [From.GetName]"
			add_manpower = -1000
			hidden_effect = {
				set_country_flag = IRE_finish_bm_lab_ongoing
				add_political_power = -50
				FROM = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.1
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_finish_bm_lab target: [From.GetName]"
			add_manpower = 1000
			add_political_power = 50
			hidden_effect = {
				clr_country_flag = IRE_finish_bm_lab_ongoing
				FROM = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.1
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_finish_bm_lab target: [From.GetName]"
			IRE_improve_bm_twice = yes
			custom_effect_tooltip = IRE_finish_bm_lab_effect_tt
			add_manpower = 1000
			hidden_effect = {
				add_to_variable = { IRE_finish_bm_lab_duration = 2 }
				IRE_finish_bm_lab_days_calc = yes
				FROM = { set_state_flag = { flag = IRE_finish_bm_lab_state value = 1 } }
				country_event = ireland.1502
				clr_country_flag = IRE_finish_bm_lab_ongoing
				FROM = { set_state_flag = { flag = IRE_finish_bm_lab_done value = 1 } }
				modify_country_flag = { flag = IRE_finish_bm_lab_total value = 1 }
				FROM = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.1
						}
					}
				}
			}
		}
	}
	IRE_finish_bm_scndp = {
		icon = GFX_decision_POL_looming_peasants_strike

		allowed = { always = no }
		priority = 100

		available = {
			hidden_trigger = { IRE_has_bm = no }
		}

		is_good = yes
		selectable_mission = no
		days_mission_timeout = IRE_finish_bm_scndp_duration

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_finish_bm_scndp"
		}

		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = IRE_can_finish_bm
				is_subject = yes
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_finish_bm_scndp"
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_finish_bm_scndp"
			IRE_remove_bm_spirit = yes
		}
	}
	IRE_finish_bm_sf = {
		icon = GFX_decision_gre_paying_ifc_debt

		allowed = { always = no }
		available = { hidden_trigger = { always = no } }
		priority = 100

		selectable_mission = no
		days_mission_timeout = IRE_finish_bm_sf_time

		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = IRE_can_finish_bm
				is_subject = yes
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_finish_bm_sf"
			hidden_effect = {
				clear_variable = IRE_finish_bm_taxes_days
			}
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_finish_bm_sf"
			if = {
				limit = { has_idea = IRE_balanced_budget_1 }
				remove_ideas = IRE_balanced_budget_1
			}
			else = { remove_ideas = IRE_balanced_budget_2 }
			clear_variable = IRE_finish_bm_taxes_days
		}
	}
	IRE_finish_bm_sf_taxes = {
		icon = GFX_decision_hand_grab
		allowed = { original_tag = IRE }

		visible = { has_active_mission = IRE_finish_bm_sf }

		cost = 75
		ai_will_do = { factor = 500 }
		days_remove = 60
		modifier = { stability_factor = -0.1 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_finish_bm_sf_taxes"
			add_days_mission_timeout = {
				mission = IRE_finish_bm_sf
				days = IRE_finish_bm_taxes_days
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_finish_bm_sf_taxes"
			multiply_variable = { IRE_finish_bm_taxes_days = 0.75 }
		}
	}
	IRE_finish_bm_sf_spending = {
		icon = GFX_decision_generic_fundraising
		allowed = { original_tag = IRE }

		visible = { has_active_mission = IRE_finish_bm_sf }

		cost = 75
		fire_only_once = yes
		days_remove = 60
		ai_will_do = { factor = 500 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_finish_bm_sf_spending"
			swap_ideas = {
				remove_idea = IRE_balanced_budget_1
				add_idea = IRE_balanced_budget_2
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_finish_bm_sf_spending"
			swap_ideas = {
				remove_idea = IRE_balanced_budget_2
				add_idea = IRE_balanced_budget_1
			}
			add_days_mission_timeout = {
				mission = IRE_finish_bm_sf
				days = -200
			}
		}
	}
	IRE_poulaphouca_reservoir = {
		icon = GFX_decision_cement_mixer

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		cancel_trigger = {
			if = {
				limit = { has_country_flag = IRE_gateway_GER_dam_benefactor }
				custom_trigger_tooltip = {
					tooltip = IRE_liffey_hydro_GER_tt
					has_socialist_government = yes
				}
			}
			else_if = {
				limit = { has_country_flag = IRE_gateway_ENG_dam_benefactor }
				custom_trigger_tooltip = {
					tooltip = IRE_liffey_hydro_ENG_tt
					is_in_faction = yes
					has_socialist_government = no
				}
			}
			else = {
				hidden_trigger = { always = no }
			}
		}

		highlight_states = {
			highlight_state_targets = { state = 113 }
		}

		is_good = yes
		days_mission_timeout = 120

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_poulaphouca_reservoir"
			clr_country_flag = IRE_gateway_GER_dam_benefactor
			clr_country_flag = IRE_gateway_ENG_dam_benefactor
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_poulaphouca_reservoir"
			113 = {
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = industrial_complex
					level = 2
					instant_build = yes
				}
			}
			clr_country_flag = IRE_gateway_GER_dam_benefactor
			clr_country_flag = IRE_gateway_ENG_dam_benefactor
		}
	}
	### Shared Eco decisions ###
	IRE_cork_steel_works = {
		icon = GFX_decision_steel

		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
			has_full_control_of_state = 135
			has_tech = excavation1
		}

		cost = 30
		fire_only_once = yes
		state_target = yes

		ai_will_do = {
			factor = 30
			modifier = {
				factor = 0
				NOT = {
					has_completed_focus = IRE_sugar_beet
					has_completed_focus = IRE_ferguson_tractors_focus
				}
			}
		}

		modifier = { civilian_factory_use = 2 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_cork_steel_works target: [From.GetName]"
			add_resource = {
				type = steel
				amount = 4
				state = 135
			}
		}
	}
	IRE_exploit_northern_bauxite = {
		icon = GFX_decision_aluminium

		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
			has_full_control_of_state = 119
			has_tech = excavation1
			if = {
				limit = { FROM = { is_demilitarized_zone = yes } }
				custom_trigger_tooltip = {
					tooltip = IRE_ni_demil		# only here because vanilla tooltip is dodgy af
					FROM = { is_demilitarized_zone = no }
				}
			}
		}

		cancel_trigger = {
			hidden_trigger = {
				FROM = { is_demilitarized_zone = yes }
			}
		}

		cost = 30
		fire_only_once = yes
		state_target = yes

		ai_will_do = {
			factor = 30
			modifier = {
				factor = 0
				NOT = {
					has_completed_focus = IRE_sugar_beet
					has_completed_focus = IRE_ferguson_tractors_focus
				}
			}
		}

		modifier = { civilian_factory_use = 2 }

		days_remove = 60
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_exploit_northern_bauxite target: [From.GetName]"
			hidden_effect = {
				add_political_power = 30
				activate_targeted_decision = { target = 119 decision = IRE_exploit_northern_bauxite }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_exploit_northern_bauxite target: [From.GetName]"
			add_resource = {
				type = aluminium
				amount = 8
				state = 119
			}
		}
	}
	IRE_leinster_coal_mines = {
		icon = GFX_decision_generic_construction

		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
			has_full_control_of_state = 113
			has_tech = excavation1
		}

		cost = 30
		fire_only_once = yes
		state_target = yes

		ai_will_do = {
			factor = 30
			modifier = {
				factor = 0
				NOT = {
					has_completed_focus = IRE_sugar_beet
					has_completed_focus = IRE_ferguson_tractors_focus
				}
			}
		}

		modifier = { civilian_factory_use = 2 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_leinster_coal_mines target: [From.GetName]"
			add_resource = {
				type = steel
				amount = 3
				state = 113
			}
		}
	}
	IRE_connacht_coal = {
		icon = GFX_decision_generic_construction
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
			has_full_control_of_state = 134
			has_tech = excavation1
		}

		cost = 30
		fire_only_once = yes
		state_target = yes

		ai_will_do = {
			factor = 30
			modifier = {
				factor = 0
				NOT = {
					has_completed_focus = IRE_sugar_beet
					has_completed_focus = IRE_ferguson_tractors_focus
				}
			}
		}

		modifier = { civilian_factory_use = 2 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_connacht_coal target: [From.GetName]"
			add_resource = {
				type = steel
				amount = 2
				state = 134
			}
		}
	}
	IRE_aer_lingus_decision = {
		icon = GFX_decision_generic_merge_plant_aircraft
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 135
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_aer_lingus_decision target: [From.GetName]"
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = arms_factory level = 1 }
			}
			custom_effect_tooltip = tooltip_white_line
			if = {
				limit = { has_dlc_aat = no }
				custom_effect_tooltip = available_designer
				show_ideas_tooltip = aer_lingus
			}
			else = {
				custom_effect_tooltip = available_mio_tt
				custom_effect_tooltip = IRE_aer_lingus_mio_tt
				mio:IRE_aer_lingus_organisation = { add_mio_size = 2 }
			}
		}
	}
	IRE_textile_subsidies = {
		icon = GFX_decision_generic_industry
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 135
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_textile_subsidies target: [From.GetName]"
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}
	}
	IRE_guinness_brewery = {
		icon = GFX_decision_generic_factory
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 113
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_guinness_brewery target: [From.GetName]"
			113 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}
	}
	IRE_midleton_distillery = {
		icon = GFX_decision_generic_factory
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 134
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_midleton_distillery target: [From.GetName]"
			134 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}
	}
	### Industrialisation ###
	IRE_wexford_agricultural_equipment = {
		icon = GFX_decision_generic_agriculture
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 113
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_wexford_agricultural_equipment target: [From.GetName]"
			113 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}

	}
	IRE_ni_furniture_makers = {
		icon = GFX_decision_hol_attract_foreign_investors
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 119
			if = {
				limit = { FROM = { is_demilitarized_zone = yes } }
				custom_trigger_tooltip = {
					tooltip = IRE_ni_demil		# only here because vanilla tooltip is dodgy af
					FROM = { is_demilitarized_zone = no }
				}
			}
		}

		cancel_trigger = {
			hidden_trigger = {
				FROM = { is_demilitarized_zone = yes }
			}
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_ni_furniture_makers target: [From.GetName]"
			hidden_effect = {
				add_political_power = 50
				activate_targeted_decision = { target = 119 decision = IRE_ni_furniture_makers }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_ni_furniture_makers target: [From.GetName]"
			119 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}

	}
	IRE_harland_wolff_expansion = {
		icon = GFX_decision_generic_merge_plant_ship
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 3
			has_full_control_of_state = 119
			has_country_flag = IRE_harland_wolff_renovated
			if = {
				limit = { FROM = { is_demilitarized_zone = yes } }
				custom_trigger_tooltip = {
					tooltip = IRE_ni_demil		# only here because vanilla tooltip is dodgy af
					FROM = { is_demilitarized_zone = no }
				}
			}
		}

		cancel_trigger = {
			hidden_trigger = {
				FROM = { is_demilitarized_zone = yes }
			}
		}

		cost = 80
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 100 }

		modifier = { civilian_factory_use = 4 }

		days_remove = 60
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_harland_wolff_expansion target: [From.GetName]"
			hidden_effect = {
				set_country_flag = IRE_harland_wolff_expanding
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_harland_wolff_expansion target: [From.GetName]"
			hidden_effect = {
				add_political_power = 75
				activate_targeted_decision = { target = 119 decision = IRE_harland_wolff_expansion }
				clr_country_flag = IRE_harland_wolff_expanding
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_harland_wolff_expansion target: [From.GetName]"
			119 = {
				add_extra_state_shared_building_slots = 3
				add_building_construction = { type = dockyard level = 1 }
				add_building_construction = { type = arms_factory level = 2 }
			}
			custom_effect_tooltip = tooltip_white_line
			if = {
				limit = { has_dlc_aat = no }
				custom_effect_tooltip = available_designer
				custom_effect_tooltip = IRE_harland_wolff_2_tt
				custom_effect_tooltip = IRE_harland_wolff_3_tt
				custom_effect_tooltip = tooltip_white_line
			}
			else = {
				mio:IRE_harland_wolff_expanded_organisation = { add_mio_size = 3 }
				custom_effect_tooltip = IRE_harland_wolff_expanded_traits_tt
				custom_effect_tooltip = tooltip_white_line
			}
			hidden_effect = {
				clr_country_flag = IRE_harland_wolff_expanding
				set_country_flag = IRE_harland_wolff_expanded
			}
		}
	}
	IRE_cork_car_factory = {
		icon = GFX_decision_generic_motorized
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 135
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_cork_car_factory target: [From.GetName]"
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}

	}
	IRE_ferguson_tractors = {
		icon = GFX_decision_generic_trucks
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 134
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_ferguson_tractors target: [From.GetName]"
			134 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}

	}
	IRE_ferguson_mil_factory = {
		icon = GFX_decision_generic_merge_plant_tank
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 113
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_ferguson_mil_factory target: [From.GetName]"
			113 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = arms_factory level = 1 }
			}
		}

	}
	IRE_carlow_sugar_beet_factory = {
		icon = GFX_decision_factories
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 113
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_carlow_sugar_beet_factory target: [From.GetName]"
			113 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}

	}
	IRE_thurles_sugar_beet_factory = {
		icon = GFX_decision_factories
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 135
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_thurles_sugar_beet_factory target: [From.GetName]"
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}
	}
	IRE_electrification = {
		icon = GFX_decision_generic_electricity

		allowed = { always = no }
		priority = 40

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = FROM
			if = {
				limit = { FROM = { is_demilitarized_zone = yes } }
				custom_trigger_tooltip = {
					tooltip = IRE_ni_demil		# only here because vanilla tooltip is dodgy af
					FROM = { is_demilitarized_zone = no }
				}
			}
		}

		cancel_trigger = {
			hidden_trigger = {
				FROM = { is_demilitarized_zone = yes }
			}
		}

		visible = {
			hidden_trigger = {
				has_full_control_of_state = FROM
			}
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		cancel_if_not_visible = yes

		modifier = { civilian_factory_use = 3 }

		ai_will_do = {
			base = 20
			modifier = {
				factor = 0
				FROM = {
					free_building_slots = { building = industrial_complex size > 0 include_locked = yes }
					free_building_slots = { building = arms_factory size > 0 include_locked = yes }
				}
			}
		}

		days_remove = 60

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_electrification target: [From.GetName]"
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_electrification target: [From.GetName]"
			if = {
				limit = { FROM = { is_demilitarized_zone = yes } }
				hidden_effect = {
					add_political_power = 50
					activate_targeted_decision = { target = 119 decision = IRE_electrification }
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_electrification target: [From.GetName]"
			FROM = { add_extra_state_shared_building_slots = 1 }
		}
	}
	### Copy of 1941 decisions for Bluesshirt Coup ###
	IRE_subsidies_for_yeoman_farmers_blueshirt = {
		icon = GFX_decision_POL_looming_peasants_strike
		name = IRE_subsidies_for_yeoman_farmers
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
			has_full_control_of_state = 134
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 2 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_subsidies_for_yeoman_farmers_blueshirt target: [From.GetName]"
			134 = { increase_state_category_by_one_level = yes }
			unlock_decision_tooltip = IRE_preparing_the_fields
			set_country_flag = IRE_farmer_subsidies_given_blueshirt
		}
	}

	IRE_preparing_the_fields_blueshirt = {
		icon = GFX_decision_generic_agriculture
		name = IRE_preparing_the_fields
		allowed = { original_tag = IRE }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 134
			has_full_control_of_state = 135
		}

		visible = { has_country_flag = IRE_farmer_subsidies_given_blueshirt }

		cost = 40
		fire_only_once = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 60
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_preparing_the_fields_blueshirt"
			134 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			unlock_decision_tooltip = IRE_reaping_what_we_sowed_blueshirt
			set_country_flag = IRE_fields_prepared_blueshirt
			clr_country_flag = IRE_farmer_subsidies_given_blueshirt
		}
	}

	IRE_reaping_what_we_sowed_blueshirt = {
		icon = GFX_decision_POL_organize_strike_two
		name = IRE_reaping_what_we_sowed
		allowed = { original_tag = IRE }

		available = {
			has_full_control_of_state = 134
			has_full_control_of_state = 135
		}

		visible = { has_country_flag = IRE_fields_prepared_blueshirt }

		cost = 40
		fire_only_once = yes

		ai_will_do = { factor = 50 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_reaping_what_we_sowed_blueshirt"
			134 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			clr_country_flag = IRE_fields_prepared_blueshirt
		}
	}
	IRE_gateway_ENG_construction_leinster_socialist = {
		icon = GFX_decision_generic_construction
		name = IRE_gateway_ENG_construction_leinster
		allowed = { always = no }

		available = {
			has_full_control_of_state = 113
			custom_trigger_tooltip = {
				check_variable = { IRE_gateway_ENG_construction_ongoing < 2 }
				tooltip = IRE_gateway_ENG_construction_ongoing_NOT
			}
		}

		visible = { hidden_trigger = { is_in_faction_with = ENG } }

		fire_only_once = yes
		highlight_states = {
			highlight_state_targets = { state = 113 }
		}

		days_remove = 180
		cancel_if_not_visible = yes

		modifier = {
			custom_modifier_tooltip = IRE_gateway_ENG_construction_tt		# If editing any of these, be sure to edit the loc tooltip aswell
		}

		ai_will_do = { base = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_ENG_construction_leinster_socialist"
			hidden_effect = {
				add_to_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				113 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.25
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_ENG_construction_leinster_socialist"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				113 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_ENG_construction_leinster_socialist"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				113 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}
	}
	IRE_gateway_ENG_construction_northern_ireland_socialist = {
		icon = GFX_decision_generic_construction
		name = IRE_gateway_ENG_construction_northern_ireland
		allowed = { always = no }

		available = {
			has_full_control_of_state = 119
			custom_trigger_tooltip = {
				check_variable = { IRE_gateway_ENG_construction_ongoing < 2 }
				tooltip = IRE_gateway_ENG_construction_ongoing_NOT
			}
		}

		visible = { hidden_trigger = { is_in_faction_with = ENG } }

		fire_only_once = yes
		highlight_states = {
			highlight_state_targets = { state = 119 }
		}

		days_remove = 180
		cancel_if_not_visible = yes

		modifier = {
			custom_modifier_tooltip = IRE_gateway_ENG_construction_tt
		}

		ai_will_do = { base = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_ENG_construction_northern_ireland_socialist"
			hidden_effect = {
				add_to_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				119 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.25
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_ENG_construction_northern_ireland_socialist"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				119 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_ENG_construction_northern_ireland_socialist"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				119 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}
	}
	IRE_gateway_ENG_construction_munster_socialist = {
		icon = GFX_decision_generic_construction
		name = IRE_gateway_ENG_construction_munster
		allowed = { always = no }

		available = {
			has_full_control_of_state = 135
			custom_trigger_tooltip = {
				check_variable = { IRE_gateway_ENG_construction_ongoing < 2 }
				tooltip = IRE_gateway_ENG_construction_ongoing_NOT
			}
		}

		visible = { hidden_trigger = { is_in_faction_with = ENG } }

		fire_only_once = yes
		highlight_states = {
			highlight_state_targets = { state = 135 }
		}

		days_remove = 180
		cancel_if_not_visible = yes

		modifier = {
			custom_modifier_tooltip = IRE_gateway_ENG_construction_tt
		}

		ai_will_do = { base = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_ENG_construction_munster_socialist"
			hidden_effect = {
				add_to_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				135 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.25
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_ENG_construction_munster_socialist"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				135 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_ENG_construction_munster_socialist"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				135 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}
	}
	IRE_gateway_ENG_construction_connacht_socialist = {
		icon = GFX_decision_generic_construction
		name = IRE_gateway_ENG_construction_connacht
		allowed = { always = no }

		available = {
			has_full_control_of_state = 134
			custom_trigger_tooltip = {
				check_variable = { IRE_gateway_ENG_construction_ongoing < 2 }
				tooltip = IRE_gateway_ENG_construction_ongoing_NOT
			}
		}

		visible = { hidden_trigger = { is_in_faction_with = ENG } }

		fire_only_once = yes
		highlight_states = {
			highlight_state_targets = { state = 134 }
		}

		days_remove = 180
		cancel_if_not_visible = yes

		modifier = {
			custom_modifier_tooltip = IRE_gateway_ENG_construction_tt
		}

		ai_will_do = { base = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_ENG_construction_connacht_socialist"
			hidden_effect = {
				add_to_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				134 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.25
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_ENG_construction_connacht_socialist"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				134 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_ENG_construction_connacht_socialist"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				134 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}
	}
}

 ### Black Monday Decisions ###

IRE_bm_decisions = {
	IRE_UCF_mission = {
		icon = GFX_decision_generic_political_address

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 180

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_UCF_mission"
			country_event = ireland.12
			clr_country_flag = IRE_UCF_ongoing
		}
	}
	IRE_UCF_meet_orange_order = {
		icon = GFX_decision_generic_agreement
		allowed = { always = no }		# Unlocked by focus IRE_bail_out_northern_industries

		available = {
			has_completed_focus = IRE_organise_northern_relief_work
		}

		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = IRE_UCF_ongoing_NOT
				NOT = { has_country_flag = IRE_UCF_ongoing }
			}
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_UCF_meet_orange_order target: [From.GetName]"
			add_political_power = 55
		}

		cost = 55
		fire_only_once = yes
		state_target = yes
		ai_will_do = { factor = 1000 }

		days_remove = 14
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_UCF_meet_orange_order target: [From.GetName]"
			custom_effect_tooltip = IRE_bm_ucf_improve_north_tt
			add_to_variable = { IRE_UCF_north = 1 }
		}
	}
	IRE_UCF_meet_loyalist_leaders = {
		icon = GFX_decision_give_cash
		allowed = { always = no }		# Unlocked by focus IRE_bail_out_northern_industries

		available = {
			has_completed_focus = IRE_subsidies_for_northern_landowners
		}

		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = IRE_UCF_ongoing_NOT
				NOT = { has_country_flag = IRE_UCF_ongoing }
			}
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_UCF_meet_loyalist_leaders target: [From.GetName]"
			add_political_power = 55
		}

		cost = 55
		fire_only_once = yes
		state_target = yes
		ai_will_do = { factor = 1000 }

		days_remove = 14
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_UCF_meet_loyalist_leaders target: [From.GetName]"
			custom_effect_tooltip = IRE_bm_ucf_improve_north_tt
			add_to_variable = { IRE_UCF_north = 1 }
		}

	}
	IRE_UCF_counter_nordie_syndies = {
		icon = GFX_decision_hol_radio_oranje
		allowed = { always = no }		# Unlocked by focus IRE_bail_out_northern_industries

		available = {
			has_completed_focus = IRE_suppress_northern_industrial_action
		}

		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = IRE_UCF_ongoing_NOT
				NOT = { has_country_flag = IRE_UCF_ongoing }
			}
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_UCF_counter_nordie_syndies target: [From.GetName]"
			add_political_power = 55
		}

		cost = 55
		fire_only_once = yes
		state_target = yes
		ai_will_do = { factor = 1000 }

		days_remove = 14
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_UCF_counter_nordie_syndies target: [From.GetName]"
			custom_effect_tooltip = IRE_bm_ucf_improve_north_tt
			add_to_variable = { IRE_UCF_north = 1 }
		}
	}
}

 ### Military Decisions ###

IRE_ira_decisions = {
	IRE_motorisation_decision = {
		icon = GFX_decision_generic_motorized
		allowed = { tag = IRE }

		visible = { has_completed_focus = IRE_limited_motorisation }

		cost = 30
		fire_only_once = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 20
				num_of_military_factories > 10
				stockpile_ratio = {
					archetype = motorized_equipment
					ratio < 0.8
				}
			}
		}

		days_remove = 180

		modifier = {
			custom_modifier_tooltip = IRE_motorisation_decision_tt
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_motorisation_decision"
			hidden_effect = {
				add_timed_idea = {
					idea = IRE_motorisation_efforts
					days = 180
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_motorisation_decision"
		}
	}

	IRE_shannon_forts_west = {
		icon = GFX_decision_fortress
		allowed = { tag = IRE }

		available = {
			has_full_control_of_state = 134
			has_full_control_of_state = 135
		}

		visible = {
			hidden_trigger = {
				owns_state = 134
				owns_state = 135
				has_completed_focus = IRE_fortify_shannon
			}
		}

		custom_cost_text = 250_guns
		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 249 }
		}
		days_remove = 60
		fire_only_once = yes
		cancel_if_not_visible = yes

		ai_will_do = { base = 20 }

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_shannon_forts_west"
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = 250
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_shannon_forts_west"
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -250
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_shannon_forts_west"
			custom_effect_tooltip = IRE_shannon_forts_stage_2
			custom_effect_tooltip = tooltip_white_line
			effect_tooltip = {
				134 = {
					add_building_construction = {
						type = bunker
						province = {
							all_provinces = yes
							limit_to_border = yes
						}
						level = 2
						instant_build = yes
					}
				}
				135 = {
					add_building_construction = {
						type = bunker
						province = {
							all_provinces = yes
							limit_to_border = yes
						}
						level = 2
						instant_build = yes
					}
				}
			}
			hidden_effect = {
				134 = {
					add_building_construction = {
						type = bunker
						province = 11371
						level = 2
					}
					add_building_construction = {
						type = bunker
						province = 11238
						level = 2
					}
					add_building_construction = {
						type = bunker
						province = 11287
						level = 2
					}
					add_building_construction = {
						type = bunker
						province = 3396
						level = 2
					}
				}
				135 = {
					add_building_construction = {
						type = bunker
						province = 13622
						level = 2
					}
				}
			}
		}
	}

	IRE_shannon_forts_east = {
		icon = GFX_decision_generic_fortification
		allowed = { tag = IRE }

		available = {
			has_full_control_of_state = 113
			has_full_control_of_state = 135
		}

		visible = {
			hidden_trigger = {
				owns_state = 113
				owns_state = 135
				has_completed_focus = IRE_fortify_shannon
			}
		}

		custom_cost_text = 250_guns
		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 249 }
		}

		days_remove = 60
		fire_only_once = yes
		cancel_if_not_visible = yes

		ai_will_do = {
			base = 5
			modifier = {
				factor = 20
				has_socialist_government = yes
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_shannon_forts_east"
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = 250
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_shannon_forts_east"
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -250
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_shannon_forts_east"
			custom_effect_tooltip = IRE_shannon_forts_stage_3
			custom_effect_tooltip = tooltip_white_line
			effect_tooltip = {
				113 = {
					add_building_construction = {
						type = bunker
						province = {
							all_provinces = yes
							limit_to_border = yes
						}
						level = 2
						instant_build = yes
					}
				}
				135 = {
					add_building_construction = {
						type = bunker
						province = {
							all_provinces = yes
							limit_to_border = yes
						}
						level = 2
						instant_build = yes
					}
				}
			}
			hidden_effect = {
				113 = {
					add_building_construction = {
						type = bunker
						province = 369
						level = 2
					}
					add_building_construction = {
						type = bunker
						province = 303
						level = 2
					}
				}
				135 = {
					add_building_construction = {
						type = bunker
						province = 9310
						level = 2
					}
					add_building_construction = {
						type = bunker
						province = 6313
						level = 2
					}
					add_building_construction = {
						type = bunker
						province = 13622
						level = 2
					}
				}
			}
		}
	}

	IRE_coastal_fort_expansions = {
		icon = GFX_decision_generic_coastal_fortification
		allowed = { tag = IRE }

		available = {
			has_full_control_of_state = 119
			has_full_control_of_state = 113
			has_full_control_of_state = 135
		}

		visible = {
			hidden_trigger = {
				owns_state = 119
				owns_state = 113
				owns_state = 135
				has_completed_focus = IRE_reactivate_balla_eireannach
			}
		}

		custom_cost_text = IRE_forts_lv_2
		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 249 }
			has_equipment = { artillery_equipment > 99 }
		}

		days_remove = 90
		fire_only_once = yes
		cancel_if_not_visible = yes

		ai_will_do = { base = 20 }

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_coastal_fort_expansions"
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = 250
				}
				add_equipment_to_stockpile = {
					type = artillery_equipment
					amount = 100
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_coastal_fort_expansions"
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -250
				}
				add_equipment_to_stockpile = {
					type = artillery_equipment
					amount = -100
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_coastal_fort_expansions"
			custom_effect_tooltip = IRE_renovate_balla_eireannach_tt
			hidden_effect = {
				119 = {
					add_building_construction = {
						type = coastal_bunker
						province = 3329
						level = 2
					}
					add_building_construction = {
						type = coastal_bunker
						province = 3379
						level = 2
					}
					add_building_construction = {
						type = coastal_bunker
						province = 3242
						level = 2
					}
				}
				113 = {
					add_building_construction = {
						type = coastal_bunker
						province = 3359
						level = 2
					}
					add_building_construction = {
						type = coastal_bunker
						province = 11293
						level = 2
					}
					add_building_construction = {
						type = coastal_bunker
						province = 7377
						level = 2
					}
					add_building_construction = {
						type = coastal_bunker
						province = 285
						level = 2
					}
				}
				135 = {
					add_building_construction = {
						type = coastal_bunker
						province = 11237
						level = 2
					}
					add_building_construction = {
						type = coastal_bunker
						province = 7394
						level = 2
					}
					add_building_construction = {
						type = coastal_bunker
						province = 9405
						level = 2
					}
				}
			}
		}
	}

	IRE_public_weapons = {
		icon = GFX_decision_generic_industry
		allowed = { original_tag = IRE }
		priority = 10

		available = {
			IRE = { has_war_with_major = yes }
		}

		visible = {
			has_country_flag = IRE_mobilised_for_war
		}

		cost = 50
		fire_only_once = yes
		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_public_weapons"
			add_equipment_to_stockpile = {
				type = infantry_equipment_0
				amount = 1000
				producer = IRE
			}
		}
	}

	IRE_beer_for_bread = {
		icon = GFX_decision_generic_agriculture
		allowed = { original_tag = IRE }
		priority = 10

		available = {
			IRE = { has_war_with_major = yes }
		}

		visible = {
			has_country_flag = IRE_mobilised_for_war
		}

		cost = 75
		fire_only_once = yes
		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_beer_for_bread"
			add_war_support = 0.05
			add_stability = 0.025
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = 100
				producer = IRE
			}
		}
	}

	IRE_recruitment_campaigns = {
		icon = GFX_decision_soldierly_discipline
		allowed = { original_tag = IRE }
		priority = 10

		available = {
			IRE = { has_war_with_major = yes }
		}

		visible = {
			has_country_flag = IRE_mobilised_for_war
		}

		fire_only_once = yes
		ai_will_do = { factor = 10 }
		cancel_if_not_visible = no

		custom_cost_trigger = {
			has_war_support > 0.05
		}
		custom_cost_text = war_support5
		modifier = {
			mobilization_speed = 0.2
			weekly_manpower = 200
		}
		days_remove = 120

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_recruitment_campaigns"
			hidden_effect = { add_war_support = -0.05 }
		}
	}

	IRE_the_defenders = {
		icon = GFX_decision_generic_reorganize_irregulars
		allowed = { original_tag = IRE }
		priority = 10

		available = {
			IRE = { has_war_with_major = yes }
		}

		visible = {
			has_country_flag = IRE_mobilised_for_war
		}

		fire_only_once = yes
		ai_will_do = { factor = 10 }
		cancel_if_not_visible = no

		custom_cost_trigger = {
			command_power > 49.99
		}
		custom_cost_text = CP_50
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_the_defenders"
			hidden_effect = { add_command_power = -50 }
			if = {
				limit = { has_completed_focus = IRE_na_cosantoiri }
				division_template = {
					name = "Na Cosantóirí"
					division_names_group = IRE_INF_01
					regiments = {
						irregular_infantry = { x = 0 y = 0 }
						irregular_infantry = { x = 0 y = 1 }
						irregular_infantry = { x = 0 y = 2 }
						irregular_infantry = { x = 0 y = 3 }
						irregular_infantry = { x = 1 y = 0 }
						irregular_infantry = { x = 1 y = 1 }
						irregular_infantry = { x = 1 y = 2 }
						irregular_infantry = { x = 2 y = 0 }
						irregular_infantry = { x = 2 y = 1 }
						irregular_infantry = { x = 2 y = 2 }
						artillery_brigade = { x = 3 y = 0 }
					}
					support = {
						artillery = { x = 0 y = 0 }
					}
				}
			}
			else = {
				division_template = {
					name = "Na Cosantóirí"
					division_names_group = IRE_INF_01
					regiments = {
						irregular_infantry = { x = 0 y = 0 }
						irregular_infantry = { x = 0 y = 1 }
						irregular_infantry = { x = 0 y = 2 }
						irregular_infantry = { x = 1 y = 0 }
						irregular_infantry = { x = 1 y = 1 }
					}
				}
			}
			random_owned_controlled_state = {
				prioritize = { 113 }
				limit = {
					is_in_home_area = yes
					impassable = no
				}
				create_unit = {
					division = "name = \"2nd Carlow Brigade\" division_template = \"Na Cosantóirí\" start_experience_factor = 0.1"
					owner = ROOT
				}
				create_unit = {
					division = "name = \"3rd Kerry Brigade\" division_template = \"Na Cosantóirí\" start_experience_factor = 0.1"
					owner = ROOT
				}
			}
		}
	}

	IRE_desperate_defence = {
		icon = GFX_decision_generic_protection
		allowed = { original_tag = IRE }
		priority = 10

		available = {
			surrender_progress > 0.20
			IRE = { has_war_with_major = yes }
		}

		visible = {
			has_country_flag = IRE_mobilised_for_war
		}

		fire_only_once = yes
		ai_will_do = { factor = 10 }
		cancel_if_not_visible = no

		custom_cost_trigger = {
			command_power > 24.99
		}
		custom_cost_text = CP_25
		modifier = {
			army_core_defence_factor = 0.2
		}
		days_remove = 60

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_desperate_defence"
			hidden_effect = { add_command_power = -25 }
		}

	}

	IRE_ireland_will_not_surrender = {
		icon = GFX_decision_SWI_consolidate_council_power
		allowed = { original_tag = IRE }
		priority = 10

		available = {
			surrender_progress > 0.40
			IRE = { has_war_with_major = yes }
		}

		visible = {
			has_country_flag = IRE_mobilised_for_war
		}

		custom_cost_trigger = {
			command_power > 39.99
		}
		custom_cost_text = CP_40
		fire_only_once = yes
		ai_will_do = { factor = 10 }
		cancel_if_not_visible = no

		modifier = {
			resistance_growth_on_our_occupied_states = 0.2
		}
		days_remove = 180

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_ireland_will_not_surrender"
			hidden_effect = { add_command_power = -40 }
		}
	}

	IRE_for_king_and_his_country = {
		icon = GFX_decision_eng_ally_imperialist_coup
		allowed = { original_tag = IRE }
		priority = 20

		available = {
			is_in_faction_with = ENT
			IRE = { has_war_with_major = yes }
		}

		visible = {
			is_in_faction_with = ENT
			has_country_flag = IRE_mobilised_for_war
		}

		fire_only_once = yes
		ai_will_do = { factor = 10 }
		cancel_if_not_visible = no

		cost = 100
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_for_king_and_his_country"
			division_template = {
				name = "Loyalist Volunteers"
				division_names_group = NIR_MIL_01
				regiments = {
					infantry = { x = 0 y = 0 }
					infantry = { x = 0 y = 1 }
					infantry = { x = 0 y = 2 }
					infantry = { x = 0 y = 3 }
					infantry = { x = 1 y = 0 }
					infantry = { x = 1 y = 1 }
					infantry = { x = 1 y = 2 }
					infantry = { x = 1 y = 3 }
					infantry = { x = 2 y = 0 }
					infantry = { x = 2 y = 1 }
					infantry = { x = 2 y = 2 }
				}
				support = {
					artillery = { x = 0 y = 0 }
				}
				template_counter = 72
			}
			random_owned_controlled_state = {
				prioritize = { 119 }
				limit = {
					is_in_home_area = yes
					impassable = no
				}
				create_unit = {
					division = "name = \"Lisburn Battalion\" division_template = \"Loyalist Volunteers\" start_experience_factor = 0.1"
					owner = ROOT
				}
				create_unit = {
					division = "name = \"Ballymena Battalion\" division_template = \"Loyalist Volunteers\" start_experience_factor = 0.1"
					owner = ROOT
				}
			}
		}
	}

	IRE_german_gun_running = {
		icon = GFX_decision_generic_prepare_civil_war
		allowed = { original_tag = IRE }
		priority = 20

		available = {
			is_in_faction_with = GER
			IRE = { has_war_with_major = yes }
		}

		visible = {
			is_in_faction_with = GER
			has_country_flag = IRE_mobilised_for_war
		}

		custom_cost_trigger = {
			command_power > 74.99
		}
		custom_cost_text = CP_75
		fire_only_once = yes
		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_german_gun_running"
			hidden_effect = { add_command_power = -40 }
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 4000
				producer = GER
			}
		}
	}

	IRE_internationale_supply_lines = {
		icon = GFX_decision_generic_merge_plant_ship
		allowed = { original_tag = IRE }
		priority = 20

		available = {
			is_in_faction_with = INT
			IRE = { has_war_with_major = yes }
		}

		visible = {
			is_in_faction_with = INT
			has_country_flag = IRE_mobilised_for_war
		}

		cost = 50
		fire_only_once = yes
		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_internationale_supply_lines"
			add_equipment_to_stockpile = {
				type = convoy
				amount = 100
				producer = FRA
			}
			add_equipment_to_stockpile = {
				type = train_equipment
				amount = 10
				producer = ENG
			}
		}
	}

	IRE_union_aid = {
		icon = GFX_decision_generic_political_rally
		allowed = { original_tag = IRE }
		priority = 15

		available = {
			custom_trigger_tooltip = {
				tooltip = IRE_has_socialist_govt
				IRE_has_socialist_govt = yes
			}
			IRE = { has_war_with_major = yes }
		}

		visible = {
			IRE_has_socialist_govt = yes
			has_country_flag = IRE_mobilised_for_war
		}

		custom_cost_trigger = {
			has_war_support > 0.05
		}
		custom_cost_text = war_support5
		fire_only_once = yes
		ai_will_do = { factor = 10 }

		modifier = {
			industrial_capacity_factory = 0.05
			production_speed_buildings_factor = 0.05
			consumer_goods_factor = -0.06
		}
		days_remove = 90

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_union_aid"
			hidden_effect = { add_war_support = -0.05 }
		}
	}

	IRE_the_cause_of_the_free = {
		icon = GFX_decision_eng_propaganda_campaigns
		allowed = { original_tag = IRE }
		priority = 15

		available = {
			custom_trigger_tooltip = {
				tooltip = IRE_has_parliamentary_govt
				IRE_has_parliamentary_govt = yes
			}
			IRE = { has_war_with_major = yes }
		}

		visible = {
			IRE_has_parliamentary_govt = yes
			has_country_flag = IRE_mobilised_for_war
		}

		cost = 50
		fire_only_once = yes
		ai_will_do = { factor = 10 }

		modifier = {
			war_support_weekly = 0.01
			stability_weekly = 0.01
		}
		days_remove = 120

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_the_cause_of_the_free"
		}
	}

	IRE_cogadh_gaedheal_re_gallaibh = {
		icon = GFX_decision_generic_nationalism
		allowed = { original_tag = IRE }
		priority = 15

		available = {
			custom_trigger_tooltip = {
				tooltip = IRE_has_reactionary_govt
				IRE_has_reactionary_govt = yes
			}
			IRE = { has_war_with_major = yes }
		}

		visible = {
			IRE_has_reactionary_govt = yes
			has_country_flag = IRE_mobilised_for_war
		}

		custom_cost_trigger = {
			has_stability > 0.05
		}
		custom_cost_text = stability5
		fire_only_once = yes
		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_cogadh_gaedheal_re_gallaibh"
			add_manpower = 10000
			hidden_effect = { add_stability = -0.05 }
		}
	}
}

 ### Foreign Policy Decisions ###

IRE_foreign_policy_decisions = {
	IRE_join_reichspakt = {
		icon = generic_prepare_civil_war

		allowed = { always = no }

		available = {
			threat > 0.6
			has_country_flag = IRE_gateway_IRE_victory
			if = {
				limit = { IRE_has_bm = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_has_bm_NOT
					IRE_has_bm = no
				}
			}
			at_peace_or_same_enemies_as_FROM = yes
			if = {
				limit = { IRE_has_reactionary_govt = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_ulster_privileges_gone_tt
					NOT = {
						has_variable = IRE_nationalist_discontent
						has_variable = IRE_loyalist_anger
					}
				}
			}
		}

		visible = {
			is_valid_peace_target = yes
			owns_state = 113 #Leinster
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
			NOT = { has_country_flag = IRE_joining_faction }
			has_socialist_government = no
		}

		cost = 50
		days_remove = 5
		fire_only_once = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 250
				OR = {
					has_country_flag = IRE_forpolpath_rp
					has_country_flag = IRE_alliance_refused_ENT
					AND = {
						has_war = yes
						OR = {
							has_country_flag = IRE_got_guarantee_GER
							AND = {
								has_country_flag = IRE_got_guarantee_TEX
								TEX = { is_in_faction_with = GER }
							}
							has_country_flag = IRE_forpolpath_neut
						}
					}
				}
				NOT = { has_country_flag = IRE_gateway_ENG_dam_benefactor }
				NOT = { has_game_rule = { rule = IRE_path option = IRE_PATH_RADSOC } }
			}
			modifier = {
				add = 250
				has_game_rule = { rule = IRE_forpol option = IRE_FORPOL_GER }
			}
			modifier = {
				factor = 0
				FROM = { surrender_progress > 0.5 }
				NOT = { has_game_rule = { rule = IRE_forpol option = IRE_FORPOL_GER } }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_join_reichspakt target: [From.GetName]"
			hidden_effect = { set_country_flag = IRE_joining_faction }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_join_reichspakt target: [From.GetName]"
			FROM = { country_event = ireland.2000 }
			custom_effect_tooltip = if_they_accept_tt
			effect_tooltip = {
				FROM = { add_to_faction = ROOT }
			}
			if = {
				limit = {
					has_global_flag = GER_stay_out_france_uk
					any_state = { is_claimed_by = ROOT }
					IMP = { exists = yes }
					ENT = { exists = yes }
				}
				custom_effect_tooltip = tooltip_white_line
				effect_tooltip = {
					every_state = {
						limit = { is_claimed_by = ROOT }
						remove_claim_by = ROOT
						if = {
							limit = { is_owned_by = ROOT }
							transfer_state_to = IMP
						}
					}
				}
			}
		}
	}

	IRE_join_entente = {
		icon = generic_prepare_civil_war

		allowed = { always = no }

		available = {
			threat > 0.65
			has_country_flag = IRE_gateway_IRE_victory
			if = {
				limit = { IRE_has_bm = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_has_bm_NOT
					IRE_has_bm = no
				}
			}
			at_peace_or_same_enemies_as_FROM = yes
			if = {
				limit = { IRE_has_reactionary_govt = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_ulster_privileges_gone_tt
					NOT = {
						has_variable = IRE_nationalist_discontent
						has_variable = IRE_loyalist_anger
					}
				}
				custom_trigger_tooltip = {
					tooltip = IRE_had_NIR_revolt_NOT
					NOT = { has_global_flag = IRE_had_NIR_revolt }
				}
			}
		}

		visible = {
			is_valid_peace_target = yes
			owns_state = 113 #Leinster
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
			NOT = { has_country_flag = IRE_joining_faction }
			has_socialist_government = no
		}

		cost = 50
		days_remove = 5
		fire_only_once = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 250
				OR = {
					has_country_flag = IRE_forpolpath_ent
					has_country_flag = IRE_alliance_refused_GER
					AND = {
						has_war = yes
						OR = {
							AND = {
								has_country_flag = IRE_got_guarantee_USA
								USA = { is_in_faction_with = ENT }
							}
							AND = {
								has_country_flag = IRE_got_guarantee_TEX
								TEX = { is_in_faction_with = ENT }
							}
							AND = {
								has_country_flag = IRE_got_guarantee_PSA
								PSA = { is_in_faction_with = ENT }
							}
							AND = {
								has_country_flag = IRE_got_guarantee_NEE
								NEE = { is_in_faction_with = ENT }
							}
							has_country_flag = IRE_forpolpath_neut
						}
					}
				}
				OR = {
					CAN = {
						NOT = { has_country_flag = CAN_considering_alliance_ireland }
					}
					has_global_flag = second_weltkrieg_has_started
				}
				NOT = { has_country_flag = IRE_gateway_ENG_dam_benefactor }
				NOT = { has_game_rule = { rule = IRE_path option = IRE_PATH_RADSOC } }
			}
			modifier = {
				add = 250
				has_game_rule = { rule = IRE_forpol option = IRE_FORPOL_ENT }
				OR = {
					CAN = {
						NOT = { has_country_flag = CAN_considering_alliance_ireland }
					}
					has_global_flag = second_weltkrieg_has_started
				}
			}
			modifier = {
				factor = 0
				FROM = { surrender_progress > 0.5 }
				NOT = { has_game_rule = { rule = IRE_forpol option = IRE_FORPOL_ENT } }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_join_entente target: [From.GetName]"
			hidden_effect = { set_country_flag = IRE_joining_faction }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_join_entente target: [From.GetName]"
			FROM = { country_event = ireland.2000 }
			custom_effect_tooltip = if_they_accept_tt
			custom_effect_tooltip = IRE_join_ENT_tt
			effect_tooltip = {
				FROM = { add_to_faction = ROOT }
			}
			if = {
				limit = {
					any_state = { is_claimed_by = ROOT }
					IMP = { exists = yes }
				}
				custom_effect_tooltip = tooltip_white_line
				effect_tooltip = {
					every_state = {
						limit = { is_claimed_by = ROOT }
						remove_claim_by = ROOT
						if = {
							limit = { is_owned_by = ROOT }
							transfer_state_to = IMP
						}
					}
				}
			}
		}
	}

	IRE_join_internationale = {
		icon = generic_prepare_civil_war

		allowed = { always = no }

		available = {
			threat > 0.65
			has_country_flag = IRE_gateway_IRE_victory
			if = {
				limit = { IRE_has_bm = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_has_bm_NOT
					IRE_has_bm = no
				}
			}
			at_peace_or_same_enemies_as_FROM = yes
			GER = { exists = no }
			# AUS = { is_faction_leader = no }		### IRE eventual TO-DO
			custom_trigger_tooltip = {
				tooltip = IRE_ulster_privileges_gone_tt
				NOT = {
					has_variable = IRE_nationalist_discontent
					has_variable = IRE_loyalist_anger
				}
			}
			if = {
				limit = { has_global_flag = USA_civil_war_over_flag }
				has_global_flag = USA_civil_war_over_flag
				if = { limit = { country_exists = USA }
					OR = {
						has_country_flag = IRE_rejected_guarantee_USA
						NOT = { country_exists = USA }
						USA = { has_dictatorship_government = yes }
					}
				}
				if = { limit = { country_exists = TEX }
					OR = {
						has_country_flag = IRE_rejected_guarantee_TEX
						NOT = { country_exists = TEX }
						TEX = { has_dictatorship_government = yes }
					}
				}
				if = { limit = { country_exists = PSA }
					OR = {
						has_country_flag = IRE_rejected_guarantee_PSA
						NOT = { country_exists = PSA }
					}
				}
				if = { limit = { country_exists = NEE }
					OR = {
						has_country_flag = IRE_rejected_guarantee_NEE
						NOT = { country_exists = NEE }
					}
				}
			}
			else = {
				has_global_flag = USA_civil_war_begun_flag
				if = {
					limit = { country_exists = USA }
					NOT = { country_exists = USA }
				}
				if = {
					limit = { country_exists = TEX }
					NOT = { country_exists = TEX }
				}
				if = {
					limit = { country_exists = PSA }
					NOT = { country_exists = PSA }
				}
				if = {
					limit = { country_exists = NEE }
					NOT = { country_exists = NEE }
				}
				NOT = { ENT = { has_war_with = CSA } }
			}
		}

		visible = {
			is_valid_peace_target = yes
			owns_state = 113 #Leinster
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
			NOT = { has_country_flag = IRE_joining_faction }
		}

		cost = 50
		days_remove = 5
		fire_only_once = yes

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				has_war = no
				has_game_rule = { rule = IRE_forpol option = IRE_FORPOL_NEUT }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_join_internationale target: [From.GetName]"
			hidden_effect = { set_country_flag = IRE_joining_faction }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_join_internationale target: [From.GetName]"
			FROM = { country_event = ireland.2000 }
			custom_effect_tooltip = if_they_accept_tt
			effect_tooltip = {
				FROM = { add_to_faction = ROOT }
			}
		}
	}

	IRE_join_moscow_accord = {
		icon = generic_prepare_civil_war

		allowed = { always = no }

		available = {
			threat > 0.65
			has_country_flag = IRE_gateway_IRE_victory
			if = {
				limit = { IRE_has_bm = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_has_bm_NOT
					IRE_has_bm = no
				}
			}
			at_peace_or_same_enemies_as_FROM = yes
			if = {
				limit = { FROM = { has_elected_government = yes } }
				FROM = { has_elected_government = yes }
				ROOT = { has_democratic_government = yes }
			}
			else_if = {
				limit = { FROM = { has_authoritarian_government = yes } }
				FROM = { has_authoritarian_government = yes }
				ROOT = { has_dictatorship_government = yes }
			}
			if = {
				limit = { has_democratic_government = yes }
				OR = {
					FRA = { has_government = totalist }
					ENG = { has_government = totalist }
					64 = { owner = { is_in_faction_with = INT } }
					AND = {
						INT = { has_war_with = FROM }
						INT = { has_war_with = ROOT }
					}
				}
			}
			# AUS = { is_faction_leader = no }	### IRE eventual TO-DO
			INT = { is_faction_leader = yes }
			if = {
				limit = { has_global_flag = USA_civil_war_over_flag }
				has_global_flag = USA_civil_war_over_flag
				IRE_check_postacw_faction = yes
			}
			if = {
				limit = { NOT = { has_global_flag = USA_civil_war_over_flag } }
				has_global_flag = USA_civil_war_begun_flag
				if = {
					limit = { country_exists = USA }
					NOT = { country_exists = USA }
				}
				if = {
					limit = { country_exists = TEX }
					NOT = { country_exists = TEX }
				}
				if = {
					limit = { country_exists = PSA }
					NOT = { country_exists = PSA }
				}
				if = {
					limit = { country_exists = NEE }
					NOT = { country_exists = NEE }
				}
				NOT = { CAN = { has_war_with = CSA } }
			}
		}

		visible = {
			is_valid_peace_target = yes
			has_socialist_government = no
			owns_state = 113 #Leinster
			FROM = {
				is_faction_leader = yes
				has_socialist_government = no
				NOT = { has_war_with = ROOT }
			}
			NOT = { has_country_flag = IRE_joining_faction }
		}

		cost = 50
		days_remove = 5
		fire_only_once = yes

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				OR = {
					AND = {
						has_war = no
						OR = {
							has_country_flag = IRE_govt_labour_cnat_plp
							has_game_rule = { rule = IRE_forpol option = IRE_FORPOL_NEUT }
						}
					}
					has_game_rule = { rule = IRE_path option = IRE_PATH_RADSOC }
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_join_moscow_accord target: [From.GetName]"
			hidden_effect = { set_country_flag = IRE_joining_faction }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_join_moscow_accord target: [From.GetName]"
			FROM = { country_event = ireland.2000 }
			custom_effect_tooltip = if_they_accept_tt
			effect_tooltip = {
				FROM = { add_to_faction = ROOT }
			}
			custom_effect_tooltip = IRE_join_bonus_faction_tt
		}
	}

	IRE_join_copro = {
		icon = generic_prepare_civil_war

		allowed = { always = no }

		available = {
			threat > 0.65
			has_country_flag = IRE_gateway_IRE_victory
			if = {
				limit = { IRE_has_bm = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_has_bm_NOT
					IRE_has_bm = no
				}
			}
			at_peace_or_same_enemies_as_FROM = yes
			has_democratic_government = yes
			is_guaranteed_by = PSA
			PSA = { is_in_faction_with = JAP }
			GER = { exists = no }
			# AUS = { is_faction_leader = no }		### IRE eventual TO-DO
		}

		visible = {
			is_valid_peace_target = yes
			has_socialist_government = no
			owns_state = 113 #Leinster
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
			NOT = { has_country_flag = IRE_joining_faction }
			OR = {
				has_country_flag = IRE_got_guarantee_PSA
				is_guaranteed_by = PSA
			}
			PSA = {
				OR = {
					is_in_faction = no
					is_in_faction_with = JAP
				}
			}
		}

		cost = 50
		days_remove = 5
		fire_only_once = yes

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				OR = {
					AND = {
						has_war = no
						OR = {
							has_country_flag = IRE_govt_labour_cnat_plp
							has_game_rule = { rule = IRE_forpol option = IRE_FORPOL_NEUT }
						}
					}
					has_game_rule = { rule = IRE_path option = IRE_PATH_RADSOC }
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_join_copro target: [From.GetName]"
			hidden_effect = { set_country_flag = IRE_joining_faction }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_join_copro target: [From.GetName]"
			FROM = { country_event = ireland.2000 }
			custom_effect_tooltip = if_they_accept_tt
			effect_tooltip = {
				FROM = { add_to_faction = ROOT }
			}
			custom_effect_tooltip = IRE_join_bonus_faction_tt
		}
	}

	IRE_seek_guarantee_germany = {
		icon = GFX_decision_generic_protection

		allowed = { always = no }

		available = {
			threat > 0.5
			has_country_flag = IRE_gateway_IRE_victory
			has_completed_focus = IRE_international_recognition
			if = {
				limit = { IRE_has_bm = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_has_bm_NOT
					IRE_has_bm = no
				}
			}
			at_peace_or_same_enemies_as_FROM = yes
			GER_is_in_mitteleuropa = yes
			FROM = {
				is_faction_leader = yes
			}
		}

		visible = {
			is_subject = no
			owns_state = 113 #Leinster
			has_socialist_government = no
			FROM = {
				NOT = { has_war_with = ROOT }
			}
			NOT = { is_guaranteed_by = FROM }
			NOT = { has_country_flag = IRE_seeking_guarantee }
		}

		cost = 25
		days_remove = 5
		fire_only_once = yes

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				NOT = { has_global_flag = second_weltkrieg_has_started }
			}
			modifier = {
				factor = 0
				has_game_rule = { rule = IRE_path option = IRE_PATH_RADSOC }
				IRE_has_radsoc_in_coalition = yes
				INT = { is_faction_leader = yes }
				is_in_faction = no
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_seek_guarantee_germany target: [From.GetName]"
			hidden_effect = {
				set_country_flag = IRE_seeking_guarantee
				set_country_flag = IRE_joining_faction
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_seek_guarantee_germany target: [From.GetName]"
			FROM = { country_event = ireland.2003 }
			custom_effect_tooltip = if_they_accept_tt
			effect_tooltip = {
				FROM = { give_guarantee = ROOT }
			}
		}
	}

	IRE_seek_guarantee_america = {
		icon = GFX_decision_generic_protection

		allowed = { always = no }		# this should be activated by the forpol_start trigger.  Decision should still be available if IRE is in the ENT though.  or loses the influence mechanic

		available = {
			threat > 0.5
			has_country_flag = IRE_gateway_IRE_victory
			has_completed_focus = IRE_international_recognition
			if = {
				limit = { IRE_has_bm = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_has_bm_NOT
					IRE_has_bm = no
				}
			}
			at_peace_or_same_enemies_as_FROM = yes
			has_global_flag = USA_civil_war_over_flag
			FROM = { is_subject = no }
			IRE_check_guarantor_ideology = yes
		}

		visible = {
			is_subject = no
			owns_state = 113 #Leinster
			FROM = {
				NOT = { has_war_with = ROOT }
			}
			NOT = { is_guaranteed_by = FROM }
			NOT = { has_country_flag = IRE_seeking_guarantee }
		}

		cost = 25
		days_remove = 5
		fire_only_once = yes

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				NOT = { has_global_flag = second_weltkrieg_has_started }
			}
			modifier = {
				factor = 0
				has_game_rule = { rule = IRE_path option = IRE_PATH_RADSOC }
				IRE_has_radsoc_in_coalition = yes
				has_socialist_government = no
				FROM = { has_socialist_government = yes }
				INT = { is_faction_leader = yes }
				is_in_faction = no
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_seek_guarantee_america target: [From.GetName]"
			hidden_effect = {
				set_country_flag = IRE_seeking_guarantee
				set_country_flag = IRE_joining_faction
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_seek_guarantee_america target: [From.GetName]"
			FROM = { country_event = ireland.2003 }
			custom_effect_tooltip = if_they_accept_tt
			effect_tooltip = {
				FROM = { give_guarantee = ROOT }
			}
		}
	}

	IRE_leave_mitteleuropa = {
		icon = GFX_decision_generic_break_treaty

		allowed = { always = no }

		available = {
			has_country_flag = IRE_gateway_IRE_victory
			has_completed_focus = IRE_international_recognition
			if = {
				limit = { IRE_has_bm = yes }
				custom_trigger_tooltip = {
					tooltip = IRE_has_bm_NOT
					IRE_has_bm = no
				}
			}
			GER_is_in_mitteleuropa = yes
			NOT = { is_ally_with = FROM }
			if = {
				limit = { 119 = { is_demilitarized_zone = yes } }
				custom_trigger_tooltip = {
					tooltip = IRE_ni_demil		# only here because vanilla tooltip is dodgy af
					119 = { is_demilitarized_zone = no }
				}
			}
		}

		visible = {
			is_subject = no
			owns_state = 113 #Leinster
			owns_state = 119 #Northern Ireland
			has_socialist_government = no
			NOT = { has_country_flag = IRE_joining_faction }
			NOT = { has_country_flag = IRE_seeking_guarantee }
			NOT = { has_country_flag = IRE_considering_leaving_mitteleuropa }
		}

		cost = 25
		days_remove = 5
		fire_only_once = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				has_country_flag = IRE_govt_sc_ndp
				GER = { has_authoritarian_government = yes }
			}
			modifier = {
				add = 100
				has_country_flag = IRE_govt_cursed_coalition
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_leave_mitteleuropa target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_leave_mitteleuropa target: [From.GetName]"
			GER_leave_mitteleuropa = yes
			custom_effect_tooltip = tooltip_white_line
			add_timed_idea = {
				idea = IRE_economic_upheaval
				days = 180
			}
			if = {
				limit = { has_country_flag = IRE_got_guarantee_GER }
				clr_country_flag = IRE_got_guarantee_GER
				custom_effect_tooltip = tooltip_white_line
				GER = {
					diplomatic_relation = {
						country = ROOT
						relation = guarantee
						active = no
					}
				}
			}
			if = {
				limit = { has_variable = MIT_IRE_bonus_score }
				clear_variable = MIT_IRE_bonus_score
			}
			clr_country_flag = IRE_considering_leaving_mitteleuropa
		}
	}

	IRE_reclaim_gaelic_lands = {
		icon = GFX_decision_infiltrate_state

		allowed = { always = no }
		priority = 500

		cost = 25
		days_remove = IRE_war_decision_days
		fire_only_once = yes

		available = {
			if = {
				limit = { NOT = { has_completed_focus = IRE_end_class_war } }
				has_completed_focus = IRE_end_class_war
			}
			custom_trigger_tooltip = {
				tooltip = IRE_ulster_privileges_gone_tt
				NOT = {
					has_variable = IRE_nationalist_discontent
					has_variable = IRE_loyalist_anger
				}
			}
		}

		visible = {
			NOT = { is_in_faction_with = ENT }
			owns_state = 113 #Leinster
			NOT = { has_country_flag = IRE_joining_faction }
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				is_in_faction = no
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_reclaim_gaelic_lands target: [From.GetName]"
			hidden_effect = { set_country_flag = IRE_joining_faction }
			if = {
				limit = {
					is_in_faction = no
					can_declare_war_on = var:133.owner
				}
				setup_decision_attack_AI = yes
				FROM = { incoming_war_notification_effect = yes }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_reclaim_gaelic_lands target: [From.GetName]"
			add_state_claim = 133
			add_state_claim = 120
			if = {
				limit = {
					is_in_faction = no
					can_declare_war_on = var:133.owner
				}
				clear_decision_attack_AI = yes
				declare_war_on = {
					target = var:133.owner
					type = annex_everything
				}
			}
			clr_country_flag = IRE_joining_faction
			clear_variable = IRE_war_decision_days
		}
	}

	IRE_form_celtic_confederation = {
		icon = GFX_decision_generic_speech

		allowed = { always = no }
		priority = 500

		fire_only_once = yes

		ai_will_do = { factor = 100 }

		available = {
			owns_state = 133
			owns_state = 120
			has_subject = SCO
			has_subject = WLS
			has_subject = BRI
			is_in_faction = no
		}

		visible = {
			is_in_faction = no
			is_valid_peace_target = yes
			owns_state = 113 #Leinster
			NOT = { has_country_flag = IRE_joining_faction }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_form_celtic_confederation"
			if = {
				limit = { is_ally_with = 123.owner }
				123 = {
					add_core_of = WLS
					transfer_state_to = WLS
				}
				custom_effect_tooltip = tooltip_white_line
			}
			IRE_neven_henaff = { set_nationality = BRI }
			add_country_leader_role = {
				character = IRE_neven_henaff
				country_leader = { ideology = national_populist_subtype }
				promote_leader = yes
			}
			create_faction = celtic_confederation
			custom_effect_tooltip = IRE_join_bonus_faction_tt
			set_country_flag = IRE_celtic_confederation
		}
	}
}

 ### Gateway to the Atlantic Decisions ###

IRE_gateway_to_the_atlantic_decisions = {
	IRE_favour_a_side_GER = {
		icon = eng_trade_unions_support

		allowed = { always = no }	### Targeted decision activated by event
		priority = 200

		available = {
			NOT = { has_war_with = FROM }
			has_completed_focus = IRE_gateway
		}

		visible = { country_exists = FROM }

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		cost = IRE_favour_cost
		days_remove = 10
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				OR = {
					has_country_flag = IRE_gateway_ai_flag
					IRE_GER_winning_influence = yes
				}
				OR = {
					has_completed_focus = IRE_foreign_know_how
					IRE_has_pataut_in_coalition = yes
				}
				NOT = { has_country_flag = IRE_GER_preference }
			}
			modifier = {
				factor = 0
				date > 1938.3.17
				IRE_gateway_player_trigger = yes
				IRE_ENG_winning_influence = yes
				set_temp_variable = { IRE_ENG_larger_by = 20 }
				IRE_ENG_influence_larger_by = no
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_favour_a_side_GER target: [From.GetName]"
			hidden_effect = {
				random_list = {
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 20 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 30 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 35 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 40 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 45 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 50 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 60 value = 1 }
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_favour_a_side_GER target: [From.GetName]"
			set_temp_variable = { IRE_GER_delta = 5 }
			IRE_add_GER_influence = yes
		}
	}
	IRE_favour_a_side_ENG = {
		icon = eng_trade_unions_support

		allowed = { always = no }	### Targeted decision activated by event
		priority = 100

		available = {
			NOT = { has_war_with = FROM }
			has_completed_focus = IRE_gateway
		}

		visible = { country_exists = FROM }

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		cost = IRE_favour_cost
		days_remove = 10
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				OR = {
					has_country_flag = IRE_gateway_ai_flag
					IRE_ENG_winning_influence = yes
				}
				OR = {
					has_completed_focus = IRE_foreign_know_how
					IRE_has_pataut_in_coalition = yes
				}
				NOT = { has_country_flag = IRE_ENG_preference }
			}
			modifier = {
				factor = 0
				date > 1938.3.17
				IRE_gateway_player_trigger = yes
				IRE_GER_winning_influence = yes
				set_temp_variable = { IRE_GER_larger_by = 20 }
				IRE_GER_influence_larger_by = no
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_favour_a_side_ENG target: [From.GetName]"
			hidden_effect = {
				random_list = {
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 20 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 30 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 35 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 40 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 45 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 50 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 60 value = 1 }
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_favour_a_side_ENG target: [From.GetName]"
			set_temp_variable = { IRE_ENG_delta = 5 }
			IRE_add_ENG_influence = yes
		}
	}
	IRE_gateway_GER_guns = {
		icon = GFX_decision_generic_prepare_civil_war

		allowed = { always = no }
		priority = 50

		available = {
			set_temp_variable = { IRE_GER_at_least = 30 }
			IRE_GER_influence_at_least = yes
			NOT = { has_war_with = FROM }
		}

		visible = {
			hidden_trigger = {
				NOT = { has_war_with = FROM }
				OR = {
					is_in_faction = no
					is_in_faction_with = FROM
				}
			}
		}

		fire_only_once = no

		days_remove = 14
		days_re_enable = 180
		cancel_if_not_visible = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				stockpile_ratio = {
					archetype = infantry_equipment
					ratio < 0.75
				}
			}
			modifier = {
				add = 100
				IRE_ENG_winning_influence = yes
			}
			modifier = {
				factor = 0
				IRE_GER_winning_influence = yes
			}
		}

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		custom_cost_trigger = { has_country_flag = IRE_gateway_started }
		custom_cost_text = IRE_gateway_GER_guns_cost

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_GER_guns target: [From.GetName]"
			hidden_effect = {
				set_temp_variable = { IRE_GER_delta = 10 }
				IRE_add_GER_influence = yes
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_GER_guns target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_GER_guns target: [From.GetName]"
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 500
				producer = GER
			}
		}
	}
	IRE_gateway_GER_support_equipment = {
		icon = GFX_decision_support_equipment

		allowed = { always = no }
		priority = 50

		available = {
			set_temp_variable = { IRE_GER_at_least = 50 }
			IRE_GER_influence_at_least = yes
			NOT = { has_war_with = FROM }
		}

		visible = {
			hidden_trigger = {
				NOT = { has_war_with = FROM }
				OR = {
					is_in_faction = no
					is_in_faction_with = FROM
				}
			}
		}

		fire_only_once = no

		days_remove = 14
		days_re_enable = 180
		cancel_if_not_visible = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				stockpile_ratio = {
					archetype = support_equipment
					ratio < 0.75
				}
			}
			modifier = {
				add = 100
				IRE_ENG_winning_influence = yes
			}
			modifier = {
				factor = 0
				IRE_GER_winning_influence = yes
			}
		}

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		custom_cost_trigger = { has_country_flag = IRE_gateway_started }
		custom_cost_text = IRE_gateway_GER_support_equipment_cost

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_GER_support_equipment target: [From.GetName]"
			hidden_effect = {
				set_temp_variable = { IRE_GER_delta = 15 }
				IRE_add_GER_influence = yes
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_GER_support_equipment target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_GER_support_equipment target: [From.GetName]"
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = 100
				producer = GER
			}
		}
	}
	IRE_gateway_GER_artillery = {
		icon = GFX_decision_artillery

		allowed = { always = no }
		priority = 50

		available = {
			set_temp_variable = { IRE_GER_at_least = 50 }
			IRE_GER_influence_at_least = yes
			NOT = { has_war_with = FROM }
		}

		visible = {
			hidden_trigger = {
				NOT = { has_war_with = FROM }
				OR = {
					is_in_faction = no
					is_in_faction_with = FROM
				}
			}
		}

		fire_only_once = no

		days_remove = 14
		days_re_enable = 180
		cancel_if_not_visible = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				stockpile_ratio = {
					archetype = artillery_equipment
					ratio < 0.75
				}
			}
			modifier = {
				add = 100
				IRE_ENG_winning_influence = yes
			}
			modifier = {
				factor = 0
				IRE_GER_winning_influence = yes
			}
		}

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		custom_cost_trigger = { has_country_flag = IRE_gateway_started }
		custom_cost_text = IRE_gateway_GER_support_equipment_cost

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_GER_artillery target: [From.GetName]"
			hidden_effect = {
				set_temp_variable = { IRE_GER_delta = 15 }
				IRE_add_GER_influence = yes
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_GER_artillery target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_GER_artillery target: [From.GetName]"
			add_equipment_to_stockpile = {
				type = artillery_equipment
				amount = 100
				producer = GER
			}
		}
	}
	IRE_gateway_ENG_construction_leinster = {
		icon = GFX_decision_generic_construction

		allowed = { always = no }
		priority = 40

		available = {
			has_full_control_of_state = 113
			set_temp_variable = { IRE_ENG_at_least = 30 }
			IRE_ENG_influence_at_least = yes
			NOT = { has_war_with = ENG }
			custom_trigger_tooltip = {
				check_variable = { IRE_gateway_ENG_construction_ongoing < 2 }
				tooltip = IRE_gateway_ENG_construction_ongoing_NOT
			}
		}

		visible = {
			hidden_trigger = {
				NOT = { has_war_with = ENG }
				OR = {
					is_in_faction = no
					is_in_faction_with = ENG
				}
			}
		}

		fire_only_once = no
		highlight_states = {
			highlight_state_targets = { state = 113 }
		}

		days_remove = 180
		days_re_enable = 180
		cancel_if_not_visible = yes

		modifier = {
			custom_modifier_tooltip = IRE_gateway_ENG_construction_tt		# If editing any of these, be sure to edit the loc tooltip aswell
		}

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				IRE_ENG_winning_influence = yes
			}
		}

		custom_cost_trigger = { has_country_flag = IRE_gateway_started }
		custom_cost_text = IRE_gateway_ENG_construction_cost

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_ENG_construction_leinster"
			set_temp_variable = { IRE_ENG_delta = 10 }
			IRE_add_ENG_influence = yes
			hidden_effect = {
				add_to_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				113 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.25
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_ENG_construction_leinster"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				113 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_ENG_construction_leinster"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				113 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}
	}
	IRE_gateway_ENG_construction_northern_ireland = {
		icon = GFX_decision_generic_construction

		allowed = { always = no }
		priority = 40

		available = {
			has_full_control_of_state = 119
			set_temp_variable = { IRE_ENG_at_least = 30 }
			IRE_ENG_influence_at_least = yes
			NOT = { has_war_with = ENG }
			custom_trigger_tooltip = {
				check_variable = { IRE_gateway_ENG_construction_ongoing < 2 }
				tooltip = IRE_gateway_ENG_construction_ongoing_NOT
			}
		}

		visible = {
			hidden_trigger = {
				NOT = { has_war_with = ENG }
				OR = {
					is_in_faction = no
					is_in_faction_with = ENG
				}
			}
		}

		fire_only_once = no
		highlight_states = {
			highlight_state_targets = { state = 119 }
		}

		days_remove = 180
		days_re_enable = 180
		cancel_if_not_visible = yes

		modifier = {
			custom_modifier_tooltip = IRE_gateway_ENG_construction_tt		# If editing any of these, be sure to edit the loc tooltip aswell
		}

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				IRE_ENG_winning_influence = yes
			}
		}

		custom_cost_trigger = { has_country_flag = IRE_gateway_started }
		custom_cost_text = IRE_gateway_ENG_construction_cost

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_ENG_construction_northern_ireland"
			set_temp_variable = { IRE_ENG_delta = 10 }
			IRE_add_ENG_influence = yes
			hidden_effect = {
				add_to_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				119 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.25
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_ENG_construction_northern_ireland"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				119 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_ENG_construction_northern_ireland"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				119 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}
	}
	IRE_gateway_ENG_construction_munster = {
		icon = GFX_decision_generic_construction

		allowed = { always = no }
		priority = 40

		available = {
			has_full_control_of_state = 135
			set_temp_variable = { IRE_ENG_at_least = 30 }
			IRE_ENG_influence_at_least = yes
			NOT = { has_war_with = ENG }
			custom_trigger_tooltip = {
				check_variable = { IRE_gateway_ENG_construction_ongoing < 2 }
				tooltip = IRE_gateway_ENG_construction_ongoing_NOT
			}
		}

		visible = {
			hidden_trigger = {
				NOT = { has_war_with = ENG }
				OR = {
					is_in_faction = no
					is_in_faction_with = ENG
				}
			}
		}

		fire_only_once = no
		highlight_states = {
			highlight_state_targets = { state = 135 }
		}

		days_remove = 180
		days_re_enable = 180
		cancel_if_not_visible = yes

		modifier = {
			custom_modifier_tooltip = IRE_gateway_ENG_construction_tt		# If editing any of these, be sure to edit the loc tooltip aswell
		}

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				IRE_ENG_winning_influence = yes
			}
		}

		custom_cost_trigger = { has_country_flag = IRE_gateway_started }
		custom_cost_text = IRE_gateway_ENG_construction_cost

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_ENG_construction_munster"
			set_temp_variable = { IRE_ENG_delta = 10 }
			IRE_add_ENG_influence = yes
			hidden_effect = {
				add_to_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				135 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.25
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_ENG_construction_munster"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				135 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_ENG_construction_munster"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				135 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}
	}
	IRE_gateway_ENG_construction_connacht = {
		icon = GFX_decision_generic_construction

		allowed = { always = no }
		priority = 40

		available = {
			has_full_control_of_state = 134
			set_temp_variable = { IRE_ENG_at_least = 30 }
			IRE_ENG_influence_at_least = yes
			NOT = { has_war_with = ENG }
			custom_trigger_tooltip = {
				check_variable = { IRE_gateway_ENG_construction_ongoing < 2 }
				tooltip = IRE_gateway_ENG_construction_ongoing_NOT
			}
		}

		visible = {
			hidden_trigger = {
				NOT = { has_war_with = ENG }
				OR = {
					is_in_faction = no
					is_in_faction_with = ENG
				}
			}
		}

		fire_only_once = no
		highlight_states = {
			highlight_state_targets = { state = 134 }
		}

		days_remove = 180
		days_re_enable = 180
		cancel_if_not_visible = yes

		modifier = {
			custom_modifier_tooltip = IRE_gateway_ENG_construction_tt		# If editing any of these, be sure to edit the loc tooltip aswell
		}

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				IRE_ENG_winning_influence = yes
			}
		}

		custom_cost_trigger = { has_country_flag = IRE_gateway_started }
		custom_cost_text = IRE_gateway_ENG_construction_cost

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_gateway_ENG_construction_connacht"
			set_temp_variable = { IRE_ENG_delta = 10 }
			IRE_add_ENG_influence = yes
			hidden_effect = {
				add_to_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				134 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = 0.25
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_gateway_ENG_construction_connacht"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				113 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_gateway_ENG_construction_connacht"
			hidden_effect = {
				subtract_from_variable = { IRE_gateway_ENG_construction_ongoing = 1 }
				134 = {
					add_state_modifier = {
						modifier = {
							state_production_speed_buildings_factor = -0.25
						}
					}
				}
			}
		}
	}
	IRE_decrease_GER = {
		icon = generic_break_treaty

		allowed = { always = no }	### Targeted decision activated by focus
		priority = 200

		available = {
			NOT = { has_war_with = FROM }
			has_completed_focus = IRE_gateway
			set_temp_variable = { IRE_GER_at_least = 5 }
			IRE_GER_influence_at_least = yes
		}

		visible = { country_exists = FROM }

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		cost = 10
		days_remove = 10
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				has_country_flag = IRE_gateway_decrease_ai_flag
				if = {
					limit = { has_completed_focus = IRE_aontas_naisiunta }
					NOT = {
						AND = {
							has_completed_focus = IRE_import_quotas
							has_completed_focus = IRE_higher_tariffs
							NOT = { has_completed_focus = IRE_subsidiaries }
						}
					}
					NOT = {
						AND = {
							has_completed_focus = IRE_subsidiaries
							NOT = { has_completed_focus = IRE_unpeg_punt }
							NOT = { has_completed_focus = IRE_homegrown_ingenuity }
						}
					}
				}
			}
			modifier = {
				factor = 0
				set_temp_variable = { IRE_GER_smaller_than = 10 }
				IRE_GER_influence_smaller_than = yes
			}
			modifier = {
				factor = 0
				set_temp_variable = { IRE_ENG_larger_by = 10 }
				IRE_ENG_influence_larger_by = yes
			}
			modifier = {
				factor = 0
				date > 1938.3.17
				IRE_gateway_player_trigger = yes
				IRE_GER_winning_influence = yes
				set_temp_variable = { IRE_GER_larger_by = 20 }
				IRE_GER_influence_larger_by = no
			}
			modifier = {
				factor = 10
				IRE_GER_winning_influence = yes
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_decrease_GER target: [From.GetName]"
			hidden_effect = {
				random_list = {
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 20 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 30 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 35 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 40 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 45 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 50 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 60 value = 1 }
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_decrease_GER target: [From.GetName]"
			set_temp_variable = { IRE_GER_delta = 10 }
			IRE_subtract_GER_influence = yes
		}
	}
	IRE_decrease_ENG = {
		icon = generic_break_treaty

		allowed = { always = no }	### Targeted decision activated by focus
		priority = 100

		available = {
			NOT = { has_war_with = FROM }
			has_completed_focus = IRE_gateway
			set_temp_variable = { IRE_ENG_at_least = 5 }
			IRE_ENG_influence_at_least = yes
		}

		visible = { country_exists = FROM }

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		cost = 10
		days_remove = 10
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				has_country_flag = IRE_gateway_decrease_ai_flag
				if = {
					limit = { has_completed_focus = IRE_aontas_naisiunta }
					NOT = {
						AND = {
							has_completed_focus = IRE_import_quotas
							has_completed_focus = IRE_higher_tariffs
							NOT = { has_completed_focus = IRE_subsidiaries }
						}
					}
					NOT = {
						AND = {
							has_completed_focus = IRE_subsidiaries
							NOT = { has_completed_focus = IRE_target_british_goods }
							NOT = { has_completed_focus = IRE_homegrown_ingenuity }
						}
					}
				}
			}
			modifier = {
				factor = 0
				set_temp_variable = { IRE_ENG_smaller_than = 10 }
				IRE_ENG_influence_smaller_than = yes
			}
			modifier = {
				factor = 0
				set_temp_variable = { IRE_GER_larger_by = 10 }
				IRE_GER_influence_larger_by = yes
			}
			modifier = {
				factor = 0
				date > 1938.3.17
				IRE_gateway_player_trigger = yes
				IRE_ENG_winning_influence = yes
				set_temp_variable = { IRE_ENG_larger_by = 20 }
				IRE_ENG_influence_larger_by = no
			}
			modifier = {
				factor = 10
				IRE_ENG_winning_influence = yes
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_decrease_ENG target: [From.GetName]"
			hidden_effect = {
				random_list = {
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 20 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 30 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 35 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 40 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 45 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 50 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_decrease_ai_flag days = 60 value = 1 }
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_decrease_ENG target: [From.GetName]"
			set_temp_variable = { IRE_ENG_delta = 10 }
			IRE_subtract_ENG_influence = yes
		}
	}
	IRE_buyout_foreign_owners = {
		icon = GFX_decision_generic_fundraising

		allowed = { always = no }	### Targeted decision activated by focus
		priority = 150

		available = {
			NOT = { has_war_with = FROM }
			if = {
				limit = { FROM = { tag = ENG } }
				set_temp_variable = { IRE_ENG_at_least = 10 }
				IRE_ENG_influence_at_least = yes
			}
			else_if = {
				limit = { FROM = { tag = GER } }
				set_temp_variable = { IRE_GER_at_least = 10 }
				IRE_GER_influence_at_least = yes
			}
		}

		visible = { country_exists = FROM }

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		cost = 25
		days_remove = 35
		fire_only_once = yes
		fixed_random_seed = no

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				has_country_flag = IRE_gateway_ai_flag
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_buyout_foreign_owners target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_buyout_foreign_owners target: [From.GetName]"
			if = {
				limit = { FROM = { tag = GER } }
				set_temp_variable = { IRE_GER_delta = 10 }
				IRE_subtract_GER_influence = yes
			}
			else_if = {
				limit = { FROM = { tag = ENG } }
				set_temp_variable = { IRE_ENG_delta = 10 }
				IRE_subtract_ENG_influence = yes
			}
			add_offsite_building = { type = industrial_complex level = 1 }
		}
	}
	IRE_gateway_GER_pressure = {
		icon = eng_trade_unions_support

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		cancel_trigger = {
			hidden_trigger = {
				IRE_gateway_finished = yes
			}
		}

		days_mission_timeout = IRE_gateway_GER_pressure_duration

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_gateway_GER_pressure"
			effect_tooltip = {
				set_temp_variable = { IRE_GER_delta_tt = IRE_gateway_GER_pressure_duration }
				subtract_from_temp_variable = { IRE_GER_delta_tt = 5 }
				set_temp_variable = { IRE_GER_delta = IRE_GER_delta_tt }
				IRE_add_GER_influence = yes
			}
			if = {
				limit = {
					NOT = {
						check_variable = { IRE_gateway_GER_pressure_duration = 10 }
					}
				}
				set_variable = { IRE_gateway_GER_pressure_duration = 10 }
			}
		}
	}
	IRE_gateway_ENG_pressure = {
		icon = eng_trade_unions_support

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		cancel_trigger = {
			hidden_trigger = {
				IRE_gateway_finished = yes
			}
		}

		days_mission_timeout = IRE_gateway_ENG_pressure_duration

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_gateway_ENG_pressure"
			effect_tooltip = {
				set_temp_variable = { IRE_ENG_delta_tt = IRE_gateway_ENG_pressure_duration }
				subtract_from_temp_variable = { IRE_ENG_delta_tt = 5 }
				set_temp_variable = { IRE_ENG_delta = IRE_ENG_delta_tt }
				IRE_add_ENG_influence = yes
			}
			if = {
				limit = {
					NOT = {
						check_variable = { IRE_gateway_ENG_pressure_duration = 10 }
					}
				}
				set_variable = { IRE_gateway_ENG_pressure_duration = 10 }
			}
		}
	}
	IRE_gateway_long_countdown = {
		icon = generic_political_discourse

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 360

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_gateway_long_countdown"
			custom_effect_tooltip = IRE_gateway_countdown_finished
		}
	}
	IRE_gateway_short_countdown = {
		icon = generic_political_discourse

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 90

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_gateway_short_countdown"
			custom_effect_tooltip = IRE_gateway_countdown_finished
			hidden_effect = {
				remove_targeted_decision = { target = GER decision = IRE_favour_a_side_GER }
				remove_targeted_decision = { target = ENG decision = IRE_favour_a_side_ENG }
				#if = {
				#	limit = { has_active_mission = IRE_gateway_GER_pressure }
				#	remove_mission = IRE_gateway_GER_pressure
				#}
				#if = {
				#	limit = { has_active_mission = IRE_gateway_ENG_pressure }
				#	remove_mission = IRE_gateway_ENG_pressure
				#}
				clr_country_flag = IRE_gateway_started
				clr_country_flag = IRE_GER_preference
				clr_country_flag = IRE_ENG_preference
				if = {
					limit = {
						OR = {
							NOT = {
								has_completed_focus = IRE_greenest_fields
								has_completed_focus = IRE_liffey_hydro
							}
							IRE_has_bm = yes
						}
					}
					clr_country_flag = IRE_collins_success
				}
				if = {
					limit = { has_democratic_government = no }
					IRE_bop_end_effect = yes
				}
				if = {
					limit = { NOT = { has_completed_focus = IRE_world_stage } }
					unlock_national_focus = IRE_world_stage
					IRE_add_forpol_decisions = yes
				}
				if = {
					limit = { NOT = { has_completed_focus = IRE_gateway } }
					unlock_national_focus = IRE_gateway
				}
				GER = {
					remove_targeted_decision = { target = IRE decision = GER_pressure_ireland }
					remove_targeted_decision = { target = IRE decision = GER_pressure_ireland_guns }
					remove_targeted_decision = { target = IRE decision = GER_pressure_ireland_cp }
					remove_targeted_decision = { target = IRE decision = GER_pressure_ireland_xp }
					remove_decision = IRE_toggle_gateway_repeat_decisions_GER
					#if = {
					#	limit = { has_active_mission = IRE_gateway_repeat_decisions_GER }
					#	remove_mission = IRE_gateway_repeat_decisions_GER
					#}
					remove_ideas = IRE_repeating_decision_cost
					clr_country_flag = IRE_repeat_decisions
					clr_country_flag = GER_pressure_ireland_blocked
				}
				ENG = {
					remove_targeted_decision = { target = IRE decision = ENG_pressure_ireland }
					remove_targeted_decision = { target = IRE decision = ENG_pressure_ireland_guns }
					remove_targeted_decision = { target = IRE decision = ENG_pressure_ireland_cp }
					remove_targeted_decision = { target = IRE decision = ENG_pressure_ireland_xp }
					remove_decision = IRE_toggle_gateway_repeat_decisions_ENG
					#if = {
					#	limit = { has_active_mission = IRE_gateway_repeat_decisions_ENG }
					#	remove_mission = IRE_gateway_repeat_decisions_ENG
					#}
					remove_ideas = IRE_repeating_decision_cost
					clr_country_flag = IRE_repeat_decisions
					clr_country_flag = ENG_pressure_ireland_blocked
				}
				country_event = ireland.1050
				GER = { country_event = ireland.1050 }
				ENG = { country_event = ireland.1050 }
			}
		}
	}
}

IRE_gateway_to_the_atlantic_decisions_GER = {
	GER_pressure_ireland = {
		icon = eng_trade_unions_support

		allowed = { always = no }	### Targeted decision activated by event

		available = {
			NOT = { has_war_with = FROM }
			custom_trigger_tooltip = {
				tooltip = GER_pressure_ireland_NOT_blocked
				NOT = { has_country_flag = GER_pressure_ireland_blocked }
			}
		}

		visible = { country_exists = FROM }

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		priority = 100

		days_remove = 10
		modifier = { consumer_goods_factor = 0.15 }
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 25
			modifier = {
				factor = 0
				has_country_flag = IRE_gateway_ai_flag
			}
			modifier = {
				factor = 0
				set_temp_variable = { IRE_GER_larger_by = 20 }
				IRE_GER_influence_larger_by = yes
			}
			modifier = {
				factor = 0
				date > 1938.3.17
				IRE_gateway_player_trigger = yes
				set_temp_variable = { IRE_GER_larger_by = 10 }
				IRE_GER_influence_larger_by = yes
				IRE_GER_winning_influence = no
			}
			modifier = {
				factor = 2
				set_temp_variable = { IRE_ENG_larger_by = 15 }
				IRE_ENG_influence_larger_by = yes
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete GER_pressure_ireland target: [From.GetName]"
			hidden_effect = {
				random_list = {
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 20 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 30 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 35 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 40 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 45 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 50 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 60 value = 1 }
					}
				}
				IRE = { activate_mission = IRE_gateway_GER_pressure }
				set_country_flag = GER_pressure_ireland_blocked
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GER_pressure_ireland target: [From.GetName]"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_GER_delta = 5 }
			set_country_flag = IRE_GER_decision
			IRE_add_GER_influence = yes
			clr_country_flag = GER_pressure_ireland_blocked
		}
	}

	GER_pressure_ireland_guns = {
		icon = GFX_decision_generic_industry

		allowed = { always = no }	### Targeted decision activated by event

		available = {
			NOT = { has_war_with = FROM }
			custom_trigger_tooltip = {
				tooltip = GER_pressure_ireland_NOT_blocked
				NOT = { has_country_flag = GER_pressure_ireland_blocked }
			}
		}

		visible = { country_exists = FROM }

		custom_cost_text = 250_guns
		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 249 }
		}

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		priority = 90

		days_remove = 15
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = { factor = 0 }	### Player-only

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete GER_pressure_ireland_guns target: [From.GetName]"
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -249
				}
				IRE = {
					set_variable = { IRE_gateway_GER_pressure_duration = 15 }
					activate_mission = IRE_gateway_GER_pressure
				}
				set_country_flag = GER_pressure_ireland_blocked
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GER_pressure_ireland_guns target: [From.GetName]"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_GER_delta = 10 }
			set_country_flag = IRE_GER_decision
			IRE_add_GER_influence = yes
			clr_country_flag = GER_pressure_ireland_blocked
		}
	}

	GER_pressure_ireland_cp = {
		icon = GFX_decision_generic_military

		allowed = { always = no }	### Targeted decision activated by event

		available = {
			NOT = { has_war_with = FROM }
			custom_trigger_tooltip = {
				tooltip = GER_pressure_ireland_NOT_blocked
				NOT = { has_country_flag = GER_pressure_ireland_blocked }
			}
		}

		visible = { country_exists = FROM }

		custom_cost_text = CP_5
		custom_cost_trigger = {
			command_power > 4.99
		}

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		priority = 85

		days_remove = 20
		fire_only_once = no
		fixed_random_seed = no

		modifier = {
			command_power_gain = -0.2
		}

		ai_will_do = { factor = 0 }	### Player-only

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete GER_pressure_ireland_cp target: [From.GetName]"
			hidden_effect = {
				add_command_power = -5
				IRE = {
					set_variable = { IRE_gateway_GER_pressure_duration = 20 }
					activate_mission = IRE_gateway_GER_pressure
				}
				set_country_flag = GER_pressure_ireland_blocked
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GER_pressure_ireland_cp target: [From.GetName]"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_GER_delta = 15 }
			set_country_flag = IRE_GER_decision
			IRE_add_GER_influence = yes
			clr_country_flag = GER_pressure_ireland_blocked
		}
	}

	GER_pressure_ireland_xp = {
		icon = GFX_decision_soldierly_discipline

		allowed = { always = no }	### Targeted decision activated by event

		available = {
			NOT = { has_war_with = FROM }
			custom_trigger_tooltip = {
				tooltip = GER_pressure_ireland_NOT_blocked
				NOT = { has_country_flag = GER_pressure_ireland_blocked }
			}
		}

		visible = { country_exists = FROM }

		custom_cost_text = army_xp_3
		custom_cost_trigger = {
			has_army_experience > 2.99
		}

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		priority = 80

		days_remove = 25
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = { factor = 0 }	### Player-only

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete GER_pressure_ireland_xp target: [From.GetName]"
			hidden_effect = {
				army_experience = -3
				IRE = {
					set_variable = { IRE_gateway_GER_pressure_duration = 25 }
					activate_mission = IRE_gateway_GER_pressure
				}
				set_country_flag = GER_pressure_ireland_blocked
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove GER_pressure_ireland_xp target: [From.GetName]"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_GER_delta = 20 }
			set_country_flag = IRE_GER_decision
			IRE_add_GER_influence = yes
			clr_country_flag = GER_pressure_ireland_blocked
		}
	}

	IRE_gateway_repeat_decisions_GER = {
		icon = eng_trade_unions_support
		name = IRE_gateway_repeat_decisions_name
		priority = 75

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		cancel_trigger = {
			IRE = { IRE_gateway_finished = yes }
		}

		days_mission_timeout = 10

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_gateway_repeat_decisions_GER"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_GER_delta = 5 }
			set_country_flag = IRE_GER_decision
			IRE_add_GER_influence = yes
			activate_mission = IRE_gateway_repeat_decisions_GER
			hidden_effect = {
				IRE = { activate_mission = IRE_gateway_GER_pressure }
			}
		}
	}

	IRE_toggle_gateway_repeat_decisions_GER = {
		icon = GFX_decision_generic_decision
		name = IRE_toggle_gateway_repeat_decisions

		allowed = { tag = GER }

		available = {
			NOT = { has_war_with = IRE }
			if = {
				limit = { NOT = { has_country_flag = IRE_repeat_decisions } }
				custom_trigger_tooltip = {
					tooltip = GER_pressure_ireland_NOT_blocked
					NOT = { has_country_flag = GER_pressure_ireland_blocked }
				}
			}
		}

		visible = { country_exists = IRE }

		priority = 50

		cost = 0

		ai_will_do = { base = 0 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_toggle_gateway_repeat_decisions_GER"
			if = {
				limit = { has_country_flag = IRE_repeat_decisions }
				clr_country_flag = IRE_repeat_decisions		# Turn off
				custom_effect_tooltip = IRE_gateway_repeat_decisions_disable_tt
				hidden_effect = {
					remove_mission = IRE_gateway_repeat_decisions_GER
					activate_targeted_decision = { target = IRE decision = GER_pressure_ireland }
					IRE = { remove_mission = IRE_gateway_GER_pressure }
					remove_ideas = IRE_repeating_decision_cost
					clr_country_flag = GER_pressure_ireland_blocked
				}
			}
			else = {
				set_country_flag = IRE_repeat_decisions		# Turn on
				custom_effect_tooltip = IRE_gateway_repeat_decisions_enable_tt
				hidden_effect = {
					remove_targeted_decision = { target = IRE decision = GER_pressure_ireland }
					IRE = {
						if = {
							limit = { has_active_mission = IRE_gateway_GER_pressure }
							remove_mission = IRE_gateway_GER_pressure
						}
						activate_mission = IRE_gateway_GER_pressure
					}
					activate_mission = IRE_gateway_repeat_decisions_GER
					add_ideas = IRE_repeating_decision_cost
					set_country_flag = GER_pressure_ireland_blocked
				}
			}
		}
	}

	IRE_GER_gateway_long_countdown = {
		icon = generic_political_discourse

		allowed = { always = no }

		days_mission_timeout = 360

		available = {
			hidden_trigger = { always = no }
		}

		priority = 10

		fire_only_once = yes

		is_good = yes
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_GER_gateway_long_countdown"
			custom_effect_tooltip = IRE_gateway_countdown_finished
			custom_effect_tooltip = IRE_gateway_countdown_reward
		}
	}

	IRE_GER_gateway_short_countdown = {
		icon = generic_political_discourse
		priority = 10

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 90

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_GER_gateway_short_countdown"
			custom_effect_tooltip = IRE_gateway_countdown_finished
			custom_effect_tooltip = IRE_gateway_countdown_reward
		}
	}
}

IRE_gateway_to_the_atlantic_decisions_ENG = {
	ENG_pressure_ireland = {
		icon = eng_trade_unions_support

		allowed = { always = no }	### Targeted decision activated by event

		available = {
			NOT = { has_war_with = FROM }
			custom_trigger_tooltip = {
				tooltip = ENG_pressure_ireland_NOT_blocked
				NOT = { has_country_flag = ENG_pressure_ireland_blocked }
			}
		}

		visible = { country_exists = FROM }

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		priority = 100

		days_remove = 10
		modifier = { consumer_goods_factor = 0.15 }
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 25
			modifier = {
				factor = 0
				has_country_flag = IRE_gateway_ai_flag
			}
			modifier = {
				factor = 0
				set_temp_variable = { IRE_ENG_larger_by = 20 }
				IRE_ENG_influence_larger_by = yes
			}
			modifier = {
				factor = 0
				date > 1938.3.17
				IRE_gateway_player_trigger = yes
				set_temp_variable = { IRE_ENG_larger_by = 10 }
				IRE_ENG_influence_larger_by = yes
				IRE_ENG_winning_influence = no
			}
			modifier = {
				factor = 2
				set_temp_variable = { IRE_GER_larger_by = 15 }
				IRE_GER_influence_larger_by = yes
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ENG_pressure_ireland target: [From.GetName]"
			hidden_effect = {
				random_list = {
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 20 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 30 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 35 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 40 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 45 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 50 value = 1 }
					}
					10 = {
						set_country_flag = { flag = IRE_gateway_ai_flag days = 60 value = 1 }
					}
				}
				IRE = { activate_mission = IRE_gateway_ENG_pressure }
				set_country_flag = ENG_pressure_ireland_blocked
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ENG_pressure_ireland target: [From.GetName]"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_ENG_delta = 5 }
			set_country_flag = IRE_ENG_decision
			IRE_add_ENG_influence = yes
			clr_country_flag = ENG_pressure_ireland_blocked
		}
	}

	ENG_pressure_ireland_guns = {
		icon = GFX_decision_generic_industry

		allowed = { always = no }	### Targeted decision activated by event

		available = {
			NOT = { has_war_with = FROM }
			custom_trigger_tooltip = {
				tooltip = ENG_pressure_ireland_NOT_blocked
				NOT = { has_country_flag = ENG_pressure_ireland_blocked }
			}
		}

		visible = { country_exists = FROM }

		custom_cost_text = 250_guns
		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 249 }
		}

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		priority = 90

		days_remove = 15
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = { factor = 0 }	### Player-only

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ENG_pressure_ireland_guns target: [From.GetName]"
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -249
				}
				IRE = {
					set_variable = { IRE_gateway_ENG_pressure_duration = 15 }
					activate_mission = IRE_gateway_ENG_pressure
				}
				set_country_flag = ENG_pressure_ireland_blocked
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ENG_pressure_ireland_guns target: [From.GetName]"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_ENG_delta = 10 }
			set_country_flag = IRE_ENG_decision
			IRE_add_ENG_influence = yes
			clr_country_flag = ENG_pressure_ireland_blocked
		}
	}

	ENG_pressure_ireland_cp = {
		icon = GFX_decision_soldierly_discipline

		allowed = { always = no }	### Targeted decision activated by event

		available = {
			NOT = { has_war_with = FROM }
			custom_trigger_tooltip = {
				tooltip = ENG_pressure_ireland_NOT_blocked
				NOT = { has_country_flag = ENG_pressure_ireland_blocked }
			}
		}

		visible = { country_exists = FROM }

		custom_cost_text = CP_5
		custom_cost_trigger = {
			command_power > 4.99
		}

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		priority = 85

		days_remove = 20
		fire_only_once = no
		fixed_random_seed = no

		modifier = {
			command_power_gain = -0.2
		}

		ai_will_do = { factor = 0 }	### Player-only

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ENG_pressure_ireland_cp target: [From.GetName]"
			hidden_effect = {
				add_command_power = -5
				IRE = {
					set_variable = { IRE_gateway_ENG_pressure_duration = 20 }
					activate_mission = IRE_gateway_ENG_pressure
				}
				set_country_flag = ENG_pressure_ireland_blocked
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ENG_pressure_ireland_cp target: [From.GetName]"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_ENG_delta = 15 }
			set_country_flag = IRE_ENG_decision
			IRE_add_ENG_influence = yes
			clr_country_flag = ENG_pressure_ireland_blocked
		}
	}

	ENG_pressure_ireland_xp = {
		icon = GFX_decision_generic_military

		allowed = { always = no }	### Targeted decision activated by event

		available = {
			NOT = { has_war_with = FROM }
			custom_trigger_tooltip = {
				tooltip = ENG_pressure_ireland_NOT_blocked
				NOT = { has_country_flag = ENG_pressure_ireland_blocked }
			}
		}

		visible = { country_exists = FROM }

		custom_cost_text = army_xp_3
		custom_cost_trigger = {
			has_army_experience > 2.99
		}

		target_trigger = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
		}

		priority = 80

		days_remove = 25
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = { factor = 0 }	### Player-only

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ENG_pressure_ireland_xp target: [From.GetName]"
			hidden_effect = {
				army_experience = -3
				IRE = {
					set_variable = { IRE_gateway_ENG_pressure_duration = 25 }
					activate_mission = IRE_gateway_ENG_pressure
				}
				set_country_flag = ENG_pressure_ireland_blocked
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ENG_pressure_ireland_xp target: [From.GetName]"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_ENG_delta = 20 }
			set_country_flag = IRE_ENG_decision
			IRE_add_ENG_influence = yes
			clr_country_flag = ENG_pressure_ireland_blocked
		}
	}

	IRE_gateway_repeat_decisions_ENG = {
		icon = eng_trade_unions_support
		name = IRE_gateway_repeat_decisions_name
		priority = 75

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		cancel_trigger = {
			IRE = { IRE_gateway_finished = yes }
		}

		days_mission_timeout = 10

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_gateway_repeat_decisions_ENG"
			add_to_variable = { IRE_gateway_PP = 10 }
			set_temp_variable = { IRE_ENG_delta = 5 }
			set_country_flag = IRE_ENG_decision
			IRE_add_ENG_influence = yes
			activate_mission = IRE_gateway_repeat_decisions_ENG
			hidden_effect = {
				IRE = { activate_mission = IRE_gateway_ENG_pressure }
			}
		}
	}

	IRE_toggle_gateway_repeat_decisions_ENG = {
		icon = GFX_decision_generic_decision
		name = IRE_toggle_gateway_repeat_decisions
		priority = 50

		allowed = { original_tag = ENG }

		available = {
			NOT = { has_war_with = IRE }
			if = {
				limit = { NOT = { has_country_flag = IRE_repeat_decisions } }
				custom_trigger_tooltip = {
					tooltip = ENG_pressure_ireland_NOT_blocked
					NOT = { has_country_flag = ENG_pressure_ireland_blocked }
				}
			}
		}

		visible = {
			country_exists = IRE
			is_ai = no
		}

		cost = 0

		ai_will_do = { base = 0 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_toggle_gateway_repeat_decisions_ENG"
			if = {
				limit = { has_country_flag = IRE_repeat_decisions }
				clr_country_flag = IRE_repeat_decisions		# Turn off
				custom_effect_tooltip = IRE_gateway_repeat_decisions_disable_tt
				hidden_effect = {
					remove_mission = IRE_gateway_repeat_decisions_ENG
					activate_targeted_decision = { target = IRE decision = ENG_pressure_ireland }
					IRE = { remove_mission = IRE_gateway_ENG_pressure }
					remove_ideas = IRE_repeating_decision_cost
					clr_country_flag = ENG_pressure_ireland_blocked
				}
			}
			else = {
				set_country_flag = IRE_repeat_decisions		# Turn on
				custom_effect_tooltip = IRE_gateway_repeat_decisions_enable_tt
				hidden_effect = {
					remove_targeted_decision = { target = IRE decision = ENG_pressure_ireland }
					IRE = {
						if = {
							limit = { has_active_mission = IRE_gateway_ENG_pressure }
							remove_mission = IRE_gateway_ENG_pressure
						}
						activate_mission = IRE_gateway_ENG_pressure
					}
					activate_mission = IRE_gateway_repeat_decisions_ENG
					add_ideas = IRE_repeating_decision_cost
					set_country_flag = ENG_pressure_ireland_blocked
				}
			}
		}
	}

	IRE_ENG_gateway_long_countdown = {
		icon = generic_political_discourse
		priority = 10

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 360

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_ENG_gateway_long_countdown"
			custom_effect_tooltip = IRE_gateway_countdown_finished
			custom_effect_tooltip = IRE_gateway_countdown_reward
		}
	}

	IRE_ENG_gateway_short_countdown = {
		icon = generic_political_discourse
		priority = 10

		allowed = { always = no }

		available = {
			hidden_trigger = { always = no }
		}

		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 90

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_ENG_gateway_short_countdown"
			custom_effect_tooltip = IRE_gateway_countdown_finished
			custom_effect_tooltip = IRE_gateway_countdown_reward
		}
	}
}

 ### Political Decisions ###

IRE_election_decisions = {
	IRE_electoral_campaign_mission = {
		icon = GFX_decision_SWI_no_elected_president

		allowed = { always = no }

		days_mission_timeout = 90

		available = { hidden_trigger = { always = no } }

		fire_only_once = yes

		is_good = yes
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_electoral_campaign_mission"
			effect_tooltip = { country_event = ireland.31 }
			if = {
				limit = { has_idea = IRE_election_uphill_battle }
				remove_ideas = IRE_election_uphill_battle
			}
			clr_country_flag = IRE_electoral_campaign_ongoing
			hidden_effect = {
				remove_mission = IRE_sc_campaign_mission
				remove_mission = IRE_labour_campaign_mission
			}
		}
	}
	IRE_sf_campaign = {
		icon = GFX_decision_generic_political_address

		visible = {
			check_variable = { IRE_sf_influence < 100 }
		}

		cancel_trigger = {
			hidden_trigger = {
				NOT = { has_country_flag = IRE_electoral_campaign_ongoing }
			}
		}

		cost = IRE_election_decisions_cost
		days_remove = 15
		modifier = { political_power_cost = 0.25 }

		ai_will_do = {
			base = 0
			modifier = {
				add = 75
				IRE_sf_election = yes
				OR = {
					check_variable = { IRE_sf_influence < IRE_sc_influence }
					check_variable = { IRE_sf_influence < IRE_lab_influence }
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_sf_campaign"
			IRE_increase_sf_influence = yes
			IRE_increase_election_decisions_cost = yes
		}
	}
	IRE_smear_sc = {
		icon = GFX_decision_SWI_dismiss_council

		visible = { check_variable = { IRE_sc_influence > 0 } }

		cancel_trigger = {
			hidden_trigger = { NOT = { has_country_flag = IRE_electoral_campaign_ongoing } }
		}

		cost = IRE_election_decisions_cost
		days_remove = 15

		modifier = { political_power_cost = 0.25 }

		ai_will_do = {
			base = 0
			modifier = {
				add = 75
				OR = {
					AND = {
						IRE_sf_election = yes
						check_variable = { IRE_sf_influence < IRE_sc_influence }
					}
					AND = {
						IRE_lab_election = yes
						check_variable = { IRE_lab_influence < IRE_sc_influence }
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_smear_sc"
			hidden_effect = { add_political_power = IRE_election_decisions_cost }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_smear_sc"
			IRE_decrease_sc_influence = yes
			IRE_increase_election_decisions_cost = yes
		}
	}
	IRE_smear_labour = {
		icon = GFX_decision_SWI_dismiss_council

		visible = { check_variable = { IRE_lab_influence > 0 } }

		cancel_trigger = {
			hidden_trigger = { NOT = { has_country_flag = IRE_electoral_campaign_ongoing } }
		}

		cost = IRE_election_decisions_cost
		days_remove = 15

		modifier = { political_power_cost = 0.25 }

		ai_will_do = {
			base = 0
			modifier = {
				add = 75
				OR = {
					AND = {
						IRE_sf_election = yes
						check_variable = { IRE_sf_influence < IRE_lab_influence }
					}
					AND = {
						IRE_sc_election = yes
						check_variable = { IRE_sc_influence < IRE_lab_influence }
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_smear_labour"
			hidden_effect = { add_political_power = IRE_election_decisions_cost }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_smear_labour"
			IRE_decrease_labour_influence = yes
			IRE_increase_election_decisions_cost = yes
		}
	}
	IRE_sc_campaign_mission = {
		icon = GFX_decision_SWI_elect_confederation_president

		allowed = { always = no }

		cost = 25
		is_good = no
		days_remove = 14
		days_mission_timeout = 10
		selectable_mission = yes

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				IRE_sc_election = yes
			}
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_sc_campaign_mission"
			IRE_increase_sc_big_influence = yes
			hidden_effect = { country_event = { id = ireland.6000 days = 28 } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_sc_campaign_mission"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_sc_campaign_mission"
			hidden_effect = { activate_mission = IRE_sc_campaign_mission }
		}
	}
	IRE_labour_campaign_mission = {
		icon = GFX_decision_SWI_elect_confederation_president

		allowed = { always = no }

		cost = 25
		is_good = no
		days_remove = 14
		days_mission_timeout = 10
		selectable_mission = yes

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				IRE_lab_election = yes
			}
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_labour_campaign_mission"
			IRE_increase_labour_big_influence = yes
			hidden_effect = { country_event = { id = ireland.6001 days = 28 } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_labour_campaign_mission"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_labour_campaign_mission"
			hidden_effect = { activate_mission = IRE_labour_campaign_mission }
		}
	}
}

IRE_dem_decisions = {
	IRE_start_constitution = {
		icon = GFX_decision_generic_agreement

		allowed = { always = no }
		priority = 50

		available = {
			focus_progress = { focus = IRE_draft_new_constitution progress > 0 }
		}

		is_good = no
		selectable_mission = no
		days_mission_timeout = 100

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_start_constitution"
		}

		cancel_trigger = {
			hidden_trigger = { is_subject = yes }
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_start_constitution"
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_start_constitution"
			set_temp_variable = { IRE_loyalist_anger_delta = 0.05 }
			IRE_loyalist_anger_increase = yes
			set_temp_variable = { IRE_nationalist_discontent_delta = 0.05 }
			IRE_nationalist_discontent_increase = yes
			custom_effect_tooltip = tooltip_white_line
			add_ideas = IRE_disgruntled_populace
		}
	}
	IRE_improve_nationalist_discontent = {
		icon = GFX_decision_hand_palm_down

		allowed = { always = no }
		priority = 100

		available = {
			if = {
				limit = { has_country_flag = IRE_gateway_IRE_victory }
				has_completed_focus = IRE_draft_new_constitution
			}
			else = { has_completed_focus = IRE_restoration_sovereignty }
			if = {
				limit = { check_variable = { IRE_decision_duration < 25 } }
				custom_trigger_tooltip = {
					tooltip = IRE_federalisation_requirement
					NOT = { has_country_flag = IRE_federalisation_wip }
				}
			}
			check_variable = {
				tooltip = IRE_improve_nationalist_discontent_tt
				IRE_nationalist_discontent < 0
			}
		}

		state_target = yes

		cost = IRE_improve_nationalist_discontent_cost

		days_re_enable = 10

		modifier = {
			stability_factor = -0.1
			political_power_gain = -0.025
		}

		ai_will_do = {
			base = 500
			modifier = {
				factor = 0
				check_variable = { IRE_loyalist_anger > 0 }
			}
		}

		days_remove = IRE_decision_duration

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_improve_nationalist_discontent target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_improve_nationalist_discontent target: [From.GetName]"
			set_temp_variable = { IRE_nationalist_discontent_delta = 0.025 }
			IRE_nationalist_discontent_decrease = yes
			add_stability = -0.015
		}
	}
	IRE_improve_loyalist_anger = {
		icon = GFX_decision_hol_exchange_intelligence_data

		allowed = { always = no }
		priority = 100

		available = {
			if = {
				limit = { has_country_flag = IRE_gateway_IRE_victory }
				has_completed_focus = IRE_draft_new_constitution
			}
			else = { has_completed_focus = IRE_restoration_sovereignty }
			if = {
				limit = { check_variable = { IRE_decision_duration < 25 } }
				custom_trigger_tooltip = {
					tooltip = IRE_federalisation_requirement
					NOT = { has_country_flag = IRE_federalisation_wip }
				}
			}
			check_variable = {
				tooltip = IRE_improve_loyalist_anger_tt
				IRE_loyalist_anger < 0
			}
		}

		state_target = yes

		cost = IRE_improve_loyalist_anger_cost

		days_re_enable = 10

		modifier = {
			consumer_goods_factor = 0.15
			political_power_gain = -0.025
		}

		ai_will_do = { base = 500 }

		days_remove = IRE_decision_duration

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_improve_loyalist_anger target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_improve_loyalist_anger target: [From.GetName]"
			set_temp_variable = { IRE_loyalist_anger_delta = 0.05 }
			IRE_loyalist_anger_decrease = yes
			add_political_power = -15
		}
	}
	IRE_federalisation = {
		icon = GFX_decision_SOV_place_hq

		allowed = { always = no }
		priority = 100

		available = {
			has_full_control_of_state = FROM
		}

		visible = {
			hidden_trigger = {
				has_full_control_of_state = FROM
			}
		}

		cost = 25
		fire_only_once = yes
		state_target = yes

		cancel_if_not_visible = yes

		modifier = {
			consumer_goods_factor = 0.10
			stability_factor = -0.1
			political_power_gain = -0.1
		}

		ai_will_do = { base = 1000 }

		days_remove = 30

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_federalisation target: [From.GetName]"
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_federalisation target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_federalisation target: [From.GetName]"
			custom_effect_tooltip = IRE_federalisation_tt
			if = {
				limit = { check_variable = { IRE_federalisation_decisions_done > 2 } }
				clear_variable = IRE_federalisation_decisions_done
				clr_country_flag = IRE_federalisation_wip
				remove_targeted_decision = { target = 113 decision = IRE_federalisation }
				remove_targeted_decision = { target = 119 decision = IRE_federalisation }
				remove_targeted_decision = { target = 134 decision = IRE_federalisation }
				remove_targeted_decision = { target = 135 decision = IRE_federalisation }
				unlock_decision_tooltip = IRE_improve_nationalist_discontent
				unlock_decision_tooltip = IRE_improve_loyalist_anger
				custom_effect_tooltip = tooltip_white_line
			}
			else = {
				add_to_variable = { IRE_federalisation_decisions_done = 1 }
				custom_effect_tooltip = IRE_federalisation_outcome_tt
				custom_effect_tooltip = tooltip_white_line
			}
			add_stability = 0.015
		}
	}
}

IRE_reactionary_decisions = {
	IRE_NIR_mission = {
		icon = GFX_decision_generic_civil_support
		allowed = { always = no }
		available = { hidden_trigger = { always = no } }
		selectable_mission = no
		fixed_random_seed = no
		days_mission_timeout = IRE_NIR_mission_time
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_NIR_mission"
			IRE_update_resistance = yes
			if = {
				limit = { check_variable = { IRE_ulster_resistance < 1 } }
				add_to_variable = { IRE_ulster_resistance = IRE_ulster_resistance_delta }
				clamp_variable = {
					var = IRE_ulster_resistance
					max = 1
				}
				custom_effect_tooltip = IRE_reluctant_countrymen_modified
				custom_effect_tooltip = IRE_ulster_resistance_target_change_tt
			}
			else = { 119 = { add_resistance = 10 } }
			force_update_dynamic_modifier = yes
			activate_mission = IRE_NIR_mission
			custom_effect_tooltip = tooltip_white_line
			if = {
				limit = {
					has_country_flag = IRE_NIR_revolt_available
					OR = {
						check_variable = { IRE.IRE_loyalist_fear > 79 }
						AND = {
							has_country_flag = IRE_NIR_revolt_guaranteed
							OR = {
								AND = {
									check_variable = { IRE.IRE_loyalist_fear > 74 }
									has_country_flag = { flag = IRE_NIR_revolt_available days > 120 }
								}
								AND = {
									check_variable = { IRE.IRE_loyalist_fear > 69 }
									has_country_flag = { flag = IRE_NIR_revolt_available days > 135 }
								}
								AND = {
									check_variable = { IRE.IRE_loyalist_fear > 64 }
									has_country_flag = { flag = IRE_NIR_revolt_available days > 150 }
								}
								AND = {
									check_variable = { IRE.IRE_loyalist_fear > 59 }
									has_country_flag = { flag = IRE_NIR_revolt_available days > 165 }
								}
								has_country_flag = { flag = IRE_NIR_revolt_available days > 180 }
							}
						}
					}
					NOT = { has_country_flag = IRE_NIR_revolt_avoided }
				}
				IRE_ulster_revolt = yes
				hidden_effect = { activate_mission = IRE_take_belfast }
			}
			else = {
				custom_effect_tooltip = IRE_ulster_revolt_may_occur_tt
				custom_effect_tooltip = tooltip_white_line
				if = {
					limit = { 119 = { resistance > 90 } }
					IRE_loyalist_fear_increase_a_lot = yes
				}
				else_if = {
					limit = { 119 = { resistance > 75 } }
					random_list = {
						20 = { IRE_loyalist_fear_increase = yes }
						20 = {
							set_temp_variable = { IRE_nationalist_discontent_delta = 0.03 }
							IRE_nationalist_discontent_increase = yes
						}
						20 = {
							set_temp_variable = { IRE_loyalist_anger_delta = 0.03 }
							IRE_loyalist_anger_increase = yes
						}
					}
				}
				else_if = {
					limit = { 119 = { resistance > 50 } }
					random_list = {
						20 = { 119 = { add_resistance = 8 } }
						20 = { 119 = { add_compliance = -4 } }
						20 = {
							119 = {
								damage_building = {
									type = infrastructure
									damage = 0.6
								}
								damage_building = {
									type = industrial_complex
									damage = 0.6
								}
							}
						}
						10 = {
							set_temp_variable = { IRE_nationalist_discontent_delta = 0.02 }
							IRE_nationalist_discontent_increase = yes
						}
						10 = {
							set_temp_variable = { IRE_loyalist_anger_delta = 0.02 }
							IRE_loyalist_anger_increase = yes
						}
						20 = {}
					}
				}
				else_if = {
					limit = { 119 = { resistance > 25 } }
					random_list = {
						20 = { 119 = { add_resistance = 4 } }
						20 = { 119 = { add_compliance = -2 } }
						20 = {
							119 = {
								damage_building = {
									type = infrastructure
									damage = 0.3
								}
								damage_building = {
									type = industrial_complex
									damage = 0.3
								}
							}
						}
						40 = {}
					}
				}
				else = { custom_effect_tooltip = IRE_NIR_mission_tt }
			}
		}
	}
	IRE_take_belfast = {
		icon = GFX_decision_generic_prepare_civil_war
		allowed = { always = no }
		available = { NOT = { country_exists = NIR } }
		is_good = no
		fire_only_once = yes
		days_mission_timeout = 150
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_take_belfast"
			set_temp_variable = { IRE_nationalist_discontent_delta = 0.1 }
			IRE_nationalist_discontent_decrease = yes
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_take_belfast"
			NIR = { white_peace_with_ROOT = yes }
			country_event = ireland.3420
		}
		cancel_trigger = {
			NOT = { controls_state = 113 }
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_take_belfast"
			NIR = { white_peace_with_ROOT = yes }
			country_event = ireland.3420
		}
	}
	IRE_improve_discontent_mission = {
		icon = GFX_decision_generic_speech
		allowed = { always = no }
		available = { has_country_flag = IRE_has_improved_discontent }
		is_good = yes
		selectable_mission = yes
		days_mission_timeout = IRE_NIR_mission_time_long
		days_remove = IRE_NIR_mission_remove
		ai_will_do = { base = 1000 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_improve_discontent_mission"
			set_variable = { IRE_NIR_mission_remove = days_mission_timeout@IRE_improve_discontent_mission }
		}
		cancel_trigger = { check_variable = { IRE_nationalist_discontent = 0 } }
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_improve_discontent_mission"
			set_temp_variable = { IRE_nationalist_discontent_delta = 0.05 }
			IRE_nationalist_discontent_increase = yes
			IRE_update_resistance = yes
			activate_mission = IRE_improve_discontent_mission
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_mission_completion
			effect_tooltip = {
				119 = { add_compliance = 3 }
				activate_mission = IRE_improve_discontent_mission
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_improve_discontent_mission"
			119 = { add_compliance = 3 }
			activate_mission = IRE_improve_discontent_mission
			clr_country_flag = IRE_has_improved_discontent
		}
	}
	IRE_improve_anger_mission = {
		icon = GFX_decision_generic_nationalism
		allowed = { always = no }
		available = { has_country_flag = IRE_has_improved_anger }
		is_good = yes
		selectable_mission = yes
		days_mission_timeout = IRE_NIR_mission_time_long
		days_remove = IRE_NIR_mission_remove
		ai_will_do = { base = 1000 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_improve_anger_mission"
			set_variable = { IRE_NIR_mission_remove = days_mission_timeout@IRE_improve_anger_mission }
		}
		cancel_trigger = { check_variable = { IRE_loyalist_anger = 0 } }
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_improve_anger_mission"
			set_temp_variable = { IRE_loyalist_anger_delta = 0.05 }
			IRE_loyalist_anger_increase = yes
			IRE_update_resistance = yes
			activate_mission = IRE_improve_anger_mission
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_mission_completion
			effect_tooltip = {
				119 = { add_resistance = -5 }
				activate_mission = IRE_improve_anger_mission
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_improve_anger_mission"
			119 = { add_resistance = -5 }
			activate_mission = IRE_improve_anger_mission
			clr_country_flag = IRE_has_improved_anger
		}
	}
	IRE_counter_protestant_influence = {
		icon = GFX_decision_generic_military
		allowed = { always = no }
		available = { has_completed_focus = IRE_northern_catholics }
		is_good = no
		selectable_mission = no
		days_mission_timeout = 150
		cancel_trigger = { hidden_trigger = { has_dictatorship_government = no } }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_counter_protestant_influence"
			set_temp_variable = { ideology_change_var = 0.05 }
			change_ruling_party_popularity = yes
			119 = { add_compliance = 7.5 }
			set_variable = { IRE_ulster_resistance_delta = 0.075 }
			add_to_variable = { IRE_ulster_resistance = IRE_ulster_resistance_delta }
			force_update_dynamic_modifier = yes
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_reluctant_countrymen_modified
			custom_effect_tooltip = IRE_ulster_resistance_target_change_tt
			IRE_update_resistance = yes
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_counter_protestant_influence"
			set_temp_variable = { IRE_nationalist_discontent_delta = 0.075 }
			IRE_nationalist_discontent_increase = yes
			set_temp_variable = { IRE_loyalist_anger_delta = 0.025 }
			IRE_loyalist_anger_decrease = yes
			set_temp_variable = { ideology_change_var = -0.1 }
			change_ruling_party_popularity = yes
			119 = { add_compliance = -10 }
			IRE_update_resistance = yes
		}
	}
	IRE_an_improve_nationalist_discontent = {
		icon = GFX_decision_soldierly_discipline
		name = IRE_improve_nationalist_discontent

		allowed = { original_tag = IRE }
		priority = 40

		available = {
			check_variable = {
				tooltip = IRE_improve_nationalist_discontent_tt
				IRE_nationalist_discontent < 0
			}
			check_variable = {
				tooltip = IRE_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			owns_state = 119 # Northern Ireland
		}

		visible = { has_country_flag = IRE_nir_resistance_started }

		cost = IRE_NIR_decisions_cost

		modifier = {
			stability_factor = -0.15
		}

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				has_country_flag = IRE_has_improved_discontent
				NOT = { has_completed_focus = IRE_chinsealacht_gaelach }
				NOT = { has_completed_focus = IRE_gan_anam }
				check_variable = { IRE_loyalist_anger > 0 }
			}
		}

		days_remove = IRE_NIR_decisions_time

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_an_improve_nationalist_discontent"
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_an_improve_nationalist_discontent"
			if = {
				limit = {
					OR = {
						has_completed_focus = IRE_chinsealacht_gaelach
						has_completed_focus = IRE_gan_anam
					}
				}
				set_temp_variable = { IRE_nationalist_discontent_delta = 0.05 }
				IRE_nationalist_discontent_decrease = yes
			}
			else = {
				set_temp_variable = { IRE_nationalist_discontent_delta = 0.025 }
				IRE_nationalist_discontent_decrease = yes
				custom_effect_tooltip = tooltip_white_line
				if = {
					limit = { IRE_has_pataut_govt = yes }
					custom_effect_tooltip = IRE_eod_improve_nationalist_discontent_upgrade_tt
				}
				else = { custom_effect_tooltip = IRE_goc_improve_nationalist_discontent_upgrade_tt }
			}
			IRE_update_resistance = yes
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
			set_country_flag = IRE_has_improved_discontent
			if = {
				limit = { has_global_flag = IRE_had_NIR_revolt }
				custom_effect_tooltip = tooltip_white_line
				119 = { add_compliance = 5 }
			}
		}
	}
	IRE_an_improve_loyalist_anger = {
		icon = GFX_decision_hand_out
		name = IRE_improve_loyalist_anger

		allowed = { original_tag = IRE }
		priority = 40

		available = {
			check_variable = {
				tooltip = IRE_improve_loyalist_anger_tt
				IRE_loyalist_anger < 0
			}
			check_variable = {
				tooltip = IRE_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			owns_state = 119 # Northern Ireland
		}

		visible = { has_country_flag = IRE_nir_resistance_started }

		cost = IRE_NIR_decisions_cost

		modifier = {
			political_power_gain = -0.1
		}
		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				has_country_flag = IRE_has_improved_anger
				NOT = { has_completed_focus = IRE_rerum_novarum }
				NOT = { has_completed_focus = IRE_ecumenism }
			}
		}

		days_remove = IRE_NIR_decisions_time

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_an_improve_loyalist_anger"
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_an_improve_loyalist_anger"
			if = {
				limit = {
					OR = {
						has_completed_focus = IRE_rerum_novarum
						has_completed_focus = IRE_ecumenism
					}
				}
				set_temp_variable = { IRE_loyalist_anger_delta = 0.05 }
				IRE_loyalist_anger_decrease = yes
			}
			else = {
				set_temp_variable = { IRE_loyalist_anger_delta = 0.025 }
				IRE_loyalist_anger_decrease = yes
				custom_effect_tooltip = tooltip_white_line
				if = {
					limit = { IRE_has_pataut_govt = yes }
					custom_effect_tooltip = IRE_eod_improve_loyalist_anger_upgrade_tt
				}
				else = { custom_effect_tooltip = IRE_goc_improve_loyalist_anger_upgrade_tt }
			}
			IRE_update_resistance = yes
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
			set_country_flag = IRE_has_improved_anger
			if = {
				limit = { has_global_flag = IRE_had_NIR_revolt }
				custom_effect_tooltip = tooltip_white_line
				119 = { add_compliance = 5 }
			}
		}
	}
	IRE_an_improve_loyalist_anger_2 = {
		icon = GFX_decision_generic_welfare

		allowed = { original_tag = IRE }
		priority = 40

		available = {
			has_completed_focus = IRE_adopt_corporatism
			check_variable = {
				tooltip = IRE_improve_loyalist_anger_tt
				IRE_loyalist_anger < 0
			}
			check_variable = {
				tooltip = IRE_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			owns_state = 119 # Northern Ireland
		}

		visible = { has_country_flag = IRE_nir_resistance_started }

		cost = 40
		days_re_enable = 60

		modifier = {
			consumer_goods_factor = 0.40
		}

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				OR = {
					AND = {
						has_country_flag = IRE_has_improved_anger
						NOT = { has_country_flag = IRE_has_improved_discontent }
					}
					AND = {
						NOT = { has_country_flag = IRE_has_improved_anger }
						check_variable = { IRE_NIR_decisions_time_long = 45 }
						check_variable = { days_mission_timeout@IRE_improve_anger_mission < 46 }
					}
					AND = {
						NOT = { has_country_flag = IRE_has_improved_anger }
						check_variable = { IRE_NIR_decisions_time_long = 25 }
						check_variable = { days_mission_timeout@IRE_improve_anger_mission < 26 }
					}
				}
				has_country_flag = IRE_has_improved_anger
			}
		}

		days_remove = IRE_NIR_decisions_time_long

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_an_improve_loyalist_anger_2"
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_an_improve_loyalist_anger_2"
			set_temp_variable = { IRE_loyalist_anger_delta = 0.06 }
			IRE_loyalist_anger_decrease = yes
			if = {
				limit = { has_global_flag = IRE_had_NIR_revolt }
				custom_effect_tooltip = tooltip_white_line
				119 = { add_compliance = 15 }
			}
			else = { 119 = { add_compliance = 10 } }
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_60_days_tt
			IRE_update_resistance = yes
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
			set_country_flag = IRE_has_improved_anger
		}
	}
	IRE_an_use_blueshirts = {
		icon = GFX_decision_eng_blackshirt_march

		allowed = { original_tag = IRE }
		priority = 40

		available = {
			check_variable = {
				tooltip = IRE_improve_nationalist_discontent_tt
				IRE_nationalist_discontent < 0
			}
			check_variable = {
				tooltip = IRE_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			owns_state = 119 # Northern Ireland
		}

		visible = { has_country_flag = IRE_nir_resistance_started }

		cost = IRE_NIR_decisions_cost

		modifier = {
			stability_factor = -0.15
			political_power_gain = -0.15
		}

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				check_variable = { IRE_nationalist_discontent_delta < 0.08 }
				check_variable = { IRE_loyalist_anger_delta > 0 }
			}
		}

		days_remove = IRE_NIR_decisions_time

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_an_use_blueshirts"
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_an_use_blueshirts"
			if = {
				limit = { has_completed_focus = IRE_legitimise_blueshirts }
				set_temp_variable = { IRE_nationalist_discontent_delta = 0.08 }
				IRE_nationalist_discontent_decrease = yes
				set_temp_variable = { IRE_loyalist_anger_delta = 0.04 }
				IRE_loyalist_anger_increase = yes
			}
			else = {
				set_temp_variable = { IRE_nationalist_discontent_delta = 0.04 }
				IRE_nationalist_discontent_decrease = yes
				set_temp_variable = { IRE_loyalist_anger_delta = 0.02 }
				IRE_loyalist_anger_increase = yes
				custom_effect_tooltip = tooltip_white_line
				custom_effect_tooltip = IRE_an_use_blueshirts_upgrade_tt
			}
			IRE_update_resistance = yes
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
			set_country_flag = IRE_has_improved_discontent
			if = {
				limit = { has_global_flag = IRE_had_NIR_revolt }
				custom_effect_tooltip = tooltip_white_line
				119 = { add_compliance = 5 }
			}
		}
	}
	IRE_arm_gardai_decision = {
		icon = GFX_decision_generic_police_action

		allowed = { original_tag = IRE }
		priority = 40

		available = {
			has_completed_focus = IRE_arm_gardai
			check_variable = {
				tooltip = IRE_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			owns_state = 119 # Northern Ireland
		}

		visible = {
			has_country_flag = IRE_nir_resistance_started
			check_variable = { IRE_ulster_resistance > 0 }
		}

		custom_cost_text = IRE_arm_gardai_cost_tt
		custom_cost_trigger = {
			command_power > 39.99
			has_equipment = { infantry_equipment > 299 }
		}
		days_re_enable = 30
		state_target = yes
		targets = { 119 }

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				NOT = { has_country_flag = IRE_has_improved_anger }
				NOT = { has_country_flag = IRE_has_improved_discontent }
				check_variable = { IRE_ulster_resistance < 0.3 }
			}
		}

		days_remove = IRE_NIR_decisions_time

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_arm_gardai_decision target: [From.GetName]"
			hidden_effect = {
				add_command_power = -40
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -300
				}
			}
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_arm_gardai_decision target: [From.GetName]"
			if = {
				limit = { has_completed_focus = IRE_partisan_divide }
				119 = { add_resistance = -10 }
				set_variable = { IRE_ulster_resistance_delta = -0.1 }
				add_to_variable = { IRE_ulster_resistance = IRE_ulster_resistance_delta }
				force_update_dynamic_modifier = yes
				custom_effect_tooltip = tooltip_white_line
				custom_effect_tooltip = IRE_upgraded_arm_gardai_tt
			}
			else = {
				119 = { add_resistance = -5 }
				set_variable = { IRE_ulster_resistance_delta = -0.05 }
				add_to_variable = { IRE_ulster_resistance = IRE_ulster_resistance_delta }
				force_update_dynamic_modifier = yes
				custom_effect_tooltip = tooltip_white_line
				custom_effect_tooltip = IRE_arm_gardai_tt
				if = {
					limit = { has_government = paternal_autocrat }
					custom_effect_tooltip = IRE_arm_gardai_upgrade_tt
				}
			}
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_30_days_tt
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
		}
	}
	IRE_goc_improve_loyalist_anger = {
		icon = GFX_decision_generic_monarchy

		allowed = { original_tag = IRE }
		priority = 40

		visible = {
			has_country_flag = IRE_nir_resistance_started
			has_government = national_populist
		}

		available = {
			check_variable = {
				tooltip = IRE_improve_loyalist_anger_tt
				IRE_loyalist_anger < 0
			}
			check_variable = {
				tooltip = IRE_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			OR = {
				has_completed_focus = IRE_the_restoration
				has_completed_focus = IRE_the_regency
			}
			owns_state = 119 # Northern Ireland
		}
		visible = { has_government = national_populist }

		ai_hint_pp_cost = 40
		custom_cost_text = IRE_goc_improve_loyalist_anger_cost_tt
		custom_cost_trigger = {
			has_political_power > 39.99
		}

		modifier = {
			consumer_goods_factor = 0.15
			political_power_gain = -0.15
		}

		ai_will_do = { base = 100 }

		days_remove = IRE_NIR_decisions_time
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_goc_improve_loyalist_anger"
			hidden_effect = {
				add_political_power = -40
				add_popularity = { ideology = national_populist popularity = -0.04 }
			}
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_goc_improve_loyalist_anger"
			set_temp_variable = { IRE_loyalist_anger_delta = 0.08 }
			IRE_loyalist_anger_decrease = yes
			IRE_loyalist_fear_decrease_a_lot = yes
			IRE_update_resistance = yes
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_only_once_tt
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
			set_country_flag = IRE_has_improved_anger
			if = {
				limit = { has_global_flag = IRE_had_NIR_revolt }
				custom_effect_tooltip = tooltip_white_line
				119 = { add_compliance = 10 }
			}
		}
	}
}

IRE_socialist_decisions = {
	IRE_soc_church_forceful = {
		icon = GFX_decision_generic_brainwash

		allowed = { original_tag = IRE }
		priority = 10

		visible = {
			NOT = { has_country_flag = IRE_soc_church_taken }
			has_completed_focus = IRE_secure_countryside
		}

		days_remove = 30
		cost = 10
		fire_only_once = yes

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				has_country_flag = IRE_polpath_se
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_church_forceful"
			custom_effect_tooltip = IRE_soc_church_forceful_tt
			set_country_flag = IRE_soc_church_taken
			add_to_variable = { IRE_soc_tyranny = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_church_forceful"
			IRE_scale_down_anti_socialist_agitation = yes
			add_stability = -0.02
			add_popularity = {
				ideology = totalist
				popularity = -0.05
			}
			add_popularity = {
				ideology = social_conservative
				popularity = -0.03
			}
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_weaken_totalists_tt
		}
	}
	IRE_soc_church_not_forceful = {
		icon = GFX_decision_hol_exchange_intelligence_data

		allowed = { original_tag = IRE }
		priority = 10

		visible = {
			NOT = { has_country_flag = IRE_soc_church_taken }
			has_completed_focus = IRE_secure_countryside
		}

		days_remove = 60
		cost = 30
		fire_only_once = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 1000
				has_country_flag = IRE_polpath_se
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_church_not_forceful"
			custom_effect_tooltip = IRE_soc_church_not_forceful_tt
			set_country_flag = IRE_soc_church_taken
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_church_not_forceful"
			IRE_scale_down_anti_socialist_agitation = yes
			set_temp_variable = { IRE_legitimacy_delta = 3 }
			IRE_increase_legitimacy = yes
			add_popularity = {
				ideology = social_conservative
				popularity = 0.04
			}
		}
	}
	IRE_soc_industry_forceful = {
		icon = GFX_decision_generic_wreckers

		allowed = { original_tag = IRE }
		priority = 10

		visible = {
			NOT = { has_country_flag = IRE_soc_industry_taken }
			has_completed_focus = IRE_suppress_paramilitaries
		}

		days_remove = 30
		cost = 10
		fire_only_once = yes

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				has_country_flag = IRE_polpath_se
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_industry_forceful"
			custom_effect_tooltip = IRE_soc_industry_forceful_tt
			set_country_flag = IRE_soc_industry_taken
			add_to_variable = { IRE_soc_tyranny = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_industry_forceful"
			IRE_scale_down_anti_socialist_agitation = yes
			add_stability = -0.03
			add_popularity = {
				ideology = totalist
				popularity = -0.04
			}
			add_popularity = {
				ideology = syndicalist
				popularity = 0.04
			}
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_weaken_totalists_tt
		}
	}
	IRE_soc_industry_not_forceful = {
		icon = GFX_decision_generic_fundraising

		allowed = { original_tag = IRE }
		priority = 10

		visible = {
			NOT = { has_country_flag = IRE_soc_industry_taken }
			has_completed_focus = IRE_suppress_paramilitaries
		}

		days_remove = 60
		cost = 30
		fire_only_once = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 1000
				has_country_flag = IRE_polpath_se
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_industry_not_forceful"
			custom_effect_tooltip = IRE_soc_industry_not_forceful_tt
			set_country_flag = IRE_soc_industry_taken
		}

		modifier = {
			consumer_goods_factor = 0.4
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_industry_not_forceful"
			IRE_scale_down_anti_socialist_agitation = yes
			add_popularity = {
				ideology = totalist
				popularity = 0.04
			}
			set_temp_variable = { IRE_legitimacy_delta = 3 }
			IRE_increase_legitimacy = yes
		}
	}
	IRE_soc_civil_forceful = {
		icon = GFX_decision_gre_faction_management

		allowed = { original_tag = IRE }
		priority = 10

		visible = {
			NOT = { has_country_flag = IRE_soc_civil_taken }
			has_completed_focus = IRE_suppress_paramilitaries
		}

		days_remove = 30
		cost = 10
		fire_only_once = yes

		ai_will_do = {
			base = 0
			modifier = {
				add = 1000
				has_country_flag = IRE_polpath_se
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_civil_forceful"
			custom_effect_tooltip = IRE_soc_civil_forceful_tt
			set_country_flag = IRE_soc_civil_taken
			add_to_variable = { IRE_soc_tyranny = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_civil_forceful"
			IRE_scale_down_anti_socialist_agitation = yes
			add_stability = -0.05
			add_popularity = {
				ideology = totalist
				popularity = 0.075
			}
			add_popularity = {
				ideology = syndicalist
				popularity = 0.025
			}
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_weaken_totalists_tt
		}
	}
	IRE_soc_civil_not_forceful = {
		icon = GFX_decision_SWI_dismiss_council

		allowed = { original_tag = IRE }
		priority = 10

		visible = {
			NOT = { has_country_flag = IRE_soc_civil_taken }
			has_completed_focus = IRE_suppress_paramilitaries
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_civil_not_forceful"
			custom_effect_tooltip = IRE_soc_civil_not_forceful_tt
			set_country_flag = IRE_soc_civil_taken
		}

		days_remove = 60
		cost = 30
		fire_only_once = yes

		ai_will_do = { base = 1000 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_civil_not_forceful"
			IRE_scale_down_anti_socialist_agitation = yes
			set_temp_variable = { IRE_legitimacy_delta = 3 }
			IRE_increase_legitimacy = yes
		}
	}
	IRE_soc_countryside = {
		icon = GFX_decision_POL_organize_strike_two

		allowed = { original_tag = IRE }
		priority = 7

		visible = { has_completed_focus = IRE_secure_countryside }

		available = {
			if = {
				limit = { is_ai = no }
				divisions_in_state = {
					size > 2
					state = 113
				}
				divisions_in_state = {
					size > 1
					state = 134
				}
				divisions_in_state = {
					size > 1
					state = 135
				}
			}
		}

		days_remove = 60
		cost = 20
		fire_only_once = yes

		ai_will_do = { base = 1000 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_countryside"
			IRE_scale_down_anti_socialist_agitation = yes
			set_temp_variable = { IRE_legitimacy_delta = 3 }
			IRE_increase_legitimacy = yes
			add_command_power = 45
		}
	}
	IRE_cooperate_isp = {
		icon = GFX_decision_generic_political_rally

		allowed = { original_tag = IRE }
		priority = 10

		visible = { has_completed_focus = IRE_allies_on_left }

		days_remove = 90
		cost = 20
		fire_only_once = yes

		ai_will_do = { base = 1000 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_cooperate_isp"
			IRE_scale_down_anti_socialist_agitation = yes
			add_popularity = {
				ideology = syndicalist
				popularity = 0.05
			}
			set_temp_variable = { IRE_legitimacy_delta = 3 }
			IRE_increase_legitimacy = yes
		}
	}
	IRE_cooperate_ica = {
		icon = GFX_decision_hand_out
		ai_will_do = { base = 1000 }

		priority = 10

		visible = { has_completed_focus = IRE_allies_on_left }

		days_remove = 90
		cost = 20
		fire_only_once = yes

		allowed = { original_tag = IRE }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_cooperate_ica"
			IRE_scale_down_anti_socialist_agitation = yes
			add_popularity = {
				ideology = radical_socialist
				popularity = 0.025
			}
			add_popularity = {
				ideology = totalist
				popularity = -0.025
			}
			set_temp_variable = { IRE_legitimacy_delta = 3 }
			IRE_increase_legitimacy = yes
		}
	}
	IRE_sideline_opposition = {
		icon = GFX_decision_SWI_elect_confederation_president

		allowed = { original_tag = IRE }
		priority = 10

		visible = { has_completed_focus = IRE_ostracise_opposition }

		days_remove = 90
		cost = 20
		fire_only_once = yes

		ai_will_do = { base = 1000 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_sideline_opposition"
			IRE_scale_down_anti_socialist_agitation = yes
			add_popularity = {
				ideology = radical_socialist
				popularity = 0.025
			}
			add_popularity = {
				ideology = social_democrat
				popularity = -0.025
			}
			set_temp_variable = { IRE_legitimacy_delta = 3 }
			IRE_increase_legitimacy = yes
		}
	}
	IRE_control_narrative_decision = {
		icon = GFX_decision_eng_trade_unions_support

		allowed = { original_tag = IRE }
		priority = 10

		visible = { has_completed_focus = IRE_control_narrative }

		days_remove = 90
		cost = 20
		fire_only_once = yes

		ai_will_do = { base = 1000 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_control_narrative_decision"
			IRE_scale_down_anti_socialist_agitation = yes
			add_stability = 0.03
			set_temp_variable = { IRE_legitimacy_delta = 3 }
			IRE_increase_legitimacy = yes
		}
	}
	IRE_increase_legitimacy_decision = {
		icon = GFX_decision_eng_propaganda_campaigns

		allowed = { original_tag = IRE }
		priority = 5
		visible = {
			check_variable = { IRE_legitimacy < 96 }
			has_country_flag = IRE_legitimacy_decisions_unlocked
		}

		days_re_enable = 180
		cost = 75

		ai_will_do = { base = 0 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_increase_legitimacy_decision"
			set_temp_variable = { IRE_legitimacy_delta = 6 }
			IRE_increase_legitimacy = yes
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_180_days_tt
		}
	}
	IRE_international_propaganda = {
		icon = GFX_decision_eng_propaganda_campaigns

		allowed = { original_tag = IRE }
		priority = 5

		available = {
			NOT = { has_completed_focus = IRE_ruby_isle }
			if = {
				limit = { has_government = radical_socialist }
				has_completed_focus = IRE_progressive_labour
			}
			else = { has_completed_focus = IRE_protection_of_revolution }
		}

		visible = { has_country_flag = IRE_legitimacy_decisions_unlocked }

		custom_cost_text = IRE_legitimacy_cost_tt
		custom_cost_trigger = {
			check_variable = { IRE_legitimacy > 49 }
		}

		days_re_enable = 180

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				if = {
					limit = { has_government = radical_socialist }
					check_variable = { IRE_legitimacy_spent > 15 }
				}
				else = { check_variable = { IRE_legitimacy_spent > 9 } }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_international_propaganda"
			add_to_variable = { IRE_legitimacy_spent = IRE_legitimacy_cost }
			set_temp_variable = { IRE_legitimacy_delta = IRE_legitimacy_cost }
			IRE_decrease_legitimacy = yes
			add_war_support = 0.05
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_180_days_tt
		}
	}
	IRE_emergency_powers = {
		icon = GFX_decision_generic_political_address

		allowed = { original_tag = IRE }
		priority = 5

		available = {
			NOT = { has_completed_focus = IRE_ruby_isle }
			if = {
				limit = { has_government = radical_socialist }
				has_completed_focus = IRE_progressive_labour
			}
			else = { has_completed_focus = IRE_protection_of_revolution }
		}

		visible = { has_country_flag = IRE_legitimacy_decisions_unlocked }

		custom_cost_text = IRE_legitimacy_cost_tt
		custom_cost_trigger = {
			check_variable = { IRE_legitimacy > 49 }
		}

		days_re_enable = 180

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				if = {
					limit = { has_government = radical_socialist }
					check_variable = { IRE_legitimacy_spent > 15 }
				}
				else = { check_variable = { IRE_legitimacy_spent > 9 } }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_emergency_powers"
			add_to_variable = { IRE_legitimacy_spent = IRE_legitimacy_cost }
			set_temp_variable = { IRE_legitimacy_delta = IRE_legitimacy_cost }
			IRE_decrease_legitimacy = yes
			add_political_power = 50
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_180_days_tt
		}
	}
	IRE_class_consciousness = {
		icon = GFX_decision_SOV_the_workers_dictatorship

		allowed = { original_tag = IRE }
		priority = 5

		available = {
			NOT = { has_completed_focus = IRE_ruby_isle }
			if = {
				limit = { has_government = radical_socialist }
				has_completed_focus = IRE_progressive_labour
			}
			else = { has_completed_focus = IRE_protection_of_revolution }
		}

		visible = { has_country_flag = IRE_legitimacy_decisions_unlocked }

		custom_cost_text = IRE_legitimacy_cost_tt
		custom_cost_trigger = {
			check_variable = { IRE_legitimacy > 49 }
		}

		days_re_enable = 180

		ai_will_do = {
			base = 1000
			modifier = {
				factor = 0
				if = {
					limit = { has_government = radical_socialist }
					check_variable = { IRE_legitimacy_spent > 15 }
				}
				else = { check_variable = { IRE_legitimacy_spent > 9 } }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_class_consciousness"
			add_to_variable = { IRE_legitimacy_spent = IRE_legitimacy_cost }
			set_temp_variable = { IRE_legitimacy_delta = IRE_legitimacy_cost }
			IRE_decrease_legitimacy = yes
			add_stability = 0.05
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_180_days_tt
		}
	}
	IRE_counter_insurgency = {
		icon = GFX_decision_generic_arrest

		allowed = { original_tag = IRE }
		priority = 6

		visible = { has_active_mission = IRE_reactionary_resistance_grows }

		custom_cost_text = IRE_legitimacy_cost_small_tt
		custom_cost_trigger = {
			check_variable = { IRE_legitimacy > 0 }
		}

		days_re_enable = 45

		ai_will_do = { base = 0 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_counter_insurgency"
			set_temp_variable = { IRE_legitimacy_delta = IRE_legitimacy_cost_small }
			IRE_decrease_legitimacy = yes
			add_command_power = 30
		}
	}
	IRE_weaken_reactionary_resistance = {
		icon = GFX_decision_generic_arrest

		allowed = { always = no }
		priority = 6

		visible = {
			check_variable = { FROM.IRE_antisoc_local < 0 }
		}

		custom_cost_text = CP_15
		custom_cost_trigger = {
			command_power > 14.99
		}

		state_target = yes
		targets = { 113 134 135 }
		cost = 0

		days_remove = 15

		ai_will_do = { base = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_weaken_reactionary_resistance target: [From.GetName]"
			hidden_effect = {
				add_command_power = -15
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_weaken_reactionary_resistance target: [From.GetName]"
			if = {
				limit = { has_completed_focus = IRE_reorganise_ica }
				set_temp_variable = { temp_change_anti_socialist_agitation = 0.2 }
			}
			else = { set_temp_variable = { temp_change_anti_socialist_agitation = 0.1 } }
			FROM = { IRE_local_anti_socialist_agitation_effect = yes }
			if = {
				limit = {
					OR = {
						AND = {
							has_completed_focus = IRE_reorganise_ica
							check_variable = { FROM.IRE_antisoc_local = -0.2 }
						}
						check_variable = { FROM.IRE_antisoc_local = -0.1 }
					}
				}
				custom_effect_tooltip = tooltip_white_line
				effect_tooltip = { remove_dynamic_modifier = { modifier = IRE_anti_socialist_agitation_local_modifier } }
				if = {
					limit = {
						OR = {
							AND = {
								134 = { NOT = { has_dynamic_modifier = { modifier = IRE_anti_socialist_agitation_local_modifier } } }
								135 = { NOT = { has_dynamic_modifier = { modifier = IRE_anti_socialist_agitation_local_modifier } } }
							}
							AND = {
								113 = { NOT = { has_dynamic_modifier = { modifier = IRE_anti_socialist_agitation_local_modifier } } }
								135 = { NOT = { has_dynamic_modifier = { modifier = IRE_anti_socialist_agitation_local_modifier } } }
							}
							AND = {
								134 = { NOT = { has_dynamic_modifier = { modifier = IRE_anti_socialist_agitation_local_modifier } } }
								113 = { NOT = { has_dynamic_modifier = { modifier = IRE_anti_socialist_agitation_local_modifier } } }
							}
						}
					}
					custom_effect_tooltip = tooltip_white_line
					effect_tooltip = {
						activate_targeted_decision = { target = ROOT decision = IRE_increase_legitimacy_decision }
						activate_targeted_decision = { target = ROOT decision = IRE_international_propaganda }
						activate_targeted_decision = { target = ROOT decision = IRE_emergency_powers }
						activate_targeted_decision = { target = ROOT decision = IRE_class_consciousness }
					}
				}
			}
			if = {
				limit = { check_variable = { FROM.IRE_antisoc_local = 0 } }
				remove_targeted_decision = { target = FROM decision = IRE_weaken_reactionary_resistance }
			}
		}
	}
	IRE_reactionary_resistance_grows = {
		icon = GFX_decision_SWI_consolidate_council_power

		allowed = { always = no }
		priority = 6

		available = { hidden_trigger = { always = no } }

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel IRE_reactionary_resistance_grows"
			set_country_flag = IRE_legitimacy_decisions_unlocked
		}

		days_mission_timeout = IRE_reactionary_mission_time

		cancel_trigger = {
			hidden_trigger = {
				all_core_state = {
					check_variable = { THIS.IRE_antisoc_local = 0 }
				}
			}
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_reactionary_resistance_grows"
			activate_mission = IRE_reactionary_resistance_grows
			random_core_state = {
				limit = {
					is_owned_and_controlled_by = ROOT
					NOT = { state = 119 } # Northern Ireland
					check_variable = { THIS.IRE_antisoc_local < 0 }
					check_variable = { THIS.IRE_antisoc_local > -1 }
				}
				set_temp_variable = { temp_change_anti_socialist_agitation = -0.1 }
				IRE_local_anti_socialist_agitation_effect = yes
			}
		}
	}
	IRE_soc_NIR_mission = {
		icon = GFX_decision_generic_civil_support
		priority = 3

		allowed = { always = no }

		available = { hidden_trigger = { always = no } }

		selectable_mission = no
		fixed_random_seed = no
		days_mission_timeout = IRE_NIR_mission_time

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_soc_NIR_mission"
			IRE_update_resistance = yes
			if = {
				limit = { check_variable = { IRE_ulster_resistance < 1 } }
				add_to_variable = { IRE_ulster_resistance = IRE_ulster_resistance_delta }
				clamp_variable = {
					var = IRE_ulster_resistance
					max = 1
				}
				custom_effect_tooltip = IRE_reluctant_countrymen_modified
				custom_effect_tooltip = IRE_ulster_resistance_target_change_tt
			}
			else = { 119 = { add_resistance = 10 } }
			force_update_dynamic_modifier = yes
			activate_mission = IRE_soc_NIR_mission
			custom_effect_tooltip = tooltip_white_line
			if = {
				limit = {
					OR = {
						has_war_with = ENT
						INT = { has_war_with = ENT }
					}
					NOT = { has_country_flag = IRE_NIR_revolt_avoided }
				}
				IRE_ulster_revolt = yes
			}
			else = {
				if = {
					limit = { country_exists = ENT }
					custom_effect_tooltip = IRE_soc_ulster_revolt_may_occur_tt
					custom_effect_tooltip = tooltip_white_line
				}
				if = {
					limit = { 119 = { resistance > 90 } }
					random_list = {
						20 = {
							modifier = {
								factor = 0
								check_variable = { IRE_loyalist_fear > 99 }
							}
							IRE_loyalist_fear_increase_a_lot = yes
						}
						20 = {
							set_temp_variable = { IRE_nationalist_discontent_delta = 0.06 }
							IRE_nationalist_discontent_increase = yes
						}
						20 = {
							set_temp_variable = { IRE_loyalist_anger_delta = 0.06 }
							IRE_loyalist_anger_increase = yes
						}
					}
				}
				else_if = {
					limit = {
						119 = { resistance > 75 }
					}
					random_list = {
						20 = {
							modifier = {
								factor = 0
								check_variable = { IRE_loyalist_fear > 99 }
							}
							IRE_loyalist_fear_increase = yes
						}
						20 = {
							set_temp_variable = { IRE_nationalist_discontent_delta = 0.03 }
							IRE_nationalist_discontent_increase = yes
						}
						20 = {
							set_temp_variable = { IRE_loyalist_anger_delta = 0.03 }
							IRE_loyalist_anger_increase = yes
						}
					}
				}
				else_if = {
					limit = { 119 = { resistance > 50 } }
					random_list = {
						20 = { 119 = { add_resistance = 8 } }
						20 = { 119 = { add_compliance = -4 } }
						20 = {
							119 = {
								damage_building = {
									type = infrastructure
									damage = 0.6
								}
								damage_building = {
									type = industrial_complex
									damage = 0.6
								}
							}
						}
						10 = {
							set_temp_variable = { IRE_nationalist_discontent_delta = 0.02 }
							IRE_nationalist_discontent_increase = yes
						}
						10 = {
							set_temp_variable = { IRE_loyalist_anger_delta = 0.02 }
							IRE_loyalist_anger_increase = yes
						}
						20 = {}
					}
				}
				else_if = {
					limit = { 119 = { resistance > 25 } }
					random_list = {
						20 = { 119 = { add_resistance = 4 } }
						20 = { 119 = { add_compliance = -2 } }
						20 = {
							119 = {
								damage_building = {
									type = infrastructure
									damage = 0.3
								}
								damage_building = {
									type = industrial_complex
									damage = 0.3
								}
							}
						}
						40 = {}
					}
				}
				else = { custom_effect_tooltip = IRE_NIR_mission_tt }
			}
		}
	}
	IRE_soc_delay_NIR = {
		icon = GFX_decision_generic_prepare_civil_war
		priority = 3
		allowed = { tag = IRE }

		available = {
			has_war_with = ENT
			owns_state = 119 # Northern Ireland
		}

		visible = {
			has_country_flag = IRE_nir_resistance_started
			NOT = { has_global_flag = IRE_had_NIR_revolt }
		}

		ai_hint_pp_cost = 50
		custom_cost_text = IRE_delay_nir_cost
		custom_cost_trigger = {
			has_political_power > 49
			has_equipment = { infantry_equipment > 999 }
		}

		ai_will_do = { base = 0 }

		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_delay_NIR"
			hidden_effect = {
				add_political_power = -50
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -1000
				}
			}
			set_temp_variable = { IRE_NIR_delay_time = IRE_NIR_mission_time }
			subtract_from_temp_variable = { IRE_NIR_delay_time = days_mission_timeout@IRE_soc_NIR_mission }
			add_to_temp_variable = { IRE_NIR_delay_time = 100 }
			add_days_mission_timeout = {
				mission = IRE_soc_NIR_mission
				days = IRE_NIR_delay_time
			}
		}
	}
	IRE_soc_improve_nationalist_discontent = {
		icon = GFX_decision_soldierly_discipline
		name = IRE_improve_nationalist_discontent

		allowed = { tag = IRE }

		available = {
			if = {
				limit = { has_government = radical_socialist }
				has_completed_focus = IRE_progressive_labour
			}
			else = { has_completed_focus = IRE_protection_of_revolution }
			check_variable = {
				tooltip = IRE_improve_nationalist_discontent_tt
				IRE_nationalist_discontent < 0
			}
			check_variable = {
				tooltip = IRE_soc_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			owns_state = 119 # Northern Ireland
		}

		visible = { has_country_flag = IRE_nir_resistance_started }

		cost = IRE_NIR_decisions_cost

		modifier = {
			stability_factor = -0.15
		}

		ai_will_do = { base = 1000 }

		days_remove = IRE_NIR_decisions_time

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_improve_nationalist_discontent"
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_improve_nationalist_discontent"
			if = {
				limit = {
					OR = {
						has_completed_focus = IRE_athghabhail_na_heireann
						has_completed_focus = IRE_workplace_democracy
						has_completed_focus = IRE_connollys_legacy
					}
				}
				set_temp_variable = { IRE_nationalist_discontent_delta = 0.05 }
				IRE_nationalist_discontent_decrease = yes
				if = {
					limit = { has_completed_focus = IRE_protecting_our_freedom }
					set_temp_variable = { IRE_loyalist_anger_delta = 0.025 }
					IRE_loyalist_anger_decrease = yes
				}
				else_if = {
					limit = { IRE_has_radsoc_in_coalition = yes }
					custom_effect_tooltip = IRE_plp_freedom_nationalist_discontent_upgrade_tt
				}
			}
			else = {
				set_temp_variable = { IRE_nationalist_discontent_delta = 0.025 }
				IRE_nationalist_discontent_decrease = yes
				custom_effect_tooltip = tooltip_white_line
				if = {
					limit = { IRE_has_totalist_govt = yes }
					custom_effect_tooltip = IRE_se_improve_nationalist_discontent_upgrade_tt
				}
				else_if = {
					limit = { IRE_has_radsoc_in_coalition = yes }
					custom_effect_tooltip = IRE_plp_improve_nationalist_discontent_upgrade_tt
				}
				else = { custom_effect_tooltip = IRE_isp_improve_nationalist_discontent_upgrade_tt }
			}
			custom_effect_tooltip = tooltip_white_line
			IRE_update_resistance = yes
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
			if = {
				limit = { has_global_flag = IRE_had_NIR_revolt }
				custom_effect_tooltip = tooltip_white_line
				119 = { add_compliance = 5 }
			}
			hidden_effect = {
				if = {
					limit = {
						has_country_flag = IRE_repeat_decisions
						check_variable = {
							tooltip = IRE_improve_nationalist_discontent_tt
							IRE_nationalist_discontent < 0
						}
						has_political_power > IRE_NIR_decisions_cost
						owns_state = 119 # Northern Ireland
					}
					activate_decision = IRE_soc_improve_nationalist_discontent
				}
			}
		}
	}
	IRE_soc_improve_loyalist_anger = {
		icon = GFX_decision_hand_out
		name = IRE_improve_loyalist_anger

		allowed = { tag = IRE }

		available = {
			if = {
				limit = { has_government = radical_socialist }
				has_completed_focus = IRE_progressive_labour
			}
			else = { has_completed_focus = IRE_protection_of_revolution }
			check_variable = {
				tooltip = IRE_improve_loyalist_anger_tt
				IRE_loyalist_anger < 0
			}
			check_variable = {
				tooltip = IRE_soc_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			owns_state = 119 # Northern Ireland
		}

		visible = { has_country_flag = IRE_nir_resistance_started }

		cost = IRE_NIR_decisions_cost

		modifier = {
			political_power_gain = -0.1
		}
		ai_will_do = { base = 1000 }

		days_remove = IRE_NIR_decisions_time

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_improve_loyalist_anger"
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_improve_loyalist_anger"
			if = {
				limit = { has_completed_focus = IRE_defend_parliamentarism }
				set_temp_variable = { IRE_loyalist_anger_delta = 0.05 }
				IRE_loyalist_anger_decrease = yes
				if = {
					limit = { has_completed_focus = IRE_protecting_our_freedom }
					set_temp_variable = { IRE_nationalist_discontent_delta = 0.025 }
					IRE_nationalist_discontent_decrease = yes
				}
				else = {
					custom_effect_tooltip = IRE_plp_freedom_loyalist_anger_upgrade_tt
				}
			}
			else_if = {
				limit = { IRE_has_radsoc_in_coalition = yes }
				set_temp_variable = { IRE_loyalist_anger_delta = 0.025 }
				IRE_loyalist_anger_decrease = yes
				custom_effect_tooltip = tooltip_white_line
				custom_effect_tooltip = IRE_plp_improve_loyalist_anger_upgrade_tt
			}
			else_if = {
				limit = {
					OR = {
						has_completed_focus = IRE_red_jesus
						has_completed_focus = IRE_red_trinity
					}
				}
				set_temp_variable = { IRE_loyalist_anger_delta = 0.04 }
				IRE_loyalist_anger_decrease = yes
			}
			else = {
				set_temp_variable = { IRE_loyalist_anger_delta = 0.02 }
				IRE_loyalist_anger_decrease = yes
				custom_effect_tooltip = tooltip_white_line
				if = {
					limit = { IRE_has_totalist_govt = yes }
					custom_effect_tooltip = IRE_se_improve_loyalist_anger_upgrade_tt
				}
				else = { custom_effect_tooltip = IRE_isp_improve_loyalist_anger_upgrade_tt }
			}
			if = {
				limit = { has_global_flag = IRE_had_NIR_revolt }
				custom_effect_tooltip = tooltip_white_line
				119 = { add_compliance = 5 }
			}
			IRE_update_resistance = yes
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
			hidden_effect = {
				if = {
					limit = {
						has_country_flag = IRE_repeat_decisions
						check_variable = {
							tooltip = IRE_improve_loyalist_anger_tt
							IRE_loyalist_anger < 0
						}
						has_political_power > IRE_NIR_decisions_cost
						owns_state = 119 # Northern Ireland
					}
					activate_decision = IRE_soc_improve_loyalist_anger
				}
			}
		}
	}
	IRE_unity_through_class = {
		icon = GFX_decision_SOV_the_workers_dictatorship

		allowed = { tag = IRE }

		available = {
			has_completed_focus = IRE_enshrine_ITUC_primacy
			check_variable = {
				tooltip = IRE_improve_nationalist_discontent_tt
				IRE_nationalist_discontent < 0
			}
			check_variable = {
				tooltip = IRE_improve_loyalist_anger_tt
				IRE_loyalist_anger < 0
			}
			check_variable = {
				tooltip = IRE_soc_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			owns_state = 119 # Northern Ireland
		}

		visible = {
			has_country_flag = IRE_nir_resistance_started
			OR = {
				AND = {
					has_government = totalist
					NOT = { has_completed_focus = IRE_saor_eire }
				}
				has_government = syndicalist
			}
		}

		cost = 35

		modifier = {
			consumer_goods_expected_value = 0.05
		}
		ai_will_do = { base = 2000 }

		days_remove = IRE_NIR_decisions_time_long
		days_re_enable = 60

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_unity_through_class"
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_unity_through_class"
			set_temp_variable = { IRE_loyalist_anger_delta = 0.04 }
			IRE_loyalist_anger_decrease = yes
			set_temp_variable = { IRE_nationalist_discontent_delta = 0.04 }
			IRE_nationalist_discontent_decrease = yes
			IRE_update_resistance = yes
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_60_days_tt
			if = {
				limit = { has_global_flag = IRE_had_NIR_revolt }
				custom_effect_tooltip = tooltip_white_line
				119 = { add_compliance = 7.5 }
			}
		}
	}
	IRE_toggle_ni_repeat_decisions = {
		icon = GFX_decision_generic_decision

		allowed = { tag = IRE }

		visible = {
			OR = {
				has_country_flag = IRE_nir_resistance_started
				has_active_mission = IRE_reactionary_resistance_grows
			}
		}

		cost = 0
		priority = 2

		ai_will_do = { base = 0 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_toggle_ni_repeat_decisions"
			if = {
				limit = { has_country_flag = IRE_repeat_decisions }
				clr_country_flag = IRE_repeat_decisions
				custom_effect_tooltip = IRE_ni_repeat_decisions_disable_tt
			}
			else = {
				set_country_flag = IRE_repeat_decisions
				custom_effect_tooltip = IRE_ni_repeat_decisions_enable_tt
			}
		}
	}
	IRE_soc_gardai_decision = {
		icon = GFX_decision_generic_police_action

		allowed = { tag = IRE }

		available = {
			if = {
				limit = { has_government = radical_socialist }
				has_completed_focus = IRE_progressive_labour
			}
			else = { has_completed_focus = IRE_protection_of_revolution }
			check_variable = {
				tooltip = IRE_soc_NIR_decisions_counter_tt
				IRE_NIR_decisions_variable > 0
			}
			owns_state = 119 # Northern Ireland
		}

		visible = {
			has_country_flag = IRE_nir_resistance_started
			check_variable = { IRE_ulster_resistance_delta > 0 }
		}

		custom_cost_text = IRE_arm_gardai_cost_tt
		custom_cost_trigger = {
			command_power > 39.99
			has_equipment = { infantry_equipment > 299 }
		}
		days_re_enable = 60
		state_target = yes
		targets = { 119 }

		ai_will_do = { base = 0 }

		days_remove = IRE_NIR_decisions_time

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_soc_gardai_decision target: [From.GetName]"
			hidden_effect = {
				add_command_power = -40
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -300
				}
			}
			subtract_from_variable = { IRE_NIR_decisions_variable = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_soc_gardai_decision target: [From.GetName]"
			if = {
				limit = { has_completed_focus = IRE_old_oppressor }
				119 = { add_resistance = -10 }
				set_variable = { IRE_ulster_resistance_delta = -0.1 }
				add_to_variable = { IRE_ulster_resistance = IRE_ulster_resistance_delta }
				force_update_dynamic_modifier = yes
				custom_effect_tooltip = tooltip_white_line
				custom_effect_tooltip = IRE_upgraded_arm_gardai_tt
			}
			else = {
				119 = { add_resistance = -5 }
				set_variable = { IRE_ulster_resistance_delta = -0.05 }
				add_to_variable = { IRE_ulster_resistance = IRE_ulster_resistance_delta }
				force_update_dynamic_modifier = yes
				custom_effect_tooltip = tooltip_white_line
				custom_effect_tooltip = IRE_arm_gardai_tt
				if = {
					limit = { NOT = { has_government = radical_socialist } }
					custom_effect_tooltip = IRE_soc_arm_gardai_upgrade_tt
				}
			}
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = IRE_60_days_tt
			add_to_variable = { IRE_NIR_decisions_variable = 1 }
		}
	}
	IRE_NIR_capitulation_mission = {
		icon = generic_prepare_civil_war
		name = NIR_capitulation_mission

		allowed = { always = no }
		available = { NIR = { has_capitulated = no } }
		priority = 1
		selectable_mission = no
		days_mission_timeout = 180

		fire_only_once = no

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout IRE_NIR_capitulation_mission"
			effect_tooltip = {
				IRE = { annex_country = { target = NIR } }
			}
			hidden_effect = {
				NIR = { leave_faction = yes }
				IRE = { annex_country = { target = NIR } }
			}
		}
	}
}

 ### Second Election Decisions ###

IRE_1941_election_decisions = {

	IRE_ituc_factories = {
		icon = GFX_decision_generic_merge_plant_materiel
		allowed = { original_tag = IRE }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 135
			has_full_control_of_state = 134
		}

		visible = { has_completed_focus = IRE_collaborate_with_ITUC }

		cost = 50
		fire_only_once = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 120
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_ituc_factories"
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
			134 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}
	}

	IRE_lab_industrial_developments = {
		icon = GFX_decision_ger_military_buildup
		allowed = { original_tag = IRE }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 113
			has_full_control_of_state = 119
		}

		visible = { has_completed_focus = IRE_industrial_development }

		cost = 50
		fire_only_once = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 120
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_lab_industrial_developments"
			113 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = arms_factory level = 1 }
			}
			119 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = synthetic_refinery level = 1 }
			}
		}
	}

	IRE_drain_boglands_decision = {
		icon = GFX_decision_hol_drain_water_lines
		allowed = { original_tag = IRE }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 113
			has_full_control_of_state = 134
		}

		visible = { has_completed_focus = IRE_drain_boglands }

		cost = 40
		fire_only_once = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_drain_boglands_decision"
			113 = {
				add_extra_state_shared_building_slots = 1
			}
			134 = {
				add_extra_state_shared_building_slots = 1
			}
			unlock_decision_tooltip = IRE_bogland_farms
			set_country_flag = IRE_bogs_drained
		}
	}

	IRE_bogland_farms = {
		icon = GFX_decision_POL_looming_peasants_strike
		allowed = { original_tag = IRE }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 113
			has_full_control_of_state = 134
		}

		visible = { has_country_flag = IRE_bogs_drained }

		cost = 40
		fire_only_once = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_bogland_farms"
			113 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			134 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			activate_targeted_decision = { target = 134 decision = IRE_peat_fuel }
			clr_country_flag = IRE_bogs_drained
		}
	}

	IRE_peat_fuel = { #Connacht
		icon = oil
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 0
			controls_state = 134
			has_tech = excavation2
		}

		visible = {
			134 = { is_owned_by = ROOT }
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 20 }

		modifier = { civilian_factory_use = 1 }

		days_remove = 120
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_peat_fuel target: [From.GetName]"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_peat_fuel target: [From.GetName]"
			134 = {
				add_resource = {
					type = oil
					amount = 3
				}
			}
		}
	}

	IRE_sustainable_forestry_decision = {
		icon = GFX_decision_forestry
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 0
			has_full_control_of_state = 135
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 1 }

		days_remove = 180
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_sustainable_forestry_decision target: [From.GetName]"
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = { type = industrial_complex level = 1 }
			}
		}
	}

	IRE_belfast_steel_mills = {
		icon = GFX_decision_steel
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
			has_full_control_of_state = 119
			has_tech = excavation2
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 20 }

		modifier = { civilian_factory_use = 2 }

		days_remove = 90
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_belfast_steel_mills target: [From.GetName]"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_belfast_steel_mills target: [From.GetName]"
			add_resource = {
				type = steel
				amount = 3
				state = 119
			}
		}
	}

	IRE_slieveardagh_coal_mines = {
		icon = GFX_decision_steel

		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
			has_full_control_of_state = 135
			has_tech = excavation3
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 20 }

		modifier = { civilian_factory_use = 2 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_slieveardagh_coal_mines target: [From.GetName]"
			add_resource = {
				type = steel
				amount = 3
				state = 135
			}
		}
	}

	IRE_develop_crumlin = {
		icon = GFX_decision_generic_agriculture
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 0
			has_full_control_of_state = 113
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				113 = {
					free_building_slots = { building = industrial_complex size > 0 include_locked = yes }
					free_building_slots = { building = arms_factory size > 0 include_locked = yes }
				}
			}
		}

		modifier = { civilian_factory_use = 1 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_develop_crumlin target: [From.GetName]"
			113 = {
				add_extra_state_shared_building_slots = 1
			}
		}
	}

	IRE_develop_kinvara = {
		icon = GFX_decision_generic_agriculture
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 0
			has_full_control_of_state = 134
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				134 = {
					free_building_slots = { building = industrial_complex size > 0 include_locked = yes }
					free_building_slots = { building = arms_factory size > 0 include_locked = yes }
				}
			}
		}

		modifier = { civilian_factory_use = 1 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_develop_kinvara target: [From.GetName]"
			134 = {
				add_extra_state_shared_building_slots = 1
			}
		}
	}

	IRE_develop_silvermines = {
		icon = GFX_decision_generic_agriculture
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 0
			has_full_control_of_state = 135
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				135 = {
					free_building_slots = { building = industrial_complex size > 0 include_locked = yes }
					free_building_slots = { building = arms_factory size > 0 include_locked = yes }
				}
			}
		}

		modifier = { civilian_factory_use = 1 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_develop_silvermines target: [From.GetName]"
			135 = {
				add_extra_state_shared_building_slots = 1
			}
		}
	}

	IRE_develop_tobermore = {
		icon = GFX_decision_generic_agriculture
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 0
			has_full_control_of_state = 119
		}

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				119 = {
					free_building_slots = { building = industrial_complex size > 0 include_locked = yes }
					free_building_slots = { building = arms_factory size > 0 include_locked = yes }
				}
			}
		}

		modifier = { civilian_factory_use = 1 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_develop_tobermore target: [From.GetName]"
			119 = {
				add_extra_state_shared_building_slots = 1
			}
		}
	}

	IRE_subsidies_for_yeoman_farmers = {
		icon = GFX_decision_POL_looming_peasants_strike
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
			has_full_control_of_state = 134
		}

		visible = { has_completed_focus = IRE_nation_small_farmers }

		cost = 40
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 2 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_subsidies_for_yeoman_farmers target: [From.GetName]"
			134 = { increase_state_category_by_one_level = yes }
			unlock_decision_tooltip = IRE_preparing_the_fields
			set_country_flag = IRE_farmer_subsidies_given
		}
	}

	IRE_preparing_the_fields = {
		icon = GFX_decision_generic_agriculture
		allowed = { original_tag = IRE }

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_full_control_of_state = 134
			has_full_control_of_state = 135
		}

		visible = { has_country_flag = IRE_farmer_subsidies_given }

		cost = 40
		fire_only_once = yes

		ai_will_do = { factor = 50 }

		modifier = { civilian_factory_use = 3 }

		days_remove = 90
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_preparing_the_fields"
			134 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			unlock_decision_tooltip = IRE_reaping_what_we_sowed
			set_country_flag = IRE_fields_prepared
			clr_country_flag = IRE_farmer_subsidies_given
		}
	}

	IRE_reaping_what_we_sowed = {
		icon = GFX_decision_POL_organize_strike_two
		allowed = { original_tag = IRE }

		available = {
			has_full_control_of_state = 134
			has_full_control_of_state = 135
		}

		visible = { has_country_flag = IRE_fields_prepared }

		cost = 40
		fire_only_once = yes

		ai_will_do = { factor = 50 }

		days_remove = 120
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove IRE_reaping_what_we_sowed"
			134 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			135 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			clr_country_flag = IRE_fields_prepared
		}
	}
}

IRE_investment_decisions = {
	IRE_attracting_investments = {
		icon = GFX_decision_global_trade
		allowed = { always = no }

		available = {
			surrender_progress < 0.10
			has_full_control_of_state = 113
			if = {
				limit = {
					FROM = { has_socialist_government = yes }
				}
				NOT = { is_ally_with = GER }
				NOT = { is_ally_with = ENT }
			}
			FROM = {
				surrender_progress < 0.30
				if = {
					limit = { is_subject = yes }
					OR = {
						is_ally_with = ROOT
						overlord = { has_idea = IRE_irish_trade_partner }
					}
				}
				else_if = {
					limit = { original_tag = NFA }
					16 = {
						is_owned_and_controlled_by = NFA
						is_core_of = NFA
					}
				}
				else_if = {
					limit = { original_tag = ARG }
					FOP = { exists = no }
				}
				else_if = {
					limit = { is_italian_tag = yes }
					has_country_flag = ITA_united_italia
				}
				else_if = {
					limit = { original_tag = SPA }
					has_country_flag = SPA_aftermath_of_the_civil_war
				}
				else_if = {
					limit = { original_tag = TUR }
					OTT_cairo_pact_defeated = yes
				}
				else_if = {
					limit = { original_tag = EGY }
					OTT = { exists = no }
				}
				else_if = {
					limit = { original_tag = BUL }
					has_global_flag = balkan_war_ended
					106 = { is_owned_and_controlled_by = BUL }
				}
				else_if = {
					limit = { original_tag = POL }
					is_in_same_economic_sphere_as_ROOT = yes
				}
				else_if = {
					limit = { is_chinese_tag = yes }
					china_is_the_unifier_of_china_trigger = yes
				}
				else_if = {
					limit = { is_american_tag = yes }
					has_global_flag = USA_civil_war_over_flag
				}
			}
		}

		visible = {
			FROM = {
				exists = yes
				has_capitulated = no
				NOT = { has_war_with = ROOT }
				NOT = { has_country_flag = IRE_asked_to_invest }
				OR = {
					NOT = { is_subject_of = GER }
					is_ally_with = IRE
					IRE = { is_in_faction = no }
				}
			}
		}

		ai_will_do = { factor = 20 }

		cost = 25
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete IRE_attracting_investments target: [From.GetName]"
			FROM = {
				set_country_flag = IRE_asked_to_invest
				country_event = { id = ireland.4500 days = 2 }
			}
		}
	}
}
