TRM_japanese_influence = {

	TRM_visit_from_japan = {

		allowed = {
			original_tag = TRM
		}

		available = {
			always = no
		}

		days_mission_timeout = 90

		activation = {
			TRM = {is_subject_of = JAP}
		}

		visible = {
			is_subject_of = JAP
		}

		cancel_trigger = {
			NOT = { TRM = {is_subject_of = JAP} }
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout TRM_visit_from_japan"
			add_political_power = 40
			add_stability = 0.02
			add_popularity = {ideology = authoritarian_democrat popularity = 0.03}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove TRM_visit_from_japan"
		}
	}

	TRM_zaibatsus = {

		allowed = {
			original_tag = TRM
		}

		available = {
			is_subject_of = JAP
			NOT = {has_idea = TRM_JAP_CONS}
		}

		visible = {
			is_subject_of = JAP
			has_completed_focus = TRM_Jap_Inv2
		}

		cost = 50

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TRM_zaibatsus"
			add_popularity = {ideology = authoritarian_democrat popularity = 0.03}
			add_timed_idea = {
				idea = TRM_JAP_CONS
				days = 90
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove TRM_zaibatsus"
		}
	}

	TRM_support_anti_japanese_rally = {

		allowed = {
			original_tag = TRM
		}

		available = {
			is_subject_of = JAP
			NOT = {has_country_flag = TRM_Yui}
		}

		visible = {
			is_subject_of = JAP
			NOT = {has_country_flag = TRM_Yui}
		}

		cost = 50
		days_remove = 7

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TRM_support_anti_japanese_rally"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove TRM_support_anti_japanese_rally"
			add_popularity = {ideology = authoritarian_democrat popularity = -0.05}
		}
	}

	TRM_hold_vladivostok = {

		icon = generic_tank

		available = {
			NOT = {
				TRM = {
					controls_state = 408
				}
			}
			has_war_with = JAP
		}

		days_mission_timeout = 180
		is_good = yes

		activation = {
			tag = TRM
			has_war_with = JAP
			TRM = {
				controls_state = 408
			}
			NOT = {
				has_country_flag = TRM_mission_completed
			}
		}

		visible = {
			tag = TRM
			has_war_with = JAP
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TRM_hold_vladivostok"
			custom_effect_tooltip = dei_tt_the_timer_will_be_reset
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout TRM_hold_vladivostok"
			
			country_event = { id = tram.128 days = 1 }
			
				JAP = { country_event = tram.128}
				every_country = {
					limit = {
						has_war_with = TRM
						OR = {
							is_in_faction_with = JAP
							is_subject_of = JAP
							tag = JAP
						}
					}
					TRM = {
						white_peace = PREV
					}
				}
		
			set_country_flag = TRM_mission_completed
		}
	}

	TRM_raise_militia = {

		allowed = {
			original_tag = TRM
		}

		available = {
			tag = TRM
			has_war_with = JAP
			controls_state = 408
			NOT = {has_country_flag = TRM_militia_raised}
		}

		visible = {
			tag = TRM
			has_war_with = JAP
			controls_state = 408
			NOT = {has_country_flag = TRM_militia_raised}
		}

		cost = freedec?0
		days_remove = 7

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TRM_raise_militia"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove TRM_raise_militia"
			load_oob = TRM_militia
			set_country_flag = TRM_militia_raised
		}
	}
	
	TRM_Leave_Japan = {
		allowed = {
			original_tag = TRM
		}

		available = {
			has_war = no
			is_subject_of = JAP
			195 = { is_owned_and_controlled_by = TRM }
			219 = { is_owned_and_controlled_by = TRM }
		}

		visible = {
			has_war = no
			is_subject_of = JAP
			has_completed_focus = TRM_Claim_Russia
		}

		cost = freedec?0
		days_remove = 14

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TRM_Leave_Japan"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove TRM_Leave_Japan"
			JAP = {end_puppet = TRM}
			JAP = {country_event = {id = tram.97 days = 1 } }
		}
	}
}

TRM_russian_expansion = {

	TRM_relocate_capital_1 = {

		allowed = {
			original_tag = TRM
		}

		available = {
			has_idea = TRM_dream
		
			566 = { is_owned_and_controlled_by = TRM }
			
			NOT = {has_country_flag = TRM_capital_irk}
			NOT = {has_country_flag = TRM_capital_nov}
			NOT = {has_country_flag = TRM_capital_stp}
			NOT = {has_country_flag = TRM_capital_mos}
		
		}

		visible = {
			has_idea = TRM_dream
		
			566 = { is_owned_and_controlled_by = TRM }
			
			NOT = {has_country_flag = TRM_capital_irk}
			NOT = {has_country_flag = TRM_capital_nov}
			NOT = {has_country_flag = TRM_capital_stp}
			NOT = {has_country_flag = TRM_capital_mos}
		}

		cost = 25
		days_remove = 0

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TRM_relocate_capital_1"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove TRM_relocate_capital_1"
			
			country_event = { id = tram.129 days = 1 }

			if = {
				limit = { is_subject_of = JAP }
				
				JAP = {country_event = {id = tram.67 days = 2} }
				
				if = {
					limit = { has_dlc = "Together for Victory" }
					add_autonomy_ratio = { value = 0.5 localization = TRM_move_capital }
				}
			}
			
			set_country_flag = TRM_capital_irk
			
		}
	}
	
	TRM_relocate_capital_2 = {

		allowed = {
			original_tag = TRM
		}

		available = {
			has_idea = TRM_dream
		
			570 = { is_owned_and_controlled_by = TRM }
			
			NOT = {has_country_flag = TRM_capital_nov}
			NOT = {has_country_flag = TRM_capital_stp}
			NOT = {has_country_flag = TRM_capital_mos}
		
		}

		visible = {
			has_idea = TRM_dream
		
			570 = { is_owned_and_controlled_by = TRM }
			
			NOT = {has_country_flag = TRM_capital_nov}
			NOT = {has_country_flag = TRM_capital_stp}
			NOT = {has_country_flag = TRM_capital_mos}
		}

		cost = 25
		days_remove = 0

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TRM_relocate_capital_2"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove TRM_relocate_capital_2"
			
			country_event = { id = tram.130 days = 1 }

			if = {
				limit = { is_subject_of = JAP }
				
				JAP = {country_event = {id = tram.67 days = 2} }
				
				if = {
					limit = { has_dlc = "Together for Victory" }
					add_autonomy_ratio = { value = 0.5 localization = TRM_move_capital }
				}
			}
			
			set_country_flag = TRM_capital_nov
			
		}
	}
	
	TRM_relocate_capital_3 = {

		allowed = {
			original_tag = TRM
		}

		available = {
			has_idea = TRM_dream
		
			219 = { is_owned_and_controlled_by = TRM }
		
				NOT = {has_country_flag = TRM_capital_stp}
				NOT = {has_country_flag = TRM_capital_mos}
		
		}

		visible = {
			has_idea = TRM_dream
			
			219 = { is_owned_and_controlled_by = TRM }
			
			NOT = {has_country_flag = TRM_capital_stp}
			NOT = {has_country_flag = TRM_capital_mos}
		}

		cost = 50
		days_remove = 0

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TRM_relocate_capital_3"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove TRM_relocate_capital_3"
			
			country_event = { id = tram.131 days = 1 }
			
			set_country_flag = TRM_capital_mos
			
		}
	}
	
	TRM_relocate_capital_4 = {

		allowed = {
			original_tag = TRM
		}

		available = {
			has_idea = TRM_dream
		
			195 = { is_owned_and_controlled_by = TRM }
			
				NOT = {has_country_flag = TRM_capital_stp}
				NOT = {has_country_flag = TRM_capital_mos}
		
		}

		visible = {
			has_idea = TRM_dream
			
			195 = { is_owned_and_controlled_by = TRM }
			
			NOT = {has_country_flag = TRM_capital_stp}
			NOT = {has_country_flag = TRM_capital_mos}
		}

		cost = 50
		days_remove = 0

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision TRM_relocate_capital_4"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove TRM_relocate_capital_4"
			
			country_event = { id = tram.132 days = 1 }
			
			set_country_flag = TRM_capital_stp
			
		}
	}

}