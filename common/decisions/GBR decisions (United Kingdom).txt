foreign_support = {
	GBR_support_britain = {
		icon = generic_agreement

		allowed = {
			OR = {
				CAN_is_commonwealth_tag = yes
				original_tag = NFA
				AND = {
					is_american_tag = yes
					NOT = { original_tag = WCA }
				}
			}
		}

		available = {
			controls_all_owned_states = yes
		}

		cost = 50
		days_remove = 120
		fire_only_once = no

		targets = { GBR }
		target_root_trigger = { is_in_faction_with = GBR }
		target_trigger = {
			is_in_faction_with = FROM
			FROM = {
				OR = {
					has_dynamic_modifier = { modifier = GBR_postwar_devastation }
					has_dynamic_modifier = { modifier = GBR_legacy_revolution }
				}
			}
			is_subject = no
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_support_britain"
			country_event = united_kingdom_events.7
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				tag = NFA
				has_political_power < 200
			}
			modifier = {
				factor = 0
				has_political_power < 100
				CAN_is_commonwealth_tag = no
			}
		}
	}
}

GBR_rebuilding_empire = {
	GBR_demand_england = {
		icon = generic_nationalism

		available = {
			has_army_manpower = { size > 49999 }
			can_declare_war_on = FROM
		}

		visible = {
			FROM = {
				NOT = { tag = GBR }
				NOT = { original_tag = NIR }
				NOT = { original_tag = SCO }
				NOT = { original_tag = WLS }
				NOT = { has_war_with = ROOT }
				NOT = { is_ally_with = ROOT }
				any_owned_state = {
					is_core_of = GBR
					is_great_britain = yes
					is_claim_or_core_of_PREV = no
				}
			}
			is_subject = no
		}

		cost = 0
		days_remove = 21
		fire_only_once = yes
		war_with_target_on_remove = yes
		cancel_if_not_visible = yes

		target_root_trigger = {
			tag = GBR
			any_of_scopes = {
				array = core_states
				is_great_britain = yes
				owner = {
					NOT = { tag = GBR }
					NOT = { original_tag = NIR }
					NOT = { original_tag = SCO }
					NOT = { original_tag = WLS }
				}
			}
		}
		target_trigger = {
			tag = GBR
			is_subject = no
			FROM = {
				NOT = { original_tag = NIR }
				NOT = { original_tag = SCO }
				NOT = { original_tag = WLS }
				NOT = { has_war_with = ROOT }
				NOT = { is_ally_with = ROOT }
				any_owned_state = {
					is_core_of = GBR
					is_great_britain = yes
					is_claim_or_core_of_PREV = no
				}
			}
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_war = yes
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_demand_england"
			clear_decision_attack_AI = yes
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_demand_england"
			FROM = { incoming_war_notification_effect = yes }
			setup_decision_attack_AI = yes
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_demand_england"
			clear_decision_attack_AI = yes
			FROM = {
				country_event = { id = united_kingdom_events.135 days = 3 }
			}
		}
	}

	GBR_demand_uk_state = {
		icon = generic_nationalism

		available = {
			has_army_manpower = { size > 49999 }
			FROM = {
				surrender_progress < 0.01
				NOT = { any_claim = yes }
				OR = {
					is_subject = no
					overlord = { has_capitulated = no }
				}
			}
		}

		visible = {
			country_exists = FROM
			NOT = { has_war_with = FROM }
			NOT = { has_war_with = FROM.overlord }
			if = {
				limit = { FROM = { tag = NIR } }
				NOT = { has_subject = FROM }
			}
		}

		cost = 0
		fire_only_once = yes

		targets = { SCO WLS NIR }
		target_root_trigger = {
			tag = GBR
			owns_state = 123 #Cornwall
			owns_state = 125 #East Anglia
			owns_state = 126 #Greater London
			owns_state = 127 #Sussez
			owns_state = 128 #West Midlands
			owns_state = 129 #East Midlands
			owns_state = 130 #Yorkshire
			owns_state = 132 #Lacanshire
			owns_state = 338 #Gloucestershire
		}
		target_trigger = {
			NOT = { has_war_with = FROM }
			NOT = { has_war_with = FROM.overlord }
			is_subject = no
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_demand_uk_state"
			if = {
				limit = { has_subject = FROM }
				annex_country = { target = FROM }
			}
			else_if = {
				limit = { FROM = { is_subject = yes } }
				if = {
					limit = { is_in_faction_with = FROM.overlord }
					FROM = {
						save_event_target_as = colony_country
						overlord = { country_event = { id = united_kingdom_events.118 days = 3 } }
					}
				}
				else = {
					FROM = {
						save_event_target_as = return_country
						overlord = { country_event = { id = united_kingdom_events.132 days = 3 } }
					}
				}
			}
			else = {
				FROM = {
					save_event_target_as = return_country
					country_event = { id = united_kingdom_events.132 days = 3 }
				}
			}
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_war = yes
			}
			modifier = {
				factor = 0.1
				FROM = { tag = NIR }
			}
			modifier = {
				factor = 0.2
				FROM = { tag = WLS }
			}
			modifier = {
				factor = 0.2
				FROM = { tag = SCO }
			}
			modifier = {
				factor = 10
				FROM = {
					OR = {
						is_ally_with = ROOT
						is_in_faction = no
					}
				}
			}
		}
	}

	GBR_restore_order_in_ireland = {
		icon = generic_nationalism

		highlight_states = {
			highlight_state_targets = { state = 119 }
		}

		available = {
			controls_state = 119
		}

		visible = {
			119 = {
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
			}
		}

		cost = 50
		days_remove = 90
		fire_only_once = yes

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetLogRoot]: Decision complete GBR_restore_order_in_ireland"
		}

		remove_effect = {
			log = "[GetLogRoot]: Decision remove GBR_restore_order_in_ireland"
			add_state_core = 119
			hidden_effect = { remove_state_claim = 119 }
			if = {
				limit = { has_cosmetic_tag = GBR_britain }
				set_cosmetic_tag = GBR
			}
		}
	}

	GBR_restore_order_falklands = {
		icon = generic_nationalism

		highlight_states = {
			highlight_state_targets = { state = 299 }
		}

		available = {
			controls_state = 299
		}

		visible = {
			299 = {
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
			}
		}

		cost = 25
		days_remove = 90
		fire_only_once = yes

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetLogRoot]: Decision complete GBR_restore_order_falklands"
		}

		remove_effect = {
			log = "[GetLogRoot]: Decision remove GBR_restore_order_falklands"
			add_state_core = 299
			hidden_effect = { remove_state_claim = 299 }
			if = {
				limit = { owns_state = 720 }
				add_state_core = 720
				hidden_effect = { remove_state_claim = 720 }
			}
		}
	}

	GBR_restore_order_gibraltar = {
		icon = generic_nationalism

		highlight_states = {
			highlight_state_targets = { state = 118 }
		}

		available = {
			controls_state = 118
		}

		visible = {
			118 = {
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
			}
		}

		cost = 25
		days_remove = 90
		fire_only_once = yes

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetLogRoot]: Decision complete GBR_restore_order_gibraltar"
		}

		remove_effect = {
			log = "[GetLogRoot]: Decision remove GBR_restore_order_gibraltar"
			add_state_core = 118
			hidden_effect = { remove_state_claim = 118 }
		}
	}

	GBR_restore_order_malta = {
		icon = generic_nationalism

		highlight_states = {
			highlight_state_targets = { state = 116 }
		}

		available = {
			controls_state = 116
		}

		visible = {
			116 = {
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
			}
		}

		cost = 75
		days_remove = 90
		fire_only_once = yes

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetLogRoot]: Decision complete GBR_restore_order_malta"
		}

		remove_effect = {
			log = "[GetLogRoot]: Decision remove GBR_restore_order_malta"
			add_state_core = 116
			hidden_effect = { remove_state_claim = 116 }
		}
	}

	GBR_demand_northern_ireland = {
		icon = generic_nationalism
		allowed = { always = no }

		highlight_states = {
			highlight_state_targets = { state = 119 }
		}

		available = {
			119 = { is_fully_controlled_by = FROM }
			FROM = { has_capitulated = no }
		}

		visible = {
			119 = { is_claimed_by = ROOT }
			is_subject = no
			NOT = {
				has_country_flag = CAN_promised_ireland_ind
				has_global_flag = ENT_britain_agree_northire
			}
			FROM = {
				owns_state = 119
				has_socialist_government = no
				NOT = { has_war_with = ROOT }
				NOT = { has_country_flag = GBR_asked_northern_ireland }
				OR = {
					is_subject = no
					is_ally_with = ROOT
				}
			}
		}

		cost = 0
		fire_only_once = yes

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_demand_northern_ireland"
			FROM = {
				country_event = { id = united_kingdom_events.100 days = 3 }
			}
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				faction_leader = { is_american_tag = yes }
			}
		}
	}

	GBR_demand_gibraltar = {
		icon = generic_nationalism

		highlight_states = {
			highlight_state_targets = { state = 118 }
		}

		available = {
			118 = { is_controlled_by = FROM }
			FROM = { has_capitulated = no }
			if = {
				limit = { is_in_faction = yes }
				OR = {
					is_faction_leader = yes
					faction_leader = { is_american_tag = yes }
				}
			}
		}

		visible = {
			FROM = {
				owns_state = 118
				NOT = { has_war_with = ROOT }
				NOT = { has_country_flag = GBR_asked_gibraltar }
			}
			is_subject = no
		}

		cancel_trigger = {
			OR = {
				is_subject = yes
				has_capitulated = yes
				NOT = { 118 = { is_controlled_by = FROM } }
				has_war_with = FROM
			}
		}

		cost = 25
		days_remove = 21
		fire_only_once = no
		war_with_target_on_remove = yes

		targets = { 118.OWNER }
		target_root_trigger = {
			118 = { is_claimed_by = ROOT }
		}
		target_trigger = {
			FROM = {
				owns_state = 118
				NOT = { has_war_with = ROOT }
				NOT = { has_country_flag = GBR_asked_gibraltar }
			}
			is_subject = no
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				OR = {
					country_has_any_active_threat = yes
					FROM = { dont_start_major_faction_wars = yes }
					FROM = { default_attack_AI_trigger = yes }
				}
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_demand_gibraltar"
			clear_decision_attack_AI = yes
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_demand_gibraltar"
			setup_decision_attack_AI = yes	#warning event is not added on purpose
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_demand_gibraltar"
			clear_decision_attack_AI = yes
			118 = { save_event_target_as = demand_state }
			FROM = {
				set_country_flag = GBR_asked_gibraltar
				country_event = united_kingdom_events.115
			}
		}
	}

	GBR_demand_falklands = {
		icon = generic_nationalism

		highlight_states = {
			highlight_state_targets = { state = 299 }
		}

		available = {
			299 = { is_controlled_by = FROM }
			FROM = { has_capitulated = no }
			if = {
				limit = { is_in_faction = yes }
				OR = {
					is_faction_leader = yes
					faction_leader = { is_american_tag = yes }
				}
			}
		}

		visible = {
			FROM = {
				owns_state = 299
				NOT = { has_war_with = ROOT }
				NOT = { has_country_flag = GBR_asked_falklands }
			}
			is_subject = no
		}

		cancel_trigger = {
			OR = {
				is_subject = yes
				has_capitulated = yes
				NOT = { 299 = { is_controlled_by = FROM } }
				has_war_with = FROM
			}
		}

		cost = 25
		days_remove = 21
		fire_only_once = no
		war_with_target_on_remove = yes

		targets = { 299.OWNER }
		target_root_trigger = {
			299 = { is_claimed_by = ROOT }
		}
		target_trigger = {
			FROM = {
				owns_state = 299
				NOT = { has_war_with = ROOT }
				NOT = { has_country_flag = GBR_asked_falklands }
			}
			is_subject = no
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				OR = {
					country_has_any_active_threat = yes
					FROM = { dont_start_major_faction_wars = yes }
					FROM = { default_attack_AI_trigger = yes }
				}
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_demand_falklands"
			clear_decision_attack_AI = yes
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_demand_falklands"
			setup_decision_attack_AI = yes	#warning event is not added on purpose
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_demand_falklands"
			clear_decision_attack_AI = yes
			299 = { save_event_target_as = demand_state }
			FROM = {
				set_country_flag = GBR_asked_falklands
				country_event = united_kingdom_events.115
			}
		}
	}

	GBR_invade_ireland = {
		icon = generic_tank
		allowed = { always = no }

		available = {
			ROOT = {
				can_declare_war_on = FROM
			}
			FROM = {
				controls_state = 119
				if = {
					limit = { has_war = yes }
					has_capitulated = no
				}
			}
		}

		visible = {
			hidden_trigger = {
				119 = { is_claimed_by = ROOT }
				is_subject = no
				OR = {
					FROM = { has_socialist_government = yes }
					ROOT = { has_country_flag = GBR_may_attack_ireland }
				}
				FROM = {
					owns_state = 119
					NOT = {
						has_war_with = ROOT
						is_ally_with = ROOT
					}
				}
				NOT = {
					has_country_flag = CAN_promised_ireland_ind
					has_global_flag = ENT_britain_agree_northire
				}
			}
		}

		target_trigger = {
			is_subject = no
		}

		cost = 0
		days_remove = 30
		fire_only_once = yes
		war_with_target_on_remove = yes
		cancel_if_not_visible = yes

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				OR = {
					country_has_any_active_threat = yes
					FROM = { dont_start_major_faction_wars = yes }
					FROM = { default_attack_AI_trigger = yes }
				}
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_invade_ireland"
			clear_decision_attack_AI = yes
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_invade_ireland"
			add_war_support = 0.05
			setup_decision_attack_AI = yes
			FROM = { incoming_war_notification_effect = yes }
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_invade_ireland"
			clear_decision_attack_AI = yes
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}
	}

#	GBR_fund_loyalists = {
#		icon = generic_nationalism
#		allowed = { always = no }
#
#		available = {
#			FROM = {
#				controls_state = 119
#				if = {
#					limit = { has_war = yes }
#					has_capitulated = no
#				}
#			}
#		}
#
#		visible = {
#			119 = { is_claimed_by = ROOT }
#			OR = {
#				FROM = { has_socialist_government = yes }
#				ROOT = { has_country_flag = GBR_may_attack_ireland }
#			}
#			FROM = {
#				owns_state = 119
#				has_country_flag = IRE_ulster_conflict
#				can_declare_war_on = ROOT
#			}
#			NOT = {
#				has_country_flag = CAN_promised_ireland_ind
#				has_global_flag = ENT_britain_agree_northire
#			}
#		}
#
#		cost = 50
#		days_remove = 30
#		days_re_enable = 120
#		fire_only_once = no
#		cancel_if_not_visible = yes
#
#		cancel_effect = {
#			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_fund_loyalists"
#		}
#
#		complete_effect = {
#			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_fund_loyalists"
#		}
#
#		remove_effect = {
#			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_fund_loyalists"
#			IRE = {
#				custom_effect_tooltip = GBR_loyalist_tension_rises
#				hidden_effect = {
#					IRE_increase_loyalist_tensions = yes
#					IRE_increase_loyalist_tensions = yes
#					IRE_increase_loyalist_tensions = yes
#				}
#			}
#		}
#
#		ai_will_do = {
#			factor = 5
#			modifier = {
#				factor = 0
#				has_war = yes
#			}
#			modifier = {
#				factor = 0
#				NOT = {
#					strength_ratio = { tag = FROM ratio < 2.0 }
#					FROM = {
#						is_in_faction = yes
#						has_socialist_government = no
#					}
#				}
#			}
#		}
#	}

	GBR_decide_wif_fate = {
		icon = generic_flags
		allowed = { always = no }

		available = {
			FROM = {
				controls_all_owned_states = yes
			}
		}

		visible = {
			NOT = { has_country_flag = GBR_decided_wif_fate }
			is_subject = no
			FROM = {
				is_in_faction_with = ROOT
				OR = {
					is_subject = no
					is_subject_of = ROOT
				}
			}
		}

		cost = 0
		fire_only_once = yes

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_decide_wif_fate"
			country_event = united_kingdom_events.124
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				is_faction_leader = no
				faction_leader = { is_american_tag = no }
			}
		}
	}

	GBR_ask_for_malta_control = {
		icon = generic_flags

		available = {
			FROM = { controls_state = 116 }
		}

		visible = {
			NOT = { has_country_flag = GBR_decolonize }
			is_subject = no
			FROM = {
				owns_state = 116
				is_in_faction_with = ROOT
			}
		}

		targets = { 116.OWNER }
		target_root_trigger = { has_completed_focus = GBR_foreign_office }
		target_trigger = {
			FROM = {
				owns_state = 116
				is_in_faction_with = ROOT
			}
			NOT = { has_country_flag = GBR_decolonize }
			is_subject = no
		}

		cost = 0
		fire_only_once = yes

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_ask_for_malta_control"
			116 = { save_event_target_as = demand_state }
			FROM = {
				if = {
					limit = { is_subject = yes }
					overlord = { country_event = united_kingdom_events.131 }
				}
				else = {
					country_event = united_kingdom_events.131
				}
			}
		}

		ai_will_do = { factor = 10 }
	}

	GBR_ask_for_carib_control = {
		icon = generic_flags
		allowed = { tag = GBR }

		available = {
			FROM = {
				controls_all_owned_states = yes
				overlord = { has_capitulated = no }
			}
		}

		visible = {
			NOT = { has_country_flag = GBR_decolonize }
			is_subject = no
			FROM = {
				is_subject = yes
				NOT = { is_subject_of = ROOT }
				is_in_faction_with = ROOT
				NOT = { has_country_flag = GBR_requested_control }
				overlord = { is_in_faction_with = ROOT }
			}
		}

		cost = 0
		fire_only_once = yes

		target_root_trigger = { has_completed_focus = GBR_west_indies_question }
		targets = { WIF JAM TRI GYA }
		target_trigger = {
			FROM = {
				is_subject = yes
				NOT = { is_subject_of = ROOT }
				is_in_faction_with = ROOT
				NOT = { has_country_flag = GBR_requested_control }
			}
			NOT = { has_country_flag = GBR_decolonize }
			is_subject = no
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_ask_for_carib_control"
			FROM = {
				save_event_target_as = colony_country
				set_country_flag = GBR_requested_control
				overlord = {
					country_event = { id = united_kingdom_events.118 days = 3 }
				}
			}
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				FROM = { overlord = { CAN_is_commonwealth_tag = no } }
				is_faction_leader = no
				faction_leader = { is_american_tag = no }
			}
		}
	}

	GBR_ask_for_colony_control = {
		icon = generic_flags
		allowed = { tag = GBR }

		available = {
			FROM = {
				controls_all_owned_states = yes
				overlord = { has_capitulated = no }
			}
		}

		visible = {
			if = {
				limit = { has_global_flag = ENT_agree_colonies }
				NOT = { FROM = { is_ally_with = GER } }
			}
			FROM = {
				is_subject = yes
				overlord = { is_in_faction_with = ROOT }
				NOT = { is_subject_of = ROOT }
				NOT = { has_country_flag = GBR_requested_control }
			}
			is_subject = no
		}

		cost = 0
		fire_only_once = yes

		target_root_trigger = {
			has_country_flag = GBR_chose_empire
		}
		targets = { AST BOT BRM CEY EGY GHA KEN MAL NMB NGA NZL OMA SAF SAR SIE SUD UGA YEM ZAM ZIM }
		target_trigger = {
			FROM = {
				is_subject = yes
				overlord = { is_in_faction_with = ROOT }
				NOT = { is_subject_of = ROOT }
				NOT = { has_country_flag = GBR_requested_control }
			}
			is_subject = no
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_ask_for_colony_control"
			FROM = {
				save_event_target_as = colony_country
				set_country_flag = GBR_requested_control
				overlord = {
					country_event = { id = united_kingdom_events.118 days = 3 }
				}
			}
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				FROM = { overlord = { CAN_is_commonwealth_tag = no } }
				is_faction_leader = no
				faction_leader = { is_american_tag = no }
			}
		}
	}

	GBR_ask_for_colonies = {
		icon = generic_flags
		allowed = { tag = GBR }

		available = {
			has_army_manpower = { size > 49999 }
			has_navy_size = { size > 25 }
			FROM = { controls_all_owned_states = yes }
		}

		visible = {
			NOT = { has_country_flag = GBR_decolonize }
			FROM = {
				exists = yes
				NOT = { has_country_flag = GBR_refused_colony_transfer }
				is_in_faction_with = ROOT
				any_owned_state = {
					has_variable = ambition_area_@ROOT
					is_british_colonial_claim = yes
					is_claim_or_core_of_ROOT = no
					is_claim_or_core_of_FROM = no
				}
			}
			is_subject = no
		}

		cost = 0
		fire_only_once = no

		target_root_trigger = { has_completed_focus = GBR_foreign_office }
		target_array = faction_members
		target_trigger = {
			FROM = {
				NOT = { has_country_flag = GBR_refused_colony_transfer }
				is_in_faction_with = ROOT
				any_owned_state = {
					has_variable = ambition_area_@ROOT
					is_british_colonial_claim = yes
					is_claim_or_core_of_FROM = no
				}
			}
			is_subject = no
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_ask_for_colonies"
			FROM = {
				set_country_flag = GBR_refused_colony_transfer
				country_event = { id = united_kingdom_events.121 days = 3 }
			}
		}

		ai_will_do = { factor = 10 }
	}

	GBR_retake_colony = {
		icon = generic_flags
		allowed = { tag = GBR }

		available = {
			can_declare_war_on = FROM
		}

		visible = {
			if = {
				limit = { has_global_flag = ENT_agree_colonies }
				NOT = { FROM = { is_ally_with = GER } }
			}
			is_subject = no
			FROM = {
				exists = yes
				NOT = { is_ally_with = ROOT }
				OR = {
					any_owned_state = { is_coastal = yes }
					any_neighbor_country = { is_ally_of_ROOT = yes }
				}
				if = {
					limit = { tag = SAF }
					has_elected_government = no
				}
				if = {
					limit = { tag = CAN }
					has_elected_government = no
				}
				if = {
					limit = { tag = AST }
					has_elected_government = no
				}
			}
		}

		cost = 50
		fire_only_once = yes
		days_remove = 30
		war_with_target_on_remove = yes
		cancel_if_not_visible = yes

		target_root_trigger = {
			has_country_flag = GBR_chose_empire
		}
		targets = { AST BOT BRM CAN CEY EGY GHA KIV MAL NMB NGA NZL OMA SAF SAR SIE SUD UGA YEM ZAM ZIM }
		target_trigger = {
			is_subject = no
			FROM = {
				NOT = { is_ally_with = ROOT }
				OR = {
					any_owned_state = { is_coastal = yes }
					any_neighbor_country = { is_ally_of_ROOT = yes }
				}
			}
		}
		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_retake_colony"
			clear_decision_attack_AI = yes
		}
		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_retake_colony"
			add_war_support = -0.05
			setup_decision_attack_AI = yes
			FROM = { incoming_war_notification_effect = yes }
		}
		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_retake_colony"
			clear_decision_attack_AI = yes
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				OR = {
					has_war = yes
					is_in_faction = no
				}
			}
			modifier = {
				factor = 0
				FROM = {
					is_in_faction = yes
					any_allied_country = { is_actual_major = yes }
				}
			}
			modifier = {
				factor = 0.1
				FROM = { tag = EGY }
			}
			modifier = {
				factor = 10
				FROM = {
					OR = {
						tag = CAN
						tag = AST
						tag = NZL
						tag = SAF
					}
				}
			}
		}
	}

	GBR_demand_colonies = {
		icon = generic_flags
		allowed = { tag = GBR }

		available = {
			can_declare_war_on = FROM
		}

		visible = {
			if = {
				limit = { has_country_flag = ENT_agree_colonies }
				NOT = { FROM = { is_ally_with = GER } }
			}
			is_subject = no
			FROM = {
				exists = yes
				NOT = { is_ally_with = ROOT }
				NOT = { has_country_flag = GBR_refused_colony_transfer }
				any_owned_state = {
					has_variable = ambition_area_@ROOT
					is_british_colonial_claim = yes
					NOT = { #These have dedicated decisions, theoretically should already have by this stage though
						state = 118 #Gibraltar
						state = 299 #Falkland Islands
						state = 720 #South Georgia
						state = 119 #Northern Ireland
					}
				}
			}
		}

		cost = 50
		fire_only_once = no

		target_root_trigger = {
			has_country_flag = GBR_chose_empire
		}
		target_trigger = {
			is_subject = no
			FROM = {
				NOT = { is_ally_with = ROOT }
				NOT = { has_country_flag = GBR_refused_colony_transfer }
				any_owned_state = {
					has_variable = ambition_area_@ROOT
					is_british_colonial_claim = yes
				}
			}
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_demand_colonies"
			FROM = {
				set_country_flag = GBR_refused_colony_transfer
				country_event = { id = united_kingdom_events.121 days = 3 }
			}
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
}

GBR_reconstruction_decisions = {
	GBR_fight_syndicalists = {
		icon = oppression

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		available = {
			has_completed_focus = GBR_end_of_bra
		}

		visible = {
			has_dynamic_modifier = { modifier = GBR_syndicalist_resistance }
		}

		cancel_trigger = {
			NOT = { has_dynamic_modifier = { modifier = GBR_syndicalist_resistance } }
		}

		cost = 50
		days_remove = 90
		target_non_existing = yes

		modifier = {
			political_power_cost = 0.10
			industrial_capacity_factory = -0.10
			industrial_capacity_dockyard = -0.10
		}

		cancel_effect = {
			log = "[GetLogRoot]: Decision cancel GBR_fight_syndicalists"
			hidden_effect = { add_political_power = 50 }
		}

		complete_effect = {
			log = "[GetLogRoot]: Decision complete GBR_fight_syndicalists"
		}

		remove_effect = {
			log = "[GetLogRoot]: Decision remove GBR_fight_syndicalists"
			set_temp_variable = { GBR_resistance_temp = 0.03 }
			GBR_recalculate_syndicalist_resistance = yes
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 2
				has_stability < 0.40
			}
			modifier = {
				factor = 0.5
				has_stability > 0.50
			}
		}
	}

	GBR_land_settlement_act = {
		icon = soldierly_discipline

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cost = 50
		days_remove = 60
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_land_settlement_act"
			hidden_effect = { add_political_power = 50 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_land_settlement_act"
			add_war_support = 0.05
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_land_settlement_act"
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_land_settlement_act target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_abercrombie_plan = {
		icon = construction

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		available = {
			controls_state = 126 #Greater London
		}

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 126 } #Greater London
				NOT = { has_dynamic_modifier = { modifier = GBR_postwar_devastation } }
			}
		}

		cost = 50
		days_remove = 90

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_abercrombie_plan"
			hidden_effect = { add_political_power = 50 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_abercrombie_plan"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_abercrombie_plan"
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_abercrombie_plan target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_create_relief_council = {
		icon = generic_healthcare

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cost = 50
		days_remove = 60
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_create_relief_council"
			hidden_effect = { add_political_power = 50 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_create_relief_council"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_create_relief_council"
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_create_relief_council target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_expand_power_system = {
		icon = electrify

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cost = 75
		days_remove = 120
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_expand_power_system"
			hidden_effect = { add_political_power = 75 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_expand_power_system"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_expand_power_system"
			set_temp_variable = { GBR_pwd_multiplier = 2 }
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_expand_power_system target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_reduce_taxation = {
		icon = eng_trade_unions_support

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cost = 75
		days_remove = 90
		cancel_if_not_visible = yes

		modifier = {
			political_power_cost = 0.25
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_reduce_taxation"
			hidden_effect = { add_political_power = 75 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_reduce_taxation"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_reduce_taxation"
			set_temp_variable = { GBR_pwd_multiplier = 2 }
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_reduce_taxation target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_new_towns_act = {
		icon = construction

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cost = 100
		days_remove = 180
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_new_towns_act"
			hidden_effect = { add_political_power = 100 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_new_towns_act"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_new_towns_act"
			set_temp_variable = { GBR_pwd_multiplier = 2 }
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_new_towns_act target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_national_housing_project = {
		icon = construction

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cost = 100
		days_remove = 120
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_national_housing_project"
			hidden_effect = { add_political_power = 100 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_national_housing_project"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_national_housing_project"
			set_temp_variable = { GBR_pwd_multiplier = 2 }
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_national_housing_project target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_farming_subsidies = {
		icon = generic_agriculture

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cost = 150
		days_remove = 90
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_farming_subsidies"
			hidden_effect = { add_political_power = 150 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_farming_subsidies"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_farming_subsidies"
			set_temp_variable = { GBR_pwd_multiplier = 3 }
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_farming_subsidies target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_business_subsidies = {
		icon = hol_attract_foreign_investors

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cost = 150
		days_remove = 90
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_business_subsidies"
			hidden_effect = { add_political_power = 150 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_business_subsidies"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_business_subsidies"
			set_temp_variable = { GBR_pwd_multiplier = 3 }
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_business_subsidies target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_new_employment_act = {
		icon = eng_blackshirt_march

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_postwar_devastation }
		}

		cost = 150
		days_remove = 90
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_new_employment_act"
			hidden_effect = { add_political_power = 150 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_new_employment_act"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_new_employment_act"
			set_temp_variable = { GBR_pwd_multiplier = 3 }
			GBR_recalculate_postwar_devastation = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_new_employment_act target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_beveridge_report = {
		icon = eng_trade_unions_support

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		available = {
			hidden_trigger = { NOT = { has_country_flag = GBR_pigou_report_ongoing } }
		}

		visible = {
			has_dynamic_modifier = { modifier = GBR_legacy_revolution }
		}

		cost = 50
		days_remove = 120
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_beveridge_report"
			clr_country_flag = GBR_beveridge_report_ongoing
			hidden_effect = { add_political_power = 50 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_beveridge_report"
			set_country_flag = GBR_beveridge_report_ongoing
			GBR_reduce_legacy_revolution = yes
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_beveridge_report"
			clr_country_flag = GBR_beveridge_report_ongoing
			event_option_tooltip = united_kingdom_events.37.a
			hidden_effect = {
				country_event = united_kingdom_events.37
				remove_targeted_decision = { decision = GBR_beveridge_report target = FROM }
				remove_targeted_decision = { decision = GBR_pigou_report target = FROM }
			}
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0.1
				has_conservative_government = yes
			}
		}
	}

	GBR_pigou_report = {
		icon = eng_trade_unions_support

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		available = {
			hidden_trigger = { NOT = { has_country_flag = GBR_beveridge_report_ongoing } }
		}

		visible = {
			has_dynamic_modifier = { modifier = GBR_legacy_revolution }
		}

		cost = 50
		days_remove = 120
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot]: Decision cancel GBR_pigou_report"
			clr_country_flag = GBR_pigou_report_ongoing
			hidden_effect = { add_political_power = 50 }
		}

		complete_effect = {
			log = "[GetLogRoot]: Decision complete GBR_pigou_report"
			set_country_flag = GBR_pigou_report_ongoing
			GBR_reduce_legacy_revolution = yes
		}

		remove_effect = {
			log = "[GetLogRoot]: Decision remove GBR_pigou_report"
			clr_country_flag = GBR_pigou_report_ongoing
			event_option_tooltip = united_kingdom_events.38.a
			hidden_effect = {
				country_event = united_kingdom_events.38
				remove_targeted_decision = { decision = GBR_pigou_report target = ROOT }
				remove_targeted_decision = { decision = GBR_beveridge_report target = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0.1
				has_government = social_democrat
			}
		}
	}

	GBR_desyndicalisation = {
		icon = generic_industry

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		visible = {
			has_dynamic_modifier = { modifier = GBR_legacy_revolution }
		}

		cost = 100
		days_remove = 120
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_desyndicalisation"
			hidden_effect = { add_political_power = 100 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_desyndicalisation"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_desyndicalisation"
			set_country_flag = GBR_desyndicalisation_completed
			GBR_reduce_legacy_revolution = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_desyndicalisation target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_pass_insurance_act = {
		icon = eng_trade_unions_support

		allowed = { always = no } #activated by united_kingdom_events.37

		available = {
			OR = {
				has_completed_focus = GBR_hold_elections
				has_completed_focus = GBR_guided_parliament
			}
		}

		visible = {
			has_dynamic_modifier = { modifier = GBR_legacy_revolution }
		}

		cost = 100
		days_remove = 90
		cancel_if_not_visible = yes

		modifier = {
			political_power_cost = 0.25
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_pass_insurance_act"
			hidden_effect = { add_political_power = 100 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_pass_insurance_act"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_pass_insurance_act"
			GBR_reduce_legacy_revolution = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_pass_insurance_act target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_central_banking_act = {
		icon = eng_trade_unions_support

		allowed = { always = no } #activated by united_kingdom_events.38

		available = {
			OR = {
				has_completed_focus = GBR_hold_elections
				has_completed_focus = GBR_guided_parliament
			}
		}

		visible = {
			has_dynamic_modifier = { modifier = GBR_legacy_revolution }
		}

		cost = 100
		days_remove = 90
		cancel_if_not_visible = yes

		modifier = {
			political_power_cost = 0.25
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_central_banking_act"
			hidden_effect = { add_political_power = 100 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_central_banking_act"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_central_banking_act"
			set_country_flag = GBR_central_banking_act_completed
			GBR_reduce_legacy_revolution = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_central_banking_act target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_national_health_service = {
		icon = generic_healthcare

		allowed = { always = no } #activated by united_kingdom_events.37

		available = {
			OR = {
				has_completed_focus = GBR_hold_elections
				has_completed_focus = GBR_guided_parliament
			}
		}

		visible = {
			has_dynamic_modifier = { modifier = GBR_legacy_revolution }
		}

		cost = 100
		days_remove = 90
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_national_health_service"
			hidden_effect = { add_political_power = 100 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_national_health_service"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_national_health_service"
			GBR_reduce_legacy_revolution = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_national_health_service target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_privatisation_act = {
		icon = generic_healthcare

		allowed = { always = no } #activated by united_kingdom_events.38

		available = {
			custom_override_tooltip = {
				tooltip = { localization_key = has_completed_decision DECISION = GBR_desyndicalisation }
				has_country_flag = GBR_desyndicalisation_completed
			}
			custom_override_tooltip = {
				tooltip = { localization_key = has_completed_decision DECISION = GBR_central_banking_act }
				has_country_flag = GBR_central_banking_act_completed
			}
			OR = {
				has_completed_focus = GBR_hold_elections
				has_completed_focus = GBR_guided_parliament
			}
		}

		visible = {
			has_dynamic_modifier = { modifier = GBR_legacy_revolution }
		}

		cost = 100
		days_remove = 90
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_privatisation_act"
			hidden_effect = { add_political_power = 100 }
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_privatisation_act"
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_privatisation_act"
			clr_country_flag = GBR_desyndicalisation_completed
			clr_country_flag = GBR_central_banking_act_completed
			GBR_reduce_legacy_revolution = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_privatisation_act target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_legacy_revolution_generic = {
		icon = eng_trade_unions_support

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		available = {
			OR = {
				has_completed_focus = GBR_hold_elections
				has_completed_focus = GBR_guided_parliament
			}
		}

		visible = {
			has_dynamic_modifier = { modifier = GBR_legacy_revolution }
		}

		cost = 100
		days_remove = 90
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_legacy_revolution_generic"
			hidden_effect = { add_political_power = 100 }
			if = {
				limit = { has_variable = GBR_legacy_revolution_generic_stability_recoup }
				add_stability = var:GBR_legacy_revolution_generic_stability_recoup
			}
			else_if = {
				limit = {
					has_liberal_government = yes
					NOT = { has_variable = GBR_legacy_revolution_generic_stability_recoup }
				}
				effect_tooltip = { add_stability = 0.05 }
			}
			clear_variable = GBR_legacy_revolution_generic_stability_recoup
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_legacy_revolution_generic"
			if = {
				limit = { has_liberal_government = yes }
				set_variable = { GBR_legacy_revolution_generic_stability_recoup = 0.05 }
				add_stability = -0.05
			}
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_legacy_revolution_generic"
			GBR_reduce_legacy_revolution = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_legacy_revolution_generic target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}

	GBR_female_equality = {
		icon = eng_trade_unions_support

		allowed = { always = no } #activated by GBR_british_reconstruction_authority focus

		available = {
			OR = {
				has_completed_focus = GBR_hold_elections
				has_completed_focus = GBR_guided_parliament
			}
		}

		visible = {
			has_liberal_government = yes
			has_dynamic_modifier = { modifier = GBR_legacy_revolution }
		}

		cost = 50
		days_remove = 90
		cancel_if_not_visible = yes

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel GBR_female_equality"
			hidden_effect = { add_political_power = 50 }
			add_stability = 0.05
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete GBR_female_equality"
			add_stability = -0.05
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove GBR_female_equality"
			GBR_reduce_legacy_revolution = yes
			hidden_effect = {
				remove_targeted_decision = { decision = GBR_female_equality target = FROM }
			}
		}

		ai_will_do = { factor = 100 }
	}
}
