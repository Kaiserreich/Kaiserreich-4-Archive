###########################
# Chile Decisions
###########################

political_actions = {
	CHL_join_internationale = {
		icon = generic_prepare_civil_war

		allowed = {
			original_tag = CHL
		}

		available = {
			OR = {
				has_war = no
				custom_trigger_tooltip = {
					tooltip = BRA_same_wars_as_intl
					has_war = yes
					any_enemy_country = { has_war_with = FROM }
				}
			}
			FOP = {
				has_country_flag = FOP_we_won_against_argentina_yay_flag
			}
		}

		visible = {
			is_subject = no
			is_in_faction = no
			has_socialist_government = yes
			FROM = {
				exists = yes
				has_socialist_government = yes
				is_faction_leader = yes
			}
		}

		targets = { FRA ENG }

		target_trigger = {
			FROM = {
				has_socialist_government = yes
				is_faction_leader = yes
			}
		}

		fire_only_once = yes
		cost = 25

		ai_will_do = {
			factor = 10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHL_join_internationale target: [From.GetName]"
			FROM = { country_event = chl.ally.1 }
		}
	}

	CHL_join_phalanstere = {
		icon = generic_research

		allowed = {
			original_tag = CHL
		}

		visible = {
			is_in_faction_with = FRA
			has_global_flag = scientific_phalanstere
			NOT = { is_in_tech_sharing_group = phalanstere_research }
		}

		fire_only_once = yes
		cost = 50

		ai_will_do = {
			factor = 10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CHL_join_phalanstere"
			add_to_tech_sharing_group = phalanstere_research
		}
	}
}

