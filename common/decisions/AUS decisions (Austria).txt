###########################
# Austria Decisions
###########################
# By ~mw~
###########################

political_actions = {

	AUS_syndie_threat = {
		icon = generic_prepare_civil_war

		allowed = {
			always = no
		}

		available = {
			OR = {
				has_completed_focus = AUS_status_quo
				has_completed_focus = AUS_embrace_pluralism
				has_completed_focus = AUS_military_occupation
			}
		}
		visible = {
			is_faction_leader = yes
			has_socialist_government = no
			FROM = { has_war_with = GER }
			NOT = { has_war_with = GER }
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 0
			modifier = {
				factor = 0
				has_war = yes
				OR = {
					has_war_with = HUN
					has_war_with = SER
					has_war_with = ROM
					surrender_progress > 0
					HUN = {
						is_subject_of = AUS
						surrender_progress > 0.05
					}
					CZE = {
						is_subject_of = AUS
						surrender_progress > 0.15
					}
					CRO = {
						is_subject_of = AUS
						surrender_progress > 0.15
					}
				}
				NOT = { has_war_with = INT }
				NOT = { has_war_with = RUS }
				NOT = { has_war_with = SOV }
			}
			modifier = {
				factor = 0
				OTT = { exists = yes }
				has_game_rule = { rule = OTT_forpol option = OTT_FORPOL_AUS }
			}
			modifier = {
				add = 100
				OR = {
					GER = { surrender_progress > 0.30 }
					52 = { # France has occupied Upper Bavaria
						controller = {
							has_socialist_government = yes
							has_war_with = GER
						}
					}
				}
			}
			modifier = {
				add = 100
				OR = {
					188 = {	# SOV/UKR has occupied Memel
						controller = {
							has_socialist_government = yes
							has_war_with = GER
						}
					}
					any_owned_state = {	# Austria borders directly SOV
						any_neighbor_state = {
							is_controlled_by = SOV
						}
					}
				}
			}
			modifier = {
				add = 100	# Austria's allies border SOV
				SOV = { any_home_area_neighbor_country = { is_ally_with = ROOT } }
			}
			modifier = {
				add = 100
				POL = {
					OR = {
						AND = {
							is_in_faction = yes
							faction_leader = { has_socialist_government = yes }		# Poland is in a socialist faction
						}
						AND = {		# Poland is in the DAB and borders SOV
							is_in_faction_with = ROOT
							is_neighbor_of = SOV
						}
					}
				}
			}
			modifier = {
				add = 100	# Syndie Ukraine
				UKR = { has_socialist_government = yes }
			}
			modifier = {
				add = 100 # to make Austria join in the war if it is still a stalemate by 1942
				is_actual_major = yes
				date > 1942.1.1
				OR = {
					RUS = {
						has_war_with = GER
						NOT = { surrender_progress > 0.10 }
					}
					SOV = {
						has_war_with = GER
						NOT = { surrender_progress > 0.10 }
					}
				}
				FRA = {
					has_war_with = GER
					NOT = { surrender_progress > 0.10 }
				}
			}
			modifier = {
				add = 100	# Austria is already at war with INT / RUS / SOV
				OR = {
					has_war_with = INT
					has_war_with = RUS
					has_war_with = SOV
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete AUS_syndie_threat target: [From.GetName]"
			FROM = { save_event_target_as = threat_country }
			GER = { country_event = aus.tree.4011 }
		}
	}

	AUS_russian_threat = {
		icon = generic_prepare_civil_war

		allowed = { always = no }

		available = {
			OR = {
				has_completed_focus = AUS_status_quo
				has_completed_focus = AUS_embrace_pluralism
				has_completed_focus = AUS_military_occupation
			}
		}

		visible = {
			is_faction_leader = yes
			has_socialist_government = no
			NOT = { has_war_with = GER }
			FROM = {
				has_war_with = GER
				NOT = { is_in_faction_with = INT }
			}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = {
			factor = 0
			modifier = {
				factor = 0
				GER = {
					surrender_progress < 0.10
					NOT = { has_war_with = INT }
				}
			}
			modifier = {
				factor = 0
				has_war = yes
				OR = {
					has_war_with = HUN
					has_war_with = SER
					has_war_with = ROM
					surrender_progress > 0.01
					HUN = {
						is_subject_of = AUS
						surrender_progress > 0.05
					}
					CZE = {
						is_subject_of = AUS
						surrender_progress > 0.15
					}
					CRO = {
						is_subject_of = AUS
						surrender_progress > 0.15
					}
				}
				NOT = {
					has_war_with = INT
					has_war_with = RUS
					has_war_with = SOV
				}
			}
			modifier = {
				factor = 0
				has_war_with = INT
				GER = { surrender_progress < 0.3 }
				count_triggers = {
					amount = 2
					POL = {
						surrender_progress < 0.20
						is_in_faction_with = RUS
					}
					SER = {
						surrender_progress < 0.20
						is_in_faction_with = RUS
					}
					ROM = {
						surrender_progress < 0.20
						is_in_faction_with = RUS
					}
				}
			}
			modifier = {
				factor = 0
				OTT = { exists = yes }
				has_game_rule = { rule = OTT_forpol option = OTT_FORPOL_AUS }
			}
			modifier = {
				add = 100	# Austria's allies border RUS
				RUS = { any_home_area_neighbor_country = { is_ally_with = ROOT } }
			}
			modifier = {
				add = 100
				188 = {	# RUS has occupied Memel
					is_owned_by = GER
					controller = { is_ally_with = RUS }
				}
			}
			modifier = {
				add = 100
				POL = {	# POL is in RP and has fallen
					is_in_faction_with = GER
					has_capitulated = yes
					any_core_state = { controller = { is_ally_with = RUS } }
				}
			}
			modifier = {
				add = 100	# POL is in MA
				POL = { is_in_faction_with = RUS }
			}
			modifier = {
				add = 100	# POL is at war w/ GER
				POL = { has_war_with = GER }
			}
			modifier = {
				add = 100	# BUL is in MA and borders DAB
				BUL = {
					is_in_faction_with = RUS
					any_home_area_neighbor_country = { is_ally_with = ROOT }
				}
			}
			modifier = {
				add = 100
				OR = {
					has_war_with = INT
					has_war_with = RUS
					has_war_with = SOV
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete AUS_russian_threat target: [From.GetName]"
			FROM = { save_event_target_as = threat_country }
			GER = { country_event = aus.tree.4011 }
		}
	}

	AUS_join_vienna_circle = {
		icon = generic_research

		allowed = {
			NOT = {
				tag = AUS
				tag = FRA #always hostile
				tag = ENG
				tag = CSA
				tag = SRI
				tag = SWF
				tag = SOV
				tag = FOP
				tag = HND
				tag = RUS #can't join
				tag = JAP
				tag = TRM
				tag = USA
				tag = PSA
				tag = NEE
				tag = RAJ
				tag = GER
				tag = GEA
				tag = MAF
				tag = DEI
			}
			is_tag_in_china_area = no #can't join
		}

		targets = { AUS }
		target_root_trigger = { is_in_faction_with = AUS }
		target_trigger = {
			is_in_faction_with = FROM
			NOT = { is_in_tech_sharing_group = austrian_research }
			FROM = { is_in_tech_sharing_group = austrian_research }
			OR = {
				FROM = { is_faction_leader = yes }
				has_country_flag = AUS_former_faction_member
				OTT = {
					is_faction_leader = yes
					is_in_faction_with = FROM
				}
			}
		}

		fire_only_once = no

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete AUS_join_vienna_circle target: [From.GetName]"
			add_to_tech_sharing_group = austrian_research
		}
	}

	AUS_reform_faction = {
		icon = generic_political_discourse
		allowed = { always = no }

		available = {
			has_war = no
		}

		visible = {
			is_subject = no
			has_socialist_government = no
			OR = {
				is_in_faction = no
				is_in_faction_with = GER
			}
			has_country_flag = AUS_intervened_in_weltkrieg
			NOT = { has_global_flag = AUS_treaty_of_budapest }
		}

		cost = 0
		fire_only_once = no

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete AUS_reform_faction"
			clr_country_flag = AUS_intervened_in_weltkrieg
			if = {
				limit = { is_in_faction_with = GER }
				leave_faction = yes
				GER = { country_event = aus.tree.4015 }
			}
			create_faction = austrian_sphere
			every_country = {
				limit = {
					has_country_flag = AUS_former_faction_member
					has_socialist_government = no
					OR = {
						is_in_faction_with = GER
						is_in_faction = no
					}
					is_subject = no
				}
				country_event = aus.tree.4014
			}
		}
	}

	CZE_join_belgrade_pact = {
		icon = eng_trade_unions_demand

		allowed = { always = no }

		available = {
			is_subject = no
			has_war_with = AUS
		}

		visible = {
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete CZE_join_belgrade_pact target: [From.GetName]"
			FROM = { country_event = ausgleich.new.901 }
		}
	}

	CZE_join_germany = {
		icon = eng_trade_unions_demand

		allowed = { always = no }

		available = {
			is_in_faction = no
			at_peace_or_same_enemies_as_FROM = yes
		}

		visible = {
			is_subject = no
			has_socialist_government = no
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
		}

		cost = 10
		fire_only_once = yes

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				FROM = { surrender_progress > 0.30 }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete CZE_join_germany target: [From.GetName]"
			FROM = { country_event = generic_events.10 }
		}
	}

	CZE_leave_belgrade_pact = {
		icon = eng_trade_unions_demand

		allowed = { always = no }

		available = {
			has_war = no
		}

		visible = {
			is_subject = no
			is_in_faction_with = FROM
			FROM = { is_faction_leader = yes }
		}

		cost = 25
		fire_only_once = yes

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete CZE_leave_belgrade_pact target: [From.GetName]"
			leave_faction = yes
			FROM = { country_event = ausgleich.new.914 }
		}
	}

	CZE_form_czechoslovakia = {
		icon = eng_trade_unions_demand

		allowed = { always = no }

		available = {
			has_war = no
			owns_state = 70
			owns_state = 71
			owns_state = 1090
		}

		visible = {
			is_subject = no
			70 = { is_claimed_by = ROOT }
		}

		cost = 75
		fire_only_once = yes

		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete CZE_form_czechoslovakia"
			add_stability = 0.1
			set_cosmetic_tag = CZE_czechoslavakia
			add_state_core = 70
			add_state_core = 71
			add_state_core = 73
			add_state_core = 1090
			hidden_effect = {
				remove_state_claim = 70
				remove_state_claim = 71
				remove_state_claim = 73
				remove_state_claim = 1090
			}
		}
	}

	GAL_unite_with_poland = {
		icon = eng_trade_unions_demand

		allowed = { always = no }

		available = {
			has_war = no
		}

		visible = {
			country_exists = FROM
			NOT = { has_autonomy_state = kr_crownland }
			NOT = { has_war_with = FROM }
			FROM = { has_capitulated = no }
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete GAL_unite_with_poland target: [From.GetName]"
			if = {
				limit = { is_subject = yes }
				overlord = { country_event = gal.3 }
			}
			else = {
				FROM = { country_event = { id = gal.2 days = 2 } }
				custom_effect_tooltip = if_they_accept_tt
				effect_tooltip = {
					FROM = {
						add_state_core = 88 #Krakow
						add_state_claim = 91 #Lwow
						add_state_claim = 89 #Stanislawow
						annex_country = {
							target = ROOT
							transfer_troops = yes
						}
						change_tag_from = ROOT
					}
				}
			}
		}
	}

	GAL_join_belgrade_pact = {
		name = CZE_join_belgrade_pact
		icon = eng_trade_unions_demand

		allowed = { always = no }

		available = {
			is_subject = no
			has_war_with = AUS
		}

		visible = {
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
		}

		cost = 0
		fire_only_once = yes

		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete GAL_join_belgrade_pact target: [From.GetName]"
			FROM = { country_event = ausgleich.new.906 }
		}
	}

	AUS_unite_with_germany = {
		icon = eng_trade_unions_demand

		allowed = { always = no }

		available = {
			has_war = no
		}

		visible = {
			NOT = {
				has_cosmetic_tag = AUS_empire
				has_cosmetic_tag = AUS_united_states
				has_cosmetic_tag = AUS_danubia
			}
			is_subject = no
			OR = {
				is_in_faction = no
				is_in_faction_with = FROM
			}
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
		}

		cost = 10
		fire_only_once = yes

		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete AUS_unite_with_germany target: [From.GetName]"
			FROM = { country_event = { id = ausgleich.new.916 days = 2 } }
		}
	}
}

SER_The_Road_To_Unification_decisions = {
	SER_Ultimatum_To_Illyria = {
		icon = eng_puppet_usa
		allowed = { always = no }

		visible = {
			is_subject = no
			can_declare_war_on = FROM
			FROM = { is_subject = no }
		}

		cost = 0
		days_remove = 30
		fire_only_once = yes
		cancel_if_not_visible = yes
		war_with_target_on_remove = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				ROOT_is_threatened = yes
				NOT = { FROM = { has_war_with = AUS } } #exception - intervene ASAP in Austrian CW
			}
			modifier = {
				factor = 0
				FROM = {
					default_attack_ai_trigger_FROM = yes
					NOT = { has_war_with = AUS } #exception - intervene ASAP in Austrian CW
				}
			}
			modifier = {
				factor = 0
				surrender_progress > 0.3
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel SER_Ultimatum_To_Illyria target: [From.GetName]"
			clear_decision_attack_AI = yes
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete SER_Ultimatum_To_Illyria target: [From.GetName]"
			setup_decision_attack_AI = yes
			FROM = { country_event = { id = ausgleich.new.911 days = 2 } }	#warning event
			if = {
				limit = {
					can_declare_war_on = AUS
					AUS = {
						is_subject = no
						has_cosmetic_tag = AUS_empire
					}
				}
				custom_effect_tooltip = AUS_may_lead_to_war_with_austria
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove SER_Ultimatum_To_Illyria target: [From.GetName]"
			clear_decision_attack_AI = yes
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}
	}
}

AUS_elections_decisions = {
	AUS_endorse_CS = {
		icon = GFX_decision_generic_political_discourse
		allowed = { always = no }
		available = {
			NOT = { has_country_flag = endorsing_a_party }
		}

		visible = {
			date < 1936.3.9
		}

		modifier = {
			social_conservative_drift = 0.05
			political_power_cost = 0.1
		}

		cost = 20
		days_remove = 60
		fire_only_once = yes

		ai_will_do = {
			base = 200
			modifier = {
				factor = 0
				has_game_rule = { rule = AUS_election option = AUS_ELECTION_SOCDEM }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete AUS_endorse_CS"
			set_country_flag = endorsing_a_party
			hidden_effect = {
				remove_targeted_decision = { target = THIS decision = AUS_endorse_SDAP }
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove AUS_endorse_CS"
			clr_country_flag = endorsing_a_party
		}
	}

	AUS_endorse_SDAP = {
		icon = GFX_decision_generic_political_discourse
		allowed = { always = no }
		available = {
			NOT = { has_country_flag = endorsing_a_party }
		}

		visible = {
			date < 1936.3.9
		}

		modifier = {
			social_democrat_drift = 0.05
			political_power_cost = 0.1
		}

		cost = 20
		days_remove = 60
		fire_only_once = yes

		ai_will_do = {
			base = 200
			modifier = {
				factor = 0
				has_game_rule = { rule = AUS_election option = AUS_ELECTION_SOCCON }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete AUS_endorse_SDAP"
			set_country_flag = endorsing_a_party
			hidden_effect = {
				remove_targeted_decision = { target = THIS decision = AUS_endorse_CS }
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove AUS_endorse_SDAP"
			clr_country_flag = endorsing_a_party
		}
	}
}

