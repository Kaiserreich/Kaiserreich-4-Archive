POL_Stability_of_Nation = {
	###Missions
	POL_curtail_veto_mission = {
		icon = GFX_decision_scales
		allowed = { always = no }
		available = { has_completed_focus = POL_Abolish_The_Royal_Veto }
		is_good = no
		selectable_mission = yes
		fire_only_once = yes
		days_mission_timeout = 660
		ai_will_do = { factor = 100 }
		cancel_trigger = {
			is_monarchy = no
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_curtail_veto_mission"
			set_temp_variable = { ideology_change_var = -10 }
			change_ruling_party_popularity = yes
			set_temp_variable = { ideology_change_var = 15 }
			POL_add_republican_influence = yes
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_curtail_veto_mission"
			add_political_power = 50
			set_temp_variable = { ideology_change_var = 10 }
			change_ruling_party_popularity = yes
			set_temp_variable = { ideology_change_var = -15 }
			POL_add_republican_influence = yes
		}
	}
	POL_centrolew_reform_mission = {
		icon = GFX_decision_peace_dove
		allowed = { always = no } #manually activated
		available = {
			has_completed_focus = POL_Free_And_Secular_Education
			has_completed_focus = POL_Enshrine_Minority_Rights
			has_completed_focus = POL_Female_Suffrage
			has_completed_focus = POL_Unicameralism
		}
		is_good = no
		selectable_mission = yes
		fire_only_once = yes
		days_mission_timeout = 660
		ai_will_do = { factor = 100 }
		cancel_trigger = {
			has_left_democratic_government = no
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_centrolew_reform_mission"
			add_stability = -0.05
			set_temp_variable = { POL_influence_change = 20 }
			POL_add_syndicalist_influence = yes
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_centrolew_reform_mission"
			add_political_power = 50
			set_temp_variable = { ideology_change_var = 5 }
			change_ruling_party_popularity = yes
			set_country_flag = POL_completed_centrolew_reform
		}
	}
	POL_protest = {
		icon = GFX_decision_POL_organize_strike_two
		activation = {
			has_country_flag = POL_election_happened
			OR = {
				has_government = market_liberal
				has_government = authoritarian_democrat
			}
			OR = {
				AND = {
					check_variable = { POL_royal_influence < 10 }
					check_variable = { POL_rep_influence > 25 }
				}
				has_country_flag = POL_rep_real_victory
				has_country_flag = POL_soc_real_victory
			}
			is_monarchy = yes
			has_capitulated = no
		}

		available = {
			has_stability > 0.5
			custom_trigger_tooltip = {
				tooltip = POL_king_over_republicans
				check_variable = { POL_royal_influence > POL_rep_influence }
			}
			custom_trigger_tooltip = {
				tooltip = POL_king_over_socialists
				check_variable = { POL_royal_influence > POL_soc_influence }
			}
		}

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					is_monarchy = no
					has_capitulated = yes
					AND = {
						NOT = { has_country_flag = POL_royal_veto_used }
						has_left_democratic_government = yes
					}
					has_government = social_conservative
				}
			}
		}

		is_good = no
		selectable_mission = yes
		fire_only_once = yes
		days_mission_timeout = 60
		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				NOT = { has_game_rule = { rule = POL_election option = DEFAULT } }
				NOT = { has_game_rule = { rule = POL_election option = POL_ELECTION_MARLIB } }
				NOT = { has_game_rule = { rule = POL_election option = POL_ELECTION_SOCCON } }
				NOT = { has_game_rule = { rule = POL_election option = POL_ELECTION_AUTDEM } }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_protest"
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_protest"
			country_event = poland.197
		}
	}
	POL_syndicalist_threat = {
		icon = GFX_decision_generic_wreckers

		activation = {
			has_country_flag = POL_election_happened
			has_democratic_government = yes
			OR = {
				check_variable = { POL_soc_influence > 10 }
				has_country_flag = POL_soc_real_victory
				AND = {
					is_ai = yes
					OR = {
						has_game_rule = { rule = POL_revolts option = POL_REVOLTS_TOTALIST }
						has_game_rule = { rule = POL_revolts option = POL_REVOLTS_SYNDIE }
						has_game_rule = { rule = POL_revolts option = POL_REVOLTS_RADSOC }
					}
					NOT = { has_country_flag = POL_socialist_uprising }
				}
			}
			has_capitulated = no
			OR = {
				is_subject = no
				is_subject_of = GER
			}
		}

		available = {
			OR = {
				custom_trigger_tooltip = {
					tooltip = POL_socialist_influence_1
					check_variable = { POL_soc_influence < 5 }
				}
				if = {
					limit = { has_government = social_conservative }
					has_stability > 0.5
					custom_trigger_tooltip = {
						tooltip = POL_nationalists_over_socialists
						check_variable = { POL_nat_influence > POL_soc_influence }
					}
				}
				else_if = {
					limit = { is_monarchy = yes }
					OR = {
						has_completed_focus = POL_Constitutionalist_Coalition
						AND = {
							has_stability > 0.5
							custom_trigger_tooltip = {
								tooltip = POL_king_over_socialists
								check_variable = { POL_royal_influence > POL_soc_influence }
							}
						}
					}
				}
				else = {
					has_country_flag = POL_completed_centrolew_reform
					has_stability > 0.5
					custom_trigger_tooltip = {
						tooltip = POL_republicans_over_socialists
						check_variable = { POL_rep_influence > POL_soc_influence }
					}
				}
			}
		}

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_democratic_government = no
					has_capitulated = yes
					has_country_flag = POL_nationalist_uprising
					AND = {
						is_subject = yes
						NOT = { is_subject_of = GER }
					}
				}
			}
		}

		is_good = no
		fixed_random_seed = no
		selectable_mission = yes
		fire_only_once = yes
		days_mission_timeout = POL_revolution_timer

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				OR = {
					has_game_rule = { rule = POL_revolts option = POL_REVOLTS_TOTALIST }
					has_game_rule = { rule = POL_revolts option = POL_REVOLTS_SYNDIE }
					has_game_rule = { rule = POL_revolts option = POL_REVOLTS_RADSOC }
				}
				NOT = { has_country_flag = POL_socialist_uprising }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_syndicalist_threat"
			custom_effect_tooltip = POL_socialist_stop
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_syndicalist_threat"
			if = {
				limit = { POL_syndicalist_revolution_trigger = yes }
				custom_effect_tooltip = POL_revolution_ready
				country_event = poland.412
			}
			else = {
				if = {
					limit = {
						RUS = {
							has_socialist_government = yes
							is_faction_leader = yes
							NOT = { has_war_with = ROOT }
						}
					}
					custom_effect_tooltip = POL_revolution_preparation_EFTI
				}
				else = {
					custom_effect_tooltip = POL_revolution_preparation
				}
				activate_mission_tooltip = POL_syndicalist_threat
				random_list = {
					10 = {
						set_temp_variable = { POL_influence_change = 2 }
						POL_add_syndicalist_influence = yes
					}
					10 = {
						add_stability = -0.01
					}
				}
				hidden_effect = {
					country_event = poland.630 #should only fire on the first time I'm pretty sure
					GER = { country_event = { id = poland.420 days = 49 } }	# repeating warning event that only displays with trigger to check if revolt is happening
					if = {
						limit = { is_in_faction_with = AUS }
						AUS = { country_event = { id = poland.420 days = 49 } }	# repeating warning event that only displays with trigger to check if revolt is happening
					}
					if = {
						limit = { GER = { has_country_flag = POL_revolt_warning_syndicalist } }	# cancel warning. Flag given by above event
						GER = { country_event = poland.421 }
					}
					if = {
						limit = { AUS = { has_country_flag = POL_revolt_warning_syndicalist } }	# cancel warning. Flag given by above event
						AUS = { country_event = poland.421 }
					}
					activate_mission = POL_syndicalist_threat
				}
			}
		}
	}
	POL_nationalist_threat = {
		icon = GFX_decision_eng_blackshirt_speech

		activation = {
			has_country_flag = POL_election_happened
			NOT = { has_country_flag = POL_endecja_collaborates }
			OR = {
				has_liberal_government = yes
				has_government = authoritarian_democrat
			}
			OR = {
				check_variable = { POL_nat_influence > 10 }
				AND = {
					is_ai = yes
					OR = {
						has_game_rule = { rule = POL_revolts option = POL_REVOLTS_NATPOP }
						has_game_rule = { rule = POL_revolts option = POL_REVOLTS_PATAUT }
						has_game_rule = { rule = POL_revolts option = POL_REVOLTS_SOCCON }
					}
					NOT = { has_country_flag = POL_nationalist_uprising }
				}
			}
			has_capitulated = no
			OR = {
				is_subject = no
				is_subject_of = GER
			}
		}

		available = {
			OR = {
				custom_trigger_tooltip = {
					tooltip = POL_nationalist_influence_1
					check_variable = { POL_nat_influence < 5 }
				}
				if = {
					limit = { is_monarchy = yes }
					OR = {
						has_completed_focus = POL_The_Christian_Coalition
						AND = {
							has_stability > 0.5
							custom_trigger_tooltip = {
								tooltip = POL_king_over_nationalists
								check_variable = { POL_royal_influence > POL_nat_influence }
							}
						}
					}
				}
				else = {
					has_country_flag = POL_completed_centrolew_reform
					custom_trigger_tooltip = {
						tooltip = POL_republicans_over_nationalists
						check_variable = { POL_rep_influence > POL_nat_influence }
					}
				}
				has_country_flag = POL_rydz_coup_attempt
			}
		}

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_country_flag = POL_endecja_collaborates
					has_country_flag = POL_socialist_uprising
					NOT = {
						has_liberal_government = yes
						has_government = authoritarian_democrat
					}
					has_capitulated = yes
					AND = {
						is_subject = yes
						NOT = { is_subject_of = GER }
					}
				}
			}
		}

		is_good = no
		fixed_random_seed = no
		selectable_mission = yes
		fire_only_once = yes
		days_mission_timeout = POL_coup_timer

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				OR = {
					has_game_rule = { rule = POL_revolts option = POL_REVOLTS_NATPOP }
					has_game_rule = { rule = POL_revolts option = POL_REVOLTS_PATAUT }
					has_game_rule = { rule = POL_revolts option = POL_REVOLTS_SOCCON }
				}
				NOT = { has_country_flag = POL_nationalist_uprising }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_nationalist_threat"
			custom_effect_tooltip = POL_nationalist_stop
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_nationalist_threat"
			if = {
				limit = { POL_nationalist_uprising_trigger = yes }
				custom_effect_tooltip = POL_coup_ready
				country_event = poland.410
			}
			else = {
				custom_effect_tooltip = POL_coup_preparation
				activate_mission_tooltip = POL_nationalist_threat
				random_list = {
					10 = {
						set_temp_variable = { POL_influence_change = 2 }
						POL_add_nationalist_influence = yes

					}
					10 = {
						add_stability = -0.01
					}
				}
				hidden_effect = {
					GER = { country_event = { id = poland.420 days = 39 } }	# repeating warning event that only displays with trigger to check if revolt is happening
					if = {
						limit = { is_in_faction_with = AUS }
						AUS = { country_event = { id = poland.420 days = 39 } }	# repeating warning event that only displays with trigger to check if revolt is happening
					}
					if = {
						limit = { GER = { has_country_flag = POL_revolt_warning_nationalist } }	# cancel warning. Flag given by above event
						GER = { country_event = poland.421 }
					}
					if = {
						limit = { AUS = { has_country_flag = POL_revolt_warning_nationalist } }	# cancel warning. Flag given by above event
						AUS = { country_event = poland.421 }
					}
					activate_mission = POL_nationalist_threat
				}
			}
		}
	}
	POL_stall_nationalists = {
		icon = GFX_decision_generic_speech
		visible = { has_active_mission = POL_nationalist_threat }
		cost = POL_stall_cost
		days_remove = 40

		ai_will_do = { base = 0 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_stall_nationalists"
			add_days_mission_timeout = {
				mission = POL_nationalist_threat
				days = 50
			}
			add_to_variable = { POL_stall_cost = 10 }
		}
	}
	POL_stall_socialists = {
		icon = GFX_decision_generic_speech
		visible = { has_active_mission = POL_syndicalist_threat }
		cost = POL_stall_cost
		days_remove = 40

		ai_will_do = { base = 0 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_stall_socialists"
			add_days_mission_timeout = {
				mission = POL_syndicalist_threat
				days = 50
			}
			add_to_variable = { POL_stall_cost = 10 }
		}
	}
	#Nationalist plot
	POL_operation_parasol = {
		icon = GFX_decision_generic_military
		allowed = { always = no }
		visible = {
			has_government = social_conservative
			is_in_faction = yes
			NOT = { has_completed_focus = POL_Reinforce_The_Parliamentary_System }
		}
		available = {
			faction_leader = { has_war_with = RUS }
			surrender_progress < 0.2
			if = {
				limit = {
					POL = { is_ai = yes }
					has_game_rule = { rule = POL_revolts option = POL_REVOLTS_PATAUT }
				}
			}
			else = {
				custom_trigger_tooltip = {
					tooltip = POL_nationalists_are_majority
					check_variable = { POL_nat_influence > 49 }
				}
			}
			has_completed_focus = POL_Moral_Education
			NOT = { has_completed_focus = POL_Reinforce_The_Parliamentary_System }
		}
		days_remove = 70
		fire_only_once = yes
		war_with_target_on_remove = yes
		war_with_on_remove = GER
		war_with_on_remove = AUS

		ai_will_do = {
			factor = 0
			modifier = {
				add = 100
				POL_EE_capitulation_check = yes
			}
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_operation_parasol"
			setup_decision_attack_AI = yes
			set_country_flag = { flag = imminent_war days = 70 value = 1 }
			faction_leader = { country_event = poland.420 } #warning event
			set_temp_variable = { stab_removal = party_popularity_100@market_liberal }
			add_to_temp_variable = { stab_removal = party_popularity_100@authoritarian_democrat }
			divide_temp_variable = { stab_removal = -100 }
			add_stability = stab_removal
			set_temp_variable = { coup_strength = stab_removal }
			add_to_temp_variable = { coup_strength = party_popularity_100@social_conservative }
			divide_temp_variable = { coup_strength = 200 }
			add_popularity = { ideology = paternal_autocrat popularity = coup_strength }
			if = {
				limit = { is_subject_of = GER }
				GER = { end_puppet = POL }
			}
			leave_faction = yes
			MIE_remove_from_intermarium = yes
			hidden_effect = {
				news_event = polandnews.4
				POL_remove_guderian = yes
				set_party_name = {
					ideology = paternal_autocrat
					long_name = POL_paternal_autocrat2_party_long
					name = POL_paternal_autocrat2_party
				}
			}
			POL_overthrow_monarchy = yes
			POL_create_rydz = yes
			set_politics = {
				ruling_party = paternal_autocrat
				elections_allowed = yes
			}
			if = {
				limit = { has_completed_focus = POL_Fill_The_Railway_Gaps }
				uncomplete_national_focus = { focus = POL_Fill_The_Railway_Gaps }
				remove_ideas = POL_crossroads_of_europe
			}
			hidden_effect = {
				promote_character = {
					character = POL_edward_rydz_smigly
					ideology = paternal_autocrat_subtype
				}
				activate_advisor = POL_boguslaw_miedzinski_sic
				set_temp_variable = { coalition_partner_var = token:social_conservative }
				add_to_coalition = yes
				set_temp_variable = { coalition_partner_var = token:national_populist }
				add_to_coalition = yes
				for_each_scope_loop = {
					array = POL_polish_lands
					add_claim_by = ROOT
				}
				country_event = { id = poland.561 days = 14 }  #Economic crash
				if = {
					limit = { has_country_flag = POL_revolt_from_monarchy }
					country_event = { id = poland.562 days = 97 } #King August Escapes
				}
				activate_mission = POL_reach_the_sea
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_operation_parasol"
			clear_decision_attack_AI = yes
			every_neighbor_country = {
				display_individual_scopes = yes
				limit = {
					has_war = yes
					any_owned_state = { is_claimed_by = POL }
					NOT = { is_ally_with = RUS }
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
			log = "KR_Event_Logging;POLAND REVOLTS - Operation Parasol;[GetDateText]"
		}
	}

	#Centrolew decisions
	POL_turn_to_west = {
		icon = GFX_decision_hand_out

		available = {
			controls_all_owned_states = yes
			has_country_flag = POL_PPS_Dubois
		}
		visible = {
			OR = {
				has_government = social_democrat
				RUS = { has_socialist_government = yes }
			}
			country_exists = INT
			OR = {
				NOT = { country_exists = GER }
				GER = { has_capitulated = yes }
			}
			has_capitulated = no
			is_subject = no
			NOT = { is_in_faction_with = RUS }
			NOT = { is_in_faction_with = INT }
			has_war = yes
		}
		targets = { INT }
		fire_only_once = yes

		ai_will_do = {
			factor = 50
			modifier = {
				factor = 0
				is_in_faction_with = AUS
				AUS = { has_capitulated = no }
			}
			modifier = {
				factor = 0
				is_in_faction_with = UKR
				UKR = { is_faction_leader = yes has_capitulated = no }
			}
			modifier = {
				factor = 10
				any_owned_state = { any_neighbor_state = { is_controlled_by = FRA } }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_turn_to_west"
			INT = { country_event = poland.273 }
			set_country_flag = POL_INT_negotiations
		}
	}
	POL_turn_to_east = {
		icon = GFX_decision_hand_out

		available = {
			controls_all_owned_states = yes
		}
		visible = {
			has_socialist_government = no
			RUS = {
				is_faction_leader = yes
				has_socialist_government = no
			}
			OR = {
				NOT = { country_exists = GER }
				GER = { has_capitulated = yes }
			}
			NOT = { has_government = authoritarian_democrat }
			NOT = { has_government = market_liberal }
			has_capitulated = no
			is_subject = no
			NOT = { is_in_faction_with = INT }
			NOT = { is_in_faction_with = RUS }
			has_war = yes
		}
		targets = { RUS }
		fire_only_once = yes

		ai_will_do = {
			factor = 50
			modifier = {
				factor = 0
				is_in_faction_with = AUS
				AUS = { has_capitulated = no }
			}
			modifier = {
				factor = 0
				is_in_faction_with = UKR
				UKR = { is_faction_leader = yes has_capitulated = no }
			}
			modifier = {
				factor = 10
				any_owned_state = { any_neighbor_state = { is_controlled_by = RUS } }
			}
			modifier = {
				factor = 0.1
				has_left_democratic_government = yes
				RUS = { has_democratic_government = no }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_turn_to_east"
			RUS = { country_event = poland.273 }
			set_country_flag = POL_RUS_negotiations
		}
	}
	POL_found_intermarium = {
		icon = GFX_decision_eng_puppet_usa
		allowed = { always = no }
		visible = {
			is_subject = no
			is_in_faction = no
			has_left_democratic_government = yes
		}
		available = { has_war = no }
		fire_only_once = yes

		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_found_intermarium"
			create_faction = POL_INTERMARIUM
			every_other_country = {
				limit = {
					has_democratic_government = yes
					has_war = no
					OR = {
						is_subject = no
						is_subject_of = POL
					}
					if = {
						limit = { RUS = { is_subject = no } }
						NOT = { is_in_faction_with = RUS }
					}
					NOT = { is_in_faction_with = GER }
					if = {
						limit = { AUS = { is_subject = no } }
						NOT = { is_in_faction_with = AUS }
					}
					NOT = { is_in_faction_with = INT }
					OR = {
						tag = LIT
						tag = LAT
						tag = EST
						tag = FIN
						tag = WHR
						tag = UKR
						tag = CZE
						tag = SLO
						tag = HUN
						tag = ROM
						tag = BUL
						tag = SER
						tag = CRO
						tag = BOS
						tag = SLV
						tag = GRE
						tag = TRS
						tag = ALB
						tag = MNT
					}
				}
				country_event = poland.374 #invite them
			}
			custom_effect_tooltip = tooltip_white_line
			custom_effect_tooltip = POL_Dawn_of_Progress_effect_tooltip_wargoals
			every_neighbor_country = {
				limit = {
					NOT = { is_in_faction_with = POL }
					has_democratic_government = no
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = PREV
				}
			}
		}
	}

	#Monarchy decisions
	POL_Rally_the_Monarchists = {
		icon = GFX_decision_eng_propaganda_campaigns

		cost = POL_royal_influence
		days_remove = 30
		days_re_enable = 90
		visible = { is_monarchy = yes }

		ai_will_do = { factor = 10 }

		modifier = {
			authoritarian_democrat_drift = 0.1
			market_liberal_drift = 0.1
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Rally_the_Monarchists"
		}
	}
	POL_Endorse_Constitutionalist_Intellectuals = {
		icon = GFX_decision_hol_radio_oranje
		allowed = { always = no }

		visible = {
			is_monarchy = yes
		}

		cost = POL_royal_influence
		days_remove = 30
		days_re_enable = 90

		ai_will_do = {
			factor = 50
			modifier = {
				factor = 0
				check_variable = { POL_royal_influence > POL_rep_influence }
				check_variable = { POL_royal_influence > POL_soc_influence }
				check_variable = { POL_royal_influence > POL_nat_influence }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Endorse_Constitutionalist_Intellectuals"
			set_temp_variable = { POL_influence_change = 4 }
			POL_add_royal_influence = yes
		}
	}
	POL_Lease_Royal_Estates = {
		icon = GFX_decision_generic_agriculture
		allowed = { always = no }

		visible = {
			is_monarchy = yes
		}

		cost = POL_royal_influence
		days_remove = 120
		days_re_enable = 120

		ai_will_do = { factor = 25 }

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Lease_Royal_Estates"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Lease_Royal_Estates"
			random_owned_controlled_state = {
				limit = { is_core_of = ROOT }
				add_extra_state_shared_building_slots = 1
			}
			set_temp_variable = { POL_influence_change = -4 }
			POL_add_republican_influence = yes
		}
	}
	POL_Aristocratic_Charity = {
		icon = GFX_decision_hol_attract_foreign_investors
		allowed = { always = no }

		visible = {
			is_monarchy = yes
		}

		cost = POL_royal_influence
		days_remove = 90
		fire_only_once = yes

		ai_will_do = { factor = 100 }

		modifier = { stability_weekly = 0.01 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Aristocratic_Charity"
			set_temp_variable = { ideology_change_var = 3 }
			change_ruling_party_popularity = yes
			set_temp_variable = { POL_influence_change = 3 }
			POL_add_royal_influence = yes
		}
	}
	POL_Start_A_Royal_Tour = {
		icon = GFX_decision_eng_ally_imperialist_coup
		allowed = { always = no }

		visible = {
			is_monarchy = yes
		}

		cost = POL_royal_influence
		days_remove = 60
		days_re_enable = 120

		ai_will_do = { factor = 50 }

		modifier = { stability_weekly = 0.01 }

		fixed_random_seed = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Start_A_Royal_Tour"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Start_A_Royal_Tour"
			set_temp_variable = { POL_influence_change = 4 }
			POL_add_royal_influence = yes
		}
	}
	POL_Loyalist_Army_Commissions = {
		icon = GFX_decision_generic_army_support
		allowed = { always = no }

		visible = {
			is_monarchy = yes
		}

		cost = 30
		days_remove = 10
		days_re_enable = 90

		ai_will_do = {
			factor = 30
			modifier = {
				factor = 0
				check_variable = { POL_royal_influence > POL_nat_influence }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Loyalist_Army_Commissions"
			army_experience = 10
			if = {
				limit = { has_war_support < 0.5 }
				add_war_support = 0.05
			}
			set_temp_variable = { POL_influence_change = -4 }
			POL_add_nationalist_influence = yes
		}
	}
	POL_Commission_New_Industrial_Zone = {
		icon = GFX_decision_generic_factory
		allowed = { always = no }

		visible = {
			is_monarchy = yes
			NOT = { has_idea = POL_polish_control }
			NOT = { has_idea = POL_austrian_friendship }
			NOT = { has_idea = POL_austrian_cooperation }
		}
		available = {
			num_of_civilian_factories_available_for_projects > 1
			any_owned_state = {
				is_controlled_by = ROOT
				is_core_of = ROOT
				free_building_slots = { building = industrial_complex size > 0 }
			}
		}

		cost = POL_industry_zone_cost
		days_remove = 90
		days_re_enable = 120

		ai_will_do = { factor = 10 }

		modifier = { civilian_factory_use = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Commission_New_Industrial_Zone"
			set_temp_variable = { POL_influence_change = 5 }
			POL_add_german_influence = yes
			add_one_random_civilian_factory = yes
			add_to_variable = { POL_industry_zone_cost = 20 }
		}
	}
}
POL_Investigations = {
	POL_investigate_unions = {
		icon = GFX_decision_generic_wreckers

		available = {
			NOT = { has_country_flag = POL_ongoing_investigation }
			NOT = { has_decision = POL_compose_the_final_report }
		}

		cancel_trigger = { has_country_flag = POL_election_happened }

		cost = POL_investigation_cost
		days_remove = 30
		ai_will_do = {
			factor = 50
			modifier = {
				factor = 0
				NOT = { has_completed_focus = POL_Weapon_Against_The_Underground }
			}
		}
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_investigate_unions"
			set_country_flag = POL_ongoing_investigation
			add_popularity = {
				ideology = authoritarian_democrat
				popularity = 0.01
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_investigate_unions"
			country_event = poland.53
			add_stability = 0.02
			clr_country_flag = POL_ongoing_investigation
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_investigate_unions"
			clr_country_flag = POL_ongoing_investigation
		}
	}
	POL_investigate_bund = {
		icon = GFX_decision_wrenches

		available = {
			NOT = { has_country_flag = POL_ongoing_investigation }
			NOT = { has_decision = POL_compose_the_final_report }
		}

		cancel_trigger = { has_country_flag = POL_election_happened }

		cost = POL_investigation_cost
		days_remove = 30
		ai_will_do = {
			factor = 50
			modifier = {
				factor = 0
				NOT = { has_completed_focus = POL_Weapon_Against_The_Underground }
			}
		}
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_investigate_bund"
			set_country_flag = POL_ongoing_investigation
			add_popularity = {
				ideology = authoritarian_democrat
				popularity = 0.01
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_investigate_bund"
			country_event = poland.56
			add_stability = 0.02
			clr_country_flag = POL_ongoing_investigation
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_investigate_bund"
			clr_country_flag = POL_ongoing_investigation
		}
	}
	POL_investigate_army = {
		icon = GFX_decision_generic_army_support

		available = {
			NOT = { has_country_flag = POL_ongoing_investigation }
			NOT = { has_decision = POL_compose_the_final_report }
		}

		cancel_trigger = { has_country_flag = POL_election_happened }

		cost = POL_investigation_cost
		days_remove = 30
		ai_will_do = {
			factor = 50
			modifier = {
				factor = 0
				NOT = { has_completed_focus = POL_Weapon_Against_The_Underground }
			}
		}
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_investigate_army"
			set_country_flag = POL_ongoing_investigation
			add_popularity = {
				ideology = authoritarian_democrat
				popularity = 0.01
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_investigate_army"
			country_event = poland.61
			add_stability = 0.02
			clr_country_flag = POL_ongoing_investigation
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_investigate_army"
			clr_country_flag = POL_ongoing_investigation
		}
	}
	POL_investigate_church = {
		icon = GFX_decision_onmap_recruit_operative

		available = {
			NOT = { has_country_flag = POL_ongoing_investigation }
			NOT = { has_decision = POL_compose_the_final_report }
		}

		cancel_trigger = { has_country_flag = POL_election_happened }

		cost = POL_investigation_cost
		days_remove = 30
		ai_will_do = {
			factor = 25
			modifier = {
				factor = 0
				NOT = { has_completed_focus = POL_Weapon_Against_The_Underground }
			}
		}
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_investigate_church"
			set_country_flag = POL_ongoing_investigation
			add_popularity = {
				ideology = authoritarian_democrat
				popularity = 0.01
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_investigate_church"
			country_event = poland.48
			add_stability = 0.02
			clr_country_flag = POL_ongoing_investigation
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_investigate_church"
			clr_country_flag = POL_ongoing_investigation
		}
	}
	POL_investigate_pps = {
		icon = GFX_decision_generic_political_rally

		available = {
			NOT = { has_country_flag = POL_ongoing_investigation }
			NOT = { has_decision = POL_compose_the_final_report }
			has_country_flag = POL_can_persecute_sejm
		}

		cancel_trigger = { has_country_flag = POL_election_happened }

		cost = POL_investigation_cost
		days_remove = 30
		ai_will_do = {
			factor = 50
			modifier = {
				factor = 0
				NOT = { has_completed_focus = POL_Weapon_Against_The_Underground }
			}
		}
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_investigate_pps"
			set_country_flag = POL_ongoing_investigation
			add_popularity = {
				ideology = authoritarian_democrat
				popularity = 0.01
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_investigate_pps"
			country_event = poland.65
			add_stability = 0.02
			clr_country_flag = POL_ongoing_investigation
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_investigate_pps"
			clr_country_flag = POL_ongoing_investigation
		}
	}
	POL_investigate_psl = {
		icon = GFX_decision_binoculars

		available = {
			NOT = { has_country_flag = POL_ongoing_investigation }
			NOT = { has_decision = POL_compose_the_final_report }
			has_country_flag = POL_can_persecute_sejm
		}

		cancel_trigger = { has_country_flag = POL_election_happened }

		cost = POL_investigation_cost
		days_remove = 30
		ai_will_do = {
			factor = 50
			modifier = {
				factor = 0
				NOT = { has_completed_focus = POL_Weapon_Against_The_Underground }
			}
		}
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_investigate_psl"
			set_country_flag = POL_ongoing_investigation
			add_popularity = {
				ideology = authoritarian_democrat
				popularity = 0.01
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_investigate_psl"
			country_event = poland.67
			add_stability = 0.02
			clr_country_flag = POL_ongoing_investigation
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_investigate_psl"
			clr_country_flag = POL_ongoing_investigation
		}
	}
	POL_investigate_chjena = {
		icon = GFX_decision_generic_political_discourse

		available = {
			NOT = { has_country_flag = POL_ongoing_investigation }
			NOT = { has_decision = POL_compose_the_final_report }
			has_country_flag = POL_can_persecute_sejm
		}
		visible = { NOT = { has_country_flag = POL_endecja_collaborates } }

		cancel_trigger = {
			OR = {
				has_country_flag = POL_election_happened
				has_country_flag = POL_endecja_collaborates
			}
		}

		cost = POL_investigation_cost
		days_remove = 30
		ai_will_do = {
			factor = 25
			modifier = {
				factor = 0
				NOT = { has_completed_focus = POL_Weapon_Against_The_Underground }
			}
		}
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_investigate_chjena"
			set_country_flag = POL_ongoing_investigation
			add_popularity = {
				ideology = authoritarian_democrat
				popularity = 0.01
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_investigate_chjena"
			country_event = poland.70
			add_stability = 0.02
			clr_country_flag = POL_ongoing_investigation
		}
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_investigate_chjena"
			clr_country_flag = POL_ongoing_investigation
		}
	}
	POL_compose_the_final_report = {
		icon = eng_trade_unions_support

		available = {
			date > 1937.06.01
			hidden_trigger = {
				NOT = { has_decision = POL_investigate_unions }
				NOT = { has_decision = POL_investigate_bund }
				NOT = { has_decision = POL_investigate_army }
				NOT = { has_decision = POL_investigate_church }
				NOT = { has_decision = POL_investigate_pps }
				NOT = { has_decision = POL_investigate_psl }
				NOT = { has_decision = POL_investigate_chjena }
			}
		}

		days_remove = 15
		ai_will_do = { factor = 0 } #event to finish investigations is fired by the election event
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_compose_the_final_report"
			custom_effect_tooltip = POL_complete_report_tt
			country_event = poland.81
		}
	}
}
POL_Economy_of_the_Kingdom = {
	POL_renegotiate_economic_treaties = {
		icon = GFX_decision_generic_political_discourse

		available = { has_country_flag = POL_election_happened }

		days_remove = 30
		ai_will_do = { factor = 100 }
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_renegotiate_economic_treaties"
			custom_effect_tooltip = POL_renegotiate_tt
			country_event = poland.313
			set_country_flag = POL_trade_finished
			activate_targeted_decision = { target = GER decision = POL_industrial_aid_germany }
			activate_targeted_decision = { target = AUS decision = POL_industrial_aid_danubia }
		}
	}
	POL_sell = {
		icon = GFX_decision_cash_flow

		available = {
			set_temp_variable = { POL_influence_check = 10 }
			POL_polish_influence_check = yes
		}
		visible = {
			OR = {
				is_in_faction_with = GER
				is_in_faction_with = AUS
			}
			has_socialist_government = no
			POL_dobo = no
			POL_rydz = no
			NOT = { has_idea = POL_german_hegemony }
			NOT = { has_country_flag = POL_trade_finished }
		}

		targets = { GER AUS }
		target_trigger = {
			FROM = {
				NOT = { has_war_with = POL }
				has_socialist_government = no
				is_subject = no
			}
		}
		days_remove = 90
		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				OR = {
					AND = {
						set_temp_variable = { POL_influence_check < 60 }
						POL_polish_influence_check = no
						NOT = { has_completed_focus = POL_The_Second_Four_Year_Plan }
					}
					AND = {
						check_variable = { POL_royal_influence < 30 }
						is_monarchy = yes
					}
					AND = {
						set_temp_variable = { POL_influence_check < 40 }
						POL_polish_influence_check = no
						OR = {
							has_left_democratic_government = yes
							POL_has_nationalist = yes
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_sell target: [From.GetName]"
			if = {
				limit = { check_variable = { POL_royal_influence > 25 } }
				set_temp_variable = { POL_influence_change = -3 }
				POL_add_ruling_influence = yes
			}
			else = { add_stability = -0.02 }
			if = {
				limit = { FROM = { tag = GER } }
				set_temp_variable = { POL_influence_change = 5 }
				POL_add_german_influence = yes
			}
			else_if = {
				limit = { FROM = { tag = AUS } }
				set_temp_variable = { POL_influence_change = 5 }
				POL_add_austrian_influence = yes
			}
			add_political_power = 50
		}
	}
	POL_buy = {
		icon = GFX_decision_cash_flow

		available = {
			set_temp_variable = { POL_influence_check = 50 }
			OR = {
				POL_polish_influence_check = no
				NOT = { has_idea = POL_German_hegemony }
			}
		}

		visible = {
			set_temp_variable = { POL_influence_check = 100 }
			POL_polish_influence_check = no
		}

		cost = POL_influence
		days_remove = 60
		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				set_temp_variable = { POL_influence_check = 51 }
				POL_polish_influence_check = yes
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_buy"
			set_temp_variable = { POL_influence_change = 5 }
			POL_add_polish_influence = yes
		}
	}
	POL_Public_Work_Programmes = {
		icon = GFX_decision_generic_construction
		allowed = { always = no }

		visible = {
			has_idea = POL_black_monday
		}
		cost = POL_BM_cost
		days_remove = 180
		days_re_enable = 5

		modifier = {
			consumer_goods_factor = 0.1
		}

		ai_will_do = { factor = 500 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Public_Work_Programmes"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Public_Work_Programmes"
			add_stability = 0.05
			POL_reduce_black_monday = yes
			custom_effect_tooltip = tooltip_white_line
			set_temp_variable = { POL_influence_change = -5 }
			POL_add_syndicalist_influence = yes

		}
	}
	POL_Cooperative_Subsidies = {
		icon = GFX_decision_cash_flow
		allowed = { always = no }

		visible = {
			has_idea = POL_black_monday
		}
		cost = POL_BM_cost
		days_remove = 180
		days_re_enable = 5

		modifier = {
			political_power_cost = 0.15
		}

		ai_will_do = { factor = 500 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Cooperative_Subsidies"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Cooperative_Subsidies"
			add_stability = 0.05
			POL_reduce_black_monday = yes
			custom_effect_tooltip = tooltip_white_line
			set_temp_variable = { POL_influence_change = -5 }
			POL_add_republican_influence = yes

		}
	}
	POL_Slash_Tariffs = {
		icon = GFX_decision_global_trade
		allowed = { always = no }

		visible = {
			has_idea = POL_black_monday
		}
		cost = POL_BM_cost
		days_remove = 150
		fire_only_once = yes
		ai_will_do = { factor = 500 }

		modifier = {
			production_speed_buildings_factor = 0.075
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Slash_Tariffs"
			add_stability = -0.05
			set_temp_variable = { POL_influence_change = -5 }
			POL_add_polish_influence = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Slash_Tariffs"
			add_political_power = POL_BM_cost
			POL_reduce_black_monday = yes
			custom_effect_tooltip = tooltip_white_line
			set_temp_variable = { POL_influence_change = 4 }
			POL_add_syndicalist_influence = yes

		}
	}
	POL_Subsidise_Polish_Companies = {
		icon = GFX_decision_cash_flow
		allowed = { always = no }

		visible = {
			has_idea = POL_black_monday
		}
		cost = POL_BM_cost
		fire_only_once = yes
		days_remove = 150

		modifier = {
			political_power_cost = 0.2
		}

		ai_will_do = { factor = 500 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Subsidise_Polish_Companies"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Subsidise_Polish_Companies"
			POL_reduce_black_monday = yes
			custom_effect_tooltip = tooltip_white_line
			set_temp_variable = { POL_influence_change = 10 }
			POL_add_polish_influence = yes
			set_temp_variable = { POL_influence_change = 4 }
			POL_add_royal_influence = yes
		}
	}
	POL_Secure_Loans = {
		icon = GFX_decision_gre_investment_decisions
		fire_only_once = yes
		available = {
			set_temp_variable = { POL_influence_check = 5 }
			POL_polish_influence_check = yes
			FROM = {
				OR = {
					has_completed_focus = GER_mitteleuropa_trade
					has_completed_focus = GER_war_economic_offices
					has_completed_focus = GER_bank_controls
					has_completed_focus = AUS_sdap_5
					has_completed_focus = AUS_cs_2
				}
			}
		}
		visible = {
			has_completed_focus = POL_Loans_From_Foreign_Banks
			has_idea = POL_black_monday
			has_socialist_government = no
			FROM = {
				exists = yes
				is_subject = no
				NOT = { has_war_with = ROOT }
			}
		}

		custom_cost_text = pol_eco_texticon
		custom_cost_trigger = {
			set_temp_variable = { POL_influence_check = 5 }
			POL_polish_influence_check = yes
		}
		days_remove = 90

		targets = { AUS GER }
		target_root_trigger = { has_completed_focus = POL_Loans_From_Foreign_Banks }
		target_trigger = {
			FROM = {
				exists = yes
				is_subject = no
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Secure_Loans target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Secure_Loans target: [From.GetName]"
			if = {
				limit = {
					has_completed_focus = POL_Loans_From_Foreign_Banks
					has_idea = POL_black_monday
				}
				POL_reduce_black_monday = yes
				if = {
					limit = { FROM = { tag = GER } }
					set_temp_variable = { POL_influence_change = 5 }
					POL_add_german_influence = yes
				}
				else_if = {
					limit = { FROM = { tag = AUS } }
					set_temp_variable = { POL_influence_change = 5 }
					POL_add_austrian_influence = yes
				}

				set_temp_variable = { POL_influence_change = 4 }
				POL_add_syndicalist_influence = yes
			}
		}
	}
	POL_Stabilise_the_Zloty = {
		icon = GFX_decision_hol_attract_foreign_investors
		allowed = { always = no }

		visible = {
			has_idea = POL_black_monday
		}
		available = {
			set_temp_variable = { POL_influence_check = 50 }
			POL_polish_influence_check = yes
		}
		cost = POL_BM_cost
		days_remove = 180
		fire_only_once = yes

		modifier = {
			consumer_goods_factor = 0.1
		}

		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Stabilise_the_Zloty"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Stabilise_the_Zloty"
			add_political_power = 100
			custom_effect_tooltip = POL_modifies_black_monday
			add_to_variable = {
				POL_black_monday_consumer_goods = -0.15
				tooltip = consumer_goods_factor_tooltip
			}
			add_to_variable = {
				POL_black_monday_construction_speed = 0.05
				tooltip = POL_black_monday_construction_speed_tooltip
			}
			add_to_variable = {
				POL_black_monday_factory_output = 0.1
				tooltip = industrial_capacity_factory_tooltip
			}
			force_update_dynamic_modifier = yes
		}
	}
	POL_industrial_aid_germany = {
		icon = GFX_decision_gre_investment_decisions
		allowed = { always = no }
		visible = {
			is_in_faction_with = GER
			GER = { exists = yes }
		}
		available = {
			set_temp_variable = { POL_influence_check = 5 }
			POL_polish_influence_check = yes
			GER = { surrender_progress < 0.3 }
			OR = {
				has_idea = POL_german_hegemony
				has_idea = POL_german_cooperation
				has_idea = POL_austro_german_condominium
				is_in_faction_with = GER
			}
		}
		cost = POL_CIP_cost

		days_remove = 120
		ai_will_do = { factor = 10 }

		cancel_trigger = { has_war_with = GER }

		modifier = {
			consumer_goods_factor = 0.1
			industrial_capacity_factory = 0.15
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_industrial_aid_germany"
			hidden_effect = {
				GER = { country_event = poland.323 }
			}
		}
	}
	POL_industrial_aid_danubia = {
		icon = GFX_decision_gre_investment_decisions

		allowed = { always = no }
		visible = {
			AUS = {
				is_subject = no
				exists = yes
			}
		}
		available = {
			set_temp_variable = { POL_influence_check = 5 } POL_polish_influence_check = yes
			AUS = { surrender_progress < 0.3 }
			OR = {
				has_idea = POL_austrian_friendship
				has_idea = POL_austrian_cooperation
				has_idea = POL_austro_german_condominium
				is_in_faction_with = AUS
			}
		}
		cost = POL_CIP_cost

		days_remove = 120
		ai_will_do = { factor = 10 }

		cancel_trigger = { has_war_with = AUS }

		modifier = {
			production_speed_buildings_factor = 0.1
			political_power_gain = -0.10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_industrial_aid_danubia"
			hidden_effect = {
				random_list = {
					seed = random
					20 = { AUS = { country_event = poland.323 } }
					10 = { CZE = { country_event = poland.323 } }
					5 = { HUN = { country_event = poland.323 } }
				}
			}
		}
	}
}
POL_Austrian_Control = {
	POL_AUS_buy = {
		icon = GFX_decision_cash_flow
		visible = {
			POL = {
				NOT = { has_country_flag = POL_trade_finished }
			}
		}
		cost = POL.POL_influence
		days_remove = 90
		cancel_if_not_visible = yes
		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				has_decision = POL_AUS_sell
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_AUS_buy"
			POL = {
				set_temp_variable = { POL_influence_change = 5 }
				POL_add_austrian_influence = yes
				hidden_effect = {
					random_list = {
						seed = random
						10 = { country_event = poland.309 } #Investment
						30 = { }
					}
				}
			}
		}
	}
	POL_AUS_sell = {
		icon = GFX_decision_hol_attract_foreign_investors

		visible = {
			POL = {
				check_variable = { POL_AUS_influence > 9 }
				NOT = { has_country_flag = POL_trade_finished }
			}
		}

		custom_cost_text = pol_aus_eco_texticon_ten
		custom_cost_trigger = {
			POL = {
				set_temp_variable = { POL_influence_check = 10 }
				POL_AUS_influence_check = yes
			}
		}
		days_remove = 160
		cancel_if_not_visible = yes
		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				has_decision = POL_AUS_buy
			}
		}

		modifier = { political_power_gain = 0.2 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_AUS_sell"
			POL = {
				set_temp_variable = { POL_influence_change = -10 }
				POL_add_austrian_influence = yes
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_AUS_sell"
		}
	}
	POL_AUS_lobby = {
		icon = GFX_decision_eng_propaganda_campaigns
		visible = {
			has_authoritarian_government = no
			POL = {
				is_monarchy = yes
				NOT = { has_completed_focus = POL_Election_of_1938 }
			}
		}

		cost = 60
		days_remove = 160
		cancel_if_not_visible = yes

		ai_will_do = { factor = 3 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_AUS_lobby"
			POL = { country_event = poland.23 }
			effect_tooltip = {
				POL = {
					set_temp_variable = { POL.POL_influence_change = 8 }
					POL_add_republican_influence = yes
				}
			}
		}
	}
	POL_AUS_agricultural_exports = {
		icon = GFX_decision_generic_agriculture

		visible = {
			is_in_faction_with = POL
			has_war_with = FRA
			NOT = { POL = { has_country_flag = POL_deny_exports } }
		}

		custom_cost_text = pol_aus_eco_texticon
		custom_cost_trigger = {
			POL = {
				set_temp_variable = { POL_influence_check = 5 }
				POL_AUS_influence_check = yes
			}
		}
		days_remove = 120
		cancel_if_not_visible = yes
		remove_trigger = {
			OR = {
				has_war = no
				has_war_with = POL
				POL = { has_country_flag = POL_deny_exports }
			}
		}

		ai_will_do = { factor = 10 }

		modifier = {
			stability_factor = 0.1
			production_speed_buildings_factor = 0.04
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_AUS_agricultural_exports"
			POL = {
				set_temp_variable = { POL_influence_change = -5 }
				POL_add_austrian_influence = yes
				country_event = poland.354
			}
		}
	}
}
POL_Second_Four_Year_Plan_decisions = {
	POL_Completion_Of_The_Second_Four_Year_Plan = {
		icon = GFX_decision_eng_trade_unions_demand

		allowed = { always = no } #manually activated
		priority = 1000

		available = {
			num_of_factories > 50
			set_temp_variable = { POL_influence_check = 25 }
			POL_polish_influence_check = yes
		}

		is_good = no
		selectable_mission = no
		days_mission_timeout = 1460

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Completion_Of_The_Second_Four_Year_Plan"
			add_political_power = 100
			add_stability = 0.15
			custom_effect_tooltip = tooltip_white_line
			if = {
				limit = { is_monarchy = yes }
				set_temp_variable = { POL_influence_change = 10 }
				POL_add_royal_influence = yes
			}
			else_if = {
				limit = { has_left_democratic_government = yes }
				set_temp_variable = { POL_influence_change = 10 }
				POL_add_republican_influence = yes
			}
			else_if = {
				limit = { POL_has_nationalist = yes }
				set_temp_variable = { POL_influence_change = 10 }
				POL_add_nationalist_influence = yes
			}
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_Completion_Of_The_Second_Four_Year_Plan"
			if = {
				limit = { is_monarchy = yes }
				set_temp_variable = { POL_influence_change = -10 }
				POL_add_royal_influence = yes
			}
			else_if = {
				limit = { has_left_democratic_government = yes }
				set_temp_variable = { POL_influence_change = -10 }
				POL_add_republican_influence = yes
			}
			else_if = {
				limit = { POL_has_nationalist = yes }
				set_temp_variable = { POL_influence_change = -10 }
				POL_add_nationalist_influence = yes
			}
		}
	}
	POL_Refurbish_The_Old_Polish_Industrial_Region = {
		icon = GFX_decision_generic_construction
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost
		days_remove = 180
		fire_only_once = yes

		modifier = {
			civilian_factory_use = 2
		}

		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Refurbish_The_Old_Polish_Industrial_Region"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Refurbish_The_Old_Polish_Industrial_Region"
			90 = { increase_state_category_by_one_level = yes }
			90 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			if = {
				limit = { has_idea = POL_black_monday }
				POL_reduce_black_monday = yes
			}
		}
	}
	POL_Improve_Urban_Infrastructure = {
		icon = GFX_decision_road
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost
		days_remove = 180
		fire_only_once = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Improve_Urban_Infrastructure"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Improve_Urban_Infrastructure"
			random_owned_state = {
				limit = { is_core_of = POL }
				add_extra_state_shared_building_slots = 1
			}
			if = {
				limit = { has_idea = POL_issues_at_hand }
				custom_effect_tooltip = POL_modifies_issues_at_hand
				add_to_variable = {
					POL_issues_at_hand_consumer_goods = -0.075
					tooltip = consumer_goods_factor_tooltip
				}
			}
		}
	}
	POL_New_Towns_Act = {
		icon = GFX_decision_hol_inundate_water_lines
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost
		days_remove = 100
		fire_only_once = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_New_Towns_Act"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_New_Towns_Act"
			random_owned_controlled_state = {
				limit = { is_core_of = POL }
				add_extra_state_shared_building_slots = 1
			}
			if = {
				limit = { has_idea = POL_issues_at_hand }
				custom_effect_tooltip = POL_modifies_issues_at_hand
				add_to_variable = {
					POL_issues_at_hand_monthly_population = 0.1
					tooltip = POL_issues_at_hand_monthly_population_tooltip
				}
			}
			add_stability = 0.05
		}
	}
	POL_Build_Cheap_Housing = {
		icon = GFX_decision_generic_construction
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost
		days_remove = 100
		fire_only_once = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Build_Cheap_Housing"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Build_Cheap_Housing"
			random_owned_controlled_state = {
				limit = { is_core_of = POL }
				add_extra_state_shared_building_slots = 1
			}
			set_temp_variable = { POL_influence_change = 5 }
			POL_add_ruling_influence = yes
			if = {
				limit = { has_idea = POL_black_monday }
				POL_reduce_black_monday = yes
			}
		}
	}
	POL_Levee_Construction = {
		icon = GFX_decision_hol_drain_water_lines
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost
		days_remove = 100
		fire_only_once = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Levee_Construction"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Levee_Construction"
			random_owned_state = {
				limit = { is_core_of = POL }
				add_extra_state_shared_building_slots = 1
			}
			if = {
				limit = { has_idea = POL_issues_at_hand }
				custom_effect_tooltip = POL_modifies_issues_at_hand
				add_to_variable = {
					POL_issues_at_hand_consumer_goods = -0.075
					tooltip = consumer_goods_factor_tooltip
				}
			}
		}
	}
	POL_Lodz_Textile_Industry = {
		icon = GFX_decision_generic_factory
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost
		days_remove = 100
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Lodz_Textile_Industry target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Lodz_Textile_Industry target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			if = {
				limit = { has_idea = POL_black_monday }
				POL_reduce_black_monday = yes
			}
		}
	}
	POL_Develop_The_Auto_Industry = {
		icon = GFX_decision_generic_factory
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost
		days_remove = 100
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Develop_The_Auto_Industry target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Develop_The_Auto_Industry target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			if = {
				limit = { has_idea = POL_black_monday }
				POL_reduce_black_monday = yes
			}
		}
	}
	POL_Renovate_The_Warszawa_Glowna_Rail_Hub = {
		icon = GFX_decision_railroad
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Renovate_The_Warszawa_Glowna_Rail_Hub target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Renovate_The_Warszawa_Glowna_Rail_Hub target: [From.GetName]"
			set_country_flag = POL_warszawa_glowna_completed
			FROM = {
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			if = {
				limit = { has_idea = POL_black_monday }
				POL_reduce_black_monday = yes
			}
		}
	}
	POL_Build_Starachowice_Artillery_Factory = {
		icon = GFX_decision_ger_military_buildup
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Build_Starachowice_Artillery_Factory target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Build_Starachowice_Artillery_Factory target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_Build_Radom_Arms_Factory = {
		icon = GFX_decision_ger_military_buildup
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Build_Radom_Arms_Factory target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Build_Radom_Arms_Factory target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_Build_Skarzysko_Munitions_Factory = {
		icon = GFX_decision_ger_military_buildup
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Build_Skarzysko_Munitions_Factory target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Build_Skarzysko_Munitions_Factory target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_Build_Dabrowa_Munitions_Factory = {
		icon = GFX_decision_ger_military_buildup
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Build_Dabrowa_Munitions_Factory target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Build_Dabrowa_Munitions_Factory target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_Build_Jawidz_Munitions_Factory = {
		icon = GFX_decision_ger_military_buildup
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Build_Jawidz_Munitions_Factory target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Build_Jawidz_Munitions_Factory target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_Reopen_The_Central_Aviation_Workshops = {
		icon = GFX_decision_generic_merge_plant_aircraft
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Reopen_The_Central_Aviation_Workshops target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Reopen_The_Central_Aviation_Workshops target: [From.GetName]"
			air_experience = 10
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_Build_Aircraft_Factories_In_Lublin = {
		icon = GFX_decision_generic_merge_plant_aircraft
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Build_Aircraft_Factories_In_Lublin target: [From.GetName]"

		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Build_Aircraft_Factories_In_Lublin target: [From.GetName]"
			air_experience = 10
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_Expand_The_Podlasie_Aircraft_Factory = {
		icon = GFX_decision_generic_merge_plant_aircraft
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Expand_The_Podlasie_Aircraft_Factory target: [From.GetName]"

		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Expand_The_Podlasie_Aircraft_Factory target: [From.GetName]"
			air_experience = 10
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_Modernise_Krakow = {
		icon = GFX_decision_generic_factory
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
		}

		visible = {
			FROM = { is_owned_and_controlled_by = ROOT }
		}

		cost = POL_CIP_cost_cheap
		days_remove = 90
		fire_only_once = yes
		state_target = yes

		modifier = {
			civilian_factory_use = 2
		}
		ai_will_do = { factor = 1000 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Modernise_Krakow target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Modernise_Krakow target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_swietokrzyskie_mines = {
		icon = GFX_decision_steel
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost_cheap

		fire_only_once = yes
		days_remove = 60
		state_target = yes

		ai_will_do = { factor = 10 }

		modifier = {
			civilian_factory_use = 2
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_swietokrzyskie_mines target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_swietokrzyskie_mines target: [From.GetName]"
			FROM = {
				add_resource = {
					type = steel
					amount = 12
				}
			}
		}
	}
	POL_expand_dabarowa_basin = {
		icon = GFX_decision_steel
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
			has_tech = excavation1
		}

		cost = POL_CIP_cost_cheap

		fire_only_once = yes
		days_remove = 60
		state_target = yes

		ai_will_do = { factor = 10 }

		modifier = {
			civilian_factory_use = 2
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_expand_dabarowa_basin target: [From.GetName]"

		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_expand_dabarowa_basin target: [From.GetName]"
			FROM = {
				add_resource = {
					type = steel
					amount = 10
				}
			}
		}
	}
	POL_bogdanka_coal_mine = {
		icon = GFX_decision_steel
		allowed = { always = no }

		available = {
			FROM = { is_owned_and_controlled_by = ROOT }
			num_of_civilian_factories_available_for_projects > 1
			has_tech = excavation4
		}

		cost = POL_CIP_cost_cheap

		fire_only_once = yes
		days_remove = 120
		state_target = yes

		ai_will_do = { factor = 10 }

		modifier = {
			civilian_factory_use = 2
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_bogdanka_coal_mine target: [From.GetName]"

		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_bogdanka_coal_mine target: [From.GetName]"
			FROM = {
				add_resource = {
					type = steel
					amount = 8
				}
			}
		}
	}
	POL_formation_of_polmin = {
		icon = GFX_decision_oil
		allowed = { always = no }

		available = {
			num_of_civilian_factories_available_for_projects > 1
			has_tech = excavation3
		}
		visible = {
			FROM = { is_owned_and_controlled_by = ROOT }
		}
		cost = POL_CIP_cost_cheap

		fire_only_once = yes
		days_remove = 90
		state_target = yes

		ai_will_do = { factor = 30 }

		modifier = {
			civilian_factory_use = 2
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_formation_of_polmin target: [From.GetName]"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_formation_of_polmin target: [From.GetName]"
			FROM = {
				add_resource = {
					type = oil
					amount = 16
				}
			}
		}
	}
}
POL_land_reform = {
	POL_finalize_land_reform = {
		icon = GFX_decision_eng_trade_unions_support
		available = {
			all_core_state = {
				OR = {
					has_state_flag = POL_land_issue_solved
					NOT = { is_owned_by = ROOT }
				}
			}
			OR = {
				has_country_flag = POL_agricultural_infra
				has_country_flag = POL_deal_with_lewiatan
			}
			has_completed_focus = POL_Rural_Literacy_Campaigns
		}
		cost = POL_CIP_cost
		days_remove = 90
		fire_only_once = yes

		ai_will_do = { factor = 100 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_finalize_land_reform"
			remove_ideas = POL_issues_at_hand
			remove_dynamic_modifier = { modifier = POL_issues_at_hand_modifier }
			if = {
				limit = { has_country_flag = POL_small_land_reform }
				add_ideas = POL_export_agriculture
				set_temp_variable = { ideology_change_var = 4 }
				change_ruling_party_popularity = yes
			}
			else_if = {
				limit = { has_country_flag = POL_moderate_land_reform }
				add_ideas = POL_modernized_agriculture
				set_temp_variable = { ideology_change_var = 7 }
				change_ruling_party_popularity = yes
			}
			else_if = {
				limit = { has_country_flag = POL_radical_land_reform }
				add_ideas = POL_nation_of_small_farms
				set_temp_variable = { ideology_change_var = 10 }
				change_ruling_party_popularity = yes
			}
		}
	}
	POL_Construct_Agricultural_Infrastructure = {
		icon = GFX_decision_road

		available = {
			num_of_civilian_factories_available_for_projects > 1
		}

		cost = POL_CIP_cost
		days_remove = 120
		fire_only_once = yes

		ai_will_do = { factor = 100 }

		modifier = {
			civilian_factory_use = 2
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Construct_Agricultural_Infrastructure"

		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Construct_Agricultural_Infrastructure"
			set_country_flag = POL_agricultural_infra
			if = {
				limit = { has_idea = POL_issues_at_hand }
				custom_effect_tooltip = POL_modifies_issues_at_hand
				add_to_variable = {
					POL_issues_at_hand_max_efficiency = 0.1
					tooltip = production_factory_max_efficiency_factor_tooltip
				}
			}
		}
	}
	POL_Rural_Credit_Programme = {
		icon = GFX_decision_give_cash

		visible = {
			OR = {
				has_completed_focus = POL_Peoples_Banks
				has_socialist_government = yes
			}
		}

		cost = 50
		days_remove = 120
		fire_only_once = yes
		ai_will_do = { factor = 100 }

		modifier = {
			political_power_gain = -0.25
			stability_weekly = 0.015
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Rural_Credit_Programme"
			if = {
				limit = { has_idea = POL_issues_at_hand }
				custom_effect_tooltip = POL_modifies_issues_at_hand
				add_to_variable = {
					POL_issues_at_hand_monthly_population = 0.1
					tooltip = POL_issues_at_hand_monthly_population_tooltip
				}
			}
		}
	}

	POL_redistribute_land = {
		icon = eng_trade_unions_demand

		ai_will_do = { factor = 1000 }

		on_map_mode = map_only

		available = {
			FROM = { is_fully_controlled_by = ROOT }
			NOT = { has_decision = POL_redistribute_land }
			hidden_trigger = {
				OR = {
					has_country_flag = POL_small_land_reform
					has_country_flag = POL_moderate_land_reform
					has_country_flag = POL_radical_land_reform
				}
			}
		}

		visible = {
			FROM = {
				is_core_of = ROOT
				is_controlled_by = ROOT
				NOT = { has_state_flag = POL_land_issue_solved }
			}
		}

		cancel_trigger = {
			hidden_trigger = {
				NOT = { FROM = { is_fully_controlled_by = ROOT } }
			}
		}

		cost = POL_land_reform_cost
		days_remove = 60
		cancel_if_not_visible = no
		fire_only_once = yes

		state_target = yes
		target_array = POL.POL_polish_lands
		target_trigger = {
			FROM = {
				is_core_of = ROOT
				is_controlled_by = ROOT
				NOT = { has_state_flag = POL_land_issue_solved }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_redistribute_land target: [From.GetName]"
			add_to_variable = { POL_reform_progress = 1 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_redistribute_land target: [From.GetName]"
			ROOT = {
				if = {
					limit = {
						has_country_flag = POL_small_land_reform
					}
					set_temp_variable = { ideology_change_var = 0.01 }
					change_ruling_party_popularity = yes
				}
				else_if = {
					limit = {
						has_country_flag = POL_moderate_land_reform
					}
					set_temp_variable = { ideology_change_var = 0.01 }
					change_ruling_party_popularity = yes
					add_stability = 0.01
				}
				else = {
					if = {
						limit = { FROM = { state_population > 1000000 } }
						add_stability = 0.03
					}
					else = {
						add_stability = 0.02
					}
					set_temp_variable = { ideology_change_var = 0.01 }
					change_ruling_party_popularity = yes
				}
			}
			FROM = {
				if = {
					limit = {
						is_controlled_by = POL
						is_core_of = POL
						OR = {
							has_state_category = three
							has_state_category = two
							has_state_category = one
						}
					}
					if = {
						limit = {
							ROOT = { has_country_flag = POL_small_land_reform }
						}
						if = {
							limit = {
								has_resources_amount = {
									resource = steel
									amount > 1
								}
							}
							add_state_modifier = {
								modifier = {
									local_resources = 0.1
								}
							}
						}
						add_extra_state_shared_building_slots = 1
					}
					else_if = {
						limit = {
							ROOT = { has_country_flag = POL_moderate_land_reform }
						}
						increase_state_category_by_one_level = yes
					}
					else = {
						increase_state_category_by_one_level = yes
						add_state_modifier = {
							modifier = {
								state_production_speed_buildings_factor = 0.05
							}
						}
					}
				}
				set_state_flag = POL_land_issue_solved
			}
		}
	}
}
POL_Crossroads_Of_Europe_decisions = {
	POL_Warszawa_To_Lodz = {
		icon = train

		available = {
			controls_state = 10 #Warsaw
			has_country_flag = POL_warszawa_glowna_completed
		}

		visible = {
			owns_state = 10 #Warsaw
		}

		cancel_trigger = {
			NOT = { controls_state = 10 } #Warsaw
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 10 } #Warsaw
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Warszawa_To_Lodz"
			set_country_flag = POL_warszawa_to_lodz_completed
			build_railway = {
				level = 1
				fallback = yes
				start_province = 3544
				target_province = 9508
			}
			if = {
				limit = { NOT = { has_idea = POL_crossroads_of_europe } }
				hidden_effect = { add_ideas = POL_crossroads_of_europe }
				add_dynamic_modifier = { modifier = POL_crossroads_of_europe_modifier }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_efficiency = 0.02
				tooltip = POL_crossroads_efficiency_tt
			}
			add_to_variable = {
				POL_crossroads_resources = 0.025
				tooltip = POL_crossroads_resources_tt
			}
		}
	}
	POL_Lodz_To_Kalisz = {
		icon = train

		available = {
			controls_state = 87 #Kalisz
			has_country_flag = POL_warszawa_to_lodz_completed
		}

		visible = {
			owns_state = 87 #Kalisz
		}

		cancel_trigger = {
			NOT = { controls_state = 87 } #Kalisz
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 87 } #Kalisz
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Lodz_To_Kalisz"
			set_country_flag = POL_lodz_to_kalisz_completed
			build_railway = {
				level = 1
				fallback = yes
				start_province = 11428
				target_province = 9546
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_efficiency = 0.02
				tooltip = POL_crossroads_efficiency_tt
			}
		}
	}
	POL_Connect_To_Poznan = {
		icon = train

		available = {
			controls_state = 87 #Kalisz
			has_country_flag = POL_lodz_to_kalisz_completed
			if = {
				limit = { NOT = { owns_state = 86 } } #Posen
				86 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				86 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 87 #Kalisz
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 87 } #Kalisz
				86 = { NOT = { is_controlled_by = owner } } #Posen
				86 = { owner = { has_war_with = ROOT } } #Posen
			}
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 86 } #Posen
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Connect_To_Poznan"
			build_railway = {
				level = 1
				fallback = yes
				start_province = 9546
				target_province = 6558
			}
			if = {
				limit = { NOT = { owns_state = 86 } } #Posen
				var:86.owner = { country_event = poland.445 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_efficiency = 0.02
				tooltip = POL_crossroads_efficiency_tt
			}
		}
	}
	POL_The_Northern_Railroad = {
		icon = train

		available = {
			controls_state = 98
			has_country_flag = POL_warszawa_glowna_completed
		}

		visible = {
			owns_state = 98
		}

		cancel_trigger = {
			NOT = { controls_state = 98 } #Plock
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 98 } #Plock
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_The_Northern_Railroad"
			set_country_flag = POL_northern_railroad_completed
			build_railway = {
				level = 1
				fallback = yes
				start_province = 3544
				target_province = 9290
			}
			hidden_effect = {
				build_railway = {
					level = 1
					fallback = yes
					start_province = 6321
					target_province = 6401
				}
			}
			if = {
				limit = { NOT = { has_idea = POL_crossroads_of_europe } }
				hidden_effect = { add_ideas = POL_crossroads_of_europe }
				add_dynamic_modifier = { modifier = POL_crossroads_of_europe_modifier }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_efficiency = 0.02
				tooltip = POL_crossroads_efficiency_tt
			}
		}
	}
	POL_Connect_To_Gdansk = {
		icon = GFX_decision_generic_naval

		available = {
			controls_state = 98 #Plock
			has_country_flag = POL_northern_railroad_completed
			if = {
				limit = { NOT = { owns_state = 776 } } #West Prussia
				776 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				776 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 98 #Plock
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 98 } #Plock
				776 = { NOT = { is_controlled_by = owner } } #West Prussia
				776 = { owner = { has_war_with = ROOT } } #West Prussia
			}
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 776 } #West Prussia
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Connect_To_Gdansk"
			build_railway = {
				level = 1
				fallback = yes
				start_province = 6347
				target_province = 362
			}
			if = {
				limit = { NOT = { owns_state = 776 } } #West Prussia
				var:776.owner = { country_event = poland.446 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_consumer_goods = -0.06
				tooltip = POL_crossroads_consumer_goods_tt
			}
		}
	}
	POL_Warszawa_To_Kielce = {
		icon = train

		available = {
			controls_state = 90 #Kielce
			has_country_flag = POL_warszawa_to_lodz_completed
		}

		visible = {
			owns_state = 90 #Kielce
		}

		cancel_trigger = {
			NOT = { controls_state = 90 } #Kielce
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 90 } #Kielce
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Warszawa_To_Kielce"
			set_country_flag = POL_warszawa_to_kielce_completed
			build_railway = {
				level = 1
				fallback = yes
				start_province = 3544
				target_province = 3497
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_resources = 0.025
				tooltip = POL_crossroads_resources_tt
			}
		}
	}
	POL_Connect_To_Silesia = {
		icon = train

		available = {
			controls_state = 90 #Kielce
			has_country_flag = POL_warszawa_to_kielce_completed
			if = {
				limit = { NOT = { owns_state = 67 } } #Upper Silesia
				67 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				67 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 90 #Kielce
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 90 } #Plock
				67 = { NOT = { is_controlled_by = owner } } #Upper Silesia
				67 = { owner = { has_war_with = ROOT } } #Upper Silesia
			}
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 67 } #Upper Silesia
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Connect_To_Silesia"
			build_railway = {
				level = 1
				path = { 11532 3586 506 11467 }
			}
			if = {
				limit = { NOT = { owns_state = 67 } } #Upper Silesia
				var:67.owner = { country_event = poland.447 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_resources = 0.025
				tooltip = POL_crossroads_resources_tt
			}
		}
	}
	POL_Connect_To_Krakow = {
		icon = train

		available = {
			controls_state = 90 #Kielce
			has_country_flag = POL_warszawa_to_kielce_completed
			if = {
				limit = { NOT = { owns_state = 88 } } #Krakow
				88 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				88 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 90 #Kielce
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 90 } #Kielce
				88 = { NOT = { is_controlled_by = owner } } #Krakow
				88 = { owner = { has_war_with = ROOT } } #Krakow
			}
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 88 } #Krakow
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Connect_To_Krakow"
			build_railway = {
				level = 1
				fallback = yes
				start_province = 6416
				target_province = 9427
			}
			if = {
				limit = {
					NOT = { owns_state = 88 } #Krakow
				}
				var:88.owner = { country_event = poland.448 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_consumer_goods = -0.06
				tooltip = POL_crossroads_consumer_goods_tt
			}
		}
	}
	POL_Connect_Into_Czech_Network = {
		icon = train

		available = {
			controls_state = 90 #Kielce
			has_country_flag = POL_warszawa_to_kielce_completed
			if = {
				limit = { NOT = { owns_state = 924 } } #Tesin
				924 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				924 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 90 #Kielce
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 90 } #Kielce
				924 = { NOT = { is_controlled_by = owner } } #Tesin
				924 = { owner = { has_war_with = ROOT } } #Tesin
			}
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 924 } #Tesin
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Connect_Into_Czech_Network"
			set_country_flag = POL_cieszyn_railway_completed
			build_railway = {
				level = 1
				fallback = yes
				start_province = 9427
				target_province = 13553
			}
			if = {
				limit = { NOT = { owns_state = 924 } } #Tesin
				var:924.owner = { country_event = poland.449 }
			}
			### TODO: Austrian/Galician notification
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_efficiency = 0.02
				tooltip = POL_crossroads_efficiency_tt
			}
		}
	}
	POL_Over_The_Carpathians = {
		icon = train

		available = {
			controls_state = 90 #Kielce
			has_country_flag = POL_cieszyn_railway_completed
			if = {
				limit = { NOT = { owns_state = 70 } } #Slovakia
				70 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				70 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 90 #Kielce
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 90 } #Kielce
				70 = { NOT = { is_controlled_by = owner } } #Slovakia
				70 = { owner = { has_war_with = ROOT } } #Slovakia
			}
		}

		cost = POL_railway_cost
		days_remove = 90
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 70 } #Slovakia
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Over_The_Carpathians"
			build_railway = {
				level = 1
				fallback = yes
				start_province = 9551
				target_province = 9692
			}
			if = {
				limit = { NOT = { owns_state = 70 } } #Slovakia
				var:70.owner = { country_event = poland.444 }
			}
			### TODO: Austrian/Galician notification
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_resources = 0.025
				tooltip = POL_crossroads_resources_tt
			}
		}
	}
	POL_Cross_The_Vistula = {
		icon = train

		available = {
			controls_state = 92 #Lublin
			has_country_flag = POL_warszawa_glowna_completed
		}

		visible = {
			owns_state = 92 #Lublin
		}

		cancel_trigger = {
			NOT = { controls_state = 92 } #Lublin
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 92 } #Lublin
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Cross_The_Vistula"
			set_country_flag = POL_cross_the_vistula_completed
			build_railway = {
				level = 1
				fallback = yes
				start_province = 3544
				target_province = 3559
			}
			if = {
				limit = { NOT = { has_idea = POL_crossroads_of_europe } }
				hidden_effect = { add_ideas = POL_crossroads_of_europe }
				add_dynamic_modifier = { modifier = POL_crossroads_of_europe_modifier }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_efficiency = 0.025
				tooltip = POL_crossroads_efficiency_tt
			}
			add_to_variable = {
				POL_crossroads_resources = 0.025
				tooltip = POL_crossroads_resources_tt
			}
		}
	}
	POL_Connect_To_Bialystok = {
		icon = train

		available = {
			controls_state = 92 #Lublin
			has_country_flag = POL_cross_the_vistula_completed
			if = {
				limit = { NOT = { owns_state = 97 } } #Bialystok
				97 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				97 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 92 #Lublin
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 92 } #Kielce
				97 = { NOT = { is_controlled_by = owner } } #Bialystok
				97 = { owner = { has_war_with = ROOT } } #Bialystok
			}
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 97 } #Bialystok
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Connect_To_Bialystok"
			set_country_flag = POL_connect_to_bialystok_completed
			build_railway = {
				level = 1
				fallback = yes
				start_province = 402
				target_province = 11301
			}
			if = {
				limit = { NOT = { owns_state = 97 } } #Bialystok
				var:96.owner = { country_event = poland.450 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_consumer_goods = -0.03
				tooltip = POL_crossroads_consumer_goods_tt
			}
		}
	}
	POL_Into_The_Baltic = {
		icon = train

		available = {
			controls_state = 92 #Lublin
			has_country_flag = POL_connect_to_bialystok_completed
			if = {
				limit = { NOT = { owns_state = 96 } } #Vilnius
				96 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				96 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 92 #Lublin
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 92 } #Lublin
				96 = { NOT = { is_controlled_by = owner } } #Vilnius
				96 = { owner = { has_war_with = ROOT } } #Vilnius
			}
		}

		cost = POL_railway_cost
		days_remove = 90
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 96 } #Vilnius
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Into_The_Baltic"
			set_country_flag = POL_bialystok_to_wilno
			build_railway = {
				level = 1
				fallback = yes
				start_province = 11301
				target_province = 3320
			}
			if = {
				limit = { NOT = { owns_state = 97 } } #Wilno
				var:96.owner = { country_event = poland.451 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_consumer_goods = -0.03
				tooltip = POL_crossroads_consumer_goods_tt
			}
		}
	}
	POL_Road_To_Riga = {
		icon = train

		available = {
			has_country_flag = POL_bialystok_to_wilno
			if = {
				limit = { NOT = { owns_state = 842 } } #Riga
				842 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				842 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 92 #Lublin
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 92 } #Lublin
				842 = { NOT = { is_controlled_by = owner } } #Riga
				842 = { owner = { has_war_with = ROOT } } #Riga
			}
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		ai_will_do = { factor = 2 }

		highlight_states = { highlight_state_targets = { state = 842 } } #Riga

		modifier = { production_speed_buildings_factor = -0.1 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Road_To_Riga"
			build_railway = {
				level = 1
				fallback = yes
				start_province = 3320
				target_province = 9340
			}
			if = {
				limit = { NOT = { owns_state = 842 } } #Riga
				var:842.owner = { country_event = poland.452 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_consumer_goods = -0.03
				tooltip = POL_crossroads_consumer_goods_tt
			}
		}
	}
	POL_Lublin_To_Chelm = {
		icon = train

		available = {
			controls_state = 1088 #Chelm
			has_country_flag = POL_cross_the_vistula_completed
		}

		visible = {
			owns_state = 1088 #Chelm
		}

		cancel_trigger = {
			NOT = { controls_state = 1088 } #Lublin
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 1088 } #Chelm
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Lublin_To_Chelm"
			set_country_flag = POL_lublin_to_chelm_completed
			build_railway = {
				level = 1
				fallback = yes
				start_province = 3559
				target_province = 9452
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_efficiency = 0.02
				tooltip = POL_crossroads_efficiency_tt
			}
		}
	}
	POL_Connect_To_Lwow = {
		icon = train

		available = {
			controls_state = 1088 #Chelm
			has_country_flag = POL_lublin_to_chelm_completed
			if = {
				limit = { NOT = { owns_state = 91 } } #Lwow
				91 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				91 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 1088 #Chelm
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 1088 } #Chelm
				91 = { NOT = { is_controlled_by = owner } } #Riga
				91 = { owner = { has_war_with = ROOT } } #Riga
			}
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 91 } #Lwow
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Connect_To_Lwow"
			build_railway = {
				level = 1
				fallback = yes
				start_province = 9452
				target_province = 11479
			}
			if = {
				limit = { NOT = { owns_state = 91 } } #Lwow
				var:91.owner = { country_event = poland.453 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_efficiency = 0.02
				tooltip = POL_crossroads_efficiency_tt
			}
		}
	}
	POL_To_Brest_And_Beyond = {
		icon = train

		available = {
			controls_state = 1088 #Chelm
			has_country_flag = POL_lublin_to_chelm_completed
			if = {
				limit = { NOT = { owns_state = 93 } } #Brest
				93 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				93 = { is_owned_and_controlled_by = ROOT }
			}
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 1088 } #Chelm
				93 = { NOT = { is_controlled_by = owner } } #Brest
				93 = { owner = { has_war_with = ROOT } } #Brest
			}
		}

		visible = {
			owns_state = 1088 #Chelm
		}

		cost = POL_railway_cost
		days_remove = 60
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 93 } #Brest
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_To_Brest_And_Beyond"
			set_country_flag = POL_warzawa_to_brest_completed
			build_railway = {
				level = 1
				fallback = yes
				start_province = 402
				target_province = 3392
			}
			if = {
				limit = { NOT = { owns_state = 93 } } #Brest
				var:93.owner = { country_event = poland.454 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_consumer_goods = -0.03
				tooltip = POL_crossroads_consumer_goods_tt
			}
		}
	}
	POL_The_Warzawa-Kiev_Route = {
		icon = train

		available = {
			controls_state = 1088 #Chelm
			has_country_flag = POL_warzawa_to_brest_completed
			if = {
				limit = { NOT = { owns_state = 202 } } #Kiev
				202 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				202 = { is_owned_and_controlled_by = ROOT }
			}
		}

		visible = {
			owns_state = 1088 #Chelm
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 1088 } #Chelm
				202 = { NOT = { is_controlled_by = owner } } #Kiev
				202 = { owner = { has_war_with = ROOT } } #Kiev
			}
		}

		cost = POL_railway_cost
		days_remove = 90
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 202 } #Kiev
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_The_Warzawa-Kiev_Route"
			build_railway = {
				level = 1
				fallback = yes
				start_province = 9452
				target_province = 525
			}
			if = {
				limit = { NOT = { owns_state = 202 } } #Brest
				var:202.owner = { country_event = poland.455 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_consumer_goods = -0.03
				tooltip = POL_crossroads_consumer_goods_tt
			}
		}
	}
	POL_Around_The_Marshes = {
		icon = train

		available = {
			controls_state = 92 #Lublin
			has_country_flag = POL_warzawa_to_brest_completed
			if = {
				limit = { NOT = { owns_state = 206 } } #Minsk
				206 = {
					owner = {
						controls_state = PREV
						NOT = { has_war_with = ROOT }
					}
				}
			}
			else = {
				206 = { is_owned_and_controlled_by = ROOT }
			}
		}

		cancel_trigger = {
			OR = {
				NOT = { controls_state = 92 } #Lublin
				206 = { NOT = { is_controlled_by = owner } } #Minsk
				206 = { owner = { has_war_with = ROOT } } #Minsk
			}
		}

		visible = {
			owns_state = 92 #Lublin
		}

		cost = POL_railway_cost
		days_remove = 90
		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = { state = 206 } #Minsk
		}

		modifier = {
			production_speed_buildings_factor = -0.1
		}

		ai_will_do = { factor = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Around_The_Marshes"
			build_railway = {
				level = 1
				fallback = yes
				start_province = 402
				target_province = 11370
			}
			if = {
				limit = { NOT = { owns_state = 206 } } #Minsk
				var:206.owner = { country_event = poland.456 }
			}
			custom_effect_tooltip = POL_modifies_crossroads_tt
			add_to_variable = {
				POL_crossroads_consumer_goods = -0.03
				tooltip = POL_crossroads_consumer_goods_tt
			}
		}
	}
}

POL_National_Republic = {
	POL_found_slavic_union = {
		icon = GFX_decision_eng_puppet_usa

		visible = {
			has_completed_focus = POL_Union_of_Western_Slavs
			has_government = national_populist
		}
		available = {
			has_subject = LIT
			has_subject = CZE
			has_subject = WHR
			has_subject = UKR
		}
		fire_only_once = yes

		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_found_slavic_union"
			if = {
				limit = { is_in_faction = no }
				create_faction = POL_SLAVIC_UNION
			}
			add_stability = 0.15
			hidden_effect = { news_event = polandnews.7 }
		}
	}
	POL_add_to_union = {
		icon = GFX_decision_generic_agreement
		visible = {
			has_completed_focus = POL_Union_of_Western_Slavs
		}
		available = {
			at_peace_or_same_enemies_as_FROM = yes
		}
		targets = { CZE SLO UKR WHR LIT }
		target_root_trigger = {
			has_completed_focus = POL_Union_of_Western_Slavs
		}
		target_trigger = {
			FROM = { is_subject_of = POL }
		}
		fire_only_once = yes
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_add_to_union target: [From.GetName]"
			set_autonomy = {
				target = FROM
				autonomy_state = slavic_union_member
			}
		}
	}
	POL_invite_ally = {
		icon = GFX_decision_generic_agreement
		available = {
			at_peace_or_same_enemies_as_FROM = yes
			is_faction_leader = yes
		}

		targets = { BPL ROM BUL SER HUN LAT CZE SLO WHR LIT UKR }
		target_root_trigger = {
			has_completed_focus = POL_Union_of_Western_Slavs
		}
		target_trigger = {
			FROM = {
				has_dictatorship_government = yes
				is_subject = no
				has_capitulated = no
				OR = {
					is_in_faction = no
					tag = BPL
				}
			}
		}
		fire_only_once = yes
		cost = 25
		ai_will_do = { factor = 100 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_invite_ally target: [From.GetName]"
			if = {
				limit = {
					tag = SER
					is_faction_leader = yes
				}
				every_country = {
					limit = {
						is_in_faction_with = SER
						has_dictatorship_government = yes
					}
					country_event = poland.374
				}
			}
			else = {
				FROM = { country_event = poland.374 }
			}
		}
	}
	POL_military_takeover = {
		icon = GFX_decision_generic_military
		allowed = { always = no }
		available = {
			has_completed_focus = POL_End_Army_Factionalism
			controls_state = 86
			OR = {
				POL_edward_rydz_smigly = { is_army_chief = yes }
				NOT = { has_character = POL_edward_rydz_smigly }
				has_completed_focus = POL_Moral_Education
			}
		}
		cancel_trigger = {
			POL_dobo = no
		}
		is_good = no
		selectable_mission = yes
		fire_only_once = yes
		days_mission_timeout = POL_military_takeover_time
		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				NOT = { has_game_rule = { rule = POL_revolts option = DEFAULT } }
				NOT = { has_game_rule = { rule = POL_revolts option = POL_REVOLTS_NATPOP } }
			}
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_military_takeover"
			if = {
				limit = {
					NOT = {
						AND = {
							is_ai = yes
							has_game_rule = { rule = POL_revolts option = POL_REVOLTS_NATPOP }
						}
					}
				}
				country_event = poland.703
			}
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_military_takeover"
			add_stability = 0.08
		}
	}
	POL_sejm_revolt = {
		icon = GFX_decision_SOV_place_hq
		allowed = { always = no }
		available = {
			has_completed_focus = POL_Root_Out_Those_Without_Faith
			national_populist > 0.5
			controls_state = 86
		}
		cancel_trigger = {
			POL_dobo = no
		}
		is_good = no
		selectable_mission = yes
		fire_only_once = yes
		days_mission_timeout = POL_sejm_revolt_time
		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				NOT = { has_game_rule = { rule = POL_revolts option = DEFAULT } }
				NOT = { has_game_rule = { rule = POL_revolts option = POL_REVOLTS_NATPOP } }
			}
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_sejm_revolt"
			if = {
				limit = {
					NOT = {
						AND = {
							is_ai = yes
							has_game_rule = { rule = POL_revolts option = POL_REVOLTS_NATPOP }
						}
					}
				}
				country_event = poland.703
			}
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_sejm_revolt"
			add_stability = 0.08
		}
	}
	POL_stall_for_time = {
		icon = GFX_decision_generic_speech
		visible = { has_active_mission = POL_sejm_revolt }
		cost = 25
		days_remove = 5
		ai_will_do = { factor = 1 }
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_stall_for_time"
			hidden_effect = {
				add_days_mission_timeout = {
					mission = POL_sejm_revolt
					days = 40
				}
			}
			custom_effect_tooltip = POL_stall_for_time_tt
		}
	}
	POL_economic_restructuring_1 = {
		icon = GFX_decision_cash_flow
		allowed = { always = no }
		visible = {
			has_country_flag = POL_uprising_crash
		}

		cost = 30
		days_remove = 90
		ai_will_do = { factor = 100 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_economic_restructuring_1"
			POL_rebuilding = yes
		}
	}
}
POL_Marshals_Cult = {
	POL_Tour_The_Frontlines = {
		icon = GFX_decision_soldierly_discipline

		available = {
			has_war = yes
		}

		cost = 30
		days_remove = 40
		days_re_enable = 60
		fixed_random_seed = no
		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_Tour_The_Frontlines"
			POL_edward_rydz_smigly = {
				add_temporary_buff_to_units = {
					combat_offense = 0.1
					combat_breakthrough = 0.05
					days = 100
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Tour_The_Frontlines"
			random_list = {
				seed = random
				20 = {
					modifier = {
						factor = 0
						any_controlled_state = {
							has_resistance = no
						}
					}
					random_controlled_state = {
						limit = { has_resistance = yes }
						add_compliance = 4
					}
				}
				10 = {
					add_war_support = 0.04
				}
				10 = {
					add_popularity = { ideology = paternal_autocrat popularity = 0.03 }
				}
				10 = {
					POL_edward_rydz_smigly = {
						gain_xp = 65
					}
				}
			}
		}
	}
	POL_Marshals_Postage_Stamps = {
		icon = GFX_decision_trumpet

		fire_only_once = yes
		cost = 30
		days_remove = 60

		ai_will_do = { factor = 10 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Marshals_Postage_Stamps"
			add_popularity = { ideology = paternal_autocrat popularity = 0.04 }
		}
	}
	POL_Marshals_Town = {
		icon = GFX_decision_SOV_place_hq

		visible = {
			owns_state = 89
		}

		fire_only_once = yes
		cost = 50
		days_remove = 30

		ai_will_do = { factor = 10 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Marshals_Town"
			add_popularity = { ideology = paternal_autocrat popularity = 0.04 }
			random_owned_controlled_state = {
				limit = { is_core_of = POL }
				add_extra_state_shared_building_slots = 1
			}
		}
	}
	POL_Patron_of_Schools = {
		icon = GFX_decision_SOV_academy_of_sciences

		visible = {
			owns_state = 89
		}

		fire_only_once = yes
		cost = 50
		days_remove = 10

		ai_will_do = { factor = 10 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Patron_of_Schools"
			add_stability = 0.02
			POL_reduce_polarisation = yes
		}
	}
	POL_Reestablish_The_Legions = {
		icon = GFX_decision_generic_military

		available = {
			controls_state = 10
		}

		fire_only_once = yes
		cost = 75
		days_remove = 10

		ai_will_do = { factor = 10 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_Reestablish_The_Legions"
			add_popularity = { ideology = paternal_autocrat popularity = 0.04 }
			add_ideas = POL_rydz_legion
		}
	}
}
POL_Syndicalist_Republic = {
	POL_found_commonwealth = {
		icon = GFX_decision_eng_puppet_usa

		visible = {
			has_completed_focus = POL_Red_Intermarium
			has_socialist_government = yes
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = POL_owns_25_states
				check_variable = { num_owned_states > 24 }
			}
			owns_state = 96 #Vilnius
			owns_state = 91 #Lwów
			owns_state = 86 #Poznan
			owns_state = 93 #Brest
		}
		fire_only_once = yes

		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_found_commonwealth"
			set_temp_variable = { ideology_change_var = 10 }
			change_ruling_party_popularity = yes
			set_cosmetic_tag = POL_commonwealth
			country_event = poland.650
			if = {
				limit = { has_subject = LIT }
				LIT = {
					every_core_state = { add_claim_by = POL }
					POL = {
						annex_country = {
							target = PREV
							transfer_troops = no
						}
					}
				}
			}
			every_controlled_state = {
				limit = {
					is_claimed_by = POL
					has_resistance = yes
				}
				add_compliance = 10
			}
			hidden_effect = {
				if = {
					limit = { LIT = { exists = no } }
					LIT = {
						LIT_salomeja_neris = { set_nationality = POL }
						LIT_vincas_vitkauskas = { set_nationality = POL }
					}
				}
				if = {
					limit = {
						WHR = { exists = no }
						owns_state = 206 #Minsk
					}
					WHR = { WHR_paluta_badunova = { set_nationality = POL } }
				}
				if = {
					limit = {
						UKR = { exists = no }
						owns_state = 202 #Kiev
					}
					UKR = {
						UKR_volodymyr_chekhivskyi = { set_nationality = POL }
						UKR_sviatoslav_shramchenko = { set_nationality = POL }
					}
				}
			}
		}
	}
	POL_form_zapadoslavia = {
		icon = GFX_decision_eng_puppet_usa

		visible = {
			has_completed_focus = POL_Central_European_Commonwealth
			has_socialist_government = yes
		}
		available = {
			OR = {
				owns_state = 9 #Prague
				has_subject = CZE
			}
			OR = {
				owns_state = 70 #Western Slovakia
				has_subject = SLO
				AND = {
					has_subject = CZE
					CZE = { owns_state = 70 }
				}
			}
		}
		fire_only_once = yes

		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_form_zapadoslavia"
			if = {
				limit = { has_subject = CZE }
				CZE = {
					every_core_state = { add_claim_by = POL }
					POL = {
						annex_country = {
							target = PREV
							transfer_troops = no
						}
					}
				}
			}
			if = {
				limit = {
					OR = {
						has_subject = SLO
						AND = {
							has_subject = CZE
							CZE = { owns_state = 70 }
						}
					}
				}
				SLO = {
					every_core_state = { add_claim_by = POL }
					POL = {
						annex_country = {
							target = PREV
							transfer_troops = no
						}
					}
				}
			}
			if = {
				limit = {
					var:66.owner = { is_subject_of = POL } #Silesia
				}
				set_temp_variable = { tag_to_release = POL }
				66 = { transfer_state_and_add_claim = yes }
			}
			set_cosmetic_tag = POL_zapa
			country_event = poland.660
			every_state = {
				limit = {
					is_claimed_by = POL
					is_controlled_by = POL
					NOT = { is_core_of = POL }
				}
				add_compliance = 20
			}
			hidden_effect = {
				if = {
					limit = {
						CZE = { exists = no }
						owns_state = 9 #Prague
					}
					CZE = {
						CZE_luisa_landova_stychova = { set_nationality = POL }
						CZE_bohuslav_vrbensky = { set_nationality = POL }
						CZE_vilem_brodecky = { set_nationality = POL }
						CZE_asher_zelig_goldberger = { set_nationality = POL }
						CZE_ludvik_svoboda = { set_nationality = POL }
					}
				}
				if = {
					limit = {
						SLO = { exists = no }
						owns_state = 70 #Slovakia
					}
					SLO = {
						SLO_jan_sverma = { set_nationality = POL }
						SLO_karol_smidke = { set_nationality = POL }
						SLO_ferdinand_catlos = { set_nationality = POL }
						SLO_viliam_zingor = { set_nationality = POL }
					}
				}
			}
		}
	}

	POL_zakrzewski_consolidate_mission = {
		icon = GFX_decision_hand_grab
		allowed = { always = no }
		available = { totalist > 0.5 }
		cancel_trigger = {
			POL_zakr = no
		}
		is_good = no
		selectable_mission = no
		days_mission_timeout = 180

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_zakrzewski_consolidate_mission"
			if = {
				limit = {
					POL = { is_ai = yes }
					has_game_rule = { rule = POL_revolts option = POL_REVOLTS_TOTALIST }
				}
				country_event = poland.609 #ascension of zakrzewski
			}
			else = { country_event = poland.625 } #removal of zakrzewski
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_zakrzewski_consolidate_mission"
			add_political_power = 100
			country_event = poland.609 #ascension of zakrzewski
		}
	}
	POL_reform_the_ZZZ = {
		icon = GFX_decision_generic_brainwash

		visible = { has_completed_focus = POL_In_Zakrzewskis_Light }
		fire_only_once = yes
		cost = 50
		days_remove = 70

		ai_will_do = { factor = 10 }
		modifier = {
			syndicalist_drift = -0.06
			radical_socialist_drift = -0.06
			social_democrat_drift = -0.06
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_reform_the_ZZZ"
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_reform_the_ZZZ"
			add_stability = 0.04
			add_popularity = {
				ideology = totalist
				popularity = 0.05
			}
		}
	}
	POL_centralize_paramilitaries = {
		icon = GFX_decision_soldierly_discipline

		visible = { has_completed_focus = POL_In_Zakrzewskis_Light }
		fire_only_once = yes
		ai_will_do = { factor = 10 }

		custom_cost_text = CP_80_PP_50
		custom_cost_trigger = {
			command_power > 79.99
			has_political_power > 39.99
		}
		ai_hint_pp_cost = 40.0
		days_remove = 70

		ai_will_do = { factor = 10 }
		modifier = {
			weekly_manpower = -320
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_centralize_paramilitaries"
			hidden_effect = {
				add_command_power = -80
				add_political_power = -40
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_centralize_paramilitaries"
			add_popularity = {
				ideology = totalist
				popularity = 0.06
			}
		}
	}
	POL_propaganda_campaigns = {
		icon = GFX_decision_eng_propaganda_campaigns

		visible = {
			has_active_mission = POL_zakrzewski_consolidate_mission
		}
		fire_only_once = no
		cost = 40
		days_remove = 30

		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_propaganda_campaigns"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_propaganda_campaigns"
			add_popularity = {
				ideology = totalist
				popularity = 0.03
			}
			add_war_support = 0.02
		}
	}
	POL_truce_with_PPS = {
		icon = GFX_decision_peace_dove
		allowed = { always = no }
		visible = {
			has_socialist_government = yes
		}
		available = {
			has_completed_focus = POL_Healing_the_Scars
		}
		fire_only_once = yes
		cost = 75
		days_remove = 100

		ai_will_do = { factor = 10 }

		modifier = {
			social_democrat_drift = 0.2
			stability_weekly = 0.01
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_truce_with_PPS"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_truce_with_PPS"
			set_temp_variable = { coalition_partner_var = token:social_democrat }
			add_to_coalition = yes
		}
	}
	POL_truce_with_PSL = {
		icon = GFX_decision_peace_dove
		allowed = { always = no }
		visible = {
		}
		available = {
			has_completed_focus = POL_Healing_the_Scars
		}
		fire_only_once = yes
		cost = 100
		days_remove = 100

		ai_will_do = { factor = 10 }

		modifier = {
			social_liberal_drift = 0.2
			stability_weekly = 0.01
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_truce_with_PSL"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_truce_with_PSL"
			if = {
				limit = { has_idea = POL_polarised_society_4 }
				remove_ideas = POL_polarised_society_4
			}
			else = {
				add_stability = 0.06
			}
		}
	}
	POL_economic_restructuring_2 = {
		icon = GFX_decision_cash_flow
		allowed = { always = no }
		visible = {
			has_country_flag = POL_uprising_crash
		}

		cost = 60
		days_remove = 60
		ai_will_do = { factor = 100 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_economic_restructuring_2"
			POL_rebuilding = yes
		}
	}
}
POL_expansion = {
	POL_reach_the_sea = {
		icon = GFX_decision_generic_naval
		allowed = { always = no }
		available = {
			OR = {
				controls_state = 776
				controls_state = 777
			}
		}
		cancel_trigger = {
			has_war = no
		}
		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 120
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout POL_reach_the_sea"
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_reach_the_sea"
			add_war_support = 0.05
			country_event = polandflavor.31
		}
	}
	POL_normal_war_goals = {
		icon = hol_draw_up_staff_plans

		available = {
			OR = {
				is_owner_neighbor_of = FROM
				any_allied_country = { is_owner_neighbor_of = FROM }
			}
		}

		visible = {
			can_declare_war_on = FROM
			is_subject = no
			NOT = { has_country_flag = POL_forpol_wait }
			NOT = { is_in_faction_with = GER }
			NOT = { is_in_faction_with = AUS }
		}

		cancel_trigger = {
			NOT = { can_declare_war_on = FROM }
		}
		targets = {
			777.OWNER
			776.OWNER
			67.OWNER
			86.OWNER
			88.OWNER
			91.OWNER
			93.OWNER
			97.OWNER
			96.OWNER
		}
		target_root_trigger = {
			is_subject = no
		}
		target_trigger = {
			can_declare_war_on = FROM
		}

		cost = 30
		days_remove = 30
		fire_only_once = no
		war_with_target_on_remove = yes

		ai_will_do = {
			factor = 10
			modifier = { #Don't start faction wars against major factions
				factor = 0
				FROM = { dont_start_major_faction_wars = yes }
			}
			modifier = { #Don't start wars against syndie frens
				factor = 0
				has_socialist_government = yes
				FROM = { has_socialist_government = yes }
			}
			modifier = { #Centrolew only fights fascists
				factor = 0
				has_left_democratic_government = yes
				FROM = { has_dictatorship_government = no }
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_normal_war_goals target: [From.GetName]"
			clear_decision_attack_ai = yes
			add_political_power = 30
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_normal_war_goals target: [From.GetName]"
			setup_decision_attack_AI = yes
			FROM = { incoming_war_notification_effect = yes }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_normal_war_goals target: [From.GetName]"
			clear_decision_attack_AI = yes
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}
	}

	POL_ultimatum_to_czechia = {
		icon = GFX_decision_eng_trade_unions_demand

		visible = {
			NOT = {
				owns_state = 924
				is_in_faction_with = AUS
				is_in_faction_with = GER
			}
			is_subject = no
			NOT = { has_country_flag = POL_forpol_wait }
			can_declare_war_on = FROM
		}
		available = {
			OR = {
				owns_state = 67
				owns_state = 88
			}
		}

		cancel_trigger = {
			NOT = { can_declare_war_on = FROM }
		}
		targets = {
			924.OWNER
		}
		target_root_trigger = {
			is_subject = no
		}
		target_trigger = {
			NOT = {
				owns_state = 924
				is_in_faction_with = AUS
				is_in_faction_with = GER
			}
			is_subject = no
			can_declare_war_on = FROM
		}

		cost = 30
		days_remove = 30
		fire_only_once = yes
		war_with_target_on_remove = yes
		ai_will_do = { factor = 10 }

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_ultimatum_to_czechia target: [From.GetName]"
			clr_country_flag = imminent_war
			add_political_power = 30
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_ultimatum_to_czechia target: [From.GetName]"
			set_country_flag = { flag = imminent_war days = 30 value = 1 }
			setup_decision_attack_AI = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_ultimatum_to_czechia target: [From.GetName]"
			clear_decision_attack_AI = yes
			var:924.owner = { country_event = poland.594 }
		}
	}
	POL_establish_claim = {
		icon = GFX_decision_eng_trade_unions_demand

		ai_will_do = { factor = 1000 }

		on_map_mode = map_and_decisions_view

		available = {
			FROM = {
				is_fully_controlled_by = ROOT
				any_neighbor_state = {
					OR = {
						is_claimed_by = POL
						is_owned_by = POL
					}
				}
			}
		}

		visible = {
			FROM = {
				NOT = {
					is_claimed_by = ROOT
				}
				owner = {
					OR = {
						tag = POL
						NOT = { is_in_faction_with = POL }
					}
				}
			}
		}

		cost = POL_claim_cost
		days_remove = 15
		cancel_if_not_visible = yes
		fire_only_once = no

		state_target = yes
		target_array = POL.PLC_claims
		target_root_trigger = {
			tag = POL
			has_completed_focus = POL_Pilsudskis_Vision
		}
		target_trigger = {
			FROM = {
				NOT = {
					is_claimed_by = ROOT
				}
				owner = {
					OR = {
						tag = POL
						NOT = { is_in_faction_with = POL }
					}
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_establish_claim target: [From.GetName]"
			add_to_variable = { POL_claim_cost = 8 }
			custom_effect_tooltip = POL_claim_cost_tt
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_establish_claim target: [From.GetName]"
			add_state_claim = FROM
		}
	}
}
POL_Military_Cooperation = {
	POL_build_chelm_line = {
		icon = GFX_decision_shield
		allowed = { always = no }
		available = {
			has_full_control_of_state = 1088
		}
		highlight_states = {
			highlight_provinces = {
				6580 9452 11466
			}
		}
		custom_cost_text = POL_forts_cost
		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 749 }
			has_equipment = { support_equipment > 99 }
		}

		cost = 20
		days_remove = 90
		fire_only_once = yes
		cancel_if_not_visible = yes

		ai_will_do = { factor = 10 }

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_build_chelm_line"
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 750
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = 100
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_build_chelm_line"
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -750
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -100
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_build_chelm_line"
			1088 = {
				add_building_construction = {
					type = bunker
					province = 6580
					level = 2
					instant_build = yes
				}
				add_building_construction = {
					type = bunker
					province = 9452
					level = 2
					instant_build = yes
				}
				add_building_construction = {
					type = bunker
					province = 11466
					level = 2
					instant_build = yes
				}
			}
		}
	}
	POL_build_ostroleka_line = {
		icon = GFX_decision_shield
		allowed = { always = no }
		available = {
			has_full_control_of_state = 92
			has_full_control_of_state = 98
		}
		highlight_states = {
			highlight_provinces = {
				9544 402 11274 11329
			}
		}
		custom_cost_text = POL_forts_cost
		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 749 }
			has_equipment = { support_equipment > 99 }
		}

		cost = 30
		days_remove = 90
		fire_only_once = yes
		cancel_if_not_visible = yes

		ai_will_do = { factor = 10 }

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel POL_build_ostroleka_line"
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 750
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = 100
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_build_ostroleka_line"
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -750
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -100
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_build_ostroleka_line"
			92 = {
				add_building_construction = {
					type = bunker
					province = 9544
					level = 2
					instant_build = yes
				}
				add_building_construction = {
					type = bunker
					province = 402
					level = 2
					instant_build = yes
				}
			}
			98 = {
				add_building_construction = {
					type = bunker
					province = 11274
					level = 2
					instant_build = yes
				}
				add_building_construction = {
					type = bunker
					province = 11329
					level = 2
					instant_build = yes
				}
			}
		}
	}
	POL_plan_wschod = {
		icon = GFX_decision_shield
		available = {
			count_triggers = {
				amount = 2
				NOT = { 11 = { controller = { is_in_faction_with = ROOT } } } #Kaunas
				NOT = { 202 = { controller = { is_in_faction_with = ROOT } } } #Kiev
				NOT = { 206 = { controller = { is_in_faction_with = ROOT } } } #Minsk
			}
		}
		visible = {
			has_war_with = RUS
		}
		fire_only_once = yes
		days_remove = 60
		modifier = {
			political_power_gain = -0.25
			consumer_goods_factor = 0.2
		}
		ai_will_do = { factor = 10 }
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_plan_wschod"
			92 = {
				add_building_construction = {
					type = bunker
					province = 402
					level = 1
					instant_build = yes
				}
				add_building_construction = {
					type = bunker
					province = 9544
					level = 1
					instant_build = yes
				}
			}
			98 = {
				add_building_construction = {
					type = bunker
					province = 11274
					level = 1
					instant_build = yes
				}
				add_building_construction = {
					type = bunker
					province = 11329
					level = 1
					instant_build = yes
				}
			}
		}
	}
	POL_internationale_advisors = {
		icon = GFX_decision_generic_army_support
		allowed = { always = no }
		visible = {
			has_completed_focus = POL_Coordination_with_Communards
			is_in_faction_with = INT
		}
		cost = 100
		days_remove = 180

		modifier = {
			research_speed_factor = 0.1
			experience_gain_army = 0.12
		}

		ai_will_do = { factor = 10 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_internationale_advisors"
		}
	}
	POL_internationale_engineers = {
		icon = GFX_decision_wrenches
		allowed = { always = no }
		visible = {
			has_completed_focus = POL_Coordination_with_Communards
			is_in_faction_with = INT
		}
		cost = 100
		days_remove = 180

		modifier = {
			production_speed_buildings_factor = 0.1
		}

		ai_will_do = { factor = 10 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_internationale_engineers"
		}
	}
	POL_military_exercise_germany = {
		name = POL_military_exercise
		icon = GFX_decision_eng_blackshirt_march

		available = {
			has_war = no
			OR = {
				threat > 0.25
				has_war_support > 0.5
			}
			NOT = { has_global_flag = POL_ongoing_exercise }
		}

		cancel_trigger = {
			NOT = { is_in_faction_with = FROM }
		}

		ai_hint_pp_cost = 20
		custom_cost_text = CP_80_PP_20
		custom_cost_trigger = {
			has_political_power > 19.99
			command_power > 79.99
		}

		days_remove = 30
		days_re_enable = 240

		targets = { GER }
		target_trigger = {
			is_in_faction_with = FROM
		}

		ai_will_do = { factor = 10 }

		modifier = {
			experience_gain_army = 0.25
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_military_exercise_germany target: [From.GetName]"
			add_command_power = -80
			set_global_flag = POL_ongoing_exercise
			FROM = { country_event = poland.332 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_military_exercise_germany target: [From.GetName]"
			clr_global_flag = POL_ongoing_exercise
		}
	}
	POL_military_exercise = {
		icon = GFX_decision_eng_blackshirt_march

		available = {
			has_war = no
			OR = {
				threat > 0.25
				has_war_support > 0.5
			}
			NOT = { has_global_flag = POL_ongoing_exercise }
		}

		cancel_trigger = {
			OR = {
				NOT = { is_in_faction_with = GER }
				NOT = { is_in_faction_with = FROM }
			}
		}

		ai_hint_pp_cost = 20
		custom_cost_text = CP_80_PP_20
		custom_cost_trigger = {
			has_political_power > 19.99
			command_power > 79.99
		}

		days_remove = 30
		days_re_enable = 240

		targets = { BAT LIT WHR UKR }
		target_trigger = {
			is_in_faction_with = FROM
			is_in_faction_with = GER
		}

		ai_will_do = { factor = 10 }

		modifier = {
			experience_gain_army = 0.25
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_military_exercise target: [From.GetName]"
			add_command_power = -80
			set_global_flag = POL_ongoing_exercise
			FROM = { country_event = poland.332 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove POL_military_exercise target: [From.GetName]"
			clr_global_flag = POL_ongoing_exercise
		}
	}
}
foreign_politics = {
	POL_join_moscow = {
		icon = generic_prepare_civil_war

		allowed = { original_tag = POL }

		available = {
			at_peace_or_same_enemies_as_FROM = yes
			OR = {
				NOT = { country_exists = GER }
				GER = { has_war_with = FRA }
				GER = { has_war_with = FROM }
			}
		}

		visible = {
			is_subject = no
			is_in_faction = no
			has_socialist_government = no
			NOT = { has_country_flag = POL_forpol_wait }
			FROM = {
				is_faction_leader = yes
				has_socialist_government = no
				NOT = { has_war_with = ROOT }
			}
		}

		targets = { RUS }
		target_root_trigger = {
			is_subject = no #POL starts as subject and in a faction
			is_in_faction = no
			has_socialist_government = no
			NOT = { has_country_flag = POL_forpol_wait }
		}
		target_trigger = {
			FROM = {
				is_faction_leader = yes
				has_socialist_government = no
				NOT = { has_war_with = ROOT }
			}
		}

		fire_only_once = yes

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_join_moscow target: [From.GetName]"
			FROM = { country_event = poland.376 }
		}
	}
	POL_join_internationale = {
		icon = generic_prepare_civil_war

		allowed = { original_tag = POL }

		available = {
			at_peace_or_same_enemies_as_FROM = yes
			OR = {
				NOT = { country_exists = GER }
				GER = { has_war_with = RUS }
				GER = { has_war_with = FROM }
			}
		}

		visible = {
			has_socialist_government = yes
			is_subject = no
			is_in_faction = no
			NOT = { has_country_flag = POL_forpol_wait }
			FROM = {
				is_faction_leader = yes
				has_socialist_government = yes
				NOT = { has_war_with = ROOT }
			}
		}

		targets = { INT }
		target_root_trigger = {
			has_socialist_government = yes
			is_subject = no
			is_in_faction = no
			NOT = { has_country_flag = POL_forpol_wait }
		}

		fire_only_once = yes

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_join_internationale target: [From.GetName]"
			if = {
				limit = {
					RUS = {
						has_socialist_government = yes
						is_faction_leader = yes
						NOT = { has_war_with = ROOT }
					}
				}
				RUS = { country_event = poland.376 }
			}
			else = {
				FROM = { country_event = poland.376 }
			}
		}
	}
	POL_join_belgrade_pact = {
		icon = generic_prepare_civil_war

		allowed = { original_tag = POL }

		available = {
			at_peace_or_same_enemies_as_FROM = yes
			OR = {
				NOT = { country_exists = GER }
				GER = { has_war_with = FRA }
				GER = { has_war_with = RUS }
			}
			OR = {
				NOT = { country_exists = AUS }
				AUS = { is_subject = yes }
				BPL = { has_war_with = AUS }
			}
		}

		visible = {
			is_subject = no
			is_in_faction = no
			is_monarchy = no
			NOT = { has_country_flag = POL_forpol_wait }
			FROM = {
				is_faction_leader = yes
				both_socialist_or_both_not_socialist = yes
				NOT = { has_war_with = ROOT }
			}
		}

		targets = { BPL }
		target_root_trigger = {
			is_subject = no #POL starts as subject and in a faction
			is_in_faction = no
			is_monarchy = no
			NOT = { has_country_flag = POL_forpol_wait }
		}
		target_trigger = {
			FROM = {
				is_faction_leader = yes
				both_socialist_or_both_not_socialist = yes
				NOT = { has_war_with = ROOT }
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_socialist_government = no
				RUS = {
					is_faction_leader = yes
					has_socialist_government = no
				}
			}
			modifier = {
				factor = 0
				has_socialist_government = yes
				INT = { is_faction_leader = yes }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_join_belgrade_pact target: [From.GetName]"
			FROM = { country_event = poland.376 }
		}
	}
	POL_join_entente = {
		icon = generic_prepare_civil_war

		allowed = { original_tag = POL }
		available = {
			at_peace_or_same_enemies_as_FROM = yes
			GER = { exists = no }
			OR = {
				any_controlled_state = { is_coastal = yes }
				any_neighbor_country = { is_in_faction_with = ENT }
			}
			if = {
				limit = { INT = { exists = yes } }
				AND = {
					ENT = { has_war_with = INT }
					POL = { has_war_with = INT }
				}
			}
			else = {
				AND = {
					NFA = { owns_state = 16 } #Paris
					GBR = { owns_state = 126 } #London
				}
			}
		}
		visible = {
			is_in_faction = no
			is_subject = no
			OR = {
				POL_has_nationalist = yes
				is_monarchy = yes
			}
			is_valid_peace_target = yes
			NOT = { has_country_flag = POL_forpol_wait }
			FROM = {
				is_faction_leader = yes
				NOT = { has_war_with = ROOT }
			}
		}
		targets = { ENT }
		fire_only_once = yes

		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete POL_join_entente target: [From.GetName]"
			ENT = { country_event = poland.376 }
		}
	}
}
