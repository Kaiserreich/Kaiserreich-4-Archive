###########################
# CNT-FAI Events
###########################

add_namespace = cntd

#Ending the Civil War

country_event = { #CNT-FAI win the Civil War
	id = cntd.2
	title = cntd.2.t
	desc = cntd.2.d
	fire_only_once = yes
	trigger = {
		tag = CNT
		has_country_flag = civil_war
		has_war = no
	}		
	mean_time_to_happen = { days = 3 }
	option = {
		name = card.2.a
		#remove_country_flag = civil_war
		set_country_flag = aftermath_of_the_civil_war
 	}
}

